# Phase 10 (#148) — slot-conflict merge list

Status: **proposed** (slice 0 lands in this commit; S0 verdict, S1,
close-out open).
Part of [`batch-2026-09-23.md`](batch-2026-09-23.md) (read its §0
runbook first — it applies verbatim and is not repeated here, except
the session rule below which extends Phase 9's).
Backlog: [`backlog-tasks.md`](backlog-tasks.md) Tier 2 **#148**
(re-scope of #136 per owner ruling D5; #136 stays `CLOSED` as history).
Branch: `backlog/148-slot-conflict-merge-list` (open in **both** repos).
Predecessors:
[`02.134-141-resolver-display-residues.md`](02.134-141-resolver-display-residues.md)
S0 and its findings section "### #136 — STOP" (the capture that
contradicted #136's text and re-scoped it into #148), and
[`02.25-27-installed-node-overlay.md`](02.25-27-installed-node-overlay.md)
(#27's `SlotConflict::parents` work is the parent set this plan sits
inside — the instance set beside it is this phase's surface; S1 must
not regress it).
**Oracle:** bed `l0-fx-20260922T192350Z`, cell
`-D dev-libs/slotusegroup` — real's merge list carries both
`slotusetarget-1.0` and `-2.0` (rc 1 both sides); the slot-conflict
notices are already identical. S0.1 quotes the saved capture verbatim;
S0.2 re-runs the cell.
Merge-path gate: **no** (resolver walk + pretend/scheduler-display
only; if the diff reaches `ebuild_merge.rs`, `emerge_build.rs` or
`emerge_getbinpkg.rs`, the slice is in the wrong phase).
Bed at close: **yes** — fixture-oracle (the S0 atomlist cell, still
outside the default list until S1 goes green) plus L0 re-run quoted
against `l0-20260922T183907Z` (120/101/0.842).

## 0. Rules for this phase

- **Session rule while the Phase 7a `l3-core` campaign is live**
  (extends `06.142-143-blocker-fidelity.md` §0's rule): **no cargo
  command and no file edit of any kind in the live checkouts**
  (`../portuale`, `../pmtest`). All product work happens in the
  sibling worktrees `/home/vivo/repo/PORTUALE/portuale-148` and
  `/home/vivo/repo/PORTUALE/pmtest-148` (branches
  `backlog/148-slot-conflict-merge-list`, based on `main` @
  `f43e8f9b` / `3de87b7`), whose `cargo` target dirs are disjoint
  from the campaign's mounted `rust/target` by construction.
  `pgrep -af l3-source-parity` before touching anything under
  `differential-test-bed/`; never edit a `.sh` there while it
  matches (bash reads them incrementally).
- **`managers/managers.yaml` in the pmtest worktree is repointed**
  at `../portuale-148` (registry `repo` + all three binary paths) so
  the suite builds and grades the worktree binary. This hunk is
  scaffolding: **revert it before any pmtest commit** (the paired
  commits must build on their own siblings). A pre-commit
  `git diff` that shows `managers.yaml` is a STOP.
- **Bed runs happen at close-out, not per slice.** The
  fixture-oracle and L0 beds contend with the 7a campaign for the
  container lane; S0/S1 verify locally (worktree release binary
  against the worktree fixtures with `conftest.py::fixture_env`'s
  exact env, `cargo test --release`, the contract suite). S0's
  "both sides" evidence is the saved `l0-fx-20260922T192350Z`
  real/portuale cell outputs, quoted verbatim in S0.1 below, plus a
  local portuale-side re-run — no live bed was run for S0. (Fixture
  ebuilds for this shape are untouched since the initial import,
  `git log` verified, so the saved real capture is still valid.)
- **Fixtures exist; no new fixture is expected.**
  `dev-libs/slotusegroup` (RDEPENDs `slotuseplain slotusex
  slotusey`) + `dev-libs/slotusetarget-1.0` (`IUSE="+x +y"`) /
  `-2.0` (`IUSE="-x"`) already isolate the shape, and
  `-D dev-libs/slotusetarget` alone is the clean control (only 2.0
  matches the bare atom). A twin needs a `fixtures/repo` +
  md5-cache collision check first — S1 says why none is needed.
- **Corpus drift is a STOP unless the slice says it moves output.**
  S0 moves nothing. S1 moves `slotconflict*` keys (the merge-list
  head gains the 2.0 row) — bless only reviewed keys, in pmtest's
  commit only.
- **#27's `SlotConflict::parents` filing stays green.** S1 runs the
  `slotconf*` pins from `02.25-27-installed-node-overlay.md`
  explicitly; a fix that requires rewriting the parent filing is a
  residue with a new number, not part of this phase. Likewise the
  cluster-I same-slot dedup (`mergebound_cp_slots` retain): S0 rules
  it in or out, and if it is the site, the fix keeps that slice's
  reason for existing (install + reinstall of one slot as one node)
  — stop and ask rather than weaken it.

## S0 — oracle re-run + the collapse site (this phase's oracle slice)

### S0.1 Saved capture, verbatim (`l0-fx-20260922T192350Z`)

Cell `-D dev-libs/slotusegroup` (`l0-report.json`: `real_rc: 1`,
`ptl_rc: 1`). Real:

```text
[ebuild  N     ] dev-libs/slotusetarget-1.0 to /tmp/l0-fixture-oracle/fixtures/ USE="x y"
[ebuild  N     ] dev-libs/slotusetarget-2.0 to /tmp/l0-fixture-oracle/fixtures/ USE="(-x)"
[ebuild  N     ] dev-libs/slotuseplain-1.0 to /tmp/l0-fixture-oracle/fixtures/
[ebuild  N     ] dev-libs/slotusex-1.0 to /tmp/l0-fixture-oracle/fixtures/
[ebuild  N     ] dev-libs/slotusey-1.0 to /tmp/l0-fixture-oracle/fixtures/
[ebuild  N     ] dev-libs/slotusegroup-1.0 to /tmp/l0-fixture-oracle/fixtures/

!!! Multiple package instances within a single package slot have been pulled
!!! into the dependency graph, resulting in a slot conflict:

dev-libs/slotusetarget:0 for /tmp/l0-fixture-oracle/fixtures/

  (dev-libs/slotusetarget-1.0:0/0::testrepo, ebuild scheduled for merge to '/tmp/l0-fixture-oracle/fixtures/') USE="x y" pulled in by
    >=dev-libs/slotusetarget-1.0[y] required by (dev-libs/slotusey-1.0:0/0::testrepo, ebuild scheduled for merge to '/tmp/l0-fixture-oracle/fixtures/') USE="" ELIBC="glibc"
    ...
    >=dev-libs/slotusetarget-1.0[x] required by (dev-libs/slotusex-1.0:0/0::testrepo, ebuild scheduled for merge to '/tmp/l0-fixture-oracle/fixtures/') USE="" ELIBC="glibc"
    ...

  (dev-libs/slotusetarget-2.0:0/0::testrepo, ebuild scheduled for merge to '/tmp/l0-fixture-oracle/fixtures/') USE="(-x)" ELIBC="glibc" pulled in by
    >=dev-libs/slotusetarget-2.0 required by (dev-libs/slotuseplain-1.0:0/0::testrepo, ebuild scheduled for merge to '/tmp/l0-fixture-oracle/fixtures/') USE="" ELIBC="glibc"
    ...
```

Portuale (same cell): the identical notice block (both instances,
both USE columns, all three parents with their `^` markers), rc 1 —
but the merge list is one row short:

```text
[ebuild  N     ] dev-libs/slotusetarget-1.0  USE="x y"
[ebuild  N     ] dev-libs/slotuseplain-1.0
[ebuild  N     ] dev-libs/slotusex-1.0
[ebuild  N     ] dev-libs/slotusey-1.0
[ebuild  N     ] dev-libs/slotusegroup-1.0
```

The comparator's verdict (`l0-report.txt`): exactly one finding,
`[use] dev-libs/slotusetarget USE: real ['(-x)'] vs portuale
['x', 'y']`. The defect is "portuale's merge list drops one of two
same-slot instances real keeps", not the notice. `build_slot_conflict`
(the site #136 named) is not the site — it builds the notice, which
is already right.

### S0.2 Re-run + rule the candidates in or out (read-only + findings)

1. **Re-run the cell locally on both sides of the worktree state.**
   Portuale side: the worktree release binary with
   `fixture_env`'s exact env (batch §0 addition 3). Real side: the
   S0.1 capture stands (fixtures untouched since import); confirm
   byte-identity of the portuale side against the saved
   `portuale/-D_dev-libs_slotusegroup.txt` modulo the staging-root
   path (`/tmp/l0-fixture-oracle/fixtures/` vs the fixture root).
   Any contradiction with S0.1 is a STOP.
2. **Rule the two candidates in or out, with evidence, before code:**
   (a) the same-slot dedup (`mergebound_cp_slots` retain — the
   cluster-I slice-12 site); (b) the slot-conflict path that builds
   the plan real would print before it gives up. Decisive probes:
   `PORTUALE_MO_SEL=1` (the scheduler's node census — how many
   nodes reach `serialize_merge_order`, and is 2.0 among them?),
   and a temporary gated trace at `record_slot_conflict` /
   `build_slot_conflict` (existing version, current version,
   triggering atom, installed side) showing which walk arm records
   the conflict and whether it also creates an entry. Probes are
   scratch: reverted before any commit.
3. **Deliverable:** this section filled with the re-run outputs and
   the named site (or the stop-and-ask if it is the cluster-I
   dedup). **No product code, no pin edits, no fixture edits in
   this diff.** Then commit as a pair (pmtest atomlist first).

## S1 — the fix and the pinned-output test

- [ ] **Step 1: failing pin first.** Update
  `test_slot_conflict_use_reason_keys_unconditional_before_violated`
  (`pytests-contract-suite/test_emerge_pretend_contract.py`) to
  real's S0.1 merge-list head: the `slotusetarget-2.0 USE="(-x)"`
  row present, directly after the 1.0 row, with the four owner rows
  unchanged — and rewrite its docstring's "known dual-instance
  display gap" paragraph (that paragraph *is* #148; it stops
  describing the bug and starts asserting real). Watch it fail on
  the five-row list. The CASES entry (`--pretend
  dev-libs/slotusegroup`, rc 1) is unchanged. Control: the
  `slotusetarget`-alone cell and every `slotconf*` pin from #27
  must already pass and must not move.
- [ ] **Step 2: the fix, at S0's named site.** Expected shape
  (S0 confirms or corrects): the walk's merge-vs-merge
  `resolved_slots` conflict arm records the `SlotConflict` and
  `continue`s without creating a `GraphEntry` for the current
  (second) version — the notice renders from the record, the merge
  list renders from entries, so the second instance has a notice
  and no row. Real graphs both instances as full nodes. The fix
  creates the second entry beside the record (same slot/sub-slot/
  repo/USE display the record already carries) and walks its deps,
  so the scheduler sees both nodes. Constraints the fix must
  respect, each verified in this slice: (i) `resolved_slots` keeps
  pointing at the first-resolved instance (later atoms' reuse
  semantics unchanged); (ii) one entry per (cp, version) per pass
  (a second conflicting atom for the same version must not push a
  twin — guard in `PassState`); (iii) the direct-solve removal
  machinery still deletes the extra row when the conflict solves
  (it matches entries by version — S1 proves it against a solved
  twin cell, e.g. an orbtblocked-shaped fixture, or states why no
  existing cell covers it); (iv) #27's parent filing untouched
  (the `slotconf*` pins are the gate).
- [ ] **Step 3: Rust unit test** in `rust/portage-repo/src/lib.rs`'s
  `mod tests` over the fixture root (the `fixtures_root()` shape):
  resolving `-D dev-libs/slotusegroup` yields two merge-bound
  `slotusetarget` entries (1.0 and 2.0). RED-verified against the
  pre-fix product (one entry), like S0's probes.
- [ ] **Step 4: run the blast radius.** `cargo fmt --check`,
  `cargo clippy --release --all-targets` (zero warnings),
  `cargo test --release`, then the contract suite diffed **by test
  name** against the pre-S1 baseline. Named blast radius: every
  `slotconf*` / `slotuse*` / `slotop*` / `orbtblocked*` /
  `multislot*` pin, every `--tree` pin (the cp-keyed
  `required_by` map hands both same-cp entries the same owners —
  tree nesting must be read, not assumed), and the corpus
  (`slotconflict*` keys are the likely movers — inventory before
  any bless).
- **Stop rules:** a pin outside the named blast radius changing; a
  solved-conflict cell gaining or losing a row (that is the
  removal machinery, not this item — file it); the fix requiring a
  change to `SlotConflict::parents`' filing or to the
  cluster-I dedup's reason for existing (both are stop-and-ask,
  not silent rewrites). Then commit as a pair (pmtest first so
  this repo's message can quote its sha; corpus bless, if any,
  rides in pmtest's commit only).

## Close-out

- Re-run the fixture-oracle bed on the S0 atomlist (now including
  the cell; the default list gains `-D dev-libs/slotusegroup`
  **only once portuale's side is green**, batch §0 step 6's
  tightened rule) and the L0 bed; quote probe/clean/parity numbers
  against `l0-fx-20260922T192350Z` and `l0-20260922T183907Z`
  (120/101/0.842). If Phase 7a is still live, coordinate the lane
  (`pgrep -af l3-source-parity`) rather than stacking heavy runs.
- Full verification pass (batch §0 step 6): `cargo fmt --check`,
  `cargo clippy --release --all-targets` (zero warnings),
  `cargo test --release` (whole workspace), then in `../pmtest`
  `python3 -m pytest pytests-contract-suite -q` diffed by test name.
  Corpus drift is a STOP unless S1's inventory names the keys.
- Docs in the same commit (AGENTS.md step 11): flip #148 to
  `DONE <date>` with the commit pair; flip this file's `Status:`
  header to `done <date>` (with branch and commits); append the
  `what-this-proves.md` paragraph (never rewrite a prior slice's)
  with a runnable, live-verified example. Any residue filed takes
  the next free number after scanning **all** tiers
  (`grep -nE '^[0-9]+\. \*\*' docs/backlog-tasks.md`).

## What this phase deliberately does not do

- It does not re-open #136 (closed as mis-stated; kept as history),
  #27 (parents), #90 (walk order), or #142 (solved-conflict
  removal rows ride the machinery S1 reuses, not revises).
- It does not touch the resolver's *choices* — rc, the conflict's
  solvability, and which versions collide are unchanged. Only the
  surviving-conflict merge list gains the row real prints.
- It does not add a `known-divergences` row: the cell goes green,
  unsuppressed (batch §0 addition 1 has nothing to remove here —
  the cell was never allowlisted).
- It does not touch the merge path (no safety-gate merge), run
  L1/L2, or flip `--shell`'s default.
