# #74 — bdeps-on `--deep` walk package-set divergences (the #65 slice-15 follow-up)

Status: **proposed 2026-09-16** — last slice of the #71–#74 batch
(`backlog/71-74`); batch execution order **#73 → #72 → #71 → #74**. Not
started. S0 is a diagnosis slice: S1/S2 are provisional until S0 names
each class's root cause, and the S0 commit must update §3 of this plan
with what it found.
Backlog entry: [`backlog-tasks.md`](backlog-tasks.md) Tier 2 #74.
Evidence: `TEST/findings/l0.md` "#65 follow-up (slice 15)" (the exact
host diff: portuale extra `dev-lang/rust-1.94.0 NS`; real-only 13:
`gtk-doc-am`/`gtk-doc` `U` + real's own unsatisfied `[blocks B]`,
`mdit-py-plugins`, `myst-parser`, `audiofile`, the xemacs chain) and
`docs/evidence/2026-09-16-post63-probes/`.
Interaction: #71 (same complete-mode walk of installed consumers; run
#71's host diff first), #22 (`||` preference), #24 (slot-op trigger),
#65 (whose slot-op half already matches and must stay matched), #66
(withdrawn -- do **not** resurrect its "never walk installed build-time
deps" rule; it is false).
Branch: `backlog/71-74` (user merges).

**Read first:** `AGENTS.md`, [`agent-context.md`](agent-context.md), this
file, then `enqueue_dependencies` (the `--deep`/AlreadyInstalled
recursion) and the installed-parent block in `resolve_pretend_graph`,
`disjunction_preference` / `promote_tied_alternative` /
`atom_cp_installed` / `atom_matches_installed` /
`atom_installed_in_slot_of` / `atoms_all_in_graph` in
`rust/portage-repo/src/lib.rs`, and `merge_order::key_priority` /
`dep_edges_from_metadata` (the `optional=pkg.built` model). Find code by
symbol name; line numbers drift.

## 0. Rules for every slice

- **Every slice lands as its own commit**, subject `#74 S<n>: ...`, with
  the `Co-Authored-By` trailer. **The executing LLM commits each slice
  as soon as that slice's verification is green, before starting the
  next slice.** No squashing, no push.
- Expected behaviour is grounded in real Portage 3.0.82.2
  (`_emerge/depgraph.py`, `portage/dep/dep_check.py`, citations below)
  and in host/container captures of the probe:
  `emerge -puDvN --ignore-default-opts --oneshot app-containers/runc`
  (bdeps auto, dynamic deps on).
- **No fix lands on a guess.** S0 must classify each divergent package
  by its pulling chain and name the real and portuale code paths before
  S1/S2 touch code; a class that resists a deterministic rule is filed
  back, not patched with a heuristic.
- Full verification pass per `AGENTS.md` step 8; #71's host diff is the
  baseline; re-run the L0 host probe and the slot-op bed (the slot-op
  half must stay matched).
- Flip this plan's `Status:` header to `done <date>` (with commit
  hashes) in the last slice's commit.

## 1. What real does (grounding for the two classes)

The probe runs with build deps walked (`--ignore-default-opts` drops the
host's `--getbinpkg`, so `bdeps` is `auto`, `create_depgraph_params.py:
97-103`). `_add_pkg_deps` (`depgraph.py:4194-4247`) walks an installed
(`built`) parent's `DEPEND`/`BDEPEND` with `optional=pkg.built`, and
`_add_pkg` (`:3796-3817`) recuses with
`want_update = not complete_mode and (arg_atoms or update) and not
too_deep(depth)`. In complete mode (`_complete_graph`, `:8660-8745`) the
required-set walk pulls installed nodes in with `want_update = False`.

**Class A — portuale extra `dev-lang/rust-1.94.0 NS`.** The world-only
installed `sys-block/thin-provisioning-tools` (pulled by the complete
closure) has `BDEPEND="llvm-core/clang:21 virtual/pkgconfig ||
( dev-lang/rust-bin:1.94.1[llvm_slot_21] dev-lang/rust:1.94.1[llvm_slot_21]
dev-lang/rust-bin:1.94.0[llvm_slot_21] dev-lang/rust:1.94.0[llvm_slot_21]
… )"`. The host has `dev-lang/rust-1.94.1` installed with
`llvm_slot_21` enabled. Real installs no rust here: either it never
selects that optional dep on a world-only complete-mode parent, or it
selects the installed `rust:1.94.1` (`dep_zapdeps`' bin 0 aliases
`preferred_in_graph`/`preferred_installed`/`preferred_any_slot` when a
graph_db is present, `dep_check.py:377-402`, and the in-bin ordering
promotes `all_installed_slots`, `:765-809`). Portuale's preference lands
on the uninstalled `rust:1.94.0`.

**Class B — real-only 13 packages** (`gtk-doc-am`/`gtk-doc` `U` +
`[blocks B]`, `mdit-py-plugins`, `myst-parser`, `audiofile`, the xemacs
chain). Real's same `--deep` bdeps-on closure reaches them; portuale
"reaches fewer". These are newly merged/upgraded packages, so they are
not complete-mode-only nodes -- some real walk path resolves them and
portuale's does not.

## 2. Portuale today

- `enqueue_dependencies` (the `AlreadyInstalled` recursion) walks the
  installed parent's `*DEPEND` (effective view by default) with the
  installed `USE`, and its `||` selection goes through the shared
  `disjunction_preference` / `promote_tied_alternative`. Build-time
  keys are marked `optional` in `merge_order::key_priority(key, built)`
  but the walk does not otherwise distinguish an optional dep.
- `disjunction_preference` returns `AltPreference::Installed` when
  `all_available && all_use_satisfied && atoms.iter().all(atom_cp_installed)`
  -- cp-level, so an alternative whose *slot* is not installed can still
  rank in bin 0; `atom_matches_installed` (version/slot/USE-aware) is
  only used in the `!all_available` "other_*" path. `want_update` is a
  documented cut (pinned `false`) in `promote_tied_alternative`.
- The finding's current attribution is "the disjunction preference lands
  on the uninstalled alternative", but **S0 must verify that** against
  `--json`/`--debug` rather than assume it.

## 3. Design (S1/S2 provisional pending S0)

**S0 — diagnosis and fixture repros.** Host + container, no production
code change except the (allowed) debug capture support:

1. Re-run the probe with `--debug` for both PMs and save the outputs
   under `TEST/findings/l0.md` "#74 S0" (or
   `docs/evidence/2026-09-17-74/` if the volume is large).
2. For each real-only package, extract its parent chain from real's
   `--debug` `Parent Dep:` rows / `--tree` output, and bucket by the
   real code path that reached it (argument walk, complete closure,
   `||` branch, optional dep, slot-op cascade).
3. For the rust choice, dump portuale's `--json`/`--debug` and real's
   `--tree` around `thin-provisioning-tools`, then decide which of
   these is true: (a) portuale should not walk that dep at all
   (optional-dep semantics on a complete-mode parent); (b) portuale
   walks it but must rank the installed `rust:1.94.1` first (bin
   classification: `all_installed_slots` vs cp-level `atom_cp_installed`,
   missing `want_update` in the promotion); (c) both.
4. Build fixture-scale repros under `fixtures/repo/` (+
   `metadata/md5-cache/`) for each confirmed root cause, checked for
   name collisions first: a tiny `|| ( bin-alt installed-alt ... )`
   BDEPEND shape reproduces (b) without the host's rust; the
   complete-mode optional-dep shape reproduces (a) with two fixture
   packages.
5. Write the classification table (package -> class -> real path ->
   portuale path -> fixture) and update §3 here with the final S1/S2
   split; commit as `#74 S0: diagnosis and fixture repros`.

Difficulty: hard (host-scale attribution; two independent classes).

**S1 — Class A fix (per S0).** Expected home: `disjunction_preference` /
`promote_tied_alternative` (e.g. promote a slot-installed alternative
using the full `atom_installed_in_slot_of` test, or thread the
`want_update` the #22 slice cut, whichever S0 shows real using), or the
installed-parent optional-dep gate in `enqueue_dependencies`. Rust unit
test(s) on the chosen helper plus a fixture-oracle capture of the new
fixture. Difficulty: hard (provisional).

**S2 — Class B fix (per S0).** Expected home: `enqueue_dependencies` /
the installed-parent recursion and its `||`/optional handling, if S0
shows portuale's walk stops before real's. If S0 splits class B into
independent mechanisms, split this slice (`S2a`/`S2b`) rather than
bundling unrelated fixes in one commit; each carries its own fixture and
pin. Difficulty: hard (provisional).

**S3 — pins + host re-diff + docs.** Re-run the host probe and diff the
package set against `docs/evidence/2026-09-16-post63-probes/`: the
portuale extra and the real-only set must both shrink to empty for this
probe (or every residue must be explained in the findings note). Keep
the slot-op half of the probe byte-matched (`go-md2man` + `runc` `rR`,
`go` `r U`); re-run the slot-op bed; `what-this-proves.md` paragraph;
backlog DONE; plan status. Difficulty: easy.

## 4. Slices and difficulty

| slice | commit subject | contents | difficulty |
|---|---|---|---|
| S0 | `#74 S0: diagnosis and fixture repros` | `--debug` attribution, class table, fixtures, §3 re-scope | hard |
| S1 | `#74 S1: <the Class A fix>` | preference/walk fix + Rust test + oracle | hard (provisional) |
| S2 | `#74 S2: <the Class B fix>` | walk-completeness fix + Rust test + oracle | hard (provisional) |
| S3 | `#74 S3: pins, host re-diff, docs` | probe re-diff, slot-op bed, docs/status | easy |

If S0 finds one of the two classes has no deterministic rule, stop,
report, and keep that class as a filed follow-up with the evidence --
do not stretch the other fix to cover it.

## 5. Tests and pins

- Rust: one unit test per fixed mechanism, on the helper directly
  (`disjunction_preference` choice bin or the installed-parent walk),
  using the new fixtures; keep all `bdepcascadepkg`/`bdepcascadedep`
  pins from #66 S1 untouched.
- Oracle: a real container capture for each new fixture shape
  (`fixture-oracle` atom list entry or a dedicated probe note), plus the
  host probe's package-set diff.
- L0 host re-diff is the acceptance bar for the whole item; the findings
  note records both directions (extra and missing) before and after.
- Determinism: no hash-order dependence in the new walk/preference path
  (`tests/test_output_invariants.py` runs over `CASES`; run it).

## 6. File-conflict map

- `rust/portage-repo/src/lib.rs`: `enqueue_dependencies`,
  `disjunction_preference`, `promote_tied_alternative`, the
  installed-parent recursion, unit tests.
- `rust/portage-repo/src/merge_order.rs` only if `key_priority`'s
  optional model needs to feed the walk selection (avoid if possible).
- `fixtures/repo/…` + `fixtures/repo/metadata/md5-cache/…` (new
  fixtures; regenerate cache with `portuale emerge --regen` or real
  `egencache` -- check `scripts/md5_cache_audit.py`).
- `tests/test_emerge_pretend_contract.py` (`CASES` + pinned function if
  a fixture output is stable), `TEST/findings/l0.md`,
  `docs/evidence/…`, `docs/what-this-proves.md`, this plan,
  `docs/backlog-tasks.md`.
- #71 is already landed by then; #74 is the last item touching
  `lib.rs` in this batch.

## 7. Non-goals and residue

- #22's documented cuts (`minimize_slots`, full `want_update` threading)
  beyond what Class A needs; state the cut in the code comment if the
  fix narrows it.
- #26 dynamic-deps divergence; #24b/c probe family.
- Real's 13-package set is host-registry-specific (gtk-doc/xemacs
  versions move); the fixture repros are the durable pins, the host diff
  is the acceptance check.
- Do **not** implement #66's withdrawn "never walk installed build-time
  deps" rule; #66 S1's both-ways pin is the guard.

## 8. Risks

- **Host-only reproducibility.** The classes depend on this host's
  installed world; S0 must land fixture repros or the fix cannot be
  regression-tested. If a class cannot be reduced to a fixture, say so
  in the findings and pin the host probe output as the evidence.
- **Preference swings.** Changing `disjunction_preference` ranks touches
  every `||` in the tree; the upstream-oracle `test_or_choices.py` cases
  (`TEST/logs/r4-20260915/`) and the `||` contract suite are the guard.
  Keep the change behind the S0-confirmed condition.
- **Walk depth.** A broader installed-parent walk can pull large
  subtrees into unrelated resolves. Measure the L0 host run
  (`-puDvN @world`) before/after for both package set and wall clock;
  a set that matches real but takes minutes more is still a regression.
- **Slot-op coupling.** The #65 S2 `walked` gate consumes `entries`; a
  changed walk can silently change the build-time slot-op set. The
  fixture-oracle bed with `FX_SLOTOP_BDEP=1` must stay green.
