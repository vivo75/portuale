# #91 S0 — matrice sonde withhold edk2 (findings, 2026-09-19)

Piano: `docs/01.91-79_residue.md`. Box: solo sonde rapide (<60 s
ciascuna), nessun codice prodotto, nessun fixture committato —
commit standalone qui, senza gemello pmtest.

Fonte oracolo per ogni claim: Portage reale 3.0.82.2 su questo host
(capture sotto). Portuale: `rust/target/release/portuale` al commit
di base (`01a37b8`, tree pulito).

## 1. Re-diff `@world` corrente (due run consecutivi, identici)

`emerge -p -uDN @world` reale **non propone più `edk2-bin`**: la
merge list (27 pacchetti) non lo contiene, nessun conflitto
riportato. Il withhold è riproducibile in forma diretta:

- `emerge -p -uDN sys-firmware/edk2-bin` reale (2 run consecutivi):
  nessun merge; `WARNING: One or more updates/rebuilds have been
  skipped due to a dependency conflict:` —
  `(edk2-bin-202608 … scheduled for merge) conflicts with
  ~sys-firmware/edk2-bin-202408[qemu_softmmu_targets_x86_64(+)]
  required by (app-emulation/qemu-10.2.3-r1-1 …, installed)`.
- Stessa sonda portuale: `[ebuild U] sys-firmware/edk2-bin-202608
  [202408]`, rc 0. **Delta confermato** (era l'unico delta stabile
  di `@world`; ora è un withhold diretto stabile).

## 2. Matrice sulle 4 piste (tutte host-exact, `-p`, mai merge in `/`)

1. **Consumer solo-vdb — PRESENTE nella catena.** Il `--debug`
   reale mostra il parent
   `(x11-misc/grub2-theme-preview-2.10.0-1 …, installed)` con
   depstring contenente il plain `app-emulation/qemu`, e il figlio
   `(app-emulation/qemu-10.2.3-r1-1 …, installed)` mai walked come
   merge: il pin entra nel grafo solo via nodi installed.
2. **`-r1` / downgrade disponibile — non isolato, non necessario.**
   qemu è `10.2.3-r1` con downgrade ad albero, ma il meccanismo è
   interamente visibile senza variare questo asse (vedi §3).
3. **Gruppo condizionale — valutato, non `unevaluated_atom`.** Il
   raw Depstring di qemu porta ancora
   `qemu_softmmu_targets_x86_64? ( pin-upstream-blobs? ( ~… ) )`
   con i tre fratelli (`ipxe`, `seabios-bin`, `sgabios`), ma il
   secondo blocco `Parent:` riporta la depstring **valutata**:
   `… ~sys-firmware/edk2-bin-202408[qemu_softmmu_targets_x86_64(+)]
   ~sys-firmware/ipxe-1.21.1_p20230601[binary,qemu]
   ~sys-firmware/seabios-bin-1.16.3
   ~sys-firmware/sgabios-0.1_pre10[binary] …`.
   Il pin conflittuale è quindi `Parent Dep:
   ~sys-firmware/edk2-bin-202408[qemu_softmmu_targets_x86_64(+)]
   required by (app-emulation/qemu-10.2.3-r1-1 …, installed)` con
   riga `Slot Conflict:
   (sys-firmware/edk2-bin-202608 … scheduled for merge)`.
4. **`--backtrack=0` — il withhold PERSISTE** (stesso
   `WARNING … skipped due to a dependency conflict`, rc 0).
   Il verdetto vive nel solve diretto, non nel retry.

## 3. Verdetto `--debug` (mai catturato dalle 15 sonde, che non usavano `--debug`)

- `!!! Slot conflict handler started.` **presente** ⇒ il withhold
  è dentro il solve path (pista 4 confermata nella direzione
  solve-diretto, funzione di #90).
- `!!! Slot conflict solution:` → `Conflict:
  (/, sys-firmware/edk2-bin:0)` → `remove: (…-202608 … scheduled
  for merge)` / `keep: (…-202408-5 … installed)` →
  `Removing package: (…-202608 …)`.
- Quindi: il pin installed partecipa come parent edge del
  conflitto di slot e il solve diretto emette il verdetto
  keeping-installed. A bt0 identico ⇒ unificare con #90 come
  notato nella voce #91 (un solo fix, non due, se #90 porta il
  solve diretto).

## 4. Cosa distinguerà S1 (non codificato qui)

Il punto di #79 resta valido per D1: lì reale **non registra
alcun conflitto** (consumer `rdctarget-1.0` `@selected`, pin
`BDEPEND="~rdcblocker-1.0"`, cella
`--update --deep --newuse --oneshot dev-libs/rdcpuller` con
`FX_WORLD_EXTRA=dev-libs/rdctarget`) e fonde `rdcblocker-2.0` +
`[blocks B]` rc 1; qui reale registra il conflitto e trattiene.
Candidati ancora da sciogliere in S1 contro gli oracoli:

- chiave `RDEPEND` (qemu, visibile con bdeps spenti) vs `BDEPEND`
  (D1, svuotata per i built quando bdeps è spento —
  `depgraph.py:4194-4247`; resta la tensione con la sonda
  ermetica `RDEPEND <` che aggiorna, le cui condizioni non sono
  committate da nessuna parte);
- il percorso sonda-operatore (`_slot_operator_check_reverse_dependencies`,
  `depgraph.py:2472-2538`, su cui è tarato l'ignore di #79) vs il
  parent edge del conflitto di slot (il percorso di §3 qui).

S1 deve citare per ognuna delle tre restrizioni rifiutate di
`01.91-79_residue.md` §1 la sonda oracolo che la esclude; senza
quella, nessun restringimento dell'ignore di #79.
