# #90 S2 — direct solve: removal + skip notice (implementation note, 2026-09-20)

Plan: `docs/01.90-real_silently_reconciles.md` §S2 (as re-scoped: S1
did selection/traversal, S2 does removal + notice).

## 1. What was ported (`rust/portage-repo/src/lib.rs`, `rust/portuale/src/pretend.rs`)

Real `_solve_non_slot_operator_slot_conflicts` (`depgraph.py:1774-2106`),
run unconditionally: `direct_solve_slot_conflicts` over the recorded
`SlotConflict`s (forced/non-forced sets, `or_tuple` first-pulled with
walk-order insertion ranks, `non_matching_forced` protection, the
`is_arg_parent` installed exclusion, the slot-op replace skip, and a
need_rebuild-trailer protection for stuck built-`:=` parents).
Removal repoints the merge row at the kept instance (installed keepers
become `AlreadyInstalled`); single-survivor conflicts dissolve; the
missed-update rows (`SkippedUpdate`) render real's `WARNING` block
with operator + version `^` spans. A second producer,
`constraint_withheld_updates`, covers withholds that never form a
conflict (enforced holdable pins settling the installed version while
a higher visible candidate exists -- host qemu/edk2, whpin cell),
closing #92 in the same shape. Both run inside `run_pass` /
`assemble_result` on every pass (real runs the solve at every budget),
gated on recorded conflicts / enforced pins.

## 2. Oracle groundings (all staged host-side vs real 3.0.82.2)

- 2-atom old,new (bt0 AND default): 1.0 rows + `WARNING`, rc 0.
- unsolvable / btparent-bt0 / triangle (both argv orders): both
  instances, 1.0-first notice order, rc 1.
- orbtblocked-bt0: 1.0-first, rc 1.
- need_rebuild trailer (excluded + useoldpkg paths): conflict +
  trailer + rc 1 preserved (the `:=` fast-path + stuck-parent gate).
- qemu/edk2 + whpin: withhold + `WARNING`, rc 0.
- needer/othermod default: unchanged; D1/keeper/#35/ingraphany green.

## 3. Deliberate narrowings

- No indirect-candidate closure (all oracle cells hold leaves).
- No orphan cascade and no `_create_graph` re-walk after removal
  (removal runs before the blocker phases, like real's solve before
  `_validate_blockers`, but blocker rows anchored to a removed entry
  are out of scope -- no S2 cell has them).
- One merge row per cp for dual-instance conflicts (pre-existing
  display gap, now documented in S3 pins where it bites).
- `or_tuple` insertion ranks are merge-in-entries-order then
  installed (real's tracker order for installed nodes is
  walk-dependent; every oracle tuple is all-merge).
- Skip notice silent under `-q`/`--json`/`--columns`; no root
  suffixes (`for/to/in '<root>'`) and no profile-default ELIBC-style
  expand vars the fixture profiles do not declare (same display-cut
  family as the existing notice rows).
- The container L0 + fixture-oracle bed cannot run on this host (no
  image): first in-container execution belongs to S3 verification
  elsewhere.
