# #91 S2 — guardie regressione + re-diff (2026-09-19)

Piano: `docs/01.91-79_residue.md` §S2. Nessun codice prodotto in
questa slice (il gating è S1, `3f5e21c`); qui solo le guardie che
provano che il restringimento dell'ignore non ha aperto buchi
altrove, più la cella bed committata (gemello pmtest).

## 1. D1 di #79 (il pin build-time resta ignorato)

- Or Specifico container registrato (`TEST/findings/l0.md` "#79 D1",
  `l0-fx-20260918T065403Z`): reale fonde `rdcblocker-2.0` +
  `[blocks B]` rc 1; il pin `BDEPEND="~rdcblocker-1.0"` non trattiene.
- Guardia host-side (bed irraggiungibile qui: nessuna immagine
  container; la cella BDEPEND legge il vdb dal build-root `/`,
  replica fedele solo in container): portuale su tree staged con
  `rdctarget` in world stampa byte-identico all'oracolo —
  `[ebuild U] rdcblocker-2.0 [1.0]`, `[ebuild U] rdcpuller-2.0
  [1.0]`, `[blocks B] <rdctarget-2.0`, rc 1.
- Livello unit: `reverse_dependency_constraints_enforces_a_runtime_pin_but_ignores_a_build_time_pin`
  (metà `rdctarget`: né enforced né dropped).

## 2. Triangolo #57, keeper/#54, disgiunzioni, nomerge-owner

11 pin contrattuali verdi sul binario S1 (stessa selezione S1 §S2,
estesa a disjowner/nomowner-wait):
`needer_othermod_triangle`, `keeper_reachable` (×3),
`reverse_dependency_constraints_skips_…` e gemelli (`revdep`,
`paired`, `unreachable_installed_consumer`), `slotop_revdeps`
(×3), `wait_follows…disjunctive…`, `nomowner`-adiacenti —
`11 passed` in `test_emerge_pretend_contract.py`.

## 3. Re-diff host `@world` (due run consecutivi, §7 rischio host-flakiness)

- Portuale deterministico sui due run (diff vuoto).
- Contro reale 3.0.82.2 (`-puDN @world`): insiemi pacchetto
  identici a meno di artefatti di whitespace/`[f1r]`; `edk2-bin`
  assente da entrambe le liste (il withhold S1 chiude l'unico delta
  stabile di #91), nient'altro mosso (27 pacchetti + rebuild
  `qtdeclarative`/`qttools` invariati).

## 4. Cella bed committata (gemello pmtest)

`differential-test-bed/atomlists/l0-fixture-oracle-whpin.txt`:
la forma trattenuta come cella fixture-oracle, speculare a
`l0-fixture-oracle-rdcpin.txt` (stesso `FX_WORLD_EXTRA`, attesa
opposta: nessun upgrade di `whblocker`, rc 0). Non eseguita qui
(nessuna immagine `test-portuale` su questo host); la prima
esecuzione in container decide `clean` senza allowlist.
