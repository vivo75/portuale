# Recap: backlog ops 2026-09-19 — agent read-only index

> **READ-ONLY — LLM AGENTS ONLY. DO NOT EDIT THIS FILE.**
> Closed historical index. Source of truth for status is
> `docs/backlog-tasks.md` + `docs/scope-backlog.md` + `git log`.
> Source of truth for what shipped is `docs/what-this-proves.md`.
> Do not add paragraphs here, do not re-open items here, do not update
> line numbers here. If a residue below is fixed, update
> `backlog-tasks.md` / `scope-backlog.md`, not this file.
> `file:line` citations below have drifted — re-locate by symbol name.

Keeps only what a future session needs to **continue the project
without re-deriving it**: status, open residue, deliberate cuts,
revisit triggers, and traps that already cost a wrong turn. Mechanism
detail lives in the cited plan / findings / `what-this-proves.md`
entry and is not repeated here. The 28 source docs are retired to
`docs/history/` (list at the end).

---

## Tier 1

### #63 — `EMERGE_DEFAULT_OPTS` — DONE 2026-09-16

- Shipped: config resolves before argv parse; defaults *prepended* so
  argv wins (`args_with_emerge_defaults`); `--ignore-default-opts` /
  `--ask-enter-invalid` implemented.
- Residue filed as #69 (binpkg+newuse drop). Remaining host diff at
  the time: #65, #67, #68, #69.
- Trap: real parses argv twice (`_emerge/main.py`); `wants_help`
  stays before config; error precedence shifts (broken config fails
  before unknown-option). Tests must set a private env (conftest
  strips the var).

### #64 — `@world` gains `@profile` — DONE 2026-09-16

- Shipped: `Config::profile_packages`; `@world` = profile + selected
  + system; `@profile` is a first-class target.
- Residue: `-C/--deselect @world` expands `@selected`-only — separate
  item. `saveNomergeFavorites` unchanged (profile member not in
  `world`).
- Traps: `@system` = all levels, `*` kept; `@profile` = only repos
  declaring `profile-set`, unstarred lines only
  (`_sets/profiles.py`, `_sets/__init__.py:97-98`). Order:
  profile-first; system `sorted(key=str)`. Never infer `profile-set`
  from `portage-2`. Committed fixture declares no `profile-set`, so
  its pins must stay byte-identical — reproduce host shapes with a
  copied configroot.

### #67 — USE `-<prefix>_*` wildcard — DONE 2026-09-16

- Shipped: `apply_use_incremental(_iter)`; every USE token-list call
  site migrated. Host effect: xorg-drivers / xf86-video-* / full
  `sane_backends_*` reinstalls gone.
- Still cut: positive `foo_*` beyond IUSE-aware path;
  `USE_EXPAND` values in `package.use`; non-USE wildcards (real has
  none).
- Trap: prefix-removal runs *before* the exact removal per token
  (`config.py:2888-2960`); the fold is USE-scoped only — mask/force/
  keywords folds must not migrate. Land before #69.

### #68 — blocker on replaced installed version — DONE 2026-09-16, display repaired 2026-09-17

- Shipped: same-slot replaced check (installed `v` leaves the
  tracker if merge-bound `≠v` same slot); `owner_merging`; satisfied
  inline `b` row; graph-parents test (`deps` + `installed_closure`)
  replacing the vdb reverse scan. Host `@world` gtk-doc back to rc 0.
- Residue filed: #72 (`[uninstall]` rows), #73 (set-arg parents).
  Merge-path execution of the removal relies on the same-slot
  upgrade, not modelled separately.
- Traps: real `_validate_blockers` (`depgraph.py:9095-9255`):
  `blocked_initial` = vdb, `blocked_final` = tracker (graph +
  unreplaced installed). The runc `--oneshot` probe is the free
  unsatisfied-`B` oracle.

### #69 — binpkg + `--newuse` rebuild drop — DONE 2026-09-16, repaired 2026-09-17 (R0–R3)

- Shipped: `binpkg_respect_use_ok` follows real's gate
  (`depgraph.py:8260-8294`); no-ebuild binary compares against the
  *profile selection over its own IUSE*; `forced_flags`/`masked`
  subtracted before compare. Host qttools back as `[ebuild R]`.
- Traps: `identical_binary (:8001-8017)` is about the *installed*
  candidate, not the binary skip — never route the comparison to the
  ebuild unconditionally. `--usepkgonly` leaves respect-use unset
  (`create_depgraph_params.py:47-55`). Crafted `.tbz2` are rejected
  by real — the oracle pipeline must build a real binary first
  (`--buildpkgonly` + `--usepkgonly`, then add the flag).

### #14 — fetch candidates / mirrors — DONE 2026-09-14 (repair #70 R1–R5 2026-09-16/17)

- Shipped: `layout.rs` + per-mirror `layout.conf` + real-format
  `.mirror-cache.json` (TTL 86400, lazy); `tried_locations`;
  checksum→primary-URI switch + cap + deterministic rename;
  `fsmirrors` copy; one candidate list per distfile.
- Deliberate cut (D4): third-party mirror shuffle — deterministic
  order instead, digest-verified so correctness-neutral.
- Traps: `layout.conf` publishes `0=filename-hash BLAKE2B 8` — flat
  URLs 404, hashed 200. Cache must round-trip both ways
  real↔portuale. Download slices re-run L2. Never hand a `/`-rooted
  mirror entry to `wget`.

### #66 — installed build-time deps — WITHDRAWN 2026-09-16 (not a bug)

- "Do not implement the original S1": the host symptom was the
  pre-#63 options mismatch (`--getbinpkg` ⇒ `--usepkg` ⇒ `bdeps`
  unset), not a walk bug. Post-#63 both PMs agree on the probes.
- What landed: the both-ways pin only
  (`test_oracle_bdeps_default_of_an_installed_parent_drives_dependency_updates`).
- Trap: `optional` matters only when no child is selectable
  (`depgraph.py:3396-3398`); the killer is `bdeps`
  (`create_depgraph_params.py:97-103`, `depgraph.py:4194-4247`).
  Never resurrect the "never walk installed bdeps" rule (#74's pin
  guards it).
- Residue: bdeps-on `--deep` divergences stay a #65-§7/#74 follow-up.

### #90 — bt0 silent reconcile — proposed (residue of #62, owner-confirmed 2026-09-18)

- Shape: `--backtrack=0 slotconflictparent` — real silent rc 0,
  portuale records + rc 1. Recording divergence, not exit-code:
  #62's rule (registered ⇒ rc 1) stays.
- Design (parent `history/02.78-87-tier2-closeout.md` #62 S2): S0 oracle
  matrix → S1 forced-set (no visible change) → S2 removal + re-walk
  (flips the pin) → S3 pins + docs.
- Traps: `_solve_non_slot_operator_slot_conflicts`
  (`depgraph.py:1774-2106`) runs *unconditionally* (bt0 `0/0` yet
  reconciled). Port: skip-keys `:1784-1788`, child flood
  `:1808-1829`, `is_arg_parent` `:1903-1910`, `or_tuple` first-pulled
  `:1998-2006`, bug-522084 parent arc `:1933-1936` (over-removal if
  missed). Runs on every run — S2 must hold the L0 baseline.
- Link: #91's withhold may be this function's keeping-installed
  verdict — cite both ways if S2 closes it gratis, else it stays #91.

---

## Tier 2

### #59 — `--debug` narration fidelity — DONE 2026-09-16

- Shipped: per-instance parent-atom side-table
  (`Parent Dep: <atom> required by <parent>` under the resolved
  child, installed-shadow block); `--json backtrack:{restarts,max}`
  only — no stdout footer line.
- Still cut (K2): static preamble, `show_lru_cache_info`, timing
  value, backtracking-reason narration (#23 untouched), BFS-vs-LIFO
  order.
- Trap: `GraphEntry::required_by` is cp-keyed with no atom — the
  wrong source for narration; the side-table keyed on the resolved
  child is the fix. Argument seeds stay `(Argument)`.

### #65 — slot-op rebuild scan honours `--with-bdeps` — DONE 2026-09-16

- Shipped: `slot_operator_rebuild_scan(with_bdeps)` reads
  `DEPEND`/`BDEPEND` only when set, and on bdeps-on only for a
  consumer this pass *walked* (`entries`). Host: exactly the 19 Go
  consumers + go's `r` gone.
- Residue filed: #71 (runtime-key quirk), #74 (bdeps-on `--deep`
  package-set divergence).
- Trap: the first-draft unconditional drop was wrong — bdeps-on real
  rebuilds `runc` via `BDEPEND go:0/1.26.5=` (probes only
  `want_update` deps: `depgraph.py:3114,3802-3806`).
  `slot_operator_slot_change_probe` untouched.

### #71 — runtime-key slot-op rebuilds — DONE 2026-09-17, re-scoped, no product code

- S0 STOP: the premise was a staging artifact (`ROOT=$FX≠/`
  splits the cascade across two trees). Fix was bed-only:
  `FX_HOST_ROOTS=1`. Host-exact real rebuilds exactly what the
  current scan rebuilds in all 15 cells.
- Cut withdrawn: the original runtime-key gate. Untouched: #26,
  totals dup, #24b probe family, binary arms.
- Rule for next time: S0 requires the three `--debug` markers
  (`slot operator dependencies:`, `backtracking due to missed slot
  abi update:`, `forced rebuilds:`) before any code.

### #72 — `[uninstall]` / removal rows — DONE 2026-09-17 (Phase D B0b–B5)

- Shipped: `PretendOutcome::Uninstall` on the real `GraphEntry`
  (`required_by=[owner]`); renderer `[uninstall     ]` arm + `uninst`
  counter (after `binary`, excluded from `total_installs`);
  `--tree`/`--json`/`--columns`/`-q` handling.
- Residue: execution of the removal in a real merge stays skipped;
  non-`_validate_blockers` slot-cleanup removals → new follow-up if
  found via u7.
- Trap: `PretendOutcome` matches ~500 sites — no wildcard arm.
  Removal only for `owner_merging && installed_match` with the #68
  replaced-in-slot skip. `to <root>` row noted divergent (review P2).

### #73 — blocker set-arg parents — DONE 2026-09-17 (Phase C)

- Shipped: nomerge-owner arm ORs `installed_closure` (the #68 S3
  target-arm set; `top_level_cps` alone misses cell g); installed-
  owner rows join the trailing `[blocks B]`.
- Residue: `--buildpkgonly` `depends_on_order` clearing; virtual-
  provider blocker expansion; uninstall-task modelling = #72 (land
  #73 first, keep minimal).
- Trap: real `_validate_blockers` step 4 (`depgraph.py:9216-9246`) +
  `_complete_graph` (`:8660-8745`) + `_expand_set_args`
  (`:3271-3347`) — a set/atom argument *is* a digraph parent.
  Portuale's walked-`deps`-only scan missed it.

### #74 — bdeps `--deep` walk — DONE 2026-09-17 (Phase F)

- Shipped, Class A: atom-level `||` (`use_ok` disjunct), satisfied-
  by-installed early return, REQUIRED_USE `not built` gate,
  self-dep skip. Class B1: per-slot installed-node dedup keyed
  `(cat,pkg,vdb SLOT)`.
- Residue filed as #79 (Class B2: gtk-doc-am `B`/rc1 blocker-
  backtrack masks 1.36.1) — needs fixture + oracle; do not widen
  the fix. Host 13-package set is registry-specific; fixtures are
  the durable pins.
- Trap: real `depgraph.py:4194-4247,3796-3817,8660-8745`,
  `dep_check.py:377-402,765-809`. Cp-level vs atom-level installed
  match are different functions — don't conflate.

### `history/02.68-74.md` — batch #68–#74 — DONE 2026-09-17 (`backlog/68-74`)

- Order: #70 R1–R5 → #69 R0–R3 → #73 → #68/#72 B0–B5 → #71
  (bed fix) → #74. Filed out: #75/#76/#77 (display), #78 (bt0
  gate), #79 (B2 withhold).
- Traps T1–T12, still worth reading before touching fetch/blocker
  code: hand-built `FetchCommands` hid the parser bug (test via
  `resolve_config` + live `portage.settings` check); `None` =
  default vs error; `${FILE}` aborts only-if-renamed
  (`fetch.py:1715`); fixture-vs-host shapes differ by order;
  satisfied-inline vs unsatisfied-trailing (`output.py` +
  `_serialize_tasks`); no bare "exactly real's" claims.

### `history/02.68-74-review-through-B5.md` — audit, not an item (HEAD `04910f7`)

- Holds: slice order, B0 stop, Phase D match, B5 closeout (#68/#72
  DONE, #75/#76/#77 filed with unique numbers).
- Open defects to manage, in suggested order: D1 (#69 R2
  `retain(iuse)` over-filters — needs unfiltered variant + missing
  oracle cell G1/G2) → D5 (`ignored_binaries` block unmodelled,
  needs a new number after scanning all tiers) → D2 (#73 owner-set
  parent needs a discriminating container cell before gating) →
  D3+D4 (`substitute` env fallback, `shlex_token` EOF guards; real
  has no env: `config.py:438-465`) → P2 (`to <root>` cut into §6)
  → L0 re-run (record mtime + HEAD). Suites/bed were not re-run.

### `history/02.75-79.md` — batch #75–#79 — DONE 2026-09-18 (`backlog/75-79`)

- Shipped: #77 nomerge-owner `Uninstall{owner cpv}` anchored on the
  blocked instance; #76 `kept_alt_branches` (wait follows the kept
  branch — `||` collapses in `dep_zapdeps` before the serializer);
  #75 tree placement (C1 port; C2 serializer → #81); #79 D2
  stopped (not slot-feedback but reverse-dep pin); #78 verdict (b)
  (gate ≠ fix); review fixes #82 (edge unification) + #83 (scan
  cost) DONE.
- Residue: #80 (#77 A1 scan row); #81 (#75 C2 scheduling); #79
  (parent-set fix); #78 (→#26 probe-ordering); #84 (kept-branch
  threading); #85 (order); #86 (recorded `repository`); #87 (bed
  runner).
- Traps: grep portuale before writing "does not model"
  (`installed_dep_string`, `built_slot_operator_atoms`,
  `PORTUALE_DYNAMIC_DEPS_APPEND` exist); hoist only if the hoisted
  loop is outer (the kept-branch hoist was a ~3s pessimization);
  raw-vdb `!` prefilter rejected (`FakeVartree` reads the live
  ebuild); string-vs-`vercmp` tie-break.

### `history/02.78-87-tier2-closeout.md` — batch #78–#89 — DONE 2026-09-19 (`02.78-89-tier2`)

- Shipped in order #89 → #62 → #87 → #86 → #80 → #79 → #78 →
  #85 → #81: #89 recursion+filters on both readers; #62 rc 0→1 in
  the `pretend.rs` fail-band (next to `unsolvable_blockers`, after
  `--autounmask-only`, gated on non-empty conflicts — never notice
  text); #87 `l0-fixture-oracle-all.sh`; #86 vdb `repository`
  direct read (4 sites); #80 trailing-orphan row; #79 narrowed
  reverse-dep scan; #78 re-scoped to #26 (gate, not fix); #85
  time-boxed cut-or-fix; #81 tree `scheduled_uninstalls` on its own
  human-gated branch (`fc07b88`, reviewed 2026-09-19).
- Residue: #90 (#62), #91 (#79 host `@world`). #58/#59/#60 named as
  outside the batch; Tier 3–7 untouched.
- Traps: real rc rule = fail unless `--autounmask-only`
  (`actions.py:456-458`) or backtrack-off + buildpkgonly/fetchonly/
  fetch-all-uri/nodeps (`depgraph.py:9259-9271`). Bed probes need
  `-p` (container `ROOT=/` is real `/`). Commits are not
  pre-authorized; post-move paths are `../pmtest/…`; pair commits
  across the two repos.

---

## Tier 5

### #46 — md5-cache validation — DONE 2026-09-15

- Shipped: pregen `md5-dict` validated in `repo_aux_metadata`
  (`md5_dict.rs`, `Error::StaleMd5Cache`); EAPI short-circuit; all
  589 fixtures regenerated with real `egencache --update` +
  `portuale --regen` cross-check; `md5_cache_audit.py`,
  `test_fixture_caches.py`.
- Still cut (§7): `cache-formats` selection (#55); depcachedir
  location beyond `PORTAGE_DEPCACHEDIR`; no-ebuild entry for
  non-resolver readers (E4); `profiles/updates` on the read path.
- Traps: every fixture was once a stale `_md5_=000…` placeholder;
  empty `KEY=` ≠ absent key (grep readers first); backquote in
  `DESCRIPTION` fails the depend phase on both PMs; decision table
  E3 (no provider → return stale) / E4 (no ebuild → trust) / E5
  (provider fail → `Err`); `git clean -fdq fixtures/` deletes
  untracked fixtures.

---

## Tier 6

### #53 — merge order — DONE 2026-09-15

- Shipped: ordering honours hard edges under portuale's own
  `go-bootstrap` resolution; fixture
  `mogo/mogoboot/momd2man/mo{unzip,storage,common,conmon,podman}` +
  CASES + Rust pin; L0 podman row gone.
- Residue: G1(b) full parity (resolve go `||` to self + circular
  error like real) deferred — point at L0 finding D if pursued.
- Cut chosen: G1(a) — keep `b5b256f` bootstrap resolution, fix
  ordering only; L0 order metrics may move ± (net not worse).
- Trap: not a `:=` bug (bisection proves it); real's output here is
  `--tree` + circular self-dependency error, not a merge list.
  Fixtures need real md5s + `git add` before `git clean`.

### #54 — residual conflict reachability — DONE 2026-09-15

- Shipped: reachability gate on `reverse_dependency_constraints`
  (`required_set_reachable_cps`): unreachable non-graph consumers
  yield no pin; 4 contract pins rewritten to hermetic-real +
  keeper-reachable variants; 2 `owner:portuale-bug` allowlists
  removed.
- Residue surfaced: #57 (done); K1 exit-code parity filed
  separately (kept rc 0 informational at the time).
- Trap: the backlog text was incomplete — real auto-enables
  complete mode on ver/USE/slot change, not just in
  `_resolve_conflicts` (`_complete_graph 8562-8660`). S0 oracle
  matrix (world × case × gate) must confirm H before code (K4 stop
  rule). Rebase vs #53 and #46 S1.

### #57 — directly-requested hard-atom conflict — DONE 2026-09-16

- Shipped: `PassState::installed_slots` separate map; both-direction
  check (`AlreadyInstalled` before `other_outcomes` dedup + merge
  check consults `installed_slots`); solvable split via the existing
  backtracker; triangle + reverse-argv + solvable-control pins;
  `test_oracle_slotop_complete` xfail removed.
- Residue: K5 → #59; K1 exit-code → #60 (renumbered — the old #58
  number was taken twice). 3 latent bugs in `ec09c66` named in the
  backlog entry.
- Trap: the cited `lib.rs:~18268` is not the slot check — the gap
  was the `AlreadyInstalled` early branch bypassing
  `resolved_slots`. Argv-order asymmetry; unversioned `paired`
  solvable shape must stay silent
  (`_solve_non_slot_operator_slot_conflicts`); strict-xfail XPASS
  fails the suite; new fixtures need md5-cache entries.

---

## Tier 7

### #55 — `layout.conf` cache-formats — DONE 2026-09-15

- Shipped: `RepoConfig.cache_formats` + `pregen_md5_cache_enabled`;
  pregen rung used only when first *known* format is `md5-dict`
  and `FEATURES=metadata-transfer` is absent; `--regen` follows
  `egencache` targets (skips `pms`-only with message + rc 1).
- Still cut: no `pms` reader (deprecated 2012; slower, not
  different).
- Traps: `config.py` lowercases + splits, empty → auto (`md5-dict`
  then `pms`); `get_pregenerated_cache` takes the first only, so
  `pms md5-dict` misses. Overlap: #46 S2/S3 also edit
  `repo_aux_metadata` — second rebases.

### #56 — gpkg outer container — DONE 2026-09-15 (superseded in part by #58 S5)

- Shipped: shared no-follow check at all three outer sites
  (`read_gpkg_metadata`, `verify_gpkg_manifest`,
  `extract_gpkg_member`) + `--no-same-owner`; crafted-archive cells
  a–d,f,h (+ root-only e).
- Residue: inner `metadata.tar` walk filed as #58 (open at the
  time, since closed).
- Trap: real `gpkg.py _verify_binpkg` never extracts; portuale's
  `tar -xf` + follow-reads could read the host (symlinked prefix),
  block (FIFO), or loop (`/dev/zero`). Must error, never
  skip-and-win. Crafted archives scratch-only; device cells need
  passwordless `sudo`.

### #58 — metadata member types — DONE 2026-09-16

- Shipped (S0–S6): in-process `tar` crate reads; `read_inner_metadata`
  porting `extractfile`; regular-file writes on merge;
  `scan_image_tar` pre-scan; outer container in-process too (deleted
  #56's walk helpers). i1–i19 matrix. Amend #56 DONE as "superseded
  by #58 S5".
- Still cut: K4 nested `sub/KEY`; K5 abs/`..` + dup handling; K6
  portuale wording (no pin on real's exception text). K2 merge
  writes resolved bytes as a regular file (documented vs real
  symlink).
- Trap: real writer emits only `metadata/<KEY>` regular files; real
  reader rejects a `metadata/` dir. Decompressor stays a subprocess
  piped into `Archive::new`. S4/S5 touch every binpkg merge — needs
  L2 both directions + musl smoke. (Numbering: Tier-7 #58 vs old
  Tier-2 exit-code #58 → #60; check the number is free first.)

### #60 — gpkg populate read-errors — DONE 2026-09-16

- Shipped: `BinpkgError{Invalid,Fatal}`; `populate_local_pkgdir`
  skips Invalid with `!!! Invalid binary package: '<path>', <msg>`,
  aborts Fatal. Merge path (`verify_gpkg_manifest` /
  `extract_binpkg`) intentionally unchanged: abort-on-any.
- Still cut: `invalids` state, `--quiet` / `invalid_errors=False`,
  `Packages` reindex, merge-path `decompress()` handling.
- Trap: mirror real's classes (`bintree.py:1185-1199`) — *all* xpak
  errors are Fatal; portuale hardening (`..`, zip-bomb caps) rides
  Invalid. Retired the old "v1 cuts" doc paragraph (D2).

### #61 — musl smoke / toolchain drift — DONE 2026-09-16

- Shipped: `musl/Containerfile` 3.22.1→3.24.1 (rustc 1.87→1.96.1);
  `.cargo/config.toml` trailing `-Wl,-Bstatic`; `sched_param`
  literal → `zeroed()` + assign; workspace `rust-version=1.95`;
  structural `smoke_test.sh` (`capture` / `assert_pretend_ok` /
  `assert_rc`); `rfc2822_now #[allow(deprecated)]`.
- Still cut: no CI (opt-in `PORTUALE_RUN_MUSL_SMOKE=1`); no
  `unknown-linux-musl` cross; no bumps / `cargo update`; runtime
  stays `scratch`.
- Traps: 1.87 aborts per build-graph (1.95 floor covers them);
  `alpine-musl` lacks `crt-static-default` (`+crt-static` drags
  `-lc` after `-Bdynamic` — trailing `-Bstatic` last fixes it);
  musl `sched_param` has 4 extra fields vs glibc 1 (`E0063`);
  pilot pins go stale (`set -e` aborts on `blockerpkg`).

---

## Cross-cutting review

### `history/review-46-53-54-57-55-56.md` — audit applied 2026-09-16 (vs `main@d07f75a`)

- Verdict: all 6 plans implemented; divergences bookkeeping-only.
  Fixes A1–C4 were docs/YAML/docstring only — no source touched.
- Residue left open at the time: #58 (inner metadata.tar), #59
  footer/`Parent Dep`, #60 exit-code, retiring the 6 plans to
  `docs/history/` (owner call — this recap does it).
- Traps fixed here, don't reintroduce: duplicate #58 (Tier-2
  exit-code → #60); stale pre-rebase hashes vs `b6518f8`/`ec09c66`/
  `0b1ac8b`; `Status: proposed` left on DONE items; `05.0xx`
  prefixes on Tier-6 items (renamed `06.0xx`); §0 branch names ≠
  real branches; GLEP-82 self-contradiction in
  `glep-compliance-review.md`.
- Cut (§3 not checked): no suite / L0 / L1 / L2 / fixture-oracle
  re-exec; real line cites taken on trust; only `md5_cache_audit.py`
  re-ran (589/589 valid).

---

## Files retired to `docs/history/` (sources of the sections above)

- `01.014-fetch_candidates_mirrors.md`
- `01.063-emerge_default_opts.md`
- `01.064-world_profile_set.md`
- `01.067-use_incremental_prefix_wildcard.md`
- `01.068-blocker_replaced_version.md`
- `01.069-getbinpkg_newuse_rebuild.md`
- `01.90-real_silently_reconciles.md`
- `02.059-debug_narration_fidelity.md`
- `02.065-slot_operator_buildtime_deps.md`
- `02.066-installed_buildtime_optional.md`
- `02.071-runtime_key_slotop_rebuilds.md`
- `02.072-uninstall_merge_rows.md`
- `02.073-blocker_set_parents.md`
- `02.074-bdeps_deep_walk.md`
- `02.68-74-review-through-B5.md`
- `02.68-74.md`
- `02.75-79.md`
- `02.78-87-tier2-closeout.md`
- `05.046-metadata_md5-cache.opus.md`
- `06.053-merge_order.opus.md`
- `06.054-residual_conflict.opus.md`
- `06.057-directly_requested_hard_atom_conflict.opus.md`
- `07.55-layout_conf_cache_formats.opus.md`
- `07.56-gpkg_outer_container.opus.md`
- `07.58-check_metadata_member_types.md`
- `07.60-gpkg-populate-read-errors.opus.md`
- `07.61-musl-smoke-toolchain-drift.opus.md`
- `review-46-53-54-57-55-56.md`
