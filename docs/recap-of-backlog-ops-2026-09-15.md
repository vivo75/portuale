# Recap: backlog ops 2026-09-15 — agent read-only index

> **READ-ONLY — LLM AGENTS ONLY. DO NOT EDIT THIS FILE.**
> This is a closed historical index. Source of truth for status is
> `docs/backlog-tasks.md` + `docs/scope-backlog.md` + `git log`.
> Source of truth for what shipped is `docs/what-this-proves.md`.
> Do not add paragraphs here, do not re-open items here, do not update
> line numbers here. If a residue below is fixed, update
> `backlog-tasks.md` / `scope-backlog.md`, not this file.
> `file:line` citations below have drifted — re-locate by symbol name.

This file keeps only what a future session needs to **manage backlog
items without re-deriving it**: status, open residue, deliberate cuts,
revisit triggers, and traps that already cost a wrong turn. Mechanism
detail lives in the cited plan / findings / `what-this-proves.md` entry
and is not repeated here. Full pre-work docs are in `docs/history/`.

---

## #19 — abort-path model — DONE 2026-09-11

- Live spec: `docs/history/abort-path-spec.md`.
- Residue (not #19): plasma-meta/podman truncation = `||`-selection
  gap (`get_best_run`), parked separately.
- Trap: do not byte-match real's truncation *point* (DFS visit order);
  match membership / counters / exit 1 first.

## #22 — `dep_zapdeps` bins — DONE 2026-09-11 (+ tail #35)

- `solver_bridge.rs` was out of scope (closure over-approximation only).
- Lives in: `disjunction_preference` in `portage-repo` (both `||` call
  sites share one closure — updating only one caused F1 regression).
- Cut at the time, now closed via #35: `conflict_downgrade` /
  `installed_downgrade`. Still cut: `circular_atom`, `minimize_slots`,
  `want_update=false`.

## #23 — backtracking search — DONE 2026-09-11

- Design: `docs/history/023-backtracking_resolve.md`; oracle method: upstream
  `ResolverPlayground` case → fixture → diff (reused by #24/#35/#36).
- Open residue: `btnr` (`testBacktrackNoWrongRebuilds`) — installed
  package on the fast path never enters `resolved_slots`, so no slot
  conflict can form. Tracked under #25, not #23.
- `023-refactor-inventory.md`: reuse the *method* (exhaustive
  grep-and-verify of write sites / retry guards before splitting a big
  function), not its stale tables.

## #24 — slot-operator rebuild undo — DONE 2026-09-12

- Shipped: `_eliminate_rebuilds` ordered rules + graph-aware `:=`
  re-binder + `slot_operator_undone` latch; rebuild is a walked graph
  node (real `@__auto_slot_operator_replace_installed__` shape).
- Lesson: a522084 `B-0` was the complete-mode gate under `--deep`
  (S1), not the undo machinery (S4). Check gates before machinery.
- Open residue: bug 614390 stays `xfail(strict=True)` — same
  fast-path / `resolved_slots` gap as `btnr`; filed under #36.
- Still cut: `#24b`–`#24f`, `IUSE_EFFECTIVE`, `--rebuild-if-*`,
  `--debug` backtracking narration.

## #26 — installed metadata overlay — DONE-PARTIAL

- Model: two layers only, `InstalledMetaLayer::{Raw, Effective}`.
- Built-`:=` append stays behind `PORTUALE_DYNAMIC_DEPS_APPEND`
  (default off). Whole remaining blocker (post mirror-removal):
  `test_oracle_slotop_undo_cascade`.
- Interaction: `is_injected_libc` strip in `merge_order.rs` exists
  because that call site reads `Raw`. Moving it to `Effective` makes
  the strip dead code on that path — re-read its doc comment first.

## #25 — `_complete_graph` installed nodes — DONE-PARTIAL 2026-09-15

- R3b fixed the pre-bias divergence (real sorts `SetArg` atoms
  `sorted(key=str)`; portuale now sorts the `@system` segment).
- F-B4 theory ("no edge / vanishing in-edges") was wrong twice and
  reverted. Real mechanism (merged-stream `--debug` oracle): solver
  removes the superseded node then **re-walks parents to redirect**
  edges; gedit/nautilus abort that re-walk partway at
  `gcr[gtk]` (CPython set-order dependent, not portable). Filed as
  cluster-A abort residue (`known-divergences.yaml`), not a #25 gap.
- Genuinely open: installed instance as slot-conflict *party* needs a
  resolver change (same root cause as `btnr`/614390). Minor residues:
  `virtual/man` pop-timing, docbook hash-seeded swap.
- Lesson: capture real `--debug` merged `>log 2>&1` with
  `PYTHONUNBUFFERED=1`; separate-stream capture hid walk/solver/dump
  ordering and produced the wrong theory.

## #28 — mrg-director slots — DONE 2026-09-15

- Decision D6 "slots only": `Director` stays test-only by owner
  decision. Every slot has one production implementation.
- Promote only on: second implementation needing runtime choice, a
  non-`emerge` frontend, or wanting the `action_build` stage split.
- Consumer-less by design (not bugs): `reverse_dependents`,
  `PkgdirBinIndex`, `VolatileCache`.

## #35 — downgrade guards — DONE 2026-09-15

- No new structure was needed: the resolver's in-progress `entries`
  **is** the live `graph_db`. Read it from `disjunction_preference`.
- L0-neutral by construction (guard rarely fires on corpus); fixture
  oracle is the proof.

## #29 — L2 archive producer — DONE 2026-09-14

- Layer rule: L0 = resolver, L1 = identical prebuilt binpkgs, L2 =
  archive producer + cross-install, L3 = own bytes from source.
  File findings to the right layer.
- Gate rule: compiled-payload bytes never gate except hand-written
  `porttest` fixtures (`--tolerate-payload`); never allowlist as
  `compiler-nondeterminism`.
- Traps: `diff.py` layer filter was hardcoded to `l1`; gpkg intent is
  flat `metadata/<KEY>`, not xpak paths; `SOURCE_DATE_EPOCH` is L3.

## #30 — L3 source-build parity — DONE-PARTIAL

- `l3-core` / `@system` unblocked, full run user-triggered (not run at
  recap time).
- Bed rules: `emerge @system` alone is a no-op — need `-e` +
  `--usepkg=n` on both PMs; determinism blocks go in resolved config
  (`make.conf`), not shell env; disable `userpriv/usersandbox/
  userfetch/usersync` (portuale non-goals); partial run ≠ parity run
  (mark `partial`, keep log); never compare the running
  `portage`/`python` themselves.

## #37 — build-phase env — DONE 2026-09-13

- Real exports resolved-config ∪ process-env minus `environ_filter`
  (phase 1), whitelist only from phase 2 via saved `$T/environment`.
  Whitelist-only export can never match `environment.bz2`.
- `USE` in phase env == `PORTAGE_USE` (IUSE-filtered + implicit
  USE_EXPAND; never empty).
- Lives in: `portage_profile::phase_environ` + `entry_phase_env`;
  `depend` phase env deliberately untouched.

## #38 — packaging transforms — DONE 2026-09-13

- No stripper was written: real `install_qa_check` already runs; the
  gates were false for lack of #37 (`FEATURES`, `PORTAGE_COMPRESS`).
- `instprep` is merge-time, idempotent, first step of `treewalk` —
  implemented by explicit user decision (blast radius: every merge).
- Filed here as #42 (closed): missing pre-build `clean` phase
  (`run_clean` at real's positions).
- Residue by construction: `setuid` `.build-id` target is real's own
  `estrip` race (real disagrees with itself) — one allowlist row.

## Tier-1 brush (#5/#6) + fetch (#14) — code DONE 2026-09-14

- Brush: 5 fixes on thin fork `vivo75/brush`, portuale re-pinned.
  Still open: opening upstream PRs needs user GitHub auth (#5).
  Default stays `bash` until PRs land.
- Fetch: `layout.conf` negotiation + `.mirror-cache.json` (real
  format) + `tried_locations` + `fsmirrors` copy shipped. Deliberate
  cut: third-party mirror shuffle (deterministic order instead;
  digest-verified, correctness-neutral).

## Tier-5-and-2 plan waves 1–8 — all closed 2026-09-15

- #17 CLOSED as deliberate cut (owner time box D5): seed-order levers
  measured zero effect on all 120 L0 probes; residue is installed-node
  drain timing in `_serialize_tasks`, needs per-node tracing across
  the ~400-node graph. Lever table: `TEST/findings/l0.md` "R5".
- Owner decisions D1–D7 are binding (abort framing, mirror scope,
  `depcachedir` write-back, per-slice L0 movement budget, #17 box,
  director scope, #25 close on R3c-revalidation). Full rationale:
  plan doc §3.

## Oracle methodology (reused #23/#24/#26/#35/#36)

- Translate the matching upstream `lib/portage/tests/resolver/*.py`
  case through the vendored pinned `ResolverPlayground`; read actual
  output, don't reason from source.
- Traps: a test file may rebind `test_cases` before the loop (check
  what actually runs); pin `PYTHONHASHSEED=0`; point
  `PORTAGE_GNUPGHOME` at a writable `chmod 700` copy of the fixture
  keyring.
