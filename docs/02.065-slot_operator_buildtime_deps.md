# #65 — slot-operator ABI rebuild scan ignores `--with-bdeps` (and over-reaches when build deps are walked)

Status: **done 2026-09-16 — S0/S1/S2/S3 shipped** (S0 `98ca561`, S1
`4e7bad7`, S2+S3 this commit; the bed is green at
`TEST/logs/l0-fx-20260916T153958Z`, 0 unexplained, and the host `@world`
diff lost exactly the 19 Go consumers). The plan was revised 2026-09-16
after plan review (originally written against `main` @ `e54bd09`;
revision grounded on `explore/real_emerge` @ `0de42ac`, post-#63); the
bdeps-on `--deep` walk divergences in §7 are the still-open follow-up.
One new residue was filed while shipping: #71 (runtime-key `IDEPEND`
graph-completion quirk).
Backlog entry: [`backlog-tasks.md`](backlog-tasks.md) Tier 2 #65.
Evidence: [`docs/evidence/2026-09-16-post63-probes/`](evidence/2026-09-16-post63-probes/README.md)
(current host `@world` diff + bdeps-on/off single-atom probes) and the
original [`docs/evidence/2026-09-16-world-uDpvN/`](evidence/2026-09-16-world-uDpvN/README.md)
(`portuale-abi_rebuilds.json`, 22 pairs, 20 of them `dev-lang/go-1.27.1 →
<Go consumer>`).
Interaction: #66 was withdrawn (its symptoms were the same `bdeps`
option mismatch, fixed by #63); this plan owns the rebuild-cascade half,
which #63 did **not** fix.
Branch: `backlog/65-slotop-buildtime-deps` (user merges).

**Revision note.** The first version proposed dropping `DEPEND`/`BDEPEND`
from `slot_operator_rebuild_scan` unconditionally, on the theory that a
built package's build-time deps are "optional" and never trigger a
rebuild. That is wrong: with build-time deps walked
(`--ignore-default-opts`), real **does** rebuild `app-containers/runc`
(`rR`) through its `BDEPEND` `dev-lang/go:0/1.26.5=`. The real gate is
the `bdeps` parameter, plus (with build deps on) *which* consumers get
probed.

**Read first:** `AGENTS.md`, [`agent-context.md`](agent-context.md), this
file, then `slot_operator_rebuild_scan` and
`slot_operator_slot_change_probe` (`rust/portage-repo/src/lib.rs`) and
its three callers (the `Backtracker` pass and the two
`assemble_result`-side calls), and `with_bdeps` in `pretend.rs`
(`with_bdeps = with_bdeps_auto && !(usepkg || …)`). Find code by symbol
name; line numbers drift.

## 0. Rules for every slice

- **Every slice lands as its own commit**, subject `#65 S<n>: ...`, with
  the `Co-Authored-By` trailer. No squashing, no push.
- Expected behaviour is grounded in real Portage 3.0.82.2
  (`_emerge/depgraph.py`, `_emerge/create_depgraph_params.py`, citations
  below) plus oracle captures (fixture-oracle bed / host `emerge`).
- Full verification pass per `AGENTS.md` step 8; **re-run the L0
  fixture-oracle bed** (this touches #24's slot-operator surface).
- Corpus drift reviewed row by row; stop and report if an oracle cell
  contradicts the model.

## 1. What real Portage does (source-grounded)

- **The `bdeps` gate.** `create_depgraph_params.py:97-103`:
  `myparams["bdeps"] = "auto"` only when `--with-bdeps` is unset,
  `--with-bdeps-auto != n` and `--usepkg` is not in effect
  (`--getbinpkg` implies `--usepkg`, `actions.py:3719-3720`).
  `_add_pkg_deps` (`depgraph.py:4194-4247`): for a built package with
  `bdeps` not `y`/`auto`, `ignore_build_time_deps = True`, and unless a
  `--rebuild-if-*` mode is active (`self._rebuild.rebuild`) `DEPEND` and
  `BDEPEND` are emptied **before** any dep is added. An emptied dep is
  never passed to `_add_pkg`, so it is never registered by
  `_add_slot_operator_dep` (`depgraph.py:3810-3815`) and can never trigger
  `_slot_operator_trigger_reinstalls` (`depgraph.py:3089-3132`).
- **Who gets probed when build deps are walked.** Every `:=` dep that
  reaches `_add_pkg` is registered, build-time or not. The trigger then
  only probes when `want_update_probe = dep.want_update or not
  dep.parent.installed` (`:3114`), and `dep.want_update = not
  complete_mode and (arg_atoms or update) and not too_deep(depth)`
  (`:3802-3806`). An installed consumer reached only through
  `_complete_graph` (complete mode) is not probed.
- **Live, host defaults (`--getbinpkg` ⇒ bdeps off), `@world`:** real
  `[binary gU] dev-lang/go-1.27.1-1` with **no `r` flag** and **no**
  consumer rebuilds. Portuale marks go `r` and rebuilds 20 Go consumers
  (`rR`) — the scan reads vdb `DEPEND`/`BDEPEND` regardless of
  `with_bdeps`.
- **Live, `--ignore-default-opts --oneshot app-containers/runc`** (bdeps
  auto): real `rR` only `dev-go/go-md2man` and `app-containers/runc`
  (both reached from the argument walk); portuale `rR` 18 Go consumers
  including world-only ones (helm, kubectl, podman, …).
- Runtime control: runtime consumers of a moved sub-slot are rebuilt
  (`@__auto_slot_operator_replace_installed__`); #24 shipped this and it
  must keep working.

## 2. Portuale today

`slot_operator_rebuild_scan` scans every **reachable** installed
package's vdb deps with a hardcoded
`["RDEPEND", "PDEPEND", "DEPEND", "BDEPEND", "IDEPEND"]` list and
schedules a consumer whenever the provider entry's `(slot, sub_slot)`
differs from the consumer's built `:S/SS=` atom. It takes no
`with_bdeps` argument. `reachable` is the complete-mode set (arguments
plus `@world`/`@selected`/`@system` forward-installed deps, #54), so it
also covers consumers real would reach only in complete mode.

`slot_operator_slot_change_probe` reads all five keys of **merge-bound**
parents' tree metadata. That half is correct by construction (a merging
parent's build deps are required, `optional=pkg.built` is false) and
must not be narrowed.

The same scan drives the `r` bracket column on the provider, the
"The following packages are causing rebuilds" block and merge order.

## 3. Design

**S0 — oracle matrix.** Fixture: `provpkg-1.0:0/1` installed with a tree
`2.0:0/2` upgrade; installed consumers `cons{rdep,dep,bdep,pdep,idep}`
each with a built `:0/1=` atom in exactly one dep key. Two reachability
shapes: (i) consumers in `@world` only, run `emerge -puDvN @world`;
(ii) one consumer given as the argument, run `emerge -puDvN --oneshot
<consumer>` with the other consumers in world. Each × three option
sets: default (bdeps auto), `--usepkg` (or `--with-bdeps=n`),
`--with-bdeps=y`. Stage the fixtures in
`TEST/layers/l0-fixture-oracle/stage.sh` in this slice. Record the
`rR`/`r` columns and `--debug`'s `slot operator dependencies:` dump.
Expected (to confirm, do not assume):

| key | bdeps off | bdeps on, argument-reached | bdeps on, world-only |
|---|---|---|---|
| RDEPEND `:=` | rebuild | rebuild | rebuild (via complete-mode conflict path, #24) |
| IDEPEND / PDEPEND `:=` | confirm | confirm | confirm |
| DEPEND / BDEPEND `:=` | **no rebuild** | **rebuild** (host runc) | **confirm** (host: no rebuild) |

**S1 — honour `with_bdeps`.** Thread `with_bdeps` into
`slot_operator_rebuild_scan` (all three callers already have
`ctx.with_bdeps` or the request's value) and read `DEPEND`/`BDEPEND` from
the vdb only when it is true. Keep `RDEPEND`/`PDEPEND`/`IDEPEND`
unconditional. Do not touch `slot_operator_slot_change_probe`. Update the
function's doc comment (cite `create_depgraph_params.py:97-103` and
`depgraph.py:4194-4247`). This alone removes all 20 host `@world` pairs
and the spurious `r` on `dev-lang/go`.

**S2 — build-time keys only for update-probed consumers (bdeps on).**
S0 confirmed the world-only cell ("no rebuild" for `DEPEND`/`BDEPEND`,
and the `@world` shape still rebuilds because there the consumers are
walked seeds). Shipped as the narrower set the plan suggested: the scan
builds `walked` = every cp in this pass's `entries` (installed nodes
included) and, when `with_bdeps` is true, reads `DEPEND`/`BDEPEND` only
for a consumer in `walked`. `RDEPEND`/`PDEPEND`/`IDEPEND` keep the wider
`reachable` gate — the oracle showed the runtime keys do **not** follow a
uniform `want_update` rule (world-only runtime consumers rebuild from the
conflict path, modulo the `considep` quirk), so narrowing them would
have walked into #24's territory. Two residues: #71 (the `considep`
quirk) and §7.

**S3 — pins + docs.** §5; `what-this-proves.md`, backlog status, the
fixture-oracle allowlist for the two adjudicated rows.

## 4. Slices and difficulty

| slice | commit subject | contents | difficulty | landed |
|---|---|---|---|---|
| S0 | `#65 S0: oracle matrix` | fixtures + staging + real captures, findings note | medium | `98ca561` |
| S1 | `#65 S1: slot-op rebuild scan honours --with-bdeps` | `with_bdeps` parameter, key gate, doc comment, unit tests | easy | `4e7bad7` |
| S2 | `#65 S2: build-time slot-op rebuilds only for update-probed consumers` | narrower set for DEPEND/BDEPEND when bdeps is on (only if S0 confirms) | hard | this commit |
| S3 | `#65 S3: pins, host diff, docs` | contract pins, fixture-oracle run, host `@world` re-diff, `what-this-proves.md`, status | easy | this commit |

S1 carried the whole host-default win (the 19 Go consumers and go's `r`);
S2 followed because S0 confirmed the world-only cell exactly.

## 5. Tests and pins

Rust (portage-repo, next to `slot_operator_rebuild_scan_flags_only_the_stale_bindings`
and the `test_oracle_slotop_*` tests; keep the existing runtime and
ignore-gate assertions):

- BDEPEND-only consumer, `with_bdeps=false` → not scheduled, no pair.
- same with `DEPEND` → not scheduled.
- BDEPEND-only consumer, `with_bdeps=true`, argument-reached → scheduled
  + pair.
- RDEPEND control under both `with_bdeps` values → scheduled + pair.
- `IDEPEND`/`PDEPEND` pinned to S0's captures.
- (S2) BDEPEND-only world-only consumer, `with_bdeps=true` → per S0.

Contract / oracle:

- Shipped as `test_oracle_slotop_rebuild_scan_honours_with_bdeps`
  (contract pin on the six-consumer fixture: world default /
  `--usepkg` / `--with-bdeps=y`, plus the `consdep`/`consbdep` argument
  shapes both ways), not a `CASES` entry — the matrix needs the staged
  vdb, which the fixture-oracle bed provides.
- Host re-diff: **done**, exactly the 19 Go consumers disappeared from
  `-uDpvN @world` (nothing added, nothing else removed); `dev-lang/go`
  is `[binary gU]` with no `r`.
- Fixture-oracle run **green**:
  `TEST/logs/l0-fx-20260916T153958Z`, 22 explained / 0 unexplained, with
  the two adjudicated allowlist rows (totals display duplication,
  #71's residue).
- The `slotop*` contract/oracle pins stayed green unchanged; the
  bdeps-on `--oneshot app-containers/runc` probe is the #65 follow-up
  (slice 15).

## 6. File-conflict map

- `rust/portage-repo/src/lib.rs`: `slot_operator_rebuild_scan` signature
  + 3 callers + doc comment + unit tests; S2 also the reachability set.
- `rust/portuale/src/pretend.rs`: none expected (display consumes the
  pairs).
- `TEST/layers/l0-fixture-oracle/stage.sh`, `fixtures/repo/…`,
  `tests/test_emerge_pretend_contract.py`.
- `docs/what-this-proves.md`, plan + backlog status.
- Shares `lib.rs` with #68 (`resolve_blockers`) and #69
  (`binpkg_respect_use_ok`), different functions; rebase-level conflicts
  only.

## 7. Non-goals and residue

- `_slot_operator_update_probe`'s replacement-parent search and
  `_eliminate_rebuilds` rules beyond #24 S4.
- `slot_operator_slot_change_probe` (merge-bound parents): correct, do
  not touch.
- Other bdeps-on `--deep` walk divergences seen in the re-probe
  (portuale adds `dev-lang/rust-1.94.0` `NS`; omits
  `dev-build/gtk-doc-am`, `mdit-py-plugins`, `myst-parser` and the
  gtk-doc blocker). Not slot-operator issues; pursued as the #65
  follow-up (slice 15), not part of S0–S3.
- **#71** (new, filed while shipping S2): the runtime-key `IDEPEND`
  world-only consumer real rebuilds only in some argument shapes;
  allowlisted for the bed and left for #24's trigger/graph-completion
  work. Runtime keys were deliberately left with the `reachable` gate
  because the oracle showed no uniform `want_update` rule there.

## 8. Risks

- **Over-narrowing (S1):** `with_bdeps=true` must keep today's
  behaviour for DEPEND/BDEPEND; the runc probe is the guard.
- **S2 depth semantics:** `want_update` also depends on
  `--deep=<n>`/`_too_deep`; the oracle must include a bounded-depth
  cell if S2 is implemented.
- **Cascade interactions:** fewer scheduled rebuilds change the `r`
  column, the "causing rebuilds" block and merge order on host-scale
  trees (intended); the L0 bed and host diff are part of done.
