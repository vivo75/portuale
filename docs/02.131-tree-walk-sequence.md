# Phase 13 (#131 + #150) — `--tree` walk sequence

Status: **done 2026-09-24** (slice 0 `07417c59`, S1 + S2 + close-out in
this commit; pmtest branch same name: `c937660` + S1/S2/close-out). Part of [`batch-2026-09-23.md`](batch-2026-09-23.md)
(read its §0 runbook first — it applies verbatim and is not repeated
here). Backlog: [`backlog-tasks.md`](backlog-tasks.md) Tier 2 **#131**
(the `--tree` walk-sequence residue from #81) and Tier 2 **#150** (the
`--tree` shared-child `[nomerge]` artifact, #148 S1 residue, kin of
#131 — owner-confirmed in scope for this phase).
Branch: `backlog/131-tree-walk-sequence` (open in **both** repos).
Predecessors: `docs/history/02.78-87-tier2-closeout.md` §5 "#81 S3",
`fc07b88`'s commit body, `../pmtest/differential-test-bed/findings/l0.md`
"#75 C0"–"#75 C2", Phase 8's `merge_order.rs` unit tests
(`06.144-146-merge-order-unit-tests.md`, DONE — the `tree_*` arms S1/S2
may touch are pinned, so any move is a visible diff), Phase 9's #142
row-placement fix (which already moved u6 — see S0 verdict), Phase 10's
#148 (whose flat list is byte-real; only the tree layout is this
phase's surface).
**Oracle:** bed `l0-fx-20260924T090502Z` over atomlist
`differential-test-bed/atomlists/l131-s0.txt` (three cells, both sides,
real 3.0.82.2). **Frontier model or the owner** — the blast radius is
every `--tree` pin.
Merge-path gate: **no** (pretend/display only; if the diff reaches
`ebuild_merge.rs`, `emerge_build.rs` or `emerge_getbinpkg.rs`, the slice
is in the wrong phase).
Bed at close: **yes** — L0 (tree cells), numbers quoted.

## 0. Rules for this phase

- **Fixtures exist; no new fixture is expected.** `blockerpkg` /
  `graphblockerparent` (u6), `p2btarget` / `p2bowner` / `newpkg` (p2b),
  `slotusegroup` + `slotusetarget` / `slotuseplain` / `slotusex` /
  `slotusey` (slotusegroup) already isolate the shapes. A twin needs a
  `fixtures/repo` + md5-cache collision check first.
- **The oracle is quoted verbatim; S1/S2 move only the named rows.**
  More than the S0 verdict's rows moving is a stop.
- **#27's `SlotConflict::parents` filing and #142's flat placement stay
  green.** The `slotconf*` pins and the B4 u6 flat pin are the gates; a
  fix that requires rewriting either filing is a residue with a new
  number (next free after scanning **all** tiers — currently **#157**),
  not part of this phase.
- **Corpus drift is a STOP unless the slice says it moves output.**
  S0 moves nothing. S1/S2 move `--tree` keys only — bless reviewed keys,
  in pmtest's commit only.
- **No product code before S1's pin list is written.** The list of every
  `--tree` CASES entry, `test_oracle_81_…` pin and tree-invariant check
  S1/S2 move goes in the cluster file first (S1/S2 sections below name
  them; fill exact names before code).

## S0 — oracle re-run + residue triage (this phase's oracle slice)

Re-run the three cells on both sides (bed
`l0-fx-20260924T090502Z`, atomlist `l131-s0.txt`; portuale rev
`65fe11f1`, real 3.0.82.2; argv identical, `emerge -p` + cell argv,
`--color=n` on the real side only). Comparator verdict: 3 probes,
1 clean, 4 unexplained (1 missing, 2 order, 1 extra).

### S0 verdict (filled 2026-09-24 — no product code in this diff)

1. **u6 (`--tree dev-libs/blockerpkg dev-libs/graphblockerparent`,
   rc 1 both) is CLEAN — findings `[]`.** The #131 residue arm 1
   ("u6's `blockerpkg-1.0` line position") is gone: Phase 9's #142 S2
   F4b flat-order fix (the blocker owner waits on its unscheduled
   removal, so `blockerpkg` schedules after the
   `blockerpartnerpkg`/`weakblockerparent` pair) already moved the flat
   order onto real's tree-retlist order. Both sides print, byte-identical
   modulo real's ` to <root>/` suffix:
   `[nomerge] blockerpkg-1.0` / `[blocks b]  samepkg …` /
   `[uninstall]   samepkg-1.0` / `[ebuild N] blockerpkg-1.0` /
   `[ebuild N] graphblockerparent-1.0` /
   `[ebuild N]  weakblockerpkg-1.0` /
   `[ebuild N]  blockerpartnerpkg-1.0` / `[blocks B] blockerpartnerpkg …`.
   No code for this arm; the B4 flat pin and the u1/n5 tree pins are its
   standing coverage.
2. **p2b (`--tree --update dev-libs/p2btarget dev-libs/p2bowner
   dev-libs/newpkg`, rc 0 both) keeps two findings.** Real:
   `[U] p2bowner-1.1` / `[N] newpkg-1.0` / `[nomerge] p2bowner-1.1 [1.0]` /
   `[blocks b]  <p2btarget-2.0 …` / `[U]   p2btarget-2.0 [1.0]`.
   Portuale: `[U] p2bowner-1.1` / `[blocks b]  <p2btarget-2.0 …` /
   `[U]   p2btarget-2.0 [1.0]` / `[N] newpkg-1.0`. Residues: (a) real
   interleaves `newpkg` second and trails the `[nomerge] p2bowner`
   owner occurrence with the block + replacement nested under it
   (depths 1, 2); portuale hangs the block + replacement under the
   merge row and trails `newpkg` last (`[missing] nomerge p2bowner`,
   `[order] #1: real newpkg vs portuale p2btarget`). The #131 text's
   "`[N]`-vs-`[U]` dep child" does not reproduce on the current
   fixtures (both sides print `[U] p2btarget-2.0`, `[N] newpkg-1.0`) —
   recorded as stale, not targeted. Real source to trace in S1:
   `output_helpers.py::_ordered_tree_display` (`:434-495`) consuming
   the reversed tree-mode retlist (`depgraph.py:10618-10619`) — real's
   tree display list is the tree-mode serializer's order (no greedy
   leaf pop, `:9764-9777`), not the flat merge order portuale's
   `node_order` (`pretend.rs:1925-2317`, resolver-index order) walks.
3. **slotusegroup (`--tree -D dev-libs/slotusegroup`, rc 1 both) keeps
   two findings — this is #150.** Real:
   `[N] slotusegroup-1.0` / `[N]  slotuseplain-1.0` /
   `[N]   slotusetarget-2.0 USE="(-x)"` / `[N]  slotusey-1.0` /
   `[N]  slotusex-1.0` / `[N]   slotusetarget-1.0 USE="x y"`.
   Portuale: `[N] slotusegroup-1.0` / `[N]  slotusey-1.0` /
   `[N]  slotusex-1.0` / `[N]  slotuseplain-1.0` /
   `[N]   slotusetarget-2.0` / `[nomerge] slotusex-1.0` /
   `[N]  slotusetarget-1.0` (`[extra] nomerge slotusex`,
   `[order] #1: real slotuseplain vs portuale slotusey`). Two facets:
   (a) the `[nomerge]` artifact — portuale's walk re-descends the
   already-displayed `slotusex` as an ancestor when placing the shared
   child `1.0`, where real nests `1.0` directly under `slotusex`'s first
   occurrence (`output.py:612-614` re-labels ancestor re-occurrences;
   the line means the walk met `1.0` with no live chain to `slotusex`);
   (b) the sibling order — portuale meets the `slotusegroup` children
   in reversed-flat order (`slotusey` first), real in tree-retlist
   order (`slotuseplain` first). Same candidate root cause as p2b (2):
   the display list portuale walks is not real's tree-mode retlist.

### S0 hygiene (in this diff)

- **#150 number collision fixed (and a second one found).** Two items
  claimed #150: the `--tree` shared-child item (filed 2026-09-23, #148
  S1 residue) and the L3 control-noise item (filed 2026-09-24, Phase
  7a). The L3 item is renumbered to **#152** (`backlog-tasks.md` entry
  + pmtest `findings/l3.md` note; the merged Phase 7a commit message
  keeps its `#150` text as history). While fixing it a second
  collision surfaced: the L1-gate item (Phase 12, Tier 2) also claims
  #151 indented, invisible to the `^[0-9]+\.` scan -- both colliding
  lines are de-indented to column 0 (root-cause fix for the scan), the
  tree item keeps #150, L1 keeps #151. (An interim #151 renumber of the
  L3 item was corrected to #152 in the same slice.) Next free number
  was **#153** then; residues #153–#156 took it to **#157**.
- **Deliverable:** this file + the renumber + the pmtest atomlist/logs.
  **No product code, no pin edits, no fixture edits in this diff.**

## S1 — the #131 fix (p2b walk sequence) and its pins

- [ ] **Step 1: failing pins first.** Rewrite
  `test_oracle_81_tree_only_satisfied_row_from_the_stuck_serializer`'s
  "surrounding walk sequence stays portuale's own" paragraph into
  assertions of real's S0 bytes (newpkg second, `[nomerge] p2bowner`
  occurrence, block + replacement nested under it; flat assertions
  unchanged). Watch it fail on the current four-row list. Controls that
  must already pass and must not move: the u1/n5 `test_oracle_75_…`
  pins, diamond/treeorder/onlydeps/dualslot pins, the B4 flat u6 pin.
- [ ] **Step 2: the fix, at S0's named site.** Expected shape (S0
  confirms or corrects): `print_tree`'s `node_order` / ordered-walk
  sequencing (`rust/portuale/src/pretend.rs:1925-2317`) must follow
  real's tree-mode retlist order (tree-mode serializer, no greedy leaf
  pop) instead of the flat order, so the walk meets `newpkg` before the
  blocker nesting and keeps a live chain to the owner occurrence real
  re-uses. Constraints: flat output, `--json`, `-v` counters outside
  the moved rows, rc, and the Phase-8 `tree_*` unit tests move only as
  visible, intended diffs.
- [ ] **Step 3: Rust unit test** in the relevant crate's `mod tests`
  over the fixture root: the p2b tree display contains the `[nomerge]
  p2bowner` occurrence with the block + replacement nested under it.
  RED-verified against the pre-fix product.
- [ ] **Step 4: run the blast radius.** `cargo fmt --check`,
  `cargo clippy --release --all-targets` (zero warnings),
  `cargo test --release`, then the contract suite diffed **by test
  name** against the pre-S1 baseline. Named blast radius: every
  `--tree` CASES entry, every `test_oracle_75/81/82/85_…` pin, the tree
  invariant checker (`test_output_invariants.py`), and the corpus
  (`--tree` keys inventoried before any bless).
- **Stop rules:** a pin outside the named blast radius changing; flat
  output or rc moving anywhere; the fix requiring scheduler *policy*
  changes (which packages merge in which order outside the named tree
  cells) — that is #17 territory, file it.

### S1 verdict (filled 2026-09-24)

One product change with three parts, all RED→GREEN gated (contract pin
`test_oracle_81_tree_only_satisfied_row_from_the_stuck_serializer`
rewritten to real's full five-row bytes; Rust unit tests
`tree_display_order_sequences_the_p2b_tree_like_real` +
`tree_display_order_schedules_an_admitted_removal_after_its_owner`).

1. `merge_order::tree_display_order` (new, `pub` re-exported): runs
   `schedule_graph` + `select_nodes` in tree mode (greedy batch
   disabled, stuck branch live) and returns the merge-direction entry
   order with the shared leftover weave-back. `print_tree` builds its
   `node_order` from it and walks it reversed -- exactly real's
   `_ordered_tree_display` over the reversed tree-mode retlist
   (`depgraph.py:10618-10619`). Grounding: a `sitecustomize` probe in
   the bed container dumped real's `mylist` + graph edges for p2b,
   slotusegroup and u1 (scratch, `/tmp/probe`, reverted); the p2b
   round trace (`difference_update` batches) shows real selects
   `[p2btarget]` single round 2 (it has parents via the admitted
   uninstall) and batches `[newpkg, p2bowner]` round 3 in graph order.
2. Synthetic-uninstall edge flip (`tree_schedule_stuck`): the synthetic
   node is now the replacement's **parent** (`children[u] ∋ repl`,
   real's `mygraph.add(slot_node, uninst_task)`, `:10190`), not its
   child. As a child the replacement rejoined a later batch; as a
   parent it is the only node with parents and is picked single the
   next round. One Phase-8 test moved as an intended diff
   (`tree_schedule_stuck_pins_guards_and_node_bookkeeping` asserts the
   parent direction now); `tree_note_selection` is untouched (both
   selection arms still pinned, the replacement arm is now the live
   one).
3. Admitted removals wait on their owners in tree mode (real reverses
   the validation edge on admission, `:9998-10020`; portuale's
   virtualization only had the owner-waits half). Without it u1's
   admitted `samepkg` uninstall schedules ahead of `blockerpkg` and the
   display opens on the merge row. Tree-mode only -- flat keeps its
   standing batch behavior. Plus: the roots-last-resort batch now
   follows `g.order` (real `get_nodes()` is graph order, not heap
   order).
4. Three diagnoses that did NOT become code, recorded so nobody
   re-derives them: (a) the `#131` text's "`[N]`-vs-`[U]` dep child"
   does not reproduce (both sides print `[U] p2btarget-2.0`,
   `[N] newpkg-1.0`) -- stale, not targeted; (b) the round-3
   `[newpkg, owner]` batch order is real's graph (discovery) order --
   portuale matches it here through its own discovery plus the
   fixture-profile `@system` seed (`*dev-libs/newpkg` in
   `repo/profiles/base/packages`, which the bias pulls first exactly
   like real's); the Rust test reproduces that seed explicitly
   (plain `test_config()` has an empty system set); (c) u6 needed no
   code (S0: already clean via #142).

Pins: `test_oracle_81_…` asserts real's five rows byte-for-byte (flat
and `-v` counter assertions unchanged); u1/n5/diamond/treeorder/
onlydeps/dualslot/B4-flat pins all green unchanged. Blast radius run:
`cargo fmt --check`, `cargo clippy --release --all-targets` (zero
warnings), `cargo test --release` (whole workspace green, 559
portage-repo tests), contract suite 1780 passed / 2 skipped / 5 xfailed
(diffed by test name: only the two rewritten/new pins move). Perf:
`--tree` on the #83 probe shape within noise of flat (0.56s vs
0.54-0.61s) -- the extra schedule+select pass is I/O-dominated. Bed:
S0 atomlist re-run `l0-fx-20260924T104133Z` is 3/3 clean, parity 1.0
(was 1/3, 4 unexplained).

## S2 — the #150 fix (shared-child placement) and its pin

- [ ] **Step 1: failing pin first.** New `test_oracle_150_…` pin with
  real's S0 slotusegroup-tree bytes (no `[nomerge]` line, `slotuseplain`
  second, `1.0` nested under `slotusex`). Watch it fail on the current
  seven-row list. Flat `-D dev-libs/slotusegroup` assertions (Phase 10)
  unchanged.
- [ ] **Step 2: the fix.** Expected shape: when the walk places a
  shared child (`slotusetarget-1.0`, parents `slotusex` + `slotusey`)
  whose already-displayed parent is still on the live chain, nest under
  that occurrence instead of re-descending it as a `[nomerge]`
  ancestor. If S1's retlist-order fix already resolves the sibling
  order (facet b), S2 is only the artifact (facet a) — say which in the
  verdict.
- [ ] **Step 3: Rust unit test** (same crate shape as S1) +
  **Step 4: same blast radius as S1.**
- **Stop rules:** as S1; plus: a solved-conflict cell gaining or losing
  a row is the removal machinery (#142), not this item — file it.

### S2 verdict (filled 2026-09-24)

No separate product change: S1's tree-mode serialization subsumes both
#150 facets. The `[nomerge slotusex]` artifact was the walk meeting the
shared child with no live chain to its already-displayed parent (the
resolver-order walk had truncated the chain); walking the tree-mode
retlist keeps the chain, so `slotusetarget-1.0` nests directly under
`slotusex`'s first occurrence. The sibling order (`slotuseplain`
second) falls out of the same retlist. S2's deliverable is the pin
(`test_oracle_150_tree_shared_child_nests_without_a_nomerge_ancestor`,
real's six rows + no-`[nomerge]` + flat guard) and the drift-review bed
`l0-fx-20260924T103828Z` over atomlist `l131-s1.txt` (every `--tree`
corpus key S1/S2 move, re-verified against real cell by cell): 10
probes, 6 clean -- rdrcyc, scuseparent, slotconfgroup,
slotconflictunsolvable, socmeta and `@selected` byte-identical to real
(the corpus drift on those keys is pure improvement). Four residues
filed with the bed evidence: **#153** (fucyclec invalid-`DEPEND`
masking gap -- resolve-time, pre-existing), **#154** (gpcyclec
nomerge-ancestor USE column -- display, pre-existing), **#155**
(mg2top backtrack-skip divergence -- resolver architecture,
pre-existing), **#156** (`@world` tree order at #3 -- needs an
`MO_SEL` round comparison before scoping). New portuale bytes are
strictly closer to real on every drifted key (mg2top: first five rows
now exact; `@world`: first two rows now exact; p2b/slotusegroup fully
exact). Two flat corpus keys drift from #148's incomplete bless
inventory (scuseparent flat + slotconfgroup color flat gained the
`2.0` row #148 shipped -- membership change, impossible from this
phase's order-only diff; verified byte-identical against #148's twin
oracles); blessed here with that attribution.

## Close-out (filled 2026-09-24)

- S0 atomlist re-run `l0-fx-20260924T104133Z`: 3 probes / 3 clean /
  parity 1.0 (was 3 / 1 / 4 unexplained). Default fixture-oracle bed
  `l0-fx-20260924T104207Z`: 45 probes / 41 clean / 15 explained /
  **0 unexplained**, rc 0. L0 bed `l0-20260924T104301Z`: 120 probes /
  101 clean / parity 0.842 with a byte-identical unexplained set to
  `l0-20260922T183907Z` (41 findings, none new, none gone); the one
  `--tree` invariants violation (plasma-meta libbsd nesting) is
  identical in the baseline run. No container-lane conflicts (no bed
  was live).
- Full verification pass (batch §0 step 6): `cargo fmt --check`,
  `cargo clippy --release --all-targets` (zero warnings),
  `cargo test --release` (whole workspace green), then in `../pmtest`
  `python3 -m pytest pytests-contract-suite -q` (1780 passed / 2
  skipped / 5 xfailed, diffed by test name). Corpus drift is
  slice-scoped: blessed only the reviewed `--tree` keys (+ the two
  #148-stale flat keys with attribution), in pmtest's commit only.
- Docs in the same commit (AGENTS.md step 11): #131 and #150 flipped to
  `DONE` with the commit pair; this file's `Status:` header flipped to
  `done` (with branch and commits); `what-this-proves.md` paragraph
  appended. Residues #153–#156 filed (next free is now **#157**).

## What this phase deliberately does not do

- It does not touch the resolver's *choices* — rc, merge membership,
  and which versions collide are unchanged. Only the `--tree` display
  sequence moves.
- It does not change scheduling *policy* (which packages merge in which
  order outside the named tree cells), re-open #17/#81/#142/#148, or
  touch the merge path (no safety-gate merge).
- It does not re-pin the stale "`[N]`-vs-`[U]` dep child" wording — S0
  records it as not reproducing, not as a fix.
- It does not bless corpus drift anywhere except reviewed `--tree`
  keys, and then only in pmtest's commit.

(End of file — total lines as committed.)
