# #58 — gpkg inner `metadata.tar`: check member types (the #56 class, one level down)

Status: **proposed**. Written 2026-09-16 against `main` @ `d07f75a`;
revised the same day to read the inner tar in-process with the `tar` crate
(owner suggestion, §2a).
Backlog entry: [`backlog-tasks.md`](backlog-tasks.md) Tier 7 #58 ("Inner
`metadata.tar` member types are unchecked"). Note: `backlog-tasks.md` also
has an unrelated Tier 2 entry numbered 58 (slot-conflict exit code, #57 K1);
this plan is the **Tier 7** one. S4 renumbers nothing — flag it to the owner.
Evidence: `TEST/findings/l2.md` "### #56 S0 residue — the inner
`metadata.tar` is the same class (filed as #58)".
Predecessor: [`07.56-gpkg_outer_container.opus.md`](07.56-gpkg_outer_container.opus.md)
(outer container, DONE `de72588`).

**Read first:** `AGENTS.md`, [`agent-context.md`](agent-context.md) (hard
goal 3: static musl, dependency waivers), the `binpkg.rs` module docs, the
#56 plan above, and this file. Find things by **symbol name**
(`graft grep`/`graft callers`), not by line number.

## 0. Rules for every slice

- **Every slice of this plan lands as its own commit** — oracle-only,
  test-only and docs-only slices included. Subject `#58 S<n>: <what>`,
  ending with the repo's `Co-Authored-By` trailer. No squashing, no
  amending a previous slice, no push. A slice that finds nothing to change
  still commits its findings. (This rule applies to every plan written
  from this one onwards, not only #58.)
- Branch `backlog/07-58-check_metadata_member_types` off `main`.
- Crafted archives are built and read only under the scratchpad /
  `tmp_path` / `std::env::temp_dir()`, never in a real `PKGDIR`, `/var/db/pkg`
  or `/`. The merge-path cells run with `ROOT` pointed at a throwaway
  directory; root-only cells use the container's passwordless `sudo`
  (memory `container-has-passwordless-sudo`) inside that directory.
- Workspace-root `cargo build/test/clippy --release` (memory
  `verify-whole-workspace-build`). Full pytest: compare failing test
  *names* against a clean-`main` baseline (memory
  `contract-suite-pollutes-fixtures`; `/tmp` inode cap: memory
  `pytest-tmpfs-inode-exhaustion`).

## 1. What real does (3.0.82.2, `3rdparty/portage/lib/portage/gpkg.py`)

**Writer** — `gpkg._add_metadata`: one `TarInfo("metadata/<KEY>")` per key,
regular files only, USTAR, **no directory entries, no links**. So no
archive real produces can be affected by a regular-file-only check.

**Populate/read** — `unpack_metadata(dest_dir=None)` (via `get_metadata`):
reads the inner tar fully into memory, then for **every** member
`metadata.extractfile(k).read()` keyed by `_strip_metadata_prefix(k.name)`.
Python `tarfile.extractfile` (host `tarfile.py` `extractfile` /
`_find_link_target`): a regular member (or an unknown type) -> bytes; a
symlink -> looks up `dirname(name)/linkname` (normalized) in the **whole**
archive; a hardlink -> looks up `linkname` among members **before** it;
either link recurses into `extractfile(target)`; not found -> `KeyError`;
directory/FIFO/device -> `None` (so `.read()` -> `AttributeError`).
`_getmember` returns the **last** member with a given name.

| inner member | real result |
|---|---|
| regular `metadata/KEY` | bytes |
| name not under `metadata/` | `InvalidBinaryPackageFormat` |
| symlink, target inside the archive | the **target member's** bytes |
| symlink, target outside (`/etc/hostname`, `../..`) | `KeyError: linkname ... not found` (verified 2026-09-15) |
| hardlink to an earlier archive member | the target's bytes |
| hardlink to a non-member / a later member | `KeyError` |
| directory / FIFO / char / block device | `AttributeError` (`None.read()`) |
| nested `metadata/sub/KEY` | key `sub/KEY` (never produced by the writer) |

No host file is ever read. `bintree._populate_local` (`bintree.py` ~288,
~748) only catches `SignatureException` around `get_metadata()` — whether a
`KeyError` there aborts real `emerge` with a traceback or is caught higher
is **to be confirmed in S0**; it decides what the caller should do.

**Merge** — `unpack_metadata(dest_dir)` -> `tar_safe_extract(metadata,
"metadata").extractall(dest)`: rejects duplicate names, absolute names,
`../` traversal, **device members**, and **hardlinks whose target is not an
earlier member**; **symlinks are extracted as-is** (`fully_trusted_filter`)
into a temp dir and `shutil.move`d into build-info. For an escaping symlink
the preceding populate `get_metadata()` already failed, so real never gets
there for a package from its own pool. S0 confirms both halves.

## 2. What portuale does

`rust/portuale/src/binpkg.rs`:

| site | inner handling | problem |
|---|---|---|
| `read_gpkg_metadata`, steps 3-5 | decompress to `<scratch>/metadata.tar`, `run_tar -xf ... -C md` (no `--no-same-owner`), `read_dir_sorted(md/metadata)`, `f.is_file()` + `fs::read(&f)` | `is_file`/`fs::read` **follow** symlinks: an escaping `metadata/DESCRIPTION -> /etc/hostname` returns the host file as the value (verified). FIFO member: skipped (real errors). Names outside `metadata/` silently ignored (real errors). |
| `extract_gpkg_member(_, "metadata", build_info)` (merge, via `extract_binpkg`) | `run_tar -xpf metadata.tar -C build_info --strip-components=1` | symlinks, FIFOs and (as root) device nodes land in `${PORTAGE_BUILDDIR}/build-info`; everything that later reads build-info (`write_vdb_entry_from_dir`, `BINPKGMD5`, `environment.bz2` extraction, `pkg_*` phases) follows them, and a later **write** into build-info through a symlink is a host write as root. |
| `extract_gpkg_member(_, "image", image)` | same `tar -xpf` | image symlinks are legitimate; real rejects only device members / escaping hardlinks / abs / traversal. **Not in #58 (K2)**; S0 measures it. |

Callers that matter (`graft callers extract_binpkg` / `read_gpkg_metadata`):
`ebuild_merge::merge_binpkg` (reads metadata **before** extracting),
`remote_bundle::build_bundle` (same order), `pretend::run_info` (calls
`extract_binpkg` **directly**, no prior read; S0 verifies), and
`scan_binpkg_file` (populate; its `?` propagates — S0 records what its
caller does with the `Err`).

## 2a. Mechanism: read the inner tar in-process with the `tar` crate

Owner suggestion (2026-09-16): use a Rust tar reader
([`tar`](https://crates.io/crates/tar), `github.com/composefs/tar-rs`,
0.4.46, MIT/Apache-2.0, the de-facto standard crate) instead of
`tar -xf` + a filesystem walk for the **inner metadata tar**.

Why it fits #58 better than #56's post-extraction check:

- **Nothing inner is ever written to disk before it is judged**, so no
  symlink can be followed, no FIFO opened, no device node created — the
  class of bug disappears rather than being checked for afterwards.
- The entries come with their header type and link name
  (`Entry::header().entry_type()`, `link_name()`), so portuale can port
  real's `extractfile` **exactly** (§1 table), including in-archive link
  resolution. The narrowings the first draft of this plan needed (in-archive
  symlink rejected, nested keys rejected) go away.
- The metadata tar is small (a few KiB-MiB) and real itself reads it fully
  into memory; one `Vec<(header, bytes)>` pass is the same shape.
- The decompressor stays a subprocess (`gpkg_compressions()` argv): its
  stdout is piped straight into `tar::Archive::new(stdout)` — no
  `<scratch>/metadata.tar` temp file, no second `tar` process.

Dependency cost, verified 2026-09-16 from the crate's `Cargo.toml`:
`filetime` + `libc` (both **already direct deps** of `portuale`), `xattr`
only behind the default feature. With `default-features = false` the crate
adds **zero new transitive crates**, pure Rust, no C linkage — the
musl-static goal is untouched. It is still a new direct dependency and
`binpkg.rs`'s module doc states the "shell out to `tar`" stance, so it needs
an owner-accepted waiver comment in `Cargo.toml` like `filetime`/`libc`
have (K0).

What stays on GNU `tar` in #58: the **outer** container walk (#56, shipped
and tested) and the **image** extraction (needs `-p` ownership/permission/
xattr/sparse semantics the merge relies on). Moving the outer walk to the
crate too (it is an uncompressed tar, and the crate would avoid copying
`image.tar*` into a scratch dir on every read) is a good follow-up but a
separate change; S4 files it (K7).

## 3. Decisions for the owner

Defaults are what the slices implement if the owner does not answer.
Ask before S1 only if S0 contradicts a default's premise.

| id | question | proposed default | why |
|---|---|---|---|
| **K0** | Mechanism for the inner metadata tar | **`tar` crate, `default-features = false`, reading in-process** (§2a); decompressor still a subprocess piped into it. Waiver comment in `rust/portuale/Cargo.toml` next to `filetime`/`libc`; `binpkg.rs` module doc updated to say which parts shell out and why. Fallback if rejected: the first draft's `symlink_metadata` walk after `tar -xf` (with K1/K4 narrowings). | Removes the bug class instead of checking for it, allows exact parity with real, zero new transitive crates. |
| **K1** | Read-path semantics per inner member type | **Port real's `extractfile` exactly** (§1 table): regular/unknown type -> bytes; symlink -> resolve `dirname/linkname` (normalized) over the whole archive, last match wins; hardlink -> resolve `linkname` among earlier members; links recurse with a depth cap (real would hit `RecursionError` on a cycle -> portuale errors); unresolved link, directory, FIFO, device -> error naming the member and its type (K6). A link can only ever yield **another member's bytes**. | Parity with real; no host read is possible by construction. |
| **K2** | Merge-path extraction scope | **`extract_gpkg_member(_, "metadata", _)` only**: replaced by the S1 reader + writing each resolved key as a regular file into `dest` (`OpenOptions::create_new`, mode from the header masked to `0o7777` minus setuid/setgid — S0 records the fixture's header modes and what real's `tarfile.extract` leaves). The `image` extraction is not touched; if S0 shows it diverges from real's `tar_safe_extract` (device members as root, escaping hardlinks), S4 files a **new backlog item**. | Keeps #58 to what it was filed for. Real would write an in-archive symlink as a symlink into build-info; portuale writes the resolved bytes as a regular file — same content for every reader, never a host path. Documented as the one intentional difference (real's writer never emits links). |
| **K3** | Names outside `metadata/` | **Error** on both paths (real: `InvalidBinaryPackageFormat`). A `metadata/` directory entry itself is skipped only if S0 shows real tolerates it — real's own `extractfile` on it returns `None` -> error, so the default is error. | Parity. |
| **K4** | Nested `metadata/sub/KEY` | **Key `sub/KEY`** on the read path (parity); on the merge path, **error** (would need directory creation in build-info; real's writer never emits it). | Parity where cheap; the merge-path cut is harmless and documented. |
| **K5** | Unsafe names | **Error** for absolute names and any `..` component on both paths (real `tar_safe_extract` on merge; on read real would produce a key containing `..`, portuale refuses — a key is later joined to a path). Duplicate names: read path last-wins (real `dict` comprehension over `getmembers()`), merge path **error** (real `tar_safe_extract` "Duplicate files detected"). | Parity on merge; a narrowing on read that only affects archives real's writer never makes. |
| **K6** | Error wording | **Portuale's own message** (`<gpkg>: inner metadata member <name> is a <type>, not a regular file` / `... link target <t> not in the archive`). No contract pin on real's exception text. | Real's behaviour is an exception, not a user-facing message (#56 N4). |
| **K7** | Outer container on the crate too | **Not in #58**; S4 files a follow-up backlog item (outer walk + drop `outer_regular_member`/`walk_outer_members` once the crate reads the outer tar). | #56 is shipped and tested; changing it mid-#58 widens the review surface. |

## 4. Model tiers

| Tier | Meaning | Models |
|---|---|---|
| **F** | frontier | Claude Opus 5 / Fable 5.1 |
| **M** | mid | Claude Sonnet 5 |
| **S** | small | Claude Haiku 4.5 |

"F review" = a frontier model reads the full slice diff before the commit
(security-relevant code: always reviewed here). The executor of a slice may
be stronger than listed, never weaker.

## 5. Slices

Order: **S0 -> S1 -> S2 -> S3 -> S4**. Each is one commit (§0). Do not
parallelise S1-S3 (same file, sequential commits on one branch).

| slice | what | product change | executor | review |
|---|---|---|---|---|
| S0 | inner-member crafted matrix: real vs portuale (read, merge, caller) + crate spike | none (script + findings) | **M** | F reads the findings, confirms/adjusts K0-K5 |
| S1 | add the `tar` crate; in-process inner-metadata reader porting real's `extractfile`; unit tests on it | **yes** (dependency + new helper, not yet wired) | **F** | **F review** |
| S2 | `read_gpkg_metadata` uses the S1 reader; crafted-archive tests | **yes** | **M** | **F review** |
| S3 | `extract_gpkg_member(metadata)` uses the S1 reader + regular-file writes; tests; L2 porttest; musl smoke | **yes** | **M** | **F review** |
| S4 | close-out docs, follow-up items, memory | docs | **S** | M skim |

### S0 — crafted inner-member matrix + crate spike (executor **M**, F reads)

1. Extend `scripts/gpkg_crafted_members.py` with an **inner** cell family
   (keep the outer `a`-`h` cells unchanged; new ids `i1`...). Each inner
   cell starts from the committed
   `fixtures/pkgdir/dev-libs/gpkgreadpkg-1.0.gpkg.tar`, decompresses its
   `metadata.tar.zst`, rebuilds the inner tar with Python `tarfile` (one
   crafted header), recompresses with `zstd`, and rebuilds the outer
   container with a **recomputed** `Manifest` (`DATA` size + digests for
   the new member), so every #56 outer check passes. Reuse the 2026-09-15
   residue probe's construction (l2.md "#56 S0 residue").

   | cell | inner member | entry |
   |---|---|---|
   | i1 | `metadata/DESCRIPTION` | symlink -> `/etc/hostname` (the filed case) |
   | i2 | `metadata/DESCRIPTION` | symlink -> `SLOT` (in-archive, relative) |
   | i3 | `metadata/DESCRIPTION` | hardlink -> `metadata/SLOT` (earlier member) |
   | i3b | `metadata/AAA` (first) | hardlink -> `metadata/SLOT` (later member) |
   | i4 | `metadata/DESCRIPTION` | hardlink -> `etc/hostname` (not a member) |
   | i5 | `metadata/DESCRIPTION` | FIFO |
   | i6 | `metadata/DESCRIPTION` | char device 1,5 — root-only extraction |
   | i7 | `metadata/sub/` + `metadata/sub/KEY` | directory + nested regular file |
   | i8 | `other/KEY` | regular file outside `metadata/` |
   | i9 | `metadata/x` -> `/tmp/<scratch>/victim-dir`, then `metadata/x/pwned` | symlink-then-write-through |
   | i10 | `metadata/A` -> `B`, `metadata/B` -> `A` | symlink cycle |
   | i11 | `metadata/SLOT` twice, different bytes | duplicate name |
   | i12 | `metadata/../KEY`, `/metadata/KEY` | traversal / absolute name |
   | i13 | `image/dev/null0` in `image.tar` | char device (K2 measurement only) |
   | i14 | `image/usr/bin/x` in `image.tar` | hardlink -> `/etc/hostname` (K2 measurement only) |

2. **Real**, per cell (host portage at
   `/usr/lib/python3.*/site-packages/portage`, read-only, or vendored
   `3rdparty/portage/lib` via `PYTHONPATH`):
   `gpkg(settings, basename, path)._verify_binpkg()`, `.get_metadata()`,
   and `.unpack_metadata(dest_dir=<scratch>)`; for i13/i14
   `.decompress(<scratch>)`. Record exception class and message or the
   returned dict, and what landed in `dest_dir` (`ls -l`, link targets,
   modes, mtimes). Also record the committed fixture's inner header modes
   and what real leaves on disk for them (feeds K2's mode rule).
3. **Real caller behaviour** for i1: the archive in a throwaway `PKGDIR`,
   `PORTAGE_CONFIGROOT`/`ROOT`-isolated `emerge -pvK =dev-libs/gpkgreadpkg-1.0`:
   traceback, warning+skip, or silent skip?
4. **Portuale today**, per cell: a temporary `#[ignore]`d driver test in
   `binpkg.rs` (env `PORTUALE_CRAFTED_DIR`/`PORTUALE_CRAFTED_CELL`, same
   shape as #56 S0's `gpkg_crafted_member_probe`) calling
   `read_gpkg_metadata` and `extract_binpkg` into a scratch dest; record
   Ok/Err, host bytes read (i1), what landed in build-info/image, i9's
   victim dir. `timeout -s KILL 10` + `ulimit -f` around every run;
   i6/i13 via `sudo` with `TMPDIR` on a non-`nodev` fs (#56 S0 recipe).
   Portuale `emerge -pvK` against the step-3 `PKGDIR`: what does
   `scan_binpkg_file`'s `Err` become? By code reading: does
   `pretend::run_info`'s `extract_binpkg` call have any prior read?
5. **Crate spike** (throwaway, not committed): in a scratch cargo project
   (not the workspace), `tar = { version = "0.4", default-features = false }`;
   iterate `Archive::entries()` over each inner cell's decompressed tar and
   print `path_bytes`, `entry_type`, `link_name_bytes`, `size`. Confirm:
   the crate surfaces every crafted header as-is (does it reject or
   normalize `..`/absolute paths on *iteration*, not only on `unpack`?),
   GNU/PAX long names round-trip, and `cargo tree -e normal` for the
   dependency adds nothing beyond `filetime`/`libc`. Record the crate
   version used.
6. Write `## #58 S0 — crafted inner metadata members: real vs portuale`
   in `TEST/findings/l2.md`: commands, the matrix (real read / real merge /
   portuale read / portuale merge), caller behaviour, the spike result, and
   a verdict per cell: **fix in S2/S3 (parity)**, **documented difference
   (K2/K4/K5)**, or **out of scope -> new backlog item (K2)**. If a
   verdict contradicts a K default's premise (for example the crate hides
   a header detail real depends on), stop and ask the owner before S1
   (process rule; portuale itself never prompts — memory
   `portuale-never-prompts-unless-ask`).
7. Remove the temporary `#[ignore]` driver. **Commit** (script +
   findings): `#58 S0: inner metadata member matrix -- real vs portuale`.

### S1 — the in-process reader (executor **F**, **F review**)

Executor is frontier here: it is the security-relevant port of tarfile's
link-resolution rules, and the new dependency's API choices (bytes vs
lossy paths, header vs PAX link names) are easy to get subtly wrong.

1. `rust/portuale/Cargo.toml`: `tar = { version = "0.4", default-features
   = false }` with a waiver comment in the file's existing style (why:
   #58, in-process inner metadata read; pure Rust; deps already present;
   `xattr` off). Update `rust/Cargo.lock`. Update the `binpkg.rs` module
   doc paragraph that says it shells out to `tar` (outer + image still do;
   the inner metadata tar does not).
2. New private helper in `binpkg.rs`, e.g.
   `fn read_inner_metadata(gpkg: &Path, member: &Path, comp:
   Option<&[&str]>) -> Result<Vec<(String, Vec<u8>)>, String>`:
   - open the member (already type-checked by #56) or spawn the
     decompressor with `Stdio::piped()` and feed its stdout to
     `tar::Archive::new`; check the child's exit status after the read
     (a decompressor failure is still an error, as today).
   - collect every entry in order as `(name_bytes, entry_type,
     link_name_bytes, data)`; read data only for regular / unknown-type
     entries; cap total bytes read (a bomb guard: e.g. 64 MiB, named
     constant, error if exceeded).
   - resolve per K1/K3/K4/K5: strip `metadata/` (else error), refuse
     absolute/`..`, resolve symlinks (whole archive, last match,
     normalized `dirname/linkname`) and hardlinks (earlier members only),
     recursion depth cap, error on unresolved/dir/FIFO/device.
   - return `(key, bytes)` in archive order with last-wins de-dup, plus a
     flag or a separate check for duplicates so S3 can refuse them.
3. Unit tests for the helper alone: build tars in-memory with
   `tar::Builder` + hand-set headers (no Manifest needed at this level)
   for every S0 cell i1-i12, asserting exactly the S0 real column
   (bytes for i2/i3, error for i1/i3b/i4/i5/i6/i8/i10/i12, `sub/KEY` for
   i7, last-wins for i11); a regular fixture-shaped tar round-trips
   byte-for-byte; the decompress pipe is exercised with the `zstd` argv.
4. Workspace-root `cargo build/test/clippy --release`; not yet wired, so
   pytest is unaffected — run `pytest tests/ -k "gpkg or binpkg"` anyway.
5. F review of the diff (a second F pass, not the executor re-reading).
   **Commit**: `#58 S1: in-process inner metadata.tar reader (tar crate)`.

### S2 — read path (executor **M**, **F review**)

1. `read_gpkg_metadata` steps 3-5 become: `read_inner_metadata(...)`,
   then the existing value shaping (UTF-8 + trim; non-UTF-8 skipped —
   a content rule, unchanged). The scratch `metadata.tar` and `md/`
   directory and the inner `run_tar` call are deleted.
2. If S0 step 3 showed real aborts/skips differently from what
   `scan_binpkg_file`'s caller does with `Err`, align the **caller** here
   only if it is a one-line change; otherwise record it and S4 files it.
3. Tests next to the #56 ones: a sibling builder
   `build_gpkg_with_inner_entries` crafts the inner tar and **recomputes
   the Manifest** (assert `verify_gpkg_manifest` passes, so the error
   comes from the inner check). One end-to-end test each for i1 (error,
   and the error text does not contain the host file's bytes), i2
   (target bytes), i5 (error), i8 (error). The committed fixture still
   reads unchanged (`read_gpkg_metadata_extracts_the_embedded_scalar_metadata`),
   and the `ebuild_package.rs`/`emerge_build.rs` round-trip tests that call
   `read_gpkg_metadata` stay green.
4. Workspace-root build/test/clippy; `pytest tests/ -k "gpkg or binpkg"`;
   full pytest by failing names.
5. F review. **Commit**: `#58 S2: read_gpkg_metadata reads the inner tar
   in-process`.

### S3 — merge path (executor **M**, **F review**)

1. `extract_gpkg_member` for `want == "metadata"` only (K2): use
   `read_inner_metadata`, refuse duplicates and nested keys (K4/K5), and
   write each `(key, bytes)` into `dest` with `create_new` and the S0-
   recorded mode rule; the `image` branch keeps `tar -xpf` unchanged.
   Split the function if that is cleaner (e.g. `extract_gpkg_metadata`),
   updating `extract_binpkg` and the #56 `assert_rejects_everywhere`
   helper's call site.
2. Tests: `extract_binpkg` on the S2 crafted archives (i1, i2, i5, i7,
   i11) — errors where K says so, i2 writes a **regular** `DESCRIPTION`
   with `SLOT`'s bytes, and nothing non-regular exists in build-info;
   `extract_binpkg_unpacks_a_real_gpkg_image_and_build_info` stays green
   and additionally asserts build-info file bytes + modes equal what the
   old `tar -xpf` produced for the fixture (capture those expectations
   from `main` before editing); image symlinks in a valid archive still
   extract (K2 untouched). If S0 showed `pretend::run_info` has no prior
   read, this is what protects it — say so in the test's comment.
3. Workspace-root build/test/clippy; pytest as S2.
4. **L2 porttest normal track** (real-built gpkgs cross-installed by
   portuale; vdb compared) — expect `hard=0`, 0 unexplained; record the
   log dir. **musl smoke** `musl/smoke_test.sh` (new dependency) — rc 0.
5. F review. **Commit**: `#58 S3: extract_gpkg_member(metadata) writes
   only resolved regular files`.

### S4 — close-out (executor **S**, M skim)

1. `docs/backlog-tasks.md` Tier 7 #58 -> `DONE <date>`: mechanism (`tar`
   crate, in-process, zero new transitive crates), parity achieved, the
   documented differences (K2 link-as-regular-file on merge, K4 nested on
   merge, K5 `..` on read), S0's verdict on real, tests, L2 log dir,
   musl smoke, a pointer to this file. Point out (don't fix without
   asking) the duplicate "58." numbering with the Tier 2 entry.
2. New Tier 7 items (next free numbers), each with S0 evidence:
   - K7: outer container walk on the `tar` crate (drop the #56
     post-extraction helpers, stop copying `image.tar*` to scratch on
     every metadata read).
   - only if S0 marked i13/i14 divergent: inner `image.tar` members vs
     real `tar_safe_extract`.
   - only if S2 deferred it: the `scan_binpkg_file` caller behaviour.
3. `docs/glep-compliance-review.md`: add #58 next to #56 under GLEP 78
   (inner-tar hardening; a real-parity item, not a GLEP requirement).
4. This file: Status -> **DONE**, add the five commit hashes.
5. Memory: `gpkg-inner-metadata-member-types.md` (project type: what
   shipped, the crate decision and its waiver, the documented differences,
   follow-up item numbers) + one line in `MEMORY.md`.
6. **Commit**: `#58 S4: close-out -- backlog DONE, follow-ups, memory`.

## 6. File-conflict map

| slice | files |
|---|---|
| S0 | `scripts/gpkg_crafted_members.py`, `TEST/findings/l2.md` (temp driver in `binpkg.rs` and the crate spike in the scratchpad, neither committed) |
| S1 | `rust/portuale/Cargo.toml`, `rust/Cargo.lock`, `rust/portuale/src/binpkg.rs` (module doc, new helper, unit tests) |
| S2 | `rust/portuale/src/binpkg.rs` (`read_gpkg_metadata`, tests); maybe one caller line |
| S3 | `rust/portuale/src/binpkg.rs` (`extract_gpkg_member`/`extract_binpkg`, tests) |
| S4 | `docs/backlog-tasks.md`, `docs/glep-compliance-review.md`, this file, memory |

No overlap with open Tier 6 (#48–#52) work. Anyone else touching
`rust/Cargo.lock` between S1 and merge: rebase, re-run `cargo build`.

## 7. Done criteria

- No inner metadata member can make portuale read or write a host path,
  open a FIFO or create a device node, on either the populate or the merge
  path — by construction (nothing inner reaches the filesystem before it is
  resolved to regular bytes).
- `read_gpkg_metadata` returns exactly what real `get_metadata()` returns
  for every S0 cell real accepts, and errors wherever real raises.
- Every archive real's writer can produce reads, verifies and merges
  byte-identically to before (committed fixture + L2 normal track), and the
  musl static build still passes its smoke test.
- Every remaining difference from real is named in #58's DONE text, or
  filed as its own backlog item.
