# Backlog tasks

One line per open task. Each has a code/doc pointer so a fresh model can
start without a full context load. Source of truth for detail:
[`scope-backlog.md`](scope-backlog.md) (section letters in brackets),
`what-this-proves.md` (what already shipped), `git log`, and the memory
notes under
`.claude/projects/-home-vivo-repo-PORTUALE-portuale/memory/`.

Rules that apply to every task: take expected output from real Portage
(container bed, host `emerge`, or an upstream resolver test), pin it in
`pytests-contract-suite/test_emerge_pretend_contract.py` /
`test_portuale.py`, keep `test_output_invariants.py` green, and run the
full verification pass (`AGENTS.md` step 8). The suite and the container
bed live in the sibling `pmtest` repo since 2026-09-18
(`agent-context.md`, "Where the tests live"); `TEST/...` and `tests/...`
paths in the entries below are the pre-move spelling of what is now
`../pmtest/differential-test-bed/...` and
`../pmtest/pytests-contract-suite/...`. The Python mirror
(`python/emerge_pretend_reference.py`) was removed 2026-09-15 —
[`history/second_python_copy_removal.md`](history/second_python_copy_removal.md); older
entries below that say "dual-language" / "Rust==Python" describe how
they shipped, not a rule for new work.

---

## Tier 1 — focused slices (scoped, ~one sitting each)

> **Audited 2026-09-13:** commit `67f3b0e` (2026-09-10, "Tier 1 backlog:
> ship 14 slices, re-pin brush, document two verdicts") shipped most of
> this list but never updated this file; the entries below are marked
> accordingly. Detail: `scope-backlog.md` Parts 1–2 and the
> `what-this-proves.md` Tier-1 paragraph.
>
> **Plan review 2026-09-16** (#14/#59/#65–#70, against real 3.0.82.2
> source + post-#63 host probes in `docs/history/evidence/2026-09-16-post63-probes/`):
> #14 DONE, #66 WITHDRAWN, #65/#68/#69 plans rewritten, #59/#67 corrected.
> Recommended order across tiers: **#65 S0/S1/S3** (easy, removes the Go rebuild cascade: 19 of the
> 24 extra host `@world` entries) → **#68** (host `@world` rc 1) → **#67**
> → **#69** (after #67) → **#65 S2** → **#59** → **#70** (independent,
> any time). Per-slice difficulty is in each entry and plan.

1. **DONE 2026-09-10 — release-profile `lto`/`codegen-units`.** `rust/Cargo.toml` `[profile.release]` sets `lto = "thin"` + `codegen-units = 1` (15.5→13.2 MB); measured in `performances-tuning.md` §5.
2. **DONE 2026-09-10 — `--solver=` module-doc refresh.** `solver_bridge.rs`'s "Deliberate v1 cuts" list is current (blockers/ABI/merge-order edges/markers/circular deps all named as wired). [J]
3. **DONE 2026-09-10 — `--solver=` forced-flag markers.** The bridge render path calls the walk's own `forced_or_masked_flags` (`solver_bridge.rs:495-512`), so the `( )` wraps render. [J]
4. **DONE 2026-09-10 — `--solver=` notice fields.** `circular_deps` is wired from the bridge result via `find_hard_cycles` (`solver_bridge.rs:719`); `slot_conflicts`/`autounmask_*` stay empty **by construction** (an engine plan picks one version per CPN and no relaxation loop runs) — documented in the module doc, not a to-do. [J]
5. **brush PRs: submit staged fixes** — **five** fixes staged as `fix/*` branches on `vivo75/brush` + `git format-patch` exports in `docs/brush-pr/`; all five are in the thin-fork `main` `b9524ad5` and portuale is re-pinned to it (2026-09-14), but **none is open upstream yet** — opening the PRs needs the user's GitHub auth (B6). See `docs/brush-pin.md` "Still to do". [G]
6. **brush re-pin (recurring)** — re-pinned 2026-09-14 to upstream `25bffd54` on the thin fork (`b9524ad5`; fixes 01 tokenizer, 02 AST `Display` + quoted here-tags, 03 pipeline deadlock, 04 `source` parse-error status, 05 IFS-independent brace expansion); the next periodic bump follows `brush-pin.md`'s checklist. The 2026-09-13 `src_compile` no-op is fixed (Track B, B1–B4): quoted-tag serialization, the silent `source` abort, the missing embedded `$BASH` and the brace-expansion/IFS defect were the root causes; pinned by the `heredocpkg` Bash/Brush image-set test, the corrupt-saved-environment regression and the L2 G3 re-run (`TEST/findings/l2.md` "#38 S2", `docs/brush-pin.md` "What is *not* tracked here"). Dropping the fork (and revisiting the `--shell` default) waits on #5. [G]
7. **DONE 2026-09-10 — `FEATURES=compress-build-logs`.** `.gz` log paths + gzip pump + gunzip tails in `emerge_build.rs`. [B]
8. **DONE 2026-09-10 — elog `mail` / `mail_summary`.** Hand-rolled MIME, sendmail-binary/plain-SMTP delivery, per-package + run-wide summary; STARTTLS stays a documented cut. `rust/portuale/src/elog.rs`. [B]
9. **DONE 2026-09-10 — `env.d` eroot path.** `resolve_config` gained the `eroot` param; `env.d` reads `<eroot>/etc/profile.env` (not `config_root`). [C]
10. **DONE 2026-09-10 — `package.env`/`env.d` `${VAR}` expansion.** Per-entry expand map (`expand_package_env_files`), global-map + within-file/cross-file chaining. [C]
11. **DONE 2026-09-10 — `depend`-phase RESTRICT/PROPERTIES reduction.** Reduced on the config-USE set for the `depend` phase. [D]
12. **DONE 2026-09-10 — standalone `package.env`.** `ebuild <file> <phase>` applies per-package `package.env` via the shared md5-cache-identity matcher (`match_package_env_vars` in `rust/portuale/src/ebuild_phases.rs`, called from `phase_standalone_base_env`; pinned by the `standalone_phase_env_layers_matching_package_env_build_vars` unit test). [D]
13. **DONE 2026-09-10 — resumed binary from `$PKGDIR`.** Resumed entries always carry `remote_binary: false`, so the binhost fallback can't fire; local `$PKGDIR` only. [B]
14. **DONE 2026-09-14 (close-out verified 2026-09-16) — fetch candidates / mirrors (`docs/history/backlog_tier_1_sliced.opus.md` §2.4/§4).** The `GENTOO_MIRRORS` fallback built flat URLs, which 404 on `distfiles.gentoo.org` (it publishes only `0=filename-hash BLAKE2B 8`). Shipped 2026-09-14: F1a mirror layout math + `layout.conf` parsing (`portage-fetch/src/layout.rs`, pinned to real Python output); F1b per-mirror `layout.conf` download + `.mirror-cache.json` in real's format, lazy resolution (`fetch.rs::mirror_url`; live-fetched `which-2.23.tar.gz` from `…/distfiles/80/…`, real portage reads portuale's cache); F2 each location attempted once (`tried_locations`); F3 (1)(2) second checksum failure jumps to the primary URIs + `PORTAGE_FETCH_CHECKSUM_TRY_MIRRORS` cap; F3 (3) corrupt downloads renamed with a deterministic suffix (`_checksum_failure_<N>` + real's `Refetching...` line); F4 `fsmirrors` copy (`copy_from_fsmirrors`), `/`-rooted `GENTOO_MIRRORS` never a `wget` URL; F5 one candidate list per distfile (`group_by_filename`), `A`/`AA` deduped. F6 (third-party mirror shuffle) is a deliberate cut. Close-out S0 (stale `scope-backlog.md` §E line) landed in `8682735`; the 2026-09-16 plan review found nothing code-side left. Design + shipped state: [`history/01.014-fetch_candidates_mirrors.md`](history/01.014-fetch_candidates_mirrors.md). Transport residue is #70. [E]
15. **NOT A GAP — decided 2026-09-10 — `:=` literal-bound atom validation.** No "improper context for slot-operator built atom syntax" masking exists in the vendored checkout, site-packages 3.0.82.2, PyPI 3.0.82.2, or upstream main; all parse `:slot/sub=` and match it structurally, exactly as `portage-dep` already does. See `what-this-proves.md`. [A]
16. **DONE 2026-09-10 — slot-conflict `--color y` marker alignment.** Violated spans wrapped in red with markers placed on the displayed string (`slot_conflict_caret_idx` + `colorize_marked_spans`, `pretend.rs:7212`/`:11139`) — a deliberate divergence from real's pre-color-index marker drift (upstream bug; byte-parity unachievable anyway). [A]

63. **DONE 2026-09-16 — `EMERGE_DEFAULT_OPTS` is applied as prepended CLI defaults.** Shipped: config resolution moved above argv parsing in `pretend::run` (real's two-pass dance collapses to one — portuale has no `--config-root` CLI; `--help` still returns first), `args_with_emerge_defaults` prepends `shell_split(config.other_vars["EMERGE_DEFAULT_OPTS"])` unless `--ignore-default-opts`, and both that flag and `--ask-enter-invalid` moved out of the recognized-but-unimplemented table into the parser (the latter was the only host default with no implementation at all; `classify_yes_no`/`ask_confirm`/`ask_select` now reproduce `UserQuery.query`'s `enter_invalid` gate; `ASK_ENTER_INVALID` is reset per `run`). Host check: `--getbinpkg=y` from `/etc/make.local` now yields `[binary gU]` entries. Tests: `pretend::tests::emerge_default_opts_are_prepended_to_argv` (+ the extended `classify_yes_no_*` unit test), contract `test_emerge_default_opts_env_applies_and_argv_wins` / `..._from_a_sourced_make_conf`, help pin updated; `cargo test --release` 1125 passed, pytest 1697 passed. Residue found and filed as #69. Original report follows. **`EMERGE_DEFAULT_OPTS` is never applied (getbinpkg/usepkg/… ignored).** `resolve_config` stores the variable (`portage-profile` `ENV_SCALAR_VARS` `:1301` → `config.other_vars`), but `pretend::run` (`pretend.rs:7494`) parses argv directly and nothing consumes it, so the host's `EMERGE_DEFAULT_OPTS="--binpkg-respect-use=y --load-average=11 --autounmask=y --ask-enter-invalid --usepkg=n --getbinpkg=y"` (set in `/etc/make.local`, `source`d by `/etc/portage/make.conf`) is inert. Live: real lists `dev-lang/go`/`dev-build/cmake` as `[binary gU]` + `[f1r] Local copy of remote index…`; portuale lists both as `[ebuild]` (evidence `docs/history/evidence/2026-09-16-world-uDpvN/`). Passing `--getbinpkg` on the command line already works (`portuale emerge -pv --getbinpkg --oneshot dev-build/cmake` → `[binary gU]`), so only the defaults plumbing is missing: real prepends `shlex.split(settings["EMERGE_DEFAULT_OPTS"])` to argv in a second `parse_opts` pass (`_emerge/main.py:1221-1255,1352-1360`), argv winning. Plan: [`history/01.063-emerge_default_opts.md`](history/01.063-emerge_default_opts.md).

64. **DONE 2026-09-16 — `@world` now includes the `@profile` set.** Shipped: `Config::profile_packages` (unstarred `packages` lines of chain levels whose repo declares `profile-set`, stacked with `stack_mask_lines`; `portage-profile`'s `repo_profile_formats`/`repo_containing` reused), added in real's set order to both `@world` expansions (`pretend.rs` selected/system arms) and to `complete_seed_atoms`; `@profile` is now a first-class target. Committed fixture declares only `portage-2`, so every existing pin stays byte-identical; the positive path is pinned through a copied configroot (`test_profile_set_packages_join_world_and_are_a_target`) plus two `portage-profile` unit tests. Host effect: rclone/linux-firmware/vte/vte-common and weston now resolve, the libdisplay-info→kwin/mesa `rR` cascade disappears, and the `@world` list drops 68→50 with 24 binaries. Original report follows. **`@world` is missing the `@profile` set (rclone/linux-firmware/vte/weston dropped).** Real `@world = @profile @selected @system` (`portage/_sets/__init__.py:97-98`); `@profile` is the **unstarred** `packages` lines of profile levels whose repo declares `profile-set` (`_sets/profiles.py:13-38`), while `@system` keeps the `*`-starred lines of every level. Portuale computes only `system_packages` (`portage-profile/src/lib.rs:3152-3168`) and expands `@world` as selected+system (`pretend.rs:10093-10110`, seed `:10468`), so the host buildovl profile's 100-line `packages` file is dropped: real updates `net-misc/rclone`, `sys-kernel/linux-firmware`, `x11-libs/vte`, `gui-libs/vte-common`; portuale omits them. Missing `dev-libs/weston` also hides its `<media-libs/libdisplay-info-0.4.0:=` bound, letting portuale upgrade libdisplay-info and cascade `rR` onto `kwin`/`kwin-x11`/`mesa`. The committed fixture repo declares only `portage-2` (no `profile-set`), so its pins must stay byte-identical; the host bug is reproduced with a copied configroot. Plan: [`history/01.064-world_profile_set.md`](history/01.064-world_profile_set.md).

67. **DONE 2026-09-16 — USE incremental `-<prefix>_*` wildcard removal missing (VIDEO_CARDS/SANE_BACKENDS `-*`).** S1 `c1447df` added the USE-scoped `apply_use_incremental(_iter)` (dead code + tests), S2 `1065ecc` migrated every USE token-list call site in `portage-profile` and `portage-repo::effective_use_flags_uncached` (the generic/mask/force/keywords folds deliberately keep `apply_incremental`), S3/S4 (this commit) added the `dev-libs/videodrv` fixture + contract pin (copied configroot, real-oracle cells) and the docs. Host effect: xorg-drivers and the three xf86-video-* drivers and sane-backends all leave `@world` (5 packages, nothing added). Original report follows. **USE incremental `-<prefix>_*` wildcard removal missing (VIDEO_CARDS/SANE_BACKENDS `-*`).** The host profile sets `VIDEO_CARDS="-* intel …"` / `SANE_BACKENDS="-* hp …"`; portuale prefixes `-*` to `-video_cards_*` (`prefix_tok` in `portage-profile/src/lib.rs` `resolve_config`) but `apply_incremental_token` only knows literal `-*`, so the clear is a no-op and parent-profile cards plus the ebuild's `+sane_backends_*` IUSE defaults survive. Live (still present post-#63, `docs/history/evidence/2026-09-16-post63-probes/`): portuale reinstalls `x11-base/xorg-drivers` and pulls `xf86-video-{dummy,fbdev,vesa}`, and reinstalls `media-gfx/sane-backends` with every `+sane_backends_*` on; real does neither. Real expands `-*` to `-<var>_*` per profile level (`config.py:2837-2866`), then the USE loop's `x[-2:] == "_*"` arm removes by prefix (`config.py:2925`); the fix must stay USE-scoped (mask/force/keywords folds and generic incrementals have no wildcard arm). Plan reviewed 2026-09-16 (call-site list completed: three `"USE" =>` arms, per-level and conf USE_EXPAND folds, UNPREFIXED, global user USE, `effective_use_flags_uncached`). Slices: S1 easy, S2 medium, S3 medium, S4 trivial. Land **before** #69. Plan: [`history/01.067-use_incremental_prefix_wildcard.md`](history/01.067-use_incremental_prefix_wildcard.md).

68. **DONE 2026-09-16 — false "unsatisfied block" when the blocked installed version is being replaced (host `@world` exits 1).** S1 `9da318f` drops an installed match replaced in its slot (the host shape: `emerge -uDpvN @world` now rc 0, gtk-doc upgraded); **display repair 2026-09-17 (`#68/#72 B0b`–`B5`, branch `backlog/68-74`):** the satisfied `[blocks b      ] <dev-util/gtk-doc-1.36.1 …` row now prints inline right after `gtk-doc-1.36.1` with `Conflict: 1 block (all satisfied)` (B2, the stuck-serializer predicate B0b confirmed), the removal `[uninstall]`/inline `b` rows + `uninst` counter + `--json` entry match real (B3/B4), and the `--columns`/`-q`/`--color` shapes are pinned; host `-uDpvN @world` is 27 identical packages with the same `Conflict:` line. Residues filed: #75 (`--tree`), #76 (`||` wait chains), #77 (nomerge-owner removal rows); evidence `TEST/findings/l0.md` "#68/#72 B0b"–"B4"; S2+S3 `6e2f743` read the owner's operation from its final entry, mark merge-bound matches with a merging parent unresolved, ignore an installed parent's installed matches, apply the soft same-slot skip, and replace the vdb reverse scan with the walked-graph parents test (the 2026-09-16 follow-up below adds the required-set closure after L0 caught the over-narrowing); the renderer gained the teal `b`/red `B` split and the `(all satisfied)` counters suffix. Container oracle cells a–h in `TEST/findings/l0.md` "#68 S0". **S3 follow-up 2026-09-16:** the graph-parents test alone regressed L0's `sys-apps/systemd-utils` (real `B`/rc 1 from installed systemd's `@system` reachability, portuale satisfied); `ResolveCtx::installed_closure` now backs the arm and L0 is back to the pre-existing classes (clean 100 / parity 0.833 / 34 unexplained, `l0-20260916T191018Z`). Residues filed: #72 (`[uninstall]` rows), #73 (set-argument parents). Original report follows. **False "unsatisfied block" when the blocked installed version is being replaced (host `@world` exits 1).** `<dev-util/gtk-doc-1.36.1` from `gtk-doc-am-1.36.1` matches installed `gtk-doc-1.34.0`, but `1.36.1` replaces it in the same run, so real prints `[blocks b]`/`Conflict: 1 block (all satisfied)` and exits 0; portuale's `resolve_blockers` sets `unsolvable = blocked_installed && installed_has_foreign_dependents(...)` with no replaced-in-slot check, so it prints `[blocks B]`/`1 unsatisfied`, the `* Error: … cannot be installed at the same time` block and exits 1 (still present post-#63). **Plan review 2026-09-16 corrected the real model** (`depgraph.py:9095-9255`, read in full): `blocked_final` (package tracker) holds merge-bound matches *and* surviving installed packages; a match is uninstall-ordered when the parent merges and the match is installed, or when the parent is nomerge (uninstall the parent); only a merge-bound match with a merging parent is unresolved outright; an uninstall is abandoned if the installed instance is a digraph node with parents; installed parents' `blocked_initial` matches are ignored; soft same-slot matches are skipped. Portuale gaps: no replaced check, merge-bound matches never unsolvable, same-version reinstall deduplicated into the installed row, `PendingBlocker` has no owner merge/nomerge bit (blockers are also pushed from installed parents in `enqueue_dependencies`), pulled-in test is a vdb reverse scan instead of walked graph entries. Free unsatisfied oracle: `emerge -puDvN --ignore-default-opts --oneshot app-containers/runc` → real `[blocks B]`. Slices: S0 oracle cells a–h medium, S1 replaced-in-slot (fixes host rc) medium, S2 merge-bound + `owner_merging` hard, S3 graph-entry pulled-in test medium, S4 pins easy. Plan: [`history/01.068-blocker_replaced_version.md`](history/01.068-blocker_replaced_version.md).

69. **DONE 2026-09-16, repaired 2026-09-17 (`#69 R0`–`R3`, branch `backlog/68-74`) — `--getbinpkg`/`--usepkg` + `--newuse` drops a needed rebuild (found while shipping #63).** Repair (R0–R3, plan [`history/02.68-74.md`](history/02.68-74.md) Phase B): the R0 oracle showed the no-ebuild comparison is reached under `--usepkg` but never under plain `--usepkgonly` (real's respect-use default, `create_depgraph_params.py:47-55`); R1 compares the no-ebuild binary with the profile's selection over its IUSE (real `pkgsettings.setcpv(pkg)`), R2 subtracts force/masked flags (real `flags -= forced_flags`), R3 closes the docs. Bed cells n1–n4 and the host `@world` diff re-verified (`TEST/findings/l0.md` "#69 R0"–"#69 R2"). The 2026-09-16 closure note follows. S0 `ed11de3` staged the `binnew` fixture and captured a real-built binary matrix (12 cells; the crafted fixture `.tbz2`s turned out to be portuale-only, so the oracle pipeline builds one with real `--buildpkgonly` + `--usepkgonly` then adds `newflag` to the ebuild); S1 `33c0479` reworked `binpkg_respect_use_ok` into real's gate (`reinstall_use or (not installed and respect_use)`) with `_reinstall_for_flags`'s arms against the same-version ebuild's IUSE/USE; S2/S3 (this commit) pinned the six cells and closed the docs. Host: `-uDpvN --oneshot dev-qt/qttools` prints `[ebuild R] … LLVM_SLOT="22 -17 … (-23%)"` and qttools is back in `@world`. Original report follows. **`--getbinpkg`/`--usepkg` + `--newuse` drops a needed rebuild (found while shipping #63).** With remote binpkgs eligible (host defaults `--getbinpkg=y --binpkg-respect-use=y`), portuale omits `dev-qt/qttools` from `emerge -uDpvN --oneshot dev-qt/qttools` and from `@world`; real rebuilds it (`[ebuild R] … LLVM_SLOT="22 -17 … (-23%)"`, the ebuild's IUSE gained `llvm_slot_23`). `--usepkg` (local only), `--ignore-default-opts` and `--getbinpkg --usepkg-exclude=dev-qt/qttools` rebuild it; `--binpkg-respect-use=n` does not. **Plan review 2026-09-16 corrected the diagnosis:** real's binary rejection (`depgraph.py:8260-8294`) runs when `reinstall_use` (`--newuse` or any `--reinstall`) **or** respect-use, and compares the binary's baked USE/IUSE against the **same-version ebuild's** profile USE/IUSE (`_reinstall_for_flags`, `:3134-3162`: IUSE symmetric difference counts under `--newuse` or plain respect-use); a rejected binary is skipped and the ebuild is compared with the vdb. `identical_binary` (`:8001-8017`) keeps the *installed* candidate and is not involved (the first plan misread it). Likely root cause: `binpkg_respect_use_ok` runs only under respect-use and evaluates the binary's own IUSE, so an ebuild-only flag is invisible and the binary becomes `best`, after which the newuse diff (binary baked flags vs vdb) is empty. Slices: S0 local-binhost oracle matrix medium, S1 real's rejection gate + ebuild IUSE on both call sites medium, S2 pins + host probes medium, S3 trivial. Land after #67. Plan: [`history/01.069-getbinpkg_newuse_rebuild.md`](history/01.069-getbinpkg_newuse_rebuild.md). Evidence: plan §1 probe table, `docs/history/evidence/2026-09-16-post63-probes/`.

70. **DONE 2026-09-16, repaired 2026-09-16/17 (`#70 R1`–`R5`, branch `backlog/68-74`) — fetch transport overrides are ignored: `FETCHCOMMAND`/`RESUMECOMMAND` (and protocol variants) plus `PORTAGE_RO_DISTDIRS` (found while consolidating #14).** Repair (R1–R5, plan [`history/02.68-74.md`](history/02.68-74.md) Phase A): the S1 parser mangled `\${VAR}`/`\"` (R1), an absent `FETCHCOMMAND` errored instead of defaulting to make.globals (R2), the `${FILE}` refusal was stricter than real (R3), the substitution variables were incomplete (R4); R5 proves a live `ebuild … unpack` download with the default and with a user `FETCHCOMMAND`, verifies the `.mirror-cache.json` round-trips both ways and re-runs L2 (0 unexplained). Shipped: `portage-fetch` gained `FetchCommands` (per-protocol selection, real's `${FILE}` refusal) + `download_with_commands` (POSIX split, `${DISTDIR}`/`${URI}`/`${FILE}` substitution, fresh-failure cleanup) and a public `split_shell_words`; `WgetFetcher`/`Director::fetch` and `portuale::fetch` both take the resolved family (`fetch_commands_from_config` reads the config scalars, `resolved_fetch_commands` resolves the chain in `ebuild_phases`); `FetchOptions::ro_distdirs` (`resolved_ro_distdirs`, existing dirs only) is checked before the fsmirrors copy and every candidate, layout-resolved, digest-checked and symlinked. Unit + integration tests on stub commands and the RO symlink; `FEATURES=distlocks` stays the documented cut. Original report follows. **Fetch transport overrides are ignored: `FETCHCOMMAND`/`RESUMECOMMAND` (and protocol variants) plus `PORTAGE_RO_DISTDIRS` (found while consolidating #14).** `portage_fetch::download_via_wget` hardcodes `make.globals`'s default wget template, so a user's `FETCHCOMMAND="curl …"` (or `FETCHCOMMAND_<PROTO>` and the matching `RESUMECOMMAND*`) is never used; real picks the protocol variant, falls back to the plain name, substitutes `${DISTDIR}`/`${URI}`/`${FILE}` and refuses a command without `${FILE}` (`fetch.py:1652-1700`). Two call sites need it (`portuale/src/fetch.rs` and `mrg-director`'s `Director::fetch`). `PORTAGE_RO_DISTDIRS` (`fetch.py:1055-1059`, `:1456-1475`): existing dirs only, layout-resolved, digest-checked, then **symlinked** into a writable `DISTDIR`; portuale only lists the name in `phase_environ`. Slices: S1 FETCHCOMMAND family medium, S2 RO_DISTDIRS medium, S3 close-out trivial — [`history/01.014-fetch_candidates_mirrors.md`](history/01.014-fetch_candidates_mirrors.md) §5. `FEATURES=distlocks` stays a documented cut.

88. **DONE 2026-09-20 — `bin_dir()`'s per-process PATH overlay is never removed (found auditing pmtest's own /tmp exhaustion, 2026-09-18).** One `libc::atexit` registration where the overlay is created (`ebuild_phases.rs::bin_dir`) removes `$TMPDIR/portuale-bin.<pid>` on every normal process exit -- a single site covering all seven call sites across three files, instead of per-command cleanup on every exit path. Fallback paths (vendored/checkout used directly) register nothing; signal kills still leak (same as any tmp dir). Measured: one phase-touching test process leaves 1 dir before, 0 after; `remove_bin_overlay_dir` idempotency pinned by unit test. The pmtest harness-side sweep stays as defense in depth. Original entry follows. **`bin_dir()`'s per-process PATH overlay is never removed (found auditing pmtest's own /tmp exhaustion, 2026-09-18).** `ebuild_phases.rs:666` makes a fresh `$TMPDIR/portuale-bin.<pid>` directory of symlinks (the `PORTAGE_BIN_PATH` overlay: vendored `bin/` over the `3rdparty/portage` checkout) on every process that runs real ebuild phases, and nothing ever deletes it -- not on success, not on error, not `atexit`. It hid inside `/tmp`'s general churn; pointing `$TMPDIR` at a dedicated directory (`../pmtest/USAGE.AGENTS.md` "Spazio /tmp") made it visible as ~500 tiny leftover directories per full contract-suite run, growing without bound run after run until pmtest started sweeping them itself from the *outside* -- a real leak in the product, not an artifact of the harness sweeping around it. Fix needs to run inside the same process (the harness can only clean up *after* the fact): either an RAII guard (`Drop` removing `overlay` when the last user is done) or an explicit `std::fs::remove_dir_all(&overlay)` on every exit path of the command that calls `bin_dir()` -- whichever fits the existing process lifetime better, since `bin_dir()` itself is a `OnceLock`, resolved once and read from many call sites for the rest of the process. Trivial in isolation; the only judgment call is where in the process's own shutdown path the removal belongs. Evidence: `../pmtest/pytests-contract-suite/conftest.py`'s `_reclaim_portuale_bin_overlays` (the harness-side sweep, commit `0a3397d` in pmtest) and its own commit message for the measured before/after counts.

91. **DONE 2026-09-19 — host `@world` upgraded `sys-firmware/edk2-bin-202608` over installed qemu's `~202408` RDEPEND pin; real withholds it (#79 residue, filed 2026-09-18).** S0 (`fd05a5d`) captured the mechanism with the first `--debug` of the shape (slot-conflict keep-installed, persisting at `--backtrack=0`, hence the #90 link); S1 (`3f5e21c` + pmtest `919e5e9`) ports the gating (runtime-key enforcement, effective-layer read, bug-612772 skip); S2 (`340fa6e` + pmtest `5d4d40b`) guards D1/#57/#54 plus the host `@world` re-diff; S3 (this commit) pins the twin (CASES + `test_oracle_91_…`; the bed cell landed in S2) and closes the docs. Residue filed as #92 (the skip-conflict notice has no portuale rendering yet). Original entry follows. **Host `@world` upgrades `sys-firmware/edk2-bin-202608` over installed qemu's `~202408` RDEPEND pin; real withholds it (#79 residue, filed 2026-09-18).** New portuale (with #79's holdable-plain-pin ignore) upgrades edk2 where real 3.0.82.2 and pre-batch portuale hold it: the only stable `@world` delta on this host (normalized diff otherwise clean, and #86 restored the 20 missing go-consumer rebuilds on top). Some 15 hermetic probes could not isolate the trigger: operator (`=`,`<=`,`<`,`~`), pin USE-deps (present/absent/satisfied/failing), pin count, consumer visibility, walked-ness, world-membership, USE-conditional wrapping, and the full `~`+usedeps+conditional+siblings replica ALL upgrade in real -- yet host qemu (`~edk2-bin-202408[qemu_softmmu_targets_x86_64(+)]` in a `pin-upstream-blobs?()` group with three sibling pins, consumer installed 10.2.3-r1 with a tree downgrade available) withholds. Narrower rules were considered and rejected against the oracles: use-deps-gating contradicts the hermetic `~`+usedeps upgrade probe; BDEPEND/RUNTIME key-gating contradicts the hermetic RDEPEND `<` upgrade probe; `=`-only contradicts the hermetic `=` upgrade probe. Unexplored leads: the vdb-only-consumer withhold (two synthetic probes withhold; no committed shape covers it), the `-r1`/downgrade-available consumer, the conditional-group `unevaluated_atom` path (bug 764764), and the direct-solve's forced-set exploration (#90's function -- the withhold may be its keeping-installed verdict, which would unify this with #90). The `dev-python/astroid` host delta seen once during triage is NOT this: the checkout's asteroid ebuilds flip readable/unreadable across runs (overlayfs dentry corruption on this host), so neither PM's answer there is meaningful. Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) #79 residue.

93. **DONE 2026-09-20 — post-merge cleanup leaves `/var/tmp/portage/<cat>/<pf>/files` behind with `rm: cannot remove ...: Is a directory` (host `emerge -1 =sys-libs/timezone-data-2026d`, filed 2026-09-20).** Real `prepare_build_dirs._prepare_fake_filesdir` symlinks `${PORTAGE_BUILDDIR}/files` unconditionally (even dangling); portuale linked only when the repo `files/` was a real dir and otherwise let `create_directories` make a plain directory -- whose `rm -f` in upstream `__dyn_clean` then fails and, under the phase's `set -e`, aborts the rest of the cleanup. Fix mirrors real: always link (self-loop guard kept), `create_directories` skips the symlink, stale plain dirs are replaced by the link (removal strictly scoped to the known inside-builddir path, never following the link). The vendored `__dyn_clean` is untouched (byte-identical upstream). Verified: shell-level `rm -f` repro of both shapes, three `filesdir` unit tests, workspace + pretend suite green.

92. **DONE 2026-09-20 — real's withhold-skip notice has no portuale rendering (#91 residue, filed 2026-09-19).** S2 of #90 renders it: `GraphResult::skipped_updates` fed by the direct solve's removals *and* by enforced holdable pins that settle the installed version while a higher visible candidate exists (host qemu/edk2 now prints the block instead of withholding silently), same shape, rc 0. Original entry follows. **Real's withhold-skip notice has no portuale rendering (#91 residue, filed 2026-09-19).** When real withholds an upgrade through the direct-solve keep-installed verdict it prints `WARNING: One or more updates/rebuilds have been skipped due to a dependency conflict:` plus the `conflicts with <pin> required by <consumer>` block (host qemu/edk2; hermetic `whblocker` cell); portuale withholds silently -- same merge list, same rc 0, no notice. Needs a `GraphResult`/display path for the skipped-update notice, grounded on the whpin bed cell (which withholds) and the rdcpin cell (which merges, so must stay notice-free). Plan: [`01.91-79_residue.md`](01.91-79_residue.md) S3 residue.

90. **DONE 2026-09-20 — at `--backtrack=0` real silently reconciles the solvable slot conflict that portuale reports (#62 residue, owner-confirmed 2026-09-18).** S0 (`3089188`) matrixed six bt0 cells against real 3.0.82.2 and stopped the plan's §1 (the titular silence is walk-order reuse, no handler runs); S1 (`ace7f59` + pmtest `e8a155c`, re-scoped with owner sign-off: LIFO+reuse parity) ports the stack discipline; S2 (`9514d49`) ports the direct solve itself (forced sets, removal with row repointing, skip `WARNING` -- which also closes #92); S3 (this commit) pins the reversed two-target notice (CASES + `test_oracle_90_…`), the bed `whpin` cell having landed in #91 S2. Residue filed: none new (dual-instance single merge row stays the documented display gap). Original entry follows. **At `--backtrack=0` real silently reconciles the solvable slot conflict that portuale reports (#62 residue, owner-confirmed 2026-09-18).** `--backtrack=0 dev-libs/slotconflictparent`: real prints no notice and exits 0 -- `_solve_non_slot_operator_slot_conflicts` (`depgraph.py:1798`) is not gated on backtracking (the bt0 capture says `backtrack: 0/0`, zero retries, yet reconciled), while portuale reconciles only through its backtrack retry loop and so records the conflict (notice + rc 1 since #62). A recording divergence, not an exit-code one: #62's rule (recorded conflict => rc 1) is correct and stays. Closing this means porting the direct solve (conflict-graph analysis over `_package_tracker.slot_conflicts()`), a medium slice with its own oracle matrix; the re-pin (`slot conflict: --backtrack=0 disables reconciliation`, rc 1) cites it until then. Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) #62 S2.

89. **DONE 2026-09-18 — `/etc/portage/package.*` directory fragments recurse with real's name filters.** `portage_util::recursive_config_files` ports `_recursive_file_list` exactly (arbitrary depth, one ascending sort per directory over files+dirs with in-place expansion, `VCS_DIRS` + dotfile + `~`-backup skips at every level including the root, symlinks followed), shared by `read_config_lines` (every `package.*`/`binrepos.conf`/profile read) and the `repos.conf` scan (which real also reads recursively); both halves landed together since recursion without filters would activate `.git/`+backups. Pins: `recursive_config_files_matches_reals_traversal_and_filters` unit test (mirrors the S0 `_recursive_file_list` oracle table incl. symlinks and root filtering). No existing fixture uses the directory form, so zero existing pins moved. Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) #89. Original entry follows. **`/etc/portage/package.*` directory fragments are read one level deep, and real's name filters are missing (reported from the VM bed, 2026-09-18).** Real reads every one of these paths with `grabfile_package(..., recursive=1)` -> `portage/util/__init__.py::_recursive_file_list`, which walks a directory to **arbitrary depth**, skipping `VCS_DIRS` (`CVS RCS SCCS .bzr .git .hg .svn`, `const.py:278`), dot-prefixed names and `~`-suffixed backups (`_recursive_basename_filter`), and yielding files in **one merged ascending sort over files and directories, with each directory expanded in place** (stack DFS: `aa/inner` sorts between `00-first` and `nn-mid`; `zz/inner` before `zz-last`). Portuale's `portage-profile/src/lib.rs::read_config_lines` (~`:2044`) instead does a single `read_dir` filtered by `.filter(|p| p.is_file())` -- a subdirectory is silently dropped -- and applies **no** name filter, so it reads exactly the files real ignores. A second, independent copy of the same logic sits in `portage-repo/src/lib.rs` (~`:1143`) for `repos.conf`, which real also reads recursively (`repository/config.py:943-950`). Both halves are divergences and they point opposite ways, so **adding recursion without the filters makes things worse**: `.git/` and editor backups under `package.use/` would become live config. Reported from the VM bed as an `INVALID` run (`MERGE_RC=2`): the stock cloud image ships `/etc/portage/package.use/releng/no-filecaps` (`*/* -filecaps`), real honours it and merges the htop binpkg, portuale ignores the fragment, computes `filecaps` from the IUSE default, sees a binpkg USE mismatch and rebuilds from source; the same probe in the container bed (no `releng/` fragment) has both PMs agree, which is what isolates it. Reproduced host-side 2026-09-18 on a copied configroot: with `package.use/releng/frag` = `*/* nestedtestflag` and `package.use/zz-flat-control` = `*/* flattestflag`, real's `portage.config(config_root=...)['USE']` carries **both**, portuale's `emerge --info` carries only `flattestflag`; adding `zz-flat-control~` and `.hidden-frag` shows the mirror defect (real ignores both, portuale applies both). One `read_config_lines` feeds every `/etc/portage/package.{use,env,license,mask,unmask,accept_keywords,properties,accept_restrict}`, `binrepos.conf`, and the profile/repo-side `package.*`/`use.*`/`arch.list` reads, so the fix is two functions, not twenty call sites. No committed fixture uses the directory form at all today, which is why nothing caught it. Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) "#89". [C]

## Tier 2 — larger / needs design first

17. **CLOSED (deliberate cut) 2026-09-15 — cluster I merge-order timing (F-B1 drain timing).** B-series (025 plan, 2026-09-12) landed the trace harness (`TEST/scripts/mo-trace/`) plus two fixes: the installed-slot closure (`by_cp` now keeps every version; `MO_NODES` 398==398 on gtk:4) and bare multi-slot atom resolution in `build_digraph` (firefox #29→#37, thunderbird #30→#38). L0: clean 96→98, parity 0.800→0.817, order 22→20 raw (19 with the B5 gnome-shell explanation). F-B3/F-B4 moved to #25 (closed 2026-09-15, #25). **R5 (2026-09-15, D5 600s time box):** step 1 re-measured against R3b's `-pe @system` seed-sort fix (`9284f8c`) — confirmed NOT fixed (gtk:4 still 578 iterations vs real's 290, node sets equal, `TEST/logs/r3c-revalidate-20260915/` traces). Step 2, one lever: the same seed-sort gap survives in `add_installed_dependency_closure`'s complete-mode `@system` seeding (`merge_order.rs` Seed 1b), which every complete-mode probe hits, not just an explicit `@system`/`@world` argument — ported the identical fix (`merge_order.rs::add_installed_dependency_closure`, R5). Verified zero effect: 14-probe container re-run and a full L0 run (`l0-20260915T073208Z`) are byte-identical to the pre-fix baseline on all 120 probes, confirming the wall is downstream of seed/discovery order, in the frontier's installed-node drain timing itself — matching the 2026-09-09 "Deeper dig" conclusion (`TEST/findings/l0.md` "## I") that pinning it down needs per-node drain tracing across the ~400-node graph, a multi-session investigation. Filed as a deliberate cut per D5 with the lever table in `TEST/findings/l0.md` "## I" "R5 — #17 closed". The seed-sort fix is kept (harmless, matches real's `sorted(arg.pset.getAtoms(), key=str)` exactly) but reopens nothing. [A]
18. **DONE 2026-09-10 — `_FrontierDigraph` perf layer.** `merge_order.rs::SerializeFrontier` shipped (`PORTAGE_SERIALIZE_FRONTIER_DISABLE` fallback, unit-pinned behaviour-neutral; see `scope-backlog.md` and `what-this-proves.md`). Stale entry corrected 2026-09-12 (025 B6). [A]
19. **DONE 2026-09-11 — DFS-partial merge-list truncation.** Slices 1–6 shipped (`4d5bffe` S1, `6b10b16` S2, `ffdd251` S3, `eef8494` S4, `354274e` S5, `e4e83c7` S6): outcome model + `PORTUALE_ABORT_PATH` gate, `abort_outcome` membership/order, exit 1 on masked/unsat/cycle aborts, the partial list + counters + `--json` `aborted`, and the error-block wiring. The three remaining strict xfails are **not** #19 (one `--debug` narration gap, two spec-§4e autounmask findings). Real-tree plasma-meta/podman residue is a `||`-selection gap (spec §4d), tracked separately. Detail: `scope-backlog.md` §A "DFS-partial abort path" entries; oracle `docs/history/abort-path-spec.md`. [A]
20. **DONE 2026-09-14 — `[use]`-dep unsat diagnostics (residue of #20).** A `[use]`-dep dependency atom with no autounmask flip now prints real `_show_unsatisfied_dep`'s separate "no ebuilds built with USE flags to satisfy `<atom>`." block: the `!!! One of the following packages is required…` header, the `- <cpv>::<repo> (<reason>)` rows (`Change USE: +flag -flag` for adjustable flags, real's "only show the latest version" rule; `Missing IUSE: <flags>` when the flag is not a valid IUSE at all), and the shared `(dependency required by …)` chain. Dual-language: `portage-repo::UseUnsatDepReport` + `use_unsat_candidates_for_atom` / `GraphResult::use_unsat_deps`, rendered in `pretend.rs`/`emerge_pretend_reference.py` ahead of the masked block (real's precedence), with the chain walked by the existing `masked_dep_chain`. The two existing pins were re-pinned to the new text; two new fixtures (`unsatuseiuse` Missing IUSE, `unsatusealtmultidep` latest-only) with pinned-output tests + oracle captures (portage 3.0.81.3); two Rust unit tests. Real's parent-conditional row and the masked-candidate suppression of the Missing IUSE fallback stay documented narrowings (same #19 park). Evidence: `what-this-proves.md` 2026-09-14. [A]
22. **DONE 2026-09-11 — `dep_zapdeps` finer choice bins.** `all_available`/`all_use_satisfied` split, `unsat_use_*` bins with the bug-515584 unmask gate, `all_installed_slots`, in-bin upgrade-preference ordering (slice 2, `use_reduce_flat_disjunctive`'s `tie_break` + `portage-repo::promote_tied_alternative`), and the `other_*` bins + `allow_masked` two-pass return (slice 4, `portage-repo::atom_matches_installed`) all shipped, both languages, one shared probe + tie-break pair. Formally cut, documented in `disjunction_preference`'s own doc comment (slice 5): `conflict_downgrade`/`installed_downgrade` (carved out 2026-09-11 as #35 per `docs/history/023-backtracking_resolve.md` B2; **shipped by #35 2026-09-15**) and `circular_atom` (needs a `circular_dependency` dict from an earlier real pass, plus `--onlydeps`, neither threaded to this call depth); `minimize_slots` (needs `_overlap_dnf` DNF detection, not done) and `want_update` (needs the `--update`/`--newuse` "wanted" set, pinned `false`) stay cut per slice 2. Real `lib/portage/dep/dep_check.py::dep_zapdeps`. Code: `portage-repo` `disjunction_preference` + `promote_tied_alternative` + `portage-use-reduce` `resolve_disjunctions`; `solver_bridge.rs` is #34, not #22. Design brief: `docs/history/022-agent-task-22-zapdeps.fable.md`. Detail: `what-this-proves.md`'s 2026-09-11 `||`-resolution paragraphs. [A]
23. **DONE 2026-09-11 — backtracking resolver search.** Portuale's `'backtrack` loop is now real's `_backtrack_depgraph` node-stack search (`Backtracker`: DFS `get`, param-only dedup, bug-375573 `_check_runtime_pkg_mask`, config/slot-conflict/missing-dep `feedback`, `get_best_run`; `--backtrack=N` bounds `mask_steps`, config retries free; dead-end abandonment instead of settling masked graphs into the abort path), fed by a real-shaped `slot_conflict_mask_choices` (692746 append, version-desc, per-candidate `conflict_atoms`, stable len-sort, first-slot-only, one node per choice, similar-grouping) with puller pre-masking removed (parent downgrades arrive via the mask-aware missing-dep probe). Non-goals, explicitly out (see `docs/history/023-oracle.md` + `what-this-proves.md` 2026-09-11): `--debug` "backtracking due to slot conflict" narration (unpinned, merge-list parity only); "masked by backtracking" listing in `_show_unsatisfied_dep` (no such string; `get_best_run` rationale only); combined config+conflict exploration order (first-hit-wins kept); real's exact stopping rule (`max(1,(N+1)//2)` depth + total-restart cap); #35 downgrade guards (separate item). Follow-ups own the remaining divergences: #25 (btnr installed instances), #24 (a522084 `B-0` rebuild), #36 (mg3-bt1 selection fallback). Plan: `docs/history/023-backtracking_resolve.md` (+ inventory, oracle); shape: `ResolveCtx`/`BacktrackParams`/`run_pass`/`collect_feedback`/`assemble_result`. [A]
24. **DONE 2026-09-12 — slot-operator rebuild undo path.** Add backtracking for a rebuild that shifts another sub-slot, re-bind the rebuilt consumer's own `:=` deps, and the `--changed-slot` interaction (one missing piece, not three). `slot_operator_rebuild_entries`; citations in `history/scope-backlog-2026-09-05.md`. S1-S6 shipped 2026-09-12 (plan `docs/history/024-slot-operator-plan.md`, oracle `docs/history/024-oracle.md`): S1 complete-mode gate under `--deep` (the a522084 `B-0` miss was the missing auto-enabled pass, not the undo path); S2 22 oracle pins over the whole `test_slot_operator_*` family; S3 the rebuild is now a **walked graph node** -- `slot_operator_rebuild_scan` feeds `BacktrackParams::slot_operator_replace_installed` through `collect_feedback`, the next pass seeds it like real's `@__auto_slot_operator_replace_installed__` `SetArg` and flips the outcome in-walk, so deps, `:=` re-bind and merge order are real (`[A-2, B-0, C-0]`, bug 522652); no synthesised entries left on the default solver; S4 `_eliminate_rebuilds` -- the nine ordered rules + the graph-aware `:=` binder (`bind_slot_operator_deps`, real `_eval_deps` over `_graph_trees`), demotion via `slot_operator_undone` latch, `slotundo-unnecessary` now MATCHes (`[U] souprov`, `abi_rebuilds: []`); S5 `_slot_change_probe` -- bug 456208's slot-move-without-revbump now schedules the installed child (`slotchange-1`/`regslotchange` MATCH, `[rR]` child + plain parent, no block), real's rule 3 landed behaviour-neutrally, and two S2 expectations were corrected live (the `--changed-slot` flag half has no `r`/edge in real; conflict-mass is v2 `#24b`'s update probe, not the probe); S6 L0: 120 probes, 96 clean, parity 0.800, `portuale/` raw outputs byte-identical to the S0/S1 archives (the slot-op path does not fire on the corpus) -- no regressions, no findings to adjudicate (`TEST/findings/l0.md`). S7: final verdict table (`docs/history/024-oracle.md`), `what-this-proves.md` S1/S2/S5/S6 paragraphs, scope-backlog/agent-context. S4 finding: bug 614390's `complete` case is blocked by a **selection** gap (named bare `socc` resolves before meta's `=socc-1` through the already-installed fast path, which skips `resolved_slots`; real's `_add_pkg` slot-parent check catches it) -- #36-overlap, not the undo; stays strict-xfail. **Non-goals, stated:** the v2 probe family `#24b` (update probe + `check_reverse_dependencies`), `#24c` (`slot_operator_mask_built`; every binary half), `#24d` (`prune_rebuilds`), `#24e` (`_slot_conflict_backtrack_abi`), `#24f` (in-walk unsatisfied probe); `IUSE_EFFECTIVE` in the built-dep domain; `--rebuild-if-*` through the same path (G0.6); the `--debug` "backtracking due to missed slot abi update" narration and the `Dependency resolution took…` line (G0.7, cut). [A]
25. **DONE-PARTIAL 2026-09-15 — `_complete_graph` installed nomerge nodes** — real carries every `@world`/`@system`-reachable installed package as a graph node; portuale keeps only their reverse-dep atoms. **Now owns the B4 superseded-in-edge model too** (`docs/history/025-tier2-closeout.deepseek.md` §11 F-B4, 2026-09-12): real drops an installed node's in-edges when a same-slot merge supersedes it, and draws no edge for a dep initially satisfied by installed (`nghttp2 -> systemd` gone; gedit #5/nautilus #8). Walk-order dependent; needs the resolver's insertion order, so it also subsumes F-B3's `_create_graph` pre-bias order work. **R3b DONE 2026-09-15** (`docs/history/025b-complete-graph-nodes.md` §7): the pre-bias divergence was the *seed* order, not a missing `insertion` field -- real `_resolve` processes each `SetArg`'s atoms `sorted(arg.pset.getAtoms(), key=str)` (`depgraph.py:5500`) while portuale used profile order; sorting the `@system` segment takes `-pe @system`'s 368-node `MO_ORDER` to three localized residues (a docbook 4.5/4.2 swap from real's own hash-seeded `_minimize_children` sets, `depgraph.py:4751-4781`; the portage-version visibility row; a 12-node `virtual/man` disjunctive bundle pop timing). L0 flipped-probe log: `TEST/findings/l0.md` "R3b"; probe artifacts `TEST/logs/r3b-20260915T000000Z/`. **R3c attempted 2026-09-15, STOPPED** (`docs/025b` §8, `TEST/findings/l0.md` "R3c attempt"): the edge target depends on real's per-atom insertion instant; the blunt drop fixes gedit's nghttp2 but regresses `_system`/`_world` (L0 `l0-20260915T051103Z`: clean 97 / order 20 vs 100 / 17), the DFS-rank gate preserves L0 but does not fix gedit (nghttp2/systemd insertion ranks disagree). Work reverted; tree at R3b/H4. **Revalidated 2026-09-15, R3c/R3d WITHDRAWN** (`TEST/findings/l0.md` "R3c revalidation"): the "insertion instant" diagnosis was wrong. Real re-creates a superseded installed node's in-edges towards the merge via the slot-conflict solver's re-walk, and gedit/nautilus lose `nghttp2 -> systemd` only because that re-walk aborts at gnome-keyring's unsatisfiable `gcr[gtk]` (CPython-set-order dependent, so F-B4 is cluster-A abort residue). Scheduler node sets already equal real's on 16/18 L0 order probes. D7 answered 2026-09-15 (option 1); **R3e′ closed it on evidence**: `TEST/compare/known-divergences.yaml` `gedit-nautilus-cluster-a-rewalk-abort` explains the two `[order]` rows (two-probe L0 `l0-20260915T063724Z`: both explained, portuale output byte-identical to `l0-20260915T005709Z`, advice/error rows unchanged -- they are the separate masked-dependency disclosure gap), so L0 unexplained order 17 -> 15. Residues: (a) `-pe @system` `virtual/man` `||`-bundle pop timing (12 nodes, 22 positions late) and (b) the `docbook-xml-dtd` 4.5/4.2 swap from real's hash-seeded `_minimize_children` sets (both `docs/025b` §7); (c) **not closed by R3**: the 023 oracle `btnr` row (`test_oracle_backtrack_masks_are_discarded_with_their_reason`) stays DIVERGENT -- an installed `AlreadyInstalled` instance never enters `resolved_slots`, so it cannot be a slot-conflict party. That is the resolver-level half of this item; the scheduler-graph half is met (node sets equal on 16/18 L0 order probes). [A]
26. **DONE-PARTIAL 2026-09-12 — installed-package metadata overlay (`FakeVartree._apply_dynamic_deps`).** The old "walk source doesn't switch" line was false (verified live). A1-A4 landed: `InstalledMetaLayer` + `installed_dep_string` (Raw/Effective, per-pass memo, `--ignore-built-slot-operator-deps` gate), `enqueue_dependencies`/scheduler closure/`GraphEntry::deps` read the same view, installed-parent `slot_pullers` recorded. The built-`:=` append itself stays behind `PORTUALE_DYNAMIC_DEPS_APPEND` (default off) because of **F-A1**: with it on, the Python mirror folded a solvable slot conflict real resolves via the slot-op probe (slotchange-case4), and the cascade probe drops a consumer. **2026-09-15:** the mirror is gone; with the gate on, only `test_oracle_slotop_undo_cascade` (the dropped consumer) still fails, so that one Rust case is the whole remaining blocker. Residue in `docs/history/025-tier2-closeout.deepseek.md` §11 F-A1; detail plan `docs/history/024-dynamic-deps_n_disagreement.deepseek.md`. [A]
27. **DONE-PARTIAL 2026-09-12 — `need_rebuild` slot-conflict trailer.** Renderer + installed-parent `slot_pullers` recording (025 A4) + a Rust-pinned fixture for the `--exclude` and `--useoldpkg-atoms` reasons, under the A1 append gate; gate-off negative control pinned. Residue: the `--usepkgonly` silence arm and the "ebuild is masked or unavailable" reason (needs the `_DynamicDepsNotApplicable` missing-ebuild fallback), (the Rust==Python parity half is moot: the reference was removed 2026-09-15). `docs/history/025-tier2-closeout.deepseek.md` §11 F-A2. [A]
28. **DONE 2026-09-15 — mrg-director: wire the remaining scaffolded slots** — after Tier 2.28 (`261e184`, re-verified 2026-09-13) and H1/H2/H3 (2026-09-14), **7/8 slots carry production traffic**: `SchedulerPolicy` (`run_build_scheduler`), `MergeEngine` (merge dispatch, `SourceEngine`/`BinaryEngine`), `NewsSelector` (`--check-news` via `FilesystemNews`), `Fetcher` (the `fetch_src_uri` candidate loop's `FetchRequest` dispatch), `PackagesDb` (H2: `find_owners`/`owns_path` read CONTENTS through `VdbReader`; `reverse_dependents` documented consumer-less), `BinpkgIndex` (H3: `merge_one_binary_entry`'s remote `Packages` lookup via `RemoteBinhostIndex::metadata_with_source`; `PkgdirBinIndex` still test-only -- the local path resolves by `$PKGDIR` scan), `RepoCache` (H4, 2026-09-15: `entry_metadata_env`'s per-build-entry metadata read goes through `mrg_director::Md5Cache`/`RepoCache`, whose `metadata` delegates to the #41 decision point `portage_repo::repo_aux_metadata` -- cache -> depcachedir -> depend phase -- so the seam carries the fallback unchanged; behaviour-neutral, L1 porttest unchanged). **H5 (2026-09-15, D6 "slots only"): closed.** Every slot, the solver's `Resolver` re-export included, has a production caller. `Director` stays the test-side composition type, with a documented trigger list for promoting it to the `action_build` entry (`docs/history/028-director-proposal.md`). Consumer-less by design: `PackagesDb::reverse_dependents`, `PkgdirBinIndex`, `VolatileCache`. [H]
35. **DONE 2026-09-15 — `downgrade_probe` + live `graph_db` for `dep_zapdeps`** — carved out of #22/#23 (2026-09-11, plan `docs/history/023-backtracking_resolve.md` B2); R4 of `docs/history/backlog_tier_5_and_2_sliced.opus.md`. Real's `conflict_downgrade`/`installed_downgrade` guards (`lib/portage/dep/dep_check.py` soft 476-541, bug 531656) now demote a `||` alternative to the `other` bin when one of its atoms would pull a lower version into a slot the live graph already holds higher, unless `_downgrade_probe` says the downgrade is desirable: `portage-repo` `alternative_downgrade_demoted` + `downgrade_probe` + `visible_tree_matches`, called from `disjunction_preference` (both `||` call sites). The live `graph_db` needed no new structure: both call sites already pass the resolver's in-progress `entries`. Oracle: upstream `test_or_choices.py::testConflictMissedUpdate` through portage's own `ResolverPlayground` (`TEST/logs/r4-20260915/`). Default budget `[ocaml-4.02.1, labltk-8.06.0, lablgl-1.05]`; with `_downgrade_probe` forced True, real merges nothing, which is what portuale did before. Fixtures `mlocaml`/`mllablgl`/`mllabltk`, Rust-only pin `test_or_choice_avoids_downgrade_into_the_graphed_update` (default budget and `--backtrack=0` partial). Fixing it exposed a second bug: `build_residual_slot_conflicts` reported a dropped reverse-dep pin whose consumer a later backtrack pass had turned into a merge-bound rebuild. It now applies `reverse_dependency_constraints`'s own "consumer being replaced" skip against the final entries. Port narrowing, documented in the function: `highest_in_slot` counts only graph entries (real's bare-installed `_select_package` result without `--update` is not threaded). [A]
36. **DONE 2026-09-14 — mask-aware candidate fallback in selection (rescoped; not reproducible as framed).** Re-captured against the genuine upstream oracle: `test_backtracking.py::testBacktrackingGoodVersionFirst` through portage's own `ResolverPlayground` on 3.0.82.2 settles `[C-1, B-1, A-1]` at the default budget and at **`--backtrack=4`** (its minimum; `<=3` aborts) — the mg3-bt1 "real settles at bt1" claim was false (real's own bt1 aborts, see `docs/history/023-oracle.md` "R2 — genuine upstream oracle"). Portuale already applies the runtime-mask `!` negatives during selection (`resolve_pretend`'s `extra_constraints` filter) and reaches the same fixpoint at `--backtrack=2` / at real's bt4; the only residue is the *already documented* mask-step budget accounting, not a selection gap. New fixture translation `btgp`/`btgb`/`btgc` + Rust-only pin `test_backtracking_good_version_first_matches_the_upstream_oracle`. [A]
59. **DONE 2026-09-16 — `--debug` narration fidelity: `Parent Dep` child attribution and the backtrack-step count (#57 K5 + S1 residue).** S0/S1 `87fd92b` collect real `_add_pkg`'s per-instance parent-atom rows in the walker and render `Parent Dep: <atom>[(<unevaluated>)] required by <parent>` under the child instance the atom resolved to, with the installed `paired-1.0` block for the shadowed instance and bare-atom argument rows (the cp-keyed `(Argument)` shape is gone); the real container capture is in the plan. S2/S3 (this commit) add `"backtrack":{"restarts":N,"max":M}` to `--json` (K1 option (a): no stdout line; K2 keeps the static preamble a recorded cut) and close the docs. Original report follows. **`--debug` narration fidelity: `Parent Dep` child attribution and the backtrack-step count (#57 K5 + S1 residue).** **(a) `Parent Dep` attribution (#57 §2 side note, confirmed in S1):** on the `needer`/`othermod` triangle the `--debug` dump files `Parent Dep: ... required by othermod` under the **`paired-2.0`** child; real files it under the installed `1.0`, and prints the dependency **atom** (`Parent Dep: <atom>[ (<unevaluated>)] required by <parent>`, `depgraph.py:3583-3604`), not the child cp. `slot_pullers` carries both parents correctly, but the narration reads `GraphEntry::required_by` (cp-keyed, no atom, no installed-node entry). **(b) Footer / count:** real's `Dependency resolution took <T> s (backtrack: N/M).` is printed by `_show_resolution_report` on **every** non-quiet run, not only `--debug` (plan review 2026-09-16; host `@world` capture shows `35.06 s (backtrack: 0/20)`), so it sits with the static-preamble deliberate cut; the deterministic count can still ride out in `--json` (K1). `show_lru_cache_info` is env-gated (`PORTAGE_SHOW_LRU_CACHE_INFO`). Stale path in `resolver_trace.rs`'s module doc (`docs/emerge-pretend-debug.md` → `docs/history/`) confirmed. Slices: S0 captures + path fix easy, S1 per-instance parent-atom side table hard, S2 counter easy, S3 trivial. Plan: [`history/02.059-debug_narration_fidelity.md`](history/02.059-debug_narration_fidelity.md).
62. **DONE 2026-09-18 — slot-conflict/residual notice exits 1, matching real Portage 3.0.82.2.** The #57 K1 standing convention is retired: `pretend.rs::run` fails the action (rc 1) whenever `result.slot_conflicts` is non-empty, in the existing display-then-fail band next to `unsolvable_blockers` (after the `--autounmask-only` early return, before any merge dispatch), gated on the data so `-q`/`--json`/`--columns` follow the same rule. Both real escapes ported: `--autounmask-only` keeps rc 0, and backtracking-off plus `--buildpkgonly`/`--nodeps` tolerates (`_accept_blocker_conflicts`; `--fetchonly`/`--fetch-all-uri` are still unimplemented options, rc 2 before reaching the gate). Every existing rc-0-with-notice pin re-pinned against a real capture (14 CASES entries + dedicated tests; the corpus re-blessed in pmtest). Residue filed as #90: at `--backtrack=0` real *silently reconciles* the solvable shape (`_solve_non_slot_operator_slot_conflicts` runs without backtracking) while portuale records the conflict and now exits 1 -- a recording divergence, not an exit-code one. Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) #62. Original entry follows. **Exit code for informational slot-conflict notices should be revisited in a future portuale release (#57 K1).** Portuale currently exits 0 for a residual/slot-conflict notice that real Portage 3.0.82.2 reports as an error (rc 1) — e.g. the `needer`/`othermod`/`paired` triangle (`docs/history/06.057-directly_requested_hard_atom_conflict.opus.md` §3 K1, `#54` K1). This is a deliberate standing convention ("slot-conflict notice is informational"), not a bug, kept for #57 S1 because changing it touches every slot-conflict pin in the suite. Filed here so the divergence from real's exit code is not forgotten: a future breaking release should reconsider it, with a migration story for anything already depending on rc 0. Not scheduled; no design work done. *(Filed 2026-09-16 as #58; #58 had already been taken, 13 minutes later but with a live `backlog/58` work branch, by the Tier 7 gpkg inner-`metadata.tar` item, so this entry is #62 -- the "renumbered to #60" note the #62 S2 sweep corrected was itself stale: Tier 7 #60 is the gpkg populate-path item. Git history before the renumber calls this item #58.)*

65. **DONE 2026-09-16 — slot-operator ABI rebuild scan ignores `--with-bdeps` (20 spurious Go rebuilds on host `@world`).** S0 (`98ca561`) staged the six-consumer `provpkg` matrix and captured real's per-key × bdeps × reach table (`TEST/findings/l0.md` "#65 S0", `TEST/atomlists/l0-fixture-oracle-slotop.txt`, `FX_SLOTOP_BDEP=1`); S1 (`4e7bad7`) threaded `with_bdeps` into `slot_operator_rebuild_scan` and reads `DEPEND`/`BDEPEND` only when it is true; S2 (this commit) reads them only for a consumer this pass **walked** (`entries`), matching real's non-optional `want_update` probe. Runtime keys (`RDEPEND`/`PDEPEND`/`IDEPEND`) stay unconditional; `slot_operator_slot_change_probe` untouched. Host `@world` re-diff: exactly the 19 Go consumers and go's `r` gone, nothing else moved (`dev-lang/go` is `[binary gU]`). Fixture-oracle bed green (`l0-fx-20260916T153958Z`, 22 explained / 0 unexplained, two adjudicated rows). One residue filed as #71 (`considep` runtime-key graph-completion quirk); the bdeps-on `--deep` walk divergences stay the slice-15 follow-up. Original report follows. `slot_operator_rebuild_scan` reads `RDEPEND/PDEPEND/DEPEND/BDEPEND/IDEPEND` of every reachable installed consumer with no `with_bdeps` gate. Host defaults carry `--getbinpkg` ⇒ `--usepkg` ⇒ real `bdeps` unset (`create_depgraph_params.py:97-103`) ⇒ built packages' `DEPEND`/`BDEPEND` are emptied before any dep is registered (`depgraph.py:4194-4247`): real upgrades `dev-lang/go` with no `r` flag and no consumer rebuilds, portuale flags go `r` and rebuilds 20 Go consumers (still present post-#63, `docs/history/evidence/2026-09-16-post63-probes/`). **Plan review 2026-09-16 withdrew the first design** (drop build-time keys unconditionally): with build deps walked (`--ignore-default-opts`) real **does** rebuild `app-containers/runc` `rR` through its BDEPEND `go:0/1.26.5=`, but only the argument-reached consumers (`runc`, `go-md2man`; real probes only `want_update` deps, `depgraph.py:3114,3802-3806`), while portuale rebuilds 18 including world-only ones. `slot_operator_slot_change_probe` (merge-bound parents) stays five-key. Slices: S0 oracle matrix (key × bdeps off/on × argument/world-only) medium, S1 scan honours `with_bdeps` easy (removes all 20 host pairs), S2 build-time keys only for update-probed consumers when bdeps is on hard (only if S0 confirms), S3 pins + docs easy. Plan: [`history/02.065-slot_operator_buildtime_deps.md`](history/02.065-slot_operator_buildtime_deps.md).

66. **WITHDRAWN 2026-09-16 (not a bug; fixed by #63) — installed packages' build-time deps drive installs/updates/downgrades.** Filed from `docs/history/evidence/2026-09-16-world-uDpvN/`, captured at `e54bd09` before #63: real ran with the host's `--getbinpkg` (⇒ `--usepkg` ⇒ `bdeps` unset ⇒ built packages' `DEPEND`/`BDEPEND` dropped, `create_depgraph_params.py:97-103`, `depgraph.py:4194-4247`) while portuale ignored `EMERGE_DEFAULT_OPTS` and walked them. The claimed rule ("built ⇒ optional ⇒ never updated") is false: `optional` only matters when no child is selectable (`:3396-3398`), and with `--ignore-default-opts` real downgrades `flit-core`, installs `source-highlight`, updates `cargo-c`/`libgit2`. Post-#63 portuale (already `with_bdeps = with_bdeps_auto && !(usepkg||getbinpkg||…)` in `pretend.rs`) matches real package-for-package on the scipy/cloudpickle/libtracefs/runc probes and none of the #66 packages remain in the host `@world` diff (`docs/history/evidence/2026-09-16-post63-probes/`). **Do not implement the original S1** (it would introduce a divergence). S1 (the both-ways pin) **landed 2026-09-16**: fixtures `bdepcascadepkg`/`bdepcascadedep`, six real-oracle cells, portuale matching before any code change (`test_oracle_bdeps_default_of_an_installed_parent_drives_dependency_updates`). Plan: [`history/02.066-installed_buildtime_optional.md`](history/02.066-installed_buildtime_optional.md).

71. **DONE (re-scoped) 2026-09-17 (`#71 S0`–`S2`, branch `backlog/68-74`) — the world-only `IDEPEND` residue was a bed artifact, not a portuale divergence; the matrix is now a host-exact gate.** S0 `a23cd94` traced real's `_slot_operator_deps`/`_complete_graph` pipeline (`TEST/findings/l0.md` "#71 S0"): the extra probes come from the bed's `ROOT=$FX ≠ /` staging (real's slot-op cascade splits across two roots; the #65 S0 reading took the `to-FX` subset as the whole merge set), and host-exact (`ROOT=/`, `PORTAGE_CONFIGROOT=$FX`) real's sets and `Total:` rows match portuale's current scan in all 15 argument/option cells. S1 `13e4683` re-scoped the item to the bed fix: `FX_HOST_ROOTS=1` in `in-container.sh`/the runner, both artifact allowlist entries deleted, one `order` entry added for the three `@world` cells (real's complete-graph order vs portuale's discovery order; sets/`Total:` equal, the standing #17 class). S2 closes the docs. No product code changed -- the runtime-key gate had nothing to implement. Evidence: `TEST/findings/l0.md` "#71 S0"/"#71 S1"; host-exact bed `l0-fx-20260917T174011Z` rc 0, default bed `l0-fx-20260917T174105Z` rc 0. Original entry follows. **Runtime-key slot-op rebuilds are larger than real's: a world-only `IDEPEND` consumer is rebuilt only when the argument is a build-time/IDEPEND consumer.** Found in #65 S0 (`TEST/findings/l0.md` "#65 S0" residue 1; bed cells in `TEST/logs/l0-fx-20260916T153720Z/`). In the argument shape (one consumer as the argument, the other four world-only), real 3.0.82.2 rebuilds the world-only `considep` (`IDEPEND`, `installtime=runtime`) when `consdep`/`consbdep`/`considep` is the argument, but **not** when `consrdep`/`conspdep` is: its graph completion only reaches some of the world-only runtime consumers, a quirk of the trigger's `_slot_operator_deps` registration (`depgraph.py:3089-3132`), not a `want_update` rule. Portuale scans every `slot_op_reachable` runtime consumer and so adds `considep` in the two cells real leaves alone. #65 S2 deliberately left the runtime keys unscanned (only build-time keys got the walked-consumer gate); fixing this belongs next to #24's trigger/graph-completion work. Allowlisted as `slotop-idepend-world-only-arg-consumer` in `TEST/compare/known-divergences-fixture-oracle.yaml`; delete the entry when this closes.

72. **DONE 2026-09-17 (`#68/#72 B0b`–`B5`, branch `backlog/68-74`) — `[uninstall]` merge-list rows are not modelled.** B1 `2ad72d4` carries the replaced-in-slot row, B2 `cdbb8eb` prints it inline only when the replacement waits on its owner (B0b's p2c/p2d/p2e cells; `--columns` suppresses the line but keeps the count), B2c `9754242` gives uninstall-resolved blocks real's complete-mode retry closure without touching the slot-op gate, B3 `d5804d1` models `PretendOutcome::Uninstall { version }` with `required_by` = owner (every merge/execution site skips it; execution stays a non-goal), B4 `916b9b5` renders `[uninstall     ] <cpv>` (PKG_UNINSTALL, `-q` pad, `--columns` skip, `-pv` vdb decoration), prints the satisfied `b` inline after it and counts `uninst` in `Total:` after `binary`; `--json` gains the outcome. Pins u1–u6 in `tests/test_emerge_pretend_contract.py` (B0 bytes; corpus re-blessed for the three blocker cases + the expanded grid), host `-uDpvN @world` unchanged from B2. Evidence: `TEST/findings/l0.md` "#68/#72 B0b"–"B4". Residues: #75 (`--tree`), #76 (`||` wait chains), #77 (nomerge-owner removal rows). Original entry follows. **`[uninstall]` merge-list rows are not modelled.** Real prints a fake-nomerge `[uninstall     ] <cpv>` row when a blocker resolution or a slot cleanup removes an installed package (`_validate_blockers`' `depends_on_order` step, `depgraph.py:9226-9246`; the #68 S0 containers show it plainly: `emerge -p dev-libs/blockerpkg` → `[ebuild N] blockerpkg` + `[uninstall] samepkg-1.0` + `[blocks b]`, rc 0). #68 fixed the block classification, exit code and the host `@world` abort, but portuale's merge list still omits the uninstall row, so those shapes differ by one line (and the `Total: … (1 uninstall)` counter detail). Needs a `GraphEntry`-level uninstall outcome (or a display-only row) plus merge-order/counters handling; the revert path (`--depclean`/`-C`) already has its own model. Filed from #68 S2/S3 while pinning cells d (`TEST/findings/l0.md` "#68 S0").

73. **DONE 2026-09-17 (`#73 S0`–`S2`, branch `backlog/68-74`) — the blocker graph-parents test ignores set arguments, so a nomerge owner in `@world` is misclassified.** S0 `aa3ec9b` captured g/g2/g3/g4/f against real (all four `B`/rc 1; `installed_closure` holds the owner in all of them, `top_level_cps` misses g3), S1 `66af2bd` made the nomerge arm `merge_bound_match && (graph_has_parent(owner) || owner_set_parent(owner, installed_closure))` and collected the installed owner's unresolved rows into the trailing `[blocks B]` group, S2 (this commit) pins the cell and closes the docs. Evidence: `TEST/findings/l0.md` "#73 S0"/"#73 S1". Original entry follows. **The blocker graph-parents test ignores set arguments, so a nomerge owner in `@world` is misclassified.** #68 S3's `graph_has_parent` scans walked entries' deps; a `@world`/`@selected`/`@system` member has the *set* as its graph parent in real's digraph, so when a blocker atom matches a merge-bound package while its owner is an installed (nomerge) node, real uninstall-orders the owner and reports `B` rc 1. Container capture (cell g, `TEST/findings/l0.md` "#68 S0"): installed `bparent` + `=dev-libs/blocked-1.5` pinned, `-uD @world` -> real `B` rc 1, portuale no block row rc 0. Need to thread the set/top-level membership (`ResolveCtx::top_level_cps`, and the set-arg nodes themselves) into the `resolve_blockers` nomerge-owner arm; not the host shape (that one is fixed). Filed from #68 S2/S3.

74. **DONE 2026-09-17 (`#74 S0`-`S3`, branch `backlog/68-74`) - bdeps-on `--deep` walk package-set divergences (the #65 slice-15 follow-up).** Two independent classes, both diagnosed before code. **Class A** (portuale extra `dev-lang/rust-1.94.0` `NS`): real's `dep_zapdeps` `mydbapi_match_pkgs` is `_dep_check_composite_db`, whose `_select_package` matches an *installed* instance through its recorded vdb USE (`dbapi._match_use`'s built branch) and an ebuild candidate through the profile USE, then `_want_installed_pkg` keeps the installed match as a nomerge selection; portuale only checked the tree candidate, so the installed-slot alternative was demoted to `unsat_use_*` and the cp-level `Installed` of the uninstalled slot won. S1 `814925e` (fixtures `altprov`/`altconsumer` + real oracle): `disjunction_preference`'s `use_ok` gains `atom_matches_installed`, `resolve_pretend` returns `AlreadyInstalled` for a dependency only an installed instance satisfies, the queued REQUIRED_USE check skips installed outcomes (`not pkg.built`, `depgraph.py:3629-3632`), and `enqueue_dependencies` skips a dep the installed parent itself satisfies (`find_existing_node`; killed the spurious `--newuse` Reinstall from `rust-1.94.1`'s own BDEPEND). **Class B1** (real-only `myst-parser`/`mdit-py-plugins`): the `AlreadyInstalled` dedup was cp-keyed, so `llvm-core/clang:22` shadowed `:23` and clang-23.1.1's own build deps were never walked; real keys installed nodes by `pkg.slot_atom`. S2a `fe192e2` keys `other_outcomes` `(cat, pkg, vdb SLOT)` (fixtures `slotdedup`/`slotdedupconsumer`/`slotdedupmarker`, real oracle 1/1). **Class B2** (real-only `gtk-doc-am` `U` + `[blocks B]`): portuale selects 1.36.1, then the slot-conflict feedback masks it and settles on the installed 1.34.0 -- a blocker-carrying candidate backtracked away instead of reported; the mask path is the #57/#68 one, so it was stopped per the plan and filed as **#79** (`70f9cdd`). Host probe: class A extra gone, `@world` normalized diff identical incl. `Total: 73 packages … Size of downloads: 1282430 KiB` + `Conflict: 1 block (all satisfied)`; beds 20/15 clean/0 unexplained (default) and 18/15/0 (slot-op, host-exact); Rust 1153 / pytest 1717 green. The `#65` filing's remaining items (audiofile, xemacs, `gtk-doc` itself) are gone. Evidence: `TEST/findings/l0.md` "#74 S0"-"#74 S3"; plan `docs/history/02.074-bdeps_deep_walk.md` (done).

75. **DONE-PARTIAL 2026-09-18 (`#75 C0`–`C4`, branch `backlog/75-79`) — `--tree` merge-list residue for satisfied blockers (Phase D residue); the scheduling half is backlog #81.** C0's tree-mode oracle matrix (u1/u6/n5/p2c/p2b/q1-t, unordered, plus the flat-vs-tree `sys.settrace` dumps) confirmed both halves of the filed diagnosis: real's display list is the reversed retlist and `_tree_display` rewrites the blocker edges, and tree mode's serializer has no greedy leaf pop so it can schedule an uninstall flat mode never reaches. C1 ported the display half -- `_tree_display`/`_ordered_tree_display`/`_unordered_tree_display`/`_prune_tree_display` over a virtual entry+blocker graph, the `[nomerge]` ancestor rendering, real's depth-space indent and the `--onlydeps` nomerge root -- after which u1/n5/diamond/treeorder/onlydeps/dualslot/orbt/ormis/useflag byte-match real, flat output is byte-identical and the invariant checker was updated to the new model. C2 stopped per its own rule: p2b's tree-only row needs `scheduled_uninstalls` as serializer state (option (b) Phase D rejected), and tree-mode sibling order (u6, multislotparent) comes from the same serializer, so both are filed as **#81** with the C0 evidence. C3 pins the deterministic cells (bed 25 probes / 20 clean / 17 explained / 0 unexplained; `--tree` CASES entries + `test_oracle_75_tree_blocker_rows_match_real` for u1/n5); C4 re-diffs the host (`@world` set-identical with the same `Total:`/`Size of downloads:`/`Conflict:`, `runc` unchanged) and closes the docs. Evidence: `TEST/findings/l0.md` "#75 C0"–"#75 C4"; plan `docs/history/02.75-79.md` Phase C. Original entry follows. **`--tree` merge-list residue for satisfied blockers (Phase D residue).** Real's tree serializer differs from flat: `tree_mode` (`depgraph.py:9680`) disables the greedy leaf pop (`:9764-9777`), so B0's tree p2b shows a satisfied `b` row that flat p2b does not, and real's tree display reverses the retlist, so a shown row sits *before* the replacement there. Portuale applies the flat scheduling predicate and the flat placement in tree mode (named cut in `docs/history/02.68-74.md` §6); the tree p2b cell is deliberately unpinned. Closing this needs a tree-mode port of the serializer's leaf selection, a `merge_order.rs` item. Filed from Phase D B2.

76. **DONE 2026-09-18 (`#76 B0`–`B3`, branch `backlog/75-79`) — the satisfied-replacement wait predicate does not follow `|| ( … )` branches (Phase D residue).** B0's container capture (the B0b p2c root with `blocked-2.0`'s dep on `bparent-1.1` routed through `|| ( … )`, host-exact roots) showed real **does** wait: the satisfied `[blocks b]` row plus `Conflict: 1 block (all satisfied)` for a disjunctive `BDEPEND` (q1), for a group whose installed-satisfied branch loses to the in-graph one (q2, `dep_zapdeps`' all-in-graph bin), and for the `RDEPEND` control (q3); the `sys.settrace` dump confirmed the same stuck-serializer path (retlist's satisfied `Blocker` after `blocked-2.0`, `scheduled_uninstalls=[blocked-1.0]`). B1 factored `build_digraph`'s selection into a shared `digraph_prelude`/`suppressed_alt_edges` pair and exported `kept_alt_branches`, so the #53 self-branch exclusion cannot drift; B2 makes `replacement_wait_index` follow kept alternatives like plain edges (only suppressed ones are skipped), and B3 pins the shape with committed `dev-libs/disjowner`/`dev-libs/disjtarget` fixtures, a contract pin (`test_oracle_76_wait_follows_the_selected_disjunctive_branch`), a CASES entry and a host-exact bed cell (`TEST/atomlists/l0-fixture-oracle-host.txt`, `FX_HOST_ROOTS=1`; q1's BDEPEND splits across roots under the default bed's `ROOT=$FX` — B0b's artifact). Host `@world`/`runc` probes unchanged from Phase A; beds 0 unexplained (default 21/16/17, host list 1/1/0); pytest 1721 green with no corpus drift. Evidence: `TEST/findings/l0.md` "#76 B0"–"#76 B3"; plan `docs/history/02.75-79.md` Phase B. Original entry follows. **The satisfied-replacement wait predicate does not follow `|| ( … )` branches (Phase D residue).** `replacement_wait_index` (`pretend.rs`) skips `DepEdge::alt.is_some()` edges: real resolves one `||` branch through `dep_zapdeps` and there is no oracle for a disjunctive wait chain, so a `||`-only path from the replacement to the owner reads as "no wait" and hides the row. Needs a real capture of such a shape before widening. Filed from Phase D B2 (`docs/history/02.68-74.md` §6).

77. **DONE 2026-09-18 (`#77 A0`–`A3`, branch `backlog/75-79`) — a nomerge owner's removal row is not modelled (Phase D/B3 residue).** A0's container capture found the plan's "one-line tag" insufficient: real's `_validate_blockers` scans **every** installed package's recorded runtime deps (`Package._runtime_keys`, `depgraph.py:8919-9078`) and registers their blockers with the vardb `Package` as nomerge parent, while portuale only collected blockers from walked owners — so an unwalked owner's block was invisible (real printed `[uninstall] bparent-1.0` + inline `b`, portuale only the ebuild row). A1 adds `collect_unwalked_installed_blockers` (same dynamic-deps/`use_reduce_flat_disjunctive` pipeline, skipped only under `--nodeps`, real's own early exit), tags the nomerge satisfied arm `Uninstall { cpv: owner, anchor: <blocked cp> }` (real `depends_on_order.add((parent, pkg))` `:9204-9206`, `addnode(uninst_task, inst_task)` `:9240-9242`; no `merge_order.rs` change — the `Uninstall.required_by` edge is generic), and gives an absent-owner row its own removal entry as its display home. A2 commits the fixture (`dev-libs/nomowner`/`nomtarget`, which also pins the shape through the shared `fixture_env`), a bed cell (default bed 21/16 clean/17 explained/0 unexplained) and `test_oracle_77_nomerge_owner_removal_row` (n1–n4 + the n6 `B`/rc 1 control); A3 re-diffs the host (`@world` set-identical incl. `Total: 73 packages … Conflict: 1 block (all satisfied)`, `runc` unchanged) and closes the docs. Residue filed as **#80**: an unresolved row of a scan-collected owner (A0 n1b/n6b) still has no display home, so portuale prints no `[blocks B]`/rc 1 there. Evidence: `TEST/findings/l0.md` "#77 A0"–"#77 A3"; plan `docs/history/02.75-79.md` Phase A. Original entry follows. **A nomerge owner's removal row is not modelled (Phase D/B3 residue).** When a satisfied block's owner is installed (NOMERGE) and the blocked instance still merges, real's uninstall task targets the **owner** (`depends_on_order.add((parent, pkg))`, `depgraph.py:9204-9206`) — a row no oracle cell captures (B0's u-cells are merging-owner shapes; #73's cells are `B` or rowless). Portuale leaves those rows untagged (`satisfied_by: None`), so they stay in the trailing group. Needs a container capture of the satisfied nomerge shape before implementing. Filed from Phase D B3 (`docs/history/02.68-74.md` §6).

78. **DONE 2026-09-18 (re-scoped, no product-code change) — `--backtrack=0` drops real's whole slot-op cascade. The shared S0 with #79 re-confirmed E0 verdict (b) on a staged installed matrix (provpkg 0/1 + five dep-key consumers in world): the dynamic-deps append gate only re-tags the provider and, with backtracking on, empties the plan where real rebuilds -- it is not the fix. The real gap is selection-time: portuale never uses the appended built-atom dep string as a satisfaction check during candidate selection (real's resting graph reads the consumer's recorded `provpkg:0/1=`, satisfied by the installed provider). That is #26-family work by the filing's own name, so #78 closes here and re-opens there -- no new pins, no behavior change. Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) #78. Original entry follows. **`--backtrack=0` drops real's whole slot-op cascade; re-scoped by #78 E0 (verdict (b): the dynamic-deps append gate is not the fix).** Found by the #71 S0 control captures (host-exact, `TEST/findings/l0.md` "#71 S0"): `-uDvN --backtrack=0 --oneshot dev-libs/consrdep` (and `considep`) makes real print an **empty merge list**, rc 0, while portuale upgrades `provpkg` to 2.0. Real's `_slot_operator_trigger_reinstalls` is gated by `_allow_backtracking` (`depgraph.py:2131-2132`), cleared by `--backtrack=0` (`:12188-12190`), so with backtracking off neither the rebuilds nor the provider update happen; E0's real `--debug` confirms it (the bt0 cell has no `causing rebuilds` output, bt20 has it). **T13 correction:** the original entry claimed real's resting behaviour "relies on the built-atom dynamic-deps model (#26) which portuale does not model" -- portuale *does* model it (`installed_dep_string` + `built_slot_operator_atoms`, unit-tested) and gates it behind `PORTUALE_DYNAMIC_DEPS_APPEND`. E0's control experiment (host-exact slotop fixtures, both sides, portuale with and without the flag) gives **verdict (b)**: the flag does not reproduce real -- with `--backtrack=0` it only re-tags the provider (`r U`) and leaves the consumers unbuilt; with backtracking on it makes portuale print an **empty plan** where real rebuilds the three consumers and upgrades the provider -- and both #24 pins named in the gate's comment fail with it on (`slotchange-case4`'s libarchive `rR` and `slotundo-cascade`'s soccascb `rR` disappear). So the item is not the append gate; it needs the #26-family slot-op probe ordering (the gate's own doc comment already names it), re-scoped there rather than fixed by flipping the default. **Review refinement (2026-09-18, `docs/history/02.75-79.md` §7.4):** E0's "real's empty list is the backtracking gate, not a dynamic-deps model difference" is stronger than its evidence. `_allow_backtracking` explains why real schedules no *rebuilds*; it does not explain why real does not *upgrade the provider*, which portuale does in the same bt0 cell even with the append on (`r U`). Real's resting graph reads the consumer's recorded `provpkg:0/1=`, satisfied by the installed provider — an effect of the append portuale's append does not reproduce, because portuale applies it to the dep string but does not use it as a satisfaction check during candidate selection. That is the precise shape of the #26 work this item is re-scoped into. Evidence: `TEST/findings/l0.md` "#71 S0" and "#78 E0"; plan `docs/history/02.75-79.md` Phase E/§7.

79. **DONE 2026-09-18 — a holdable plain pin from the reverse-dependency scan is ignored instead of enforced.** `reverse_dependency_constraints` now skips a *holdable* non-built-`:=` pin outright (real only ever consults such pins from the slot-operator update probe, `depgraph.py:2472-2538`): the D1 `rdcblocker-2.0` merge survives and its own `RDEPEND` blocker reports `[blocks B]` rc 1, byte-identical to the container oracle. Still enforced: built slot-operator pins (#24 rebuilds) and every not-holdable pin (dropped for the residual report -- keeper's shape). Pins: the D1 bed cell (both `portuale-bug` allowlist entries deleted), `test_reachable_installed_consumer_holdable_pin_does_not_block_an_upgrade` (rewritten: the old test pinned the over-enforcement; real upgrades cleanly, verified host-side incl. `--deep`). Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) #79. Host-`@world` residue of the ignore filed as #91 (one stable extra upgrade, mechanism unidentified after 15 probes). Original entry follows. **A blocker-carrying candidate is dropped by the reverse-dependency pin scan instead of reported (the #74 S2b residue; corrected by #79 D0).** Host probe `-puDvN --ignore-default-opts --oneshot app-containers/runc`: real upgrades `dev-build/gtk-doc-am` to `1.36.1`, whose own `!<dev-util/gtk-doc-1.36.1` soft block cannot be resolved (the installed `dev-util/gtk-doc-1.34.0-r2` is an `@selected` digraph node), prints `[blocks B]` and exits 1. Portuale instead silently settles on `gtk-doc-am-1.34.0` and exits 0. **D0's trace corrected the mechanism**: real never records a slot conflict at all (`--debug` has no `Slot conflict handler started.`), and portuale writes neither `MaskReason` variant nor the solvable `BacktrackFeedback::Config` -- the producer is `reverse_dependency_constraints` (the #54/#57 reverse-dependency scan), which enforces the installed `gtk-doc-1.34.0-r2`'s recorded `BDEPEND="~dev-build/gtk-doc-am-1.34.0"` as a `slot_constraints` atom once `gtk-doc-am` is upgrading; later passes filter `1.36.1` out of `matched` (`extra_constraints=["~dev-build/gtk-doc-am-1.34.0"]`). Real's `_slot_operator_check_reverse_dependencies` (`depgraph.py:2472-2538`) is only consulted from the slot-operator update probe and skips parents whose cp has another tracker instance (`:2513-2528`); portuale's scan runs for every upgrade of a version-pinned dependency, so a plain `~` BDEPEND pin from an installed consumer is enforced where real ignores it. **D1** reproduces it hermetically (`fixtures/` `rdcblocker`/`rdctarget`/`rdcpuller`, atomlist `l0-fixture-oracle-rdcpin.txt` run with `FX_WORLD_EXTRA=dev-libs/rdctarget`, two `owner: portuale-bug` allowlist entries). **D2 stopped per its own mandatory rule**: its two planned gates (a set-level `all_match` escalation gate in `collect_feedback`; real's `is_arg_parent` exclusion) do not match the identified producer, and the real fix belongs in the #54/#57 scan's parent set -- the machinery the rule protects. Re-scope: port real's actual `_slot_operator_check_reverse_dependencies` invocation gating (which parents the slot-operator probe visits) against the scan's current plain-pin enforcement, with the #57 `othermod`/`needer` and #54 residual cells as the regression guards. Evidence: `TEST/findings/l0.md` "#74 S2b", "#79 D0"–"#79 D2"; plan `docs/history/02.75-79.md` Phase D.

80. **DONE 2026-09-18 — an unresolved row of a scan-collected blocker owner rides out as an orphan.** `resolve_blockers` now returns `FiledBlocker` (owner version attached); `file_blocker_conflicts` files entry-less *unsolvable* rows into `GraphResult::orphan_blockers` instead of dropping them, and the renderer prints them in the trailing blocker group, counts them in `Conflict: 1 block (1 unsatisfied)`, fires the `* Error` block and exits 1 -- A0 controls n1b/n6b byte-match real (host oracle, hermetic + test-local roots). `Replacement` rows can never orphan (they always carry `satisfied_by`). Pins: unit assertion on the n1b filing + `test_oracle_80_unresolved_scan_collected_row_has_a_trailing_home`. Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) #80. Original entry follows. **An unresolved row of a scan-collected blocker owner is dropped (#77 A1 residue).** Real's all-installed-packages scan (`_validate_blockers`, `depgraph.py:8919-9078`) also registers blockers of installed owners the run never walks; when such a blocker is unresolved (`unsolvable`), real prints `[blocks B]` in its trailing group and exits 1. Portuale's `file_blocker_conflicts` has no display home for an absent owner's unresolved row (only satisfied rows are tagged, and they ride their removal entry), so the row is dropped: A0 controls n1b (`=dev-libs/nomtarget-1.5` with the owner in the world file but no `@world` argument) and n6b (owner reachable through an installed world member) show real `B`/rc 1 vs portuale no row/rc 0. Fix: a trailing-orphan-row path (an orphan row list threaded into `package_counters_summary` and the trailing blocker group, or a display-only owner entry). Evidence: `TEST/findings/l0.md` "#77 A0"/"#77 A1"; plan `docs/history/02.75-79.md` §6 named cut.

81. **DONE 2026-09-19 (branch `backlog/81-tree-scheduled-uninstall`, human-reviewed and merged as `fc07b88`) — tree-mode stuck-serializer row.** Tree mode's serializer schedules uninstalls flat mode never reaches; portuale showed no row (#75 C2 residue). Real's `_serialize_tasks` disables the greedy leaf pop in tree mode (`depgraph.py:9680`, `:9764-9777`), so the stuck-serializer branch (`:9998`) can schedule an uninstall and `:10351-10358` appends the solved blocker; C0's flat-vs-tree dumps show flat has no `Blocker` node and `scheduled_uninstalls: []` while tree has both, and real's tree retlist differs from its flat retlist (also reordering siblings: u6's `blockerpkg`, multislotparent's slots). Portuale's row predicate (`replacement_wait_index`) needs a dependency path from the replacement to the owner and cannot express the scheduled-uninstall state, so C1's placement port cannot show the row. Doing this needs `scheduled_uninstalls` modelled as serializer state (option (b) `docs/history/02.68-74.md` Phase D rejected as disproportionate), the same decision C2 stopped on. Evidence: `TEST/findings/l0.md` "#75 C0"/"#75 C1"/"#75 C2"; plan `docs/history/02.75-79.md` Phase C; batch plan `docs/history/02.78-87-tier2-closeout.md` #81.

**S1+S2 status 2026-09-19 (branch `backlog/81-tree-scheduled-uninstall`, reviewed and accepted by the owner, merged to `main` as `fc07b88`; pmtest companion on `main`):** the tree-mode loop is ported, not a predicate patch -- `select_nodes` takes the real `not tree_mode` greedy gate plus the stuck branch (`scheduled_uninstalls` as synthetic leaf nodes, solved appended at selection), sharing digraph/bias/ladder/cycle code with the flat path; verdicts ride `BlockerConflict::tree_scheduled_uninstall` (resolve-side, gated on a pending Replacement row so the common case pays nothing) and feed only `--tree` Blocker nodes, the tree `-v` counters and nothing else (flat output, `--json`, exit codes untouched). Blocker nodes now sit right after their `after` entry in `node_order`, mirroring retlist adjacency (this fixed the prune eating the p2b row). Pins: `tree_simulation_solves_a_stalled_replacement_row` + `nvc_entries_do_not_participate_in_minimize_elimination` unit tests, `test_oracle_81_tree_only_satisfied_row_from_the_stuck_serializer` (row bytes + nesting + tree/flat counters + flat parity), CASES entry, default bed cell. Suite: 1724 passed (only the 3 pre-existing movepkg-dirt failures), Rust workspace green, clippy/fmt clean. **Residue (walk-sequence, NOT scheduler-verdict):** real leads the p2b tree with the merge owner and trails a nested new-slot owner occurrence, and nests the dep child as `[N]` where portuale nests `[U]`; u6 differs by the `blockerpkg-1.0` line position only (multislotparent is byte-identical). Those need display-walk sequencing surgery with every-tree-pin blast radius -- explicitly out of this branch; the row itself (the priced deliverable) is byte-exact with real byte-exact nesting and counters.

82. **DONE 2026-09-18 (branch `backlog/75-79-review-opus`) — `print_tree`'s edge selection is a drifted second copy of `build_digraph`'s, and the tree cross-mode invariant was weakened (#75 C1 review residue).** `DigraphPrelude::select_dep_target` now holds the narrowing once: `build_digraph` calls it with `merge_bound_only = false` (real's scheduler digraph edges to nomerge nodes) and the new public `resolved_dep_targets` with `true` for `print_tree` (portuale renders no line for an installed entry, so such an edge would open a hole in the tree) — the single documented difference, the shape #76 B1 established for the `||` selection; it also reports a suppressed `||` alternative as `None`, so `print_tree` dropped its own `kept_alt_branches` pass. Building the guard fixture exposed the larger half: `print_tree`'s `required_by` fallback for a deps-less entry is cp-keyed and `required_by` lists every owner that pulled *any* slot of the cp, so a slot an owner never asked for gained that owner as a tree parent; the fallback now skips an owner whose own atom already resolved to another instance. Container oracle (`TEST/findings/l0.md` "#82"): `--tree dev-libs/slotorderroot` is byte-identical to real 3.0.82.2 for the first time (the slice's one corpus drift, reviewed against that capture and blessed), pinned by `test_oracle_82_tree_edges_follow_the_resolved_dep_target`; new fixtures `dev-libs/treeslotpkg`/`treeslotparent`/`treeslotuser` + two `merge_order` unit tests pin the vercmp tie-break and the `merge_bound_only` split. The invariant checker's `--tree` row dedupe is gone (dead: `rows_key` already drops every `[nomerge]` row) with `test_checker_flags_a_merge_row_printed_twice_in_tree_mode` as the guard; the bidirectional `hidden_path_to` is **kept** — its reason is the unparsed `[blocks …]`/`[uninstall …]` rows collapsing the parsed depth (B0b's p2c), not the `[nomerge]` rows, and the comment now says so. Bed 28 probes / 22 clean / 18 explained / 0 unexplained. Residue: backlog **#85** (the two new slots merge in the opposite order, so the `treeslotuser` tree cannot be byte-pinned). Original entry follows. **`print_tree`'s edge selection is a drifted second copy of `build_digraph`'s, and the tree cross-mode invariant was weakened (#75 C1 review residue).** Two small, independent defects found reviewing the `backlog/75-79` merge (`docs/history/02.75-79.md` §7.1/§7.2). (a) `pretend.rs::print_tree` re-implements `build_digraph`'s dependency-edge narrowing instead of reusing it, and ranks the tie with a **string** compare (`jv.cmp(bv) == Ordering::Greater`, `pretend.rs:1853`) where `merge_order.rs:1772` uses `portage_versions::vercmp` — so `1.9` outranks `1.10` and slot `9` outranks slot `10`, and any atom matching two merge-bound entries of the same `cp` can nest under the wrong instance in tree mode; no pin catches it because the only committed multi-slot shape is `1.0`/`2.0`, where the two comparisons agree. Its `merge_bound` tie-break arms are also dead code (the candidate loop already skips non-merge-bound entries). Fix: export `build_digraph`'s narrowing as one shared helper, the way `#76 B1` did for `suppressed_alt_edges` — that precedent is exactly why the `||` selection cannot drift today — or at minimum swap in `vercmp` and delete the dead arms, with a multi-slot `9`/`10` fixture as the guard. (b) `tests/output_invariants.py::check_cross_mode`'s `if mode == "--tree": got = sorted(set(got))` is unnecessary: `rows_key` already filters `kind != "nomerge"`, so the ancestor occurrence never reaches `got`; removing the two lines leaves `tests/test_output_invariants.py` + `tests/test_emerge_pretend_contract.py` green (1222 passed, 5 xfailed), and keeping it hides exactly the duplicate-row bug the invariant exists to catch. The same function's `hidden_path_to(a,b) or hidden_path_to(b,a)` relaxation *is* needed, but should be narrowed to the `[nomerge]` ancestor rows that motivate it. Evidence: `docs/history/02.75-79.md` §7.

83. **DONE 2026-09-18 (branch `backlog/75-79-review-merged`) — the all-installed blocker scan makes a narrow `emerge -p` slower, and re-runs on every backtracking pass (#77 A1 review residue).** Profiled first (`TEST/findings/l0.md` "#83"): on `-p --oneshot app-editors/nano`, where the walk reaches four packages and the scan visits 2084, the cost is two halves — 391 ms in `list_candidates` locating each package's live ebuild metadata and 435 ms in `use_reduce_flat_disjunctive` (57 ms in the vdb reads, 0.5 ms listing the vdb). The reduce is now skipped when the dep string has no `!`: a blocker token always begins with one (`portage_dep::Blocker`) and `use_reduce` drops and expands tokens but never rewrites them, so the filter is exact; 1804 of the 2084 packages on this host are skipped. **1.22–1.30 s → 0.99–1.01 s** on `nano` (real 3.0.82.2: 2.23–2.61 s for the same argv), 13.86 → 13.3 s on `-puDvN --oneshot app-containers/runc`, ~57 → ~54 s on `@world`. Two cheap guards from the parallel review join it: the scan returns before touching the vdb when **no** entry is merge-bound (every row it could produce is `owner_installed`, so `resolve_blockers` would take the `!owner_merging && installed_match && !merge_bound_match` early `continue` on all of them -- an exact shortcut, not an approximation), and the walked-cp test is a `HashSet` instead of a linear `entries` scan per installed package (O(installed x entries) -> O(installed)). Neither moves this host's numbers (both shapes measured here have merge-bound entries and are I/O-bound), so they are complexity removal, not a measured win. A third variant of the `!` prefilter was assessed and **rejected**: reading the raw vdb `IDEPEND`/`PDEPEND`/`RDEPEND` *before* the live tree lookup would skip `list_candidates` too, but real reads these through the `FakeVartree` dbapi (`depgraph.py:9016` `vardb.aux_get`), whose `_apply_dynamic_deps` substitutes the **live ebuild's** deps under the default `--dynamic-deps=y` (`FakeVartree.py:142-146`) -- so an ebuild revised after the install to add a blocker is a blocker real sees and that prefilter would not. The cost it targets is #86's, and #86 removes it without the fidelity loss. Two further mitigations were measured and deliberately not taken: hoisting `PassState::installed_meta_memo` into `ResolveCtx` buys ~57 ms per extra pass (~170 ms on the four-pass `runc` probe) for a `RefCell` borrow across four call sites, and the residual `list_candidates` cost is the cold fill of its own process-global cache, free from the second pass on. The filing's "~4x" headline is corrected by the numbers: the scan is work real does too, and portuale answers the same narrow query in under half real's time. Residue: backlog **#86** (follow real's `myrepo=pkg.repo` instead of searching the repos). Original entry follows. **The all-installed blocker scan makes a narrow `emerge -p` ~4x slower, and re-runs on every backtracking pass (#77 A1 review residue).** `#77 A1`'s cost note ("56.2 s -> 58.3 s") measured the one shape where `collect_unwalked_installed_blockers` is nearly free: a complete-mode `@world` walk already has an entry for almost every installed package, so the scan's skip filter fires at once (measured 4.4–5.0 ms/pass). Instrumented on this host at `f818d1d`: `-p --ignore-default-opts --oneshot app-editors/nano` goes **0.32 s -> 1.26 s** (854 ms of scan in a single pass), `-puDvN --oneshot app-containers/runc` 12.65 s -> 14.15 s (282–290 ms x 4 passes), `sys-fs/udisks` 9.63 s -> 11.01 s. The per-pass repetition is structural: `run_pass` runs once per `Backtracker` iteration (up to `--backtrack`, default 20) and `PassState::installed_meta_memo` is per-pass. Three behaviour-preserving mitigations, in order: (1) hoist the metadata memo into `ResolveCtx` so passes 2..N reuse it; (2) `if !depstr.contains('!') { continue; }` before the token split — a blocker token always starts with `!` and `use_reduce` never introduces one, so it is exact; prototyped here it cuts the recurring pass 282 ms -> 149 ms and `nano`'s first pass 854 ms -> 676 ms (280 of 2088 installed packages carry a `!` in `IDEPEND PDEPEND RDEPEND` at all); (3) if that is not enough, cache the scan's blocker-token output per installed instance for the process lifetime — the only entries-dependent part is `disjunction_preference`, which matters solely for a blocker inside a `|| ( … )` group. Evidence: `docs/history/02.75-79.md` §7.3.

84. **DONE 2026-09-18 (branch `backlog/75-79-review-merged`) — the kept-`|| ( … )`-branch set was re-derived once per blocker row (`#76 B2` review residue).** `replacement_wait_index` (`pretend.rs`) called `portage_repo::kept_alt_branches` on every invocation, and `blocker_row_disposition` invokes it once per `Replacement` row from four walks (`collect_inline_blocker_lines`, `trailing_blocker_lines`, `count_blocker_rows`, the `--tree` node walk), so one row paid the O(graph) derivation four times over. The derivation itself was never wrong — it shares `suppressed_alt_edges` with `build_digraph`, so the #53 self-branch exclusion cannot drift — only its placement. `kept_alt_for_display` now derives it once per **walk** and threads `&[HashSet<usize>]` down into `replacement_wait_index`, which reads a missing entry as "suppressed" through `get` (so an empty set cannot panic); the `print_entry_line` call site passes `&[]` outright, since only `unsolvable` rows reach it and `resolve_blockers` writes `satisfied_by: None` whenever `unsolvable`. **The hoist is per walk, not per list:** `trailing_blocker_lines` is called once per display entry from `print_entry_line`, so deriving at list level instead paid it once per entry — measured here as a 12.4 s -> 15.8 s regression on `emerge -p @world` before the gate was narrowed to the rows each walk actually dispositions. With that gate the cost is at most four derivations per run and zero when no `Replacement` row exists, against the old once-per-row-per-walk; no measurable change on this host's shapes (`-p @world` 12.3 s, `-p --oneshot app-editors/nano` 0.99-1.00 s, `-puDvN @world` 53.5-54.0 s, all within noise of the pre-slice build), so this is an algorithmic ceiling removed, not a measured win. Pin: `kept_alt_for_display_is_derived_once_and_shared` (the shared set equals the derivation it replaces; the owner-restricted form derives only for the owner carrying the row; the kept branch waits and the empty set hides). Evidence: this commit; plan `docs/history/02.75-79.md` §7.6.

85. **DONE 2026-09-18 — S0 proved this is not the #17 wall: real's `_minimize_children`.** Real's `--debug` digraph dump shows `treeslotuser` has NO edge to 1.10: `_minimize_children` (`depgraph.py:4751-4856`) eliminates the redundant 1.10 selection (`:0` matches only 1.9, the bare atom matches both), so the bias ties 1-1 and discovery order decides. Ported into the shared `select_dep_target` (both `build_digraph` and `print_tree` via `resolved_dep_targets`); NVC entries are excluded from elimination (they are never selected packages -- real yields `(atom, None)`), preserving the `opartlya` disclosure order the elimination otherwise flips. The bed cell is byte-equal flat and tree; the `treeslot-new-slot-merge-order` allowlist entry is deleted. Pins: two `merge_order` unit tests + `test_oracle_85_unqualified_atom_collapses_onto_the_slot_qualified_target` (flat `-v` and `--tree` byte pins). Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) #85. Original entry follows. **Two new slots of one cp, pulled by different parents, merge in the opposite order (#82 residue).** Found while building #82's tree fixture, and the reason that fixture gets no byte pin. `dev-libs/treeslotuser` pulls `dev-libs/treeslotpkg:0` (`1.9`), `dev-libs/treeslotparent` (which pulls `:1`, `1.10`) and a slot-unqualified `dev-libs/treeslotpkg`; real 3.0.82.2 merges `1.9, 1.10, treeslotparent, treeslotuser` while portuale's `build_digraph` discovery order reaches the slot-1 node first and emits `1.10, 1.9`. Set, versions, flags and `Total:` all match — only the sequence differs, the standing #17 merge-order class. It propagates into `--tree`, whose display list is the reversed merge order: after #82 the tree *edge set* is real's (`treeslotparent`'s only child is `1.10`), but the nesting still differs because the walk meets the slots in the other order. Capture: `TEST/findings/l0.md` "#82", bed cell `-v dev-libs/treeslotuser` with the `treeslot-new-slot-merge-order` allowlist entry (`owner: portuale-bug`), which is deleted when this closes.

86. **DONE 2026-09-18 — installed-package live metadata comes from the vdb-recorded repository.** `live_metadata_for_installed` threads the recorded `repository` through all four `installed_dep_string` call sites (scan, resolver loop, `--deep` recursion, merge-order closure) as one shared helper, replacing `list_candidates` priority search; missing/removed recorded repo falls back to the old search (minimal fixture vdbs predate the file), and a version gone from the recorded repo falls back to Raw. Pins: `live_metadata_for_installed_prefers_the_recorded_repo` unit test + `tworepoowner`/`tworepotarget` two-repo fixtures + `test_oracle_86_installed_metadata_comes_from_the_recorded_repo` (old binary printed a spurious uninstall+b pair; fixed binary and real print the clean upgrade). Timing re-measured 2026-09-18 on the #83 probe shape (`-p --ignore-default-opts --oneshot app-editors/nano`, this host, interleaved warm runs): 0.99-1.01 s -> **0.83-0.85 s** (cold first run 4.09 s), against real 3.0.82.2's 2.23-2.61 s -- a real win, below the 0.60 s upper bound exactly as predicted (the faithful single-file read keeps more than the unfaithful skip). Order-bed re-runs pending (no container image in this environment; see the close-out). Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) #86. Original entry follows. **The live ebuild metadata of an installed package is found by searching the repos, not by its recorded repository (#83 residue).** Real's `FakeVartree._aux_get_wrapper` passes `myrepo=pkg.repo` — the vdb-recorded `repository` — straight to `portdb.aux_get` (`FakeVartree.py:136`), so the dynamic-deps view of an installed package always comes from the repo it was built from. Portuale resolves it with `list_candidates(repos, cat, pkg)` + `max_by_key(repo_priority)` at every `installed_dep_string` call site (`collect_unwalked_installed_blockers`, `enqueue_dependencies`, the `--deep` display-list recursion), which differs whenever the same version exists in two repos and the recorded one is not the highest-priority. It is also the single biggest remaining cost of the #83 scan: a `read_dir` per installed package (391 ms for 2084 on this host) where the recorded repository would need one file read (~5 ms total). The prize is measured, not estimated: a third review branch (`backlog/r01oc`) skipped the per-package lookup outright — by the unfaithful raw-record route this item exists to replace — and `emerge -p --ignore-default-opts --oneshot app-editors/nano` went **0.99-1.00 s -> 0.60 s** on this host, interleaved, three runs each. That is the win available here faithfully. Fidelity change, so it needs its own oracle — a two-repo fixture where the same cpv exists in both, with the vdb `repository` naming the lower-priority one — and all the call sites must move together. Evidence: `TEST/findings/l0.md` "#83".

87. **DONE 2026-09-18 — `run/l0-fixture-oracle-all.sh` runs all four atomlists with their documented knobs.** The standard bed step for everything after it in the batch; plus the q3 `RDEPEND` control as committed `rdisjowner`/`rdisjtarget` fixtures (survives the default `ROOT=$FX` split, unlike q1's BDEPEND), pinned by a CASES entry, `test_oracle_87_rdepend_disjunctive_wait_is_default_bed_covered` and the default atomlist cell -- real and portuale agree byte-for-byte (host oracle 2026-09-18). Plan: [`history/02.78-87-tier2-closeout.md`](history/02.78-87-tier2-closeout.md) #87. Original entry follows. **The `disjowner`/`disjtarget` kept-branch cell is host-list-only; the q3 `RDEPEND` shape may survive the default bed (#84 companion note).** `#76 B3`'s `disjowner`/`disjtarget` cell lives only in the host-exact bed list, because q1's `BDEPEND` splits across roots under the default bed's `ROOT=$FX` — so the `kept_alt_for_display` path that #84 reshaped is exercised only when someone remembers the exact command. If the q3 `RDEPEND` shape from `#76 B0` (`TEST/findings/l0.md`) survives that split, add it to the default bed (`TEST/atomlists/l0-fixture-oracle.txt` + a `CASES` entry + a contract pin) so the path has standing coverage; otherwise record in the findings why the split blocks it. Related: the bed now needs four atomlists and three env knobs (`FX_SLOTOP_BDEP`, `FX_HOST_ROOTS`, `FX_WORLD_EXTRA`) and only `l0-fixture-oracle.txt` runs by default, so `#76 B3`'s and `#79 D1`'s permanent cells have the same gap — a `TEST/run/l0-fixture-oracle-all.sh` running the four in sequence would close all of them at once (`docs/history/02.75-79.md` §7.5). Evidence: `docs/history/02.75-79.md` §7.6; review branches `backlog/75-79-review-opencode` §6 and `backlog/r01oc`.

## Tier 3 — container test bed (L2–L5)

29. **DONE 2026-09-14 — L2, portuale as builder.** The bed is shipped (`TEST/run/l2-portuale-builder.sh` + `layers/l2/*` + `compare/gpkg-{structure,diff}.sh` + `diff.py --layer l2/--tolerate-payload`) and both tracks are green: the porttest fixture track `strict hard=0 soft=0` on every archive pair with all `l2-*` allowlists deleted (`l2-20260913T232955Z`), and the real `l1-merge.txt` set rc 0 (`l2-20260914T032915Z`) — cross-install direction A (real consumes portuale-built archives) 0 hard / 0 unexplained, control (portuale consumes the portage-built set) 0/0, residue only the tolerated payload set. The producer gaps that blocked it closed as #37/#38/#39/#40/#43. Findings: `TEST/findings/l2.md`; plan: `docs/history/029_portuale-as-builder.deepseek.md`. [I, K]
30. **DONE-PARTIAL 2026-09-14 — L3 source-build parity.** Harness shipped (`TEST/run/l3-source-parity.sh` + `TEST/layers/l3/build-and-merge.sh` + `l3-{smoke,core,system}.txt`; full-tree snapshot, `diff.py --layer l3 --tolerate-payload`, metrics JSON). The portage-vs-portage control pair is **0 unexplained on `l3-smoke`** (full-tree, no normalization rules). The candidate run surfaced four producer classes; fixed: `-e`/reinstall of a sub-slotted package aborting on its own files (`find_collisions` given the full `slot/sub_slot`), and incremental config semantics (a second `FEATURES=` in one file replaces for real, stacked for portuale). Filed with repros: ncurses `OWNER 1:1` vs `0:0`, and saved-env accumulation (`A`/`RESTRICT`/stray locals) in the vdb environment. `l3-core`/`@system` not started (S4 stop rule: >3 systemic classes). **P3 DONE 2026-09-15:** the full candidate+control pair `TEST/logs/l3-20260914T234404Z` is **0 hard / 0 unexplained on both** -- the last 12 rows were #47's `BUILD_TIME`/`metadata` gap, fixed in P3 -- so the S4 stop rule is lifted in `docs/history/030_L3-source-build-parity.deepseek.md` and `l3-core` (344 ebuilds) is unblocked for a user-triggered run. Evidence: `TEST/findings/l3.md` "P3"; plan: `docs/history/030_L3-source-build-parity.deepseek.md`. [I]
31. **L4 — `mrg` remote merge over SSH** — differential test of the remote binary-merge path; design in `remote-merge.md` §6. [I]
32. **L5 — lifecycle & fault injection** — `-C` / `--depclean` diffs, soname bump → preserved-libs, `CONFIG_PROTECT`, `--resume` after SIGKILL, disk-full / corrupt-archive / binhost-500. `TEST/`. [I]

## Tier 4 — `--solver=` real-tree correctness (pubgrub / resolvo)

33. **`--solver=resolvo` cycle linearization** — `install_order` can't order any closure with a toolchain cycle (glibc↔gcc↔perl); `solver_bridge.rs` prints raw ids. Non-functional on real targets. `docs/history/solver-backends-analysis.md`. [J]
34. **`--solver=pubgrub` over-merge** — feeds pubgrub the over-approximated `flag?()` reachability closure as the real graph (`nodejs`: 48 vs 8). Feed the resolved graph instead. `solver_bridge.rs`. [J]

## Tier 5 — L2 producer parity (filed 2026-09-13, blocks #29's real half)

37. **DONE 2026-09-13 — build phase env completeness.** Source builds now run with the resolved config env (`portage_profile::phase_environ`: implicit USE, resolved `FEATURES`/`PORTAGE_FEATURES`, `MULTILIB_ABIS`/`DEFAULT_ABI`/`LIBDIR_*`, `SLOT`, `PORTAGE_REPO_NAME`/`REPO_REVISIONS`, `SOURCE_DATE_EPOCH`) on `emerge`/`--resume`/`--buildpkgonly`; the Rust execution gates read the resolved `FEATURES`; brush exports are shell-quoted. Porttest track green from a clean run, `l2-bpkgonly-env` allowlist rows deleted, oniguruma → `/usr/lib64`, `--buildpkgonly` ≡ `-b` archives (`gpkg-diff` strict hard=0). Plan: `docs/history/037_Build-phase-env-completeness.plan.md`; evidence: `TEST/findings/l2.md` S4/S5. [K]
38. **DONE 2026-09-13 — Packaging transforms: dostrip/splitdebug/docompress.** The real `install_qa_check` gates now fire on every source path (the `PORTAGE_TMPDIR` boundary default was the last byte difference, fixed as a #37 follow-up), so archives carry stripped binaries, `.debug`/`.build-id` trees with the oracle's build-ids, and `BIG.txt.bz2` docs; `porttest/restrict-strip` (`RESTRICT=strip`) pins the inverse cell. The merge-time complement `instprep` is implemented (`ebuild_phases::run_instprep`) on every source and binary merge from the resolved phase env; `--buildpkgonly` ≡ `-b`, L2 porttest 0 unexplained, L1 green, allowlists narrowed to the one real-nondeterministic `setuid` build-id link race. Filed #42 on the way (no `clean` before builds). Plan: `docs/history/038_Packaging-transforms.plan.md`; evidence: `TEST/findings/l2.md` "#38 S0-S5". [K]
39. **DONE 2026-09-13 — gpkg metadata completeness.** `write_post_install_metadata` now writes real's `IUSE` (always, empty included), the profile-computed `IUSE_EFFECTIVE`, the pre-transform `${D}`-walk `SIZE`, and evaluates dependency-atom use-deps before the `:=` bind (real `token_class=Atom`); the new `write_post_install_soname_deps` rewrites `NEEDED.ELF.2` with the 6th multilib-category field (`compute_multilib_category`), generates `PROVIDES`/`REQUIRES` (`SonameDepsProcessor` port), and injects `>=<libc>` into `RDEPEND` (`_inject_libc_dep`); the `Packages` stanza is real's own sorted/translated field set (`EAPI`/`DEFINED_PHASES`/`PROVIDES`/`REQUIRES`/`REPO`/`MTIME`). L2 porttest is `hard=0 soft=0` on every archive with all `l2-gpkg-*` allowlists deleted (`l2-20260913T232955Z`). Evidence: `TEST/findings/l2.md` "#39". [K]
40. **DONE 2026-09-13 — consumer-side env of portuale-built archives.** Fixed as a consequence of #39's `Packages`-index `EAPI`: real Portage no longer defaults the archive's EAPI to "0" (which made its merge phases export the EAPI≤6 `DESTTREE`/`INSDESTTREE`/`PORTDIR`/`ECLASSDIR` into the vdb env and skip `pkg_pretend`); the cross-install `porttest/phases` log now matches and the `l2-consumes-portuale-env-degraded` entry is deleted. Evidence: `TEST/findings/l2.md` "#39". [K]
41. **DONE 2026-09-15 — Ebuild fallback for a cache-less repo.** Real parses ebuilds when `metadata/md5-cache` is absent. **C0/C1/C2 DONE 2026-09-14:** C0 oracled real's fallback (depcachedir write, non-root in-memory, `_md5_` validation); C1 routed every aux-metadata read through `portage_repo::repo_aux_metadata` + `has_usable_md5_cache` and made cache-less listers walk `*.ebuild`; C2 registers the depend-phase provider (`ebuild_phases::depend_phase_metadata`) from the binary's `main`, so a cache miss runs the real `depend` phase, memoised per cp with a recursion guard for the phase's own nested metadata reads. **C3 DONE 2026-09-15:** the provider checks a valid `depcachedir` entry (`/var/cache/edb/dep/<repo path lstrip '/'>/<cat>/<pf>`, real `flat_hash`/`md5-dict` layout) before the phase and writes the rendered entry back after it (best-effort; unwritable keeps C2's in-process memo), sharing `regen::{entry_is_valid,render_entry,write_entry}` with `--regen`; pinned by `test_cache_less_repo_metadata_is_written_to_the_depcache` (second run does not re-run the phase; a content bump invalidates). The L2 `L2_CACHELESS=1` variant deletes the overlay's committed cache in both build containers: `TEST/logs/l2-20260915T004923Z` all ten archive pairs `hard=0 soft=0`, cross-install/control 0 unexplained (normal `l2-20260915T004600Z` rc 0). Rust-only per D2. Finding `l2-no-md5-cache-ebuild-fallback` FIXED. [K]
42. **DONE 2026-09-13 — `emerge` never pre-cleans `${PORTAGE_BUILDDIR}`.** `ebuild_phases::run_clean` (the real `bin/ebuild.sh clean` -> `__dyn_clean`) now runs at real's positions: before every source build (`merge_one_source_entry`, `build_one_source_entry`, `run_buildpkgonly`), after a `--buildpkgonly` package (unconditional, `keepwork`/`keeptemp` honored by the phase), and after a successful merge unless `FEATURES=noclean` (`run_merge`, `merge_one_built_entry`); `run_qmerge` stays clean-free like real `doebuild qmerge`. The #38 S4 repro no longer wipes the builddir between cells and is green (`TEST/logs/l2-instprep-20260913T222526Z`). Evidence: `TEST/findings/l2.md` "#42". [K]
43. **DONE 2026-09-13 — test-bed binpkg GPG check blocked the real-set cross-install.** `TEST/layers/l1/consume.sh` drops the image's signed, different-revision catalyst gentoo binrepo before the `-k --getbinpkg` merge (the `$PKGDIR` set + installed state is the set under test); portuale now honours a per-binrepo `verify-signature` via `binpkg::GpgVerify::from_binrepo` (real `gpkg.__init__`'s own folding, `gpkg.py:792-819`), used by `emerge_getbinpkg::merge_one_binary_entry` on fetched remote archives (`find_remote_binpkg` returns the repo). Evidence: `TEST/findings/l2.md` `l2-binpkg-gpg-check`. [K]
44. **DONE 2026-09-14 — L3 merge/install ownership of image `bin:bin` files.** `ebuild_merge.rs` now implements real `dblink._needs_move` (`vartree.py:6363`, bug #722270): an existing regular file with the same mode and byte-identical content is not moved, so its inode/ownership survive a byte-identical `-e` rebuild (real's `1:1` ncurses case); a differing file is still replaced and takes the source's ownership, and a diverted `._cfgNNNN_` write always moves. Root-owned regression test `test_merge_skips_an_identical_file_preserving_ownership` (fails pre-fix). L3 smoke `l3-20260914T132606Z`: **0 OWNER rows** (was 2121); L1 porttest `l1-20260914T135235Z` 0 findings. Evidence: `TEST/findings/l3.md` "P1". [I]
45. **DONE 2026-09-14 — saved phase env accumulates across phases in the vdb `environment`.** P2a's live per-phase bisect split the filed symptoms: `A` no longer reproduced (closed by intervening config work); `RESTRICT="test test"` was `flat_field_on` keeping both tokens of ncurses' use-conditional `RESTRICT` (real `config.py::_flatten` joins a *set*, deduped and sorted) and is fixed at that one shared helper; the stray `declare -- f`/`x=""` came from the source merge never setting `PORTAGE_UPDATE_ENV` before postinst (real `vartree.py:5334`), so the vdb env kept the install-time `build-info/environment` save instead of the filtered postinst regeneration -- `merge_after_install` now passes it plus the `A` value real `doebuild_environment` exports. Evidence: L3 smoke `l3-20260914T192147Z` hard findings 12, **0 environment rows** (was 6), 0 OWNER rows; L1 porttest on the same tree. Detail: `TEST/findings/l3.md` "P2b". [I]
46. **DONE 2026-09-15 — validate an existing `metadata/md5-cache` entry against the ebuild (#41 C-track follow-up).** `repo_aux_metadata` now runs real `_pull_valid_cache`'s validation on a present entry (`portage_repo::md5_dict`: `_md5_`, EAPI, `_eclasses_` pairs; raw text, memoised per path, masters resolved lazily) and treats a rejected entry as a miss: depcachedir rung -> depend phase -> best-effort write-back (`Error::StaleMd5Cache`, E5 never falls back to stale data); no provider returns it as-is (E3), an ebuild-less entry is trusted (E4), and the provider gained the ebuild-head EAPI short-circuit (real then masks `EAPI 9999`; portuale drops the candidate -- narrowing). Every committed fixture cache was reconciled first: 565/565 entries regenerated with real `egencache` and byte-identical to `portuale emerge --regen` except the writer's extra `INHERITED=` key, also fixed; the 21 deliberately cache-less ebuilds stay cache-less. Pins: four read-path tests in `tests/test_portuale.py` (cells f/a/d, P6/P7) + E3/E4 unit tests + `test_committed_fixture_md5_cache_entries_match_their_ebuilds`; `scripts/md5_cache_audit.py`; L2_STALECACHE=1, the fixture-oracle stale case, the L0 image audit (gentoo 33227/0 stale) and L1 porttest all green (`TEST/logs/{l2-20260915T161134Z,l0-fx-20260915T160454Z,l1-20260915T161521Z}`). Evidence: `TEST/findings/l2.md` S0-S4; plan `docs/history/05.046-metadata_md5-cache.opus.md`. [K]

47. **DONE 2026-09-15 — vdb `metadata`/`BUILD_TIME` producer gap (from P2b).** L3's remaining 12 VDB rows were the per-package `BUILD_TIME` file and the `metadata` consolidated file. Real writes `int(time.time())` into `build-info/BUILD_TIME` in `_post_src_install_write_metadata` (`doebuild.py:2727-2732`, before any other key, unconditionally); the vdb copy and `_consolidate_to_metadata_file` both carry it. `ebuild_phases::write_post_install_metadata` now writes the same file at the top (before its repo-metadata early returns), pinned by the `install_writes_the_real_build_time_file` unit test. Evidence: L3 smoke candidate+control `TEST/logs/l3-20260914T234404Z` is **0 hard / 0 unexplained** (was 12); `TEST/findings/l3.md` "P3". [I]

## Tier 6 — test oracle after removing the Python reference (filed 2026-09-15)

The Python mirror was removed; `history/second_python_copy_removal.md` "Where each check stands" has the full table. Done on the removal branch: §1/§2 invariants, §3 primitive differential, §4 unparsed-token counter, §8 re-pin checklist, §9 repeated-run determinism, §11 harvested corpus. Open:

48. **DONE 2026-09-15 — L0 run with the new invariant inputs, and the 18-violation triage.** Full run `TEST/logs/l0-20260915T092215Z` (new `vdb-list.txt` + `dep-classes.tsv` snapshots; `TEST/layers/l0/in-container.sh` now records them) reproduced the documented 18 portuale "merges after its owner" rows, 0 control. Triage: **14** were cp-level `required_by` vs per-instance constraints (`--json` aggregates by cp; another instance of the child already merged before the owner — `check_json` now has the mirror of its multi-slot-owner exemption); **3** were soft edges the L0 checker couldn't classify without repo access (2 PDEPEND, 1 RDEPEND — real's `ignore_priority` ladder may drop them; the new `dep-classes.tsv` drives the exemption); the recorded `installed_child` lead was **disproven** (no child cp is in the vdb snapshot) though the snapshot now feeds that exemption too. **1 genuine bug remains**, filed as #53. Evidence: `TEST/findings/l0.md` "#48". [I]
49. **DONE-PARTIAL 2026-09-15 — Real `emerge` as fixture oracle (§6).** Harness shipped: `TEST/run/l0-fixture-oracle.sh` + `TEST/layers/l0-fixture-oracle/{stage,in-container}.sh` + `TEST/atomlists/l0-fixture-oracle.txt` + `TEST/compare/known-divergences-fixture-oracle.yaml`; 12 staging deltas documented in `TEST/findings/l0-fixture-oracle.md`. The diamond spike is a GO (real and portuale produce the same merge list), and the widened run `TEST/logs/l0-fx-20260915T101508Z` is **green: 12 cases, 5 clean, 20 explained, 0 unexplained**. The oracle immediately caught **#54** (installed-consumer pin over-approximation: `--update paired`, `needer`, `=paired-2.0` differ from real), suppressed by two `owner: portuale-bug` allowlist entries pending that fix; the remaining findings are staging-path/wording differences. [A]
50. **DONE-PARTIAL 2026-09-15 — Bulk translation of upstream resolver tests (§7), verdict GO.** `scripts/upstream_resolver_translate.py` wraps the real `ResolverPlayground` (constructor inputs + `run()` results) and captures the executed oracle, not the source literal; `--all` over the pinned checkout: **99 `test_*.py` files, 99 captured, 95 with cases, 1032 cases, 0 errors/crashes**. `--emit-fixtures` writes ebuilds+md5-cache, installed vdb and world; a 12-file sample shows 4/32 playgrounds ebuilds-only, 17 emitted with installed/world, 11 needing per-batch `user_config`/`repo_configs`/`profiles`/`binpkgs` mapping. Running count: 99 files / 1032 cases captured, **0 bulk-emitted** (the ~34 hand-translated cases predate the tool); each batch still needs a fixture-name collision check and its own pins. Detail: `docs/history/second_python_copy_removal.md` §7 "Verdict 2026-09-15". [A]
51. **DONE 2026-09-15 — Determinism under shuffled input order (§9, second half).** New `rust/portage-util` `read_dir_entries` seam: sorted by default, seeded Fisher-Yates under `PORTUALE_SHUFFLE_DIRS` (test/CI-only, reproducible per (seed, dir), unit-tested); **all 56 production `read_dir(` sites** in `portage-repo`/`portage-profile`/`mrg-director`/`portuale` migrated to it (`std::fs::read_dir` remains only inside the helper). `test_output_is_identical_under_shuffled_directory_order` (every `CASES` entry × 3 seeds) and `test_repos_conf_section_order_does_not_change_output` (reversed sections in a copied configroot) both pass; no order-dependence bug found. Detail: `docs/history/second_python_copy_removal.md` §9. [A]
52. **DONE-PARTIAL 2026-09-15 — Metamorphic tests (§5) and mutation testing (§10).** §5: `tests/test_metamorphic.py`, five transforms (duplicate IUSE, unrelated package, empty overlay, consistent category rename, reordered `package.use`) × 13 representative `CASES` on private `fixtures/` copies, byte-identical output, transform-changed-tree asserted; 65 passed, no bug found. §10: `cargo-mutants` v27.1.0 installed and run with `--in-place` (the sandbox copy breaks manifest-relative test fixtures): `resolver_trace.rs` 35/35 missed (trace-only, no unit surface), `solver_bridge.rs` 44 caught / **26 missed** / 16 unviable — all 26 in the parked pubgrub/resolvo bridge, triaged as a Tier-4 test gap (#33/#34), not a default-resolver gap; `merge_order.rs` (456 mutants, ≈40 min) and `lib.rs` (2207, ≈3 h) not yet run. Detail: `TEST/findings/mutants.md`. [A]

53. **DONE 2026-09-15 — Merge order violated a hard `BDEPEND` edge in `app-containers/podman`'s L0 probe (#48 residue).** The backlog's original `:=`-slot-operator/F-A1 attribution was **wrong** (disproven in planning, `history/06.053-merge_order.opus.md` §1): the trigger was `dev-lang/go`'s own `|| ( >=go-N >=go-bootstrap-N )` BDEPEND. `merge_order.rs::build_digraph`'s `alt_suppressed` re-derives which `||` branch "wins" for scheduling-edge purposes independently of the actual resolver's choice, with no equivalent of the resolver's own `circular_self` exclusion (`b5b256f`, finding D) — so an alternative satisfied only by matching the *owning entry's own not-yet-merged graph node* trivially counted as "in graph" and beat the bootstrap branch the resolver had actually picked, adding a permanent self-edge that stalled `go` behind every dependent whose own edge to it could legitimately relax. Fixed (`a5921eb`, S2) by excluding a `||` alternative's match against the owning entry's own index unless that entry is itself installed. Fixture: `fixtures/repo/{dev-lang,dev-go,app-misc}/mo*` (`app-misc/mopodman` failing / `app-misc/mopodmanok` nearest-passing, `e05fd28`); pins: 3 `CASES` entries + a dedicated order-assertion contract test + a Rust unit test on `build_digraph` directly. Verified clean against real Portage two ways: the fixture oracle (`TEST/logs/l0-fx-20260915T152731Z`, `UNEXPLAINED 0`, byte-identical deduplicated merge order — this also fixed 2 unrelated harness gaps in `TEST/layers/l0-fixture-oracle/{stage,in-container}.sh`, `dbeea82`) and the live `app-containers/podman` L0 probe (0 portuale invariant violations, was 1). Interesting side-finding: on the isolated fixture, real does **not** hit the circular-dependency abort the original live evidence showed — it picks the bootstrap branch cleanly too, confirming that abort is a discovery-order effect of the full ~100-package graph, not inherent to this dependency shape; full parity with real's abort (a separate, larger change to finding D) stays open as a follow-up if ever wanted. Findings: `TEST/findings/l0.md` "#53 S0"-"S3". [A]

54. **DONE 2026-09-15 — Installed-consumer pin/residual conflict over-approximation (found by the #49 fixture oracle).** `reverse_dependency_constraints` scanned every installed package as a potential consumer unconditionally; real only ever sees one that is **reachable from `@world ∪ @selected ∪ @system` over the installed dependency graph** (`_complete_graph`'s required-set walk, `depgraph.py:8677-8731`) or is itself a graph entry this pass — not merely because it is installed, and not gated on an existing slot conflict (the backlog's original attribution to `depgraph.py:9446-9453` was incomplete, corrected in planning: auto-enable also fires on any plain version/USE/slot change, `depgraph.py:8591-8647`). S0 (`bc8caa8`) confirmed the hypothesis with a five-cell oracle matrix (world state × case); S1 (`b27bdb4`) threads `ResolveCtx::slot_op_reachable` (already computed once per resolve for the slot-operator-rebuild gate, same seeds) into the consumer scan instead of a second walker — `--update dev-libs/paired`, `dev-libs/needer` and `=dev-libs/paired-2.0` now merge byte-identical to real's hermetic output (no block), and the same three cases with the pinning consumer made reachable (a copied configroot) reproduce the original 511e659 shapes exactly. S2 (`6163af1`) verified 0 unexplained on the fixture oracle and no regression on the full 120-probe L0 real-tree run (100/120 clean, up from 96/120 — the `media-libs/libdisplay-info` fix stays intact). **Surfaced regression, filed separately as #57**: the `needer`+`othermod` triangle's residual-conflict block now renders nothing at all (real still shows one, `othermod`-only parent) — portuale's block was entirely driven by the now-correctly-gated dropped pin, with no independent mechanism for two directly-requested hard atoms conflicting without an installed vdb consumer. K1 (the informational-exit-0-vs-real's-exit-1 convention for a residual conflict) stays the deliberate Tier 2.25 standing convention, not filed separately here (it was filed a day later, via #57 K1, as **#62**). Findings: `TEST/findings/l0-fixture-oracle.md` "#54 S0"-"S2". [A]
57. **DONE 2026-09-16 — directly-requested hard-atom-vs-hard-atom conflict against an installed instance now reports (#54 S1 residue).** The backlog's original pointer (`lib.rs:~18268`) was off by one branch: the actual gap is the walker's `AlreadyInstalled` early branch (`let Some(version) = resolved_version else { ... }`), which dedups on `state.other_outcomes` and never consults `state.resolved_slots` — so an installed instance was invisible to slot tracking on either side of the `needer`/`othermod` triangle (`>=paired-2.0` vs `<paired-2.0`), independent of #54's `_complete_graph` reverse-dep-pin mechanism. S0 (`b6518f8`) confirmed via real 3.0.82.2's own `--debug` trace that its reported block never changes across its 4 backtrack tries (`--backtrack=0` is byte-identical to the full run) — no second mechanism, proceed as specced. S1 (`ec09c66`) added `PassState::installed_slots` (a `(cat, pkg, slot) -> version` map for `AlreadyInstalled` nodes) and checks both directions against the existing `resolved_slots`/`installed_slots` maps, reusing the existing solvable/unsolvable backtracker reconciliation (no second solver) so a bare/unversioned dependency satisfied by either instance still merges silently (K2). Exit stays 0 (K1, portuale's standing informational-notice convention; backlog #62 tracks revisiting this in a future release). `test_oracle_slotop_complete` (bug 614390, K3) XPASSed and is now a permanent pin (a second, unrelated fixture bug surfaced and fixed along the way: `test_oracle_slotop_rebuild_order` was pinned against the wrong fixture, `app-misc/Cor` added). **Three latent bugs fell out of making the collision visible for the first time** (all detailed in `ec09c66`'s message and the S1 findings, none of them in the plan's design): `resolve_pretend`'s two `avoid_update` fast paths ignored `extra_constraints`, so a *solvable* retry re-derived the same collision forever; a `NoVisibleCandidate` swallowed by the cp-keyed `other_outcomes` dedup left no NVC record, letting a *masked* backtracking attempt settle on a graph with an unsatisfiable dep (the `othermod needer` order lost the upgrade entirely) — latched as `suppressed_nvc`, gated on an active `runtime_pkg_mask` so no ordinary pass changes; and the #54 interplay (residual records routed through `record_slot_conflict` rather than `extend`, since both producers can now emit the same `(package, merge version, installed version)` triple, plus dropped reverse-dep pins collected before a slot-conflict mask feedback can return early). S2 (`0b1ac8b`) verified 0 unexplained on the fixture oracle and 0 regressions on the full 120-probe L0 real-tree run (100/120 clean, parity 0.833, identical to the #54 S2 baseline aside from a pre-existing environmental flake). `--debug` `Parent Dep` trace fidelity and the backtrack-count footer stay open, tracked as backlog #59 (K5). Findings: `TEST/findings/l0-fixture-oracle.md` "#57 S0"-"S2"; plan `docs/history/06.057-directly_requested_hard_atom_conflict.opus.md`. [A]

## Tier 7 — GLEP compliance gaps (filed 2026-09-15)

Surfaced by an audit of portuale against the 12 GLEPs flagged as technical
(78, 82, 74, 79, 81, 53, 59, 61, 64, 68, 83, 84), cross-referenced against
both portuale's Rust source and the vendored real Portage it's validated
against. Full per-GLEP writeup, including the ten that are solidly covered
or correctly out of scope (74/61/79/81/68/83/84 delegate to `gemato`/
repoman/pkgcheck/social process, none of which portuale implements or
needs to), is in `docs/glep-compliance-review.md`. Two genuine, narrow gaps:

55. **DONE 2026-09-15 — honour `layout.conf` `cache-formats` (GLEP 82 gap).** `RepoConfig` now carries the resolved `cache_formats` (lowercased + split; empty -> auto-detect `md5-dict` then `pms` from directory presence, real `parse_layout` `config.py:1564-1577`), and `pregen_md5_cache_enabled` is "the first **known** format is `md5-dict`" (real `iter_pregenerated_caches` + `get_pregenerated_cache` keep only the first yield). `has_usable_md5_cache` is that predicate plus `FEATURES=metadata-transfer` absent (real `porttree.py:322`), resolved through the same config stack the CLI uses; `repo_aux_metadata` skips the md5-cache rung entirely when false and goes straight to the depcachedir/depend provider (mrg-director's `Md5Cache::category` follows the same flag, listing `*.ebuild`). `--regen` follows the writer half (S0 cell h): real `egencache --update` writes `metadata/cache` for `pms` and both dirs for `pms md5-dict`, so `regen_writes_md5_cache` gates a repo to a message + exit 1 rather than writing the wrong directory. Pins: Rust units (cells a-f, g, L3) + Rust-only `tests/test_portuale.py` end-to-end for cells c/e (valid masking entry ignored under `pms`, trusted under `md5-dict pms`, depcache written), g (env `FEATURES=metadata-transfer`) and the L3 skip. **Narrowing (L1): no `pms` reader** — a `pms`-first repo falls to the depcachedir/depend phase, which yields the same metadata real reads from a valid `pms` cache (slower, not different). Evidence: `TEST/findings/l2.md` "## #55 S0"; plan `docs/history/07.55-layout_conf_cache_formats.opus.md`; L0 after the change byte-identical to the pre-change baseline (`TEST/logs/l0-20260915T180851Z`, clean 100 / parity 0.833). [C]
56. **DONE 2026-09-15 — gpkg outer-container tar walk trusts only regular-file members (GLEP 78 hardening).** S0's crafted-archive matrix (`scripts/gpkg_crafted_members.py`; `TEST/findings/l2.md` "## #56 S0") established what real actually does: its in-archive `tarfile.extractfile` never leaves the container (a symlinked `Manifest` -> archive-internal `KeyError`; every link/special member fails the Manifest size check or the structure check first), while portuale's `tar -xf` + filesystem walk followed a symlinked `Manifest`/`metadata.tar.zst`/prefix (cell c listed the host `/etc`, 266 dirents), blocked on a FIFO member and read a char-device member unboundedly. S1 (`de72588`) adds `outer_regular_member`/`outer_prefix_dir` (`symlink_metadata`, no follow) and the shared `walk_outer_members` behind `read_gpkg_metadata`/`extract_gpkg_member`; `verify_gpkg_manifest` uses the same helpers and checks every trusted member before reading the Manifest; a non-regular `gpkg-1`/`Manifest`/`metadata.tar*`/`image.tar*`/`.sig` is an error naming the member and its type at every site (never skipped for a later regular one), and the three outer `tar -xf` calls gained `--no-same-owner` (N3). Pins: the typed `build_gpkg_entries` builder + 7 Rust tests (S0 cells a, b, c, d, f, h across all three sites; cell e root-only), the committed fixture still reads and verifies. L2 porttest `TEST/logs/l2-20260915T195259Z` hard=0, 0 unexplained both directions. **Superseded by #58 S5 (`c33137b`, 2026-09-16):** `walk_outer_members`, `outer_regular_member` and `outer_prefix_dir` no longer exist — the outer container is read in process with the `tar` crate and the trusted-name check is a tar-header type check (`read_outer_members`); the seven crafted-cell tests keep their assertions, re-pointed at the new path. Detail: [`history/07.56-gpkg_outer_container.opus.md`](history/07.56-gpkg_outer_container.opus.md). [K]
58. **DONE 2026-09-16 — no gpkg member (outer container, inner `metadata.tar`, inner `image.tar`) can make portuale read or write a host path (Tier 7 #58; supersedes #56's post-extraction helpers).** K0-K7 answered by the owner 2026-09-16: the inner metadata tar is read **in process** with the `tar` crate (`default-features = false`: zero new transitive crates, no C linkage), real `tarfile.extractfile` semantics ported exactly, and the owner widened the slice to the inner `image.tar` (K2) and the outer container (K7). S0 (`5e6fcf7`) built the crafted inner/image cell matrix and the crate spike (findings `TEST/findings/l2.md` "## #58 S0"); S1 (`1507808`) added the reader (regular/unknown -> bytes, symlink -> `dirname/linkname` normalized over the whole archive last-match, hardlink -> earlier members only, depth cap, 64 MiB/entry bomb guards, last-wins + duplicate flag); S2 (`28f83cf`) wired `read_gpkg_metadata` to it and regenerated the committed fixture without the inner `metadata/` directory member real's writer never emits and its reader rejects (real now reads it 13/13); S3 (`ebe1286`) moved `extract_gpkg_member(metadata)` off `tar -xpf` onto resolved regular-file writes (`create_new`, header mode masked to `0o7777` minus setuid/setgid), refusing duplicates (K5) and nested keys (K4); S4 (`f96804c`) added real `tar_safe_extract`'s rule set as an in-process pre-scan of the scratch `image.tar` before GNU tar unpacks (owner-approved extra rule: a member whose path traverses an earlier symlink member, S0 cell i18), plus the S0 i19 regression pin (symlink, hardlink, sparse, xattr); S5 (`c33137b`) moved the **outer container** in process too (`read_outer_members` header checks: one level deep, one prefix, no duplicates, trusted names regular), `verify_gpkg_manifest` streams member digests through the new `portage_fetch::verify_digests_reader`, and #56's `walk_outer_members`/`outer_regular_member`/`outer_prefix_dir` plus every outer `tar -xf --no-same-owner` are deleted — `binpkg.rs` now has one tar mechanism, GNU tar only unpacks the image. Parity: every S0 cell real accepts reads identically and every cell real raises on now errors (S0 verdict table); documented differences (all stricter, none reachable from real's writer): an in-archive metadata symlink is written as a **regular file** holding the target's bytes (K2), nested keys error on merge (K4), and a `..` component errors on read (K5). Tests: 20+ new Rust units/end-to-end crafted-archive tests plus the re-pointed #56 seven-cell suite (cell c's needle moved to the structure error: the crafted prefix member has zero slashes, which real's own `f.count("/") != 1` rule refuses before any type check); full pytest 1693 passed; L2 porttest both directions rc 0 with 0 unexplained (`TEST/logs/l2-20260916T004658Z` S4, `TEST/logs/l2-20260916T063732Z` S5); read-path timing on a synthetic 200 MB gpkg 26-34 ms with no scratch copy. **Deferred residue filed as #60/#61.** Note: this Tier 7 entry shares its number with the pre-existing Tier 2 #58 (slot-conflict exit code); renumbering not done here (flagged to the owner per the plan). Detail: [`07.058-check_metadata_member_types.md`](07.058-check_metadata_member_types.md). [K]

60. **DONE 2026-09-16 — gpkg populate-path read errors: real's invalid class is skipped, the uncaught class still aborts (Tier 7, #58 S0 residue).** `BinpkgError { Invalid, Fatal }` (`bcfae67`, S1) now tags every error site of the populate-reachable readers (`read_gpkg_metadata`/`read_xpak_metadata` + helpers; `Display` + `From<BinpkgError> for String` keep the merge path byte-identical) exactly where real's `bintree._populate_local` catch (`bintree.py:1185-1199`) splits: `PortagePackageException`/`SignatureException` -> Invalid (outer structure/marker/duplicates, missing or unsupported metadata member, metadata caps, decompressor missing/failed, unsafe/outside-`metadata/` inner names), everything real does not catch -> Fatal (inner-tar parse/read errors, unresolved links, directory/FIFO/device members, link cycles, IO), and **every `.xpak` read error Fatal** (real's `tbz2.scan()` -> None -> `AttributeError` on `None.get`). `populate_local_pkgdir` (`fb7e509`, S2) prints real's own `!!! Invalid binary package: '<path>', <reason>` (the reader's `'<path>: '` prefix stripped for real's one-path shape) and continues, while a Fatal file still returns Err -> the CLI's existing `emerge: scanning <pkgdir>: <msg>` rc 1. Live on the S0 setup: cell i8's crafted archive no longer hides `dev-libs/binaryonlypkg` (rc 0 with the warning; rc 1 before), cell i1 still aborts. Pins: `binpkg_error_classes_mirror_reals_caller` + the re-classed crafted-cell tests + `populate_local_pkgdir_skips_invalid_files_and_keeps_the_pool` / `..._aborts_on_fatal_files` (S1/S2), and the hermetic contract test `test_pkgdir_scan_skips_real_invalid_class_and_aborts_the_rest` (`d7ce18d`, S3). S0 (`c0eda75`) holds real's source classes, the live `--mode real` probe and the pool-poisoning reproduction. Plan: [`history/07.60-gpkg-populate-read-errors.opus.md`](history/07.60-gpkg-populate-read-errors.opus.md). [K]

61. **DONE 2026-09-16 — `musl/smoke_test.sh` restored: builder, source portability, MSRV guard, and the stale assertion layer (Tier 7 infra).** The original diagnosis was partial; the bed had three walls, all reproduced on the real tree (`TEST/findings/musl.md` S0 matrix) and all fixed on `backlog/61-musl`. **(1)** alpine:3.22.1's rustc 1.87 vs the workspace's 1.95 floor (`gentoo-interner`/`portage-atom*`/`portage-solver`; pubgrub 1.92, smol_str 1.89 behind them) aborts cargo per build graph. **(2)** the `x86_64-alpine-linux-musl` target is dynamic-musl by default, so `+crt-static`'s `-Wl,-Bstatic ... -Wl,-Bdynamic` wrapper left gcc's appended trailing `-lc` dynamic while `-static` was in force (`attempted static link of dynamic object .../libc.so`). **S1** (`2b2f19d`) moves the base to `alpine:3.24.1` (rustc 1.96.1, first stable release over the floor; Alpine's own rust/cargo apks kept, no rustup) and adds a trailing `-C link-arg=-Wl,-Bstatic` (lands after rustc's `-Bdynamic`, before gcc's libraries); the same tree's five binaries are `INTERP=0 NEEDED=0`. **(3)** `pretend.rs::apply_scheduling_policy` built a glibc-shaped `libc::sched_param` literal, E0063 on musl; **S2** (`c550214`) zero-inits and assigns `sched_priority`, matching CPython's `os.sched_param` conversion behind real `actions.py:3379`. **S3** (`9616339`) declares `rust-version = "1.95"` workspace-wide (all 15 members inherit) so an old builder fails with Cargo's own message (verified on 3.22.1). **S4** (`df403fd`) rewrote the script's pilot-era exact strings as structural assertions (rc + exact cpv order + markers; byte-exact output stays the contract suite's job), replaced the obsolete `--jobs` and `ebuild`-stub checks (`--nobindeps` refusal, `ebuild --help` + bad-phase refusal) and routed every command through a `capture` helper so `set -e` no longer aborts at the first expected-nonzero rc (`blockerpkg`). **S5** (`23d51fc`) silences the musl-only `libc::time_t` deprecation. S0 (`7052b35`) holds the wall matrix, the link-line root cause and the base/toolchain table. Verification: `bash musl/smoke_test.sh` **PASS, 23/23 checks, builder warning-free**. Plan: [`history/07.61-musl-smoke-toolchain-drift.opus.md`](history/07.61-musl-smoke-toolchain-drift.opus.md). [K]


---

## Deliberate cuts — do NOT pick these (documented non-goals)

- Backtrack **timing** report line `Dependency resolution took X s (backtrack: N/M)` — non-deterministic, portuale is a deterministic tool. [A]
- Static `--pretend` preamble (`These are the packages that would be merged, in order:` / `Calculating dependencies ... done!`) — unpinned today, and aligning it would move every display pin for zero resolution signal; the timing half is the cut above. Backlog #59 K2. [A]
- `soname` slot-conflict reason key — unreachable: `portage-dep` can't parse soname atoms, dep flattening drops them. [A]
- Third-party mirror list **shuffle** (real's `random.shuffle`; portuale stays deterministic) — every candidate is digest-verified regardless, so shuffle order affects only which mirror is tried first (load balancing), not correctness. Decided 2026-09-14 (D4). [E]
- `--autounmask-write` / any config-writing autounmask mode — "never writes `/etc/portage`". [Part 3]
- PyO3 / in-process FFI; EAPI 0/1/2/3/4/6; `bsd_chflags`; RPM; `emerge --sync`; GLSA/`@security`; `equery`/`portageq`; `clap` for the emerge/ebuild parsers; `os.listdir()`-order directory merge. [Part 3]
- `--metadata` (portuale reads `md5-cache` directly); `xpak` for `mrg`. [Part 3]
- Cycle `--tree` **nesting** — real's `[nomerge]` rows, duplicated nodes and row-counted `Total:` inside a circular-dependency tree. Portuale's tree model dedups by design (Gate G0.2); `large_cycle_count`, elementary-cycle enumeration (`merge_order.rs::elementary_cycles`) and the flat cycle re-display already match real (the `cyc4a` pin). Decided 2026-09-14 (D1, former backlog #21). [A]
