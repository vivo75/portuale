# Backlog tasks

One line per open task. Each has a code/doc pointer so a fresh model can
start without a full context load. Source of truth for detail:
[`scope-backlog.md`](scope-backlog.md) (section letters in brackets),
`what-this-proves.md` (what already shipped), `git log`, and the memory
notes under
`.claude/projects/-home-vivo-repo-PORTUALE-portuale/memory/`.

Rules that apply to every task: take expected output from real Portage
(container bed, host `emerge`, or an upstream resolver test), pin it in
`tests/test_emerge_pretend_contract.py` / `tests/test_portuale.py`, keep
`tests/test_output_invariants.py` green, and run the full verification
pass (`AGENTS.md` step 8). The Python mirror
(`python/emerge_pretend_reference.py`) was removed 2026-09-15 —
[`second_python_copy_removal.md`](second_python_copy_removal.md); older
entries below that say "dual-language" / "Rust==Python" describe how
they shipped, not a rule for new work.

---

## Tier 1 — focused slices (scoped, ~one sitting each)

> **Audited 2026-09-13:** commit `67f3b0e` (2026-09-10, "Tier 1 backlog:
> ship 14 slices, re-pin brush, document two verdicts") shipped most of
> this list but never updated this file; the entries below are marked
> accordingly. Detail: `scope-backlog.md` Parts 1–2 and the
> `what-this-proves.md` Tier-1 paragraph.

1. **DONE 2026-09-10 — release-profile `lto`/`codegen-units`.** `rust/Cargo.toml` `[profile.release]` sets `lto = "thin"` + `codegen-units = 1` (15.5→13.2 MB); measured in `performances-tuning.md` §5.
2. **DONE 2026-09-10 — `--solver=` module-doc refresh.** `solver_bridge.rs`'s "Deliberate v1 cuts" list is current (blockers/ABI/merge-order edges/markers/circular deps all named as wired). [J]
3. **DONE 2026-09-10 — `--solver=` forced-flag markers.** The bridge render path calls the walk's own `forced_or_masked_flags` (`solver_bridge.rs:495-512`), so the `( )` wraps render. [J]
4. **DONE 2026-09-10 — `--solver=` notice fields.** `circular_deps` is wired from the bridge result via `find_hard_cycles` (`solver_bridge.rs:719`); `slot_conflicts`/`autounmask_*` stay empty **by construction** (an engine plan picks one version per CPN and no relaxation loop runs) — documented in the module doc, not a to-do. [J]
5. **brush PRs: submit staged fixes** — **five** fixes staged as `fix/*` branches on `vivo75/brush` + `git format-patch` exports in `docs/brush-pr/`; all five are in the thin-fork `main` `b9524ad5` and portuale is re-pinned to it (2026-09-14), but **none is open upstream yet** — opening the PRs needs the user's GitHub auth (B6). See `docs/brush-pin.md` "Still to do". [G]
6. **brush re-pin (recurring)** — re-pinned 2026-09-14 to upstream `25bffd54` on the thin fork (`b9524ad5`; fixes 01 tokenizer, 02 AST `Display` + quoted here-tags, 03 pipeline deadlock, 04 `source` parse-error status, 05 IFS-independent brace expansion); the next periodic bump follows `brush-pin.md`'s checklist. The 2026-09-13 `src_compile` no-op is fixed (Track B, B1–B4): quoted-tag serialization, the silent `source` abort, the missing embedded `$BASH` and the brace-expansion/IFS defect were the root causes; pinned by the `heredocpkg` Bash/Brush image-set test, the corrupt-saved-environment regression and the L2 G3 re-run (`TEST/findings/l2.md` "#38 S2", `docs/brush-pin.md` "What is *not* tracked here"). Dropping the fork (and revisiting the `--shell` default) waits on #5. [G]
7. **DONE 2026-09-10 — `FEATURES=compress-build-logs`.** `.gz` log paths + gzip pump + gunzip tails in `emerge_build.rs`. [B]
8. **DONE 2026-09-10 — elog `mail` / `mail_summary`.** Hand-rolled MIME, sendmail-binary/plain-SMTP delivery, per-package + run-wide summary; STARTTLS stays a documented cut. `rust/portuale/src/elog.rs`. [B]
9. **DONE 2026-09-10 — `env.d` eroot path.** `resolve_config` gained the `eroot` param; `env.d` reads `<eroot>/etc/profile.env` (not `config_root`). [C]
10. **DONE 2026-09-10 — `package.env`/`env.d` `${VAR}` expansion.** Per-entry expand map (`expand_package_env_files`), global-map + within-file/cross-file chaining. [C]
11. **DONE 2026-09-10 — `depend`-phase RESTRICT/PROPERTIES reduction.** Reduced on the config-USE set for the `depend` phase. [D]
12. **DONE 2026-09-10 — standalone `package.env`.** `ebuild <file> <phase>` applies per-package `package.env` via the shared md5-cache-identity matcher (`match_package_env_vars` in `rust/portuale/src/ebuild_phases.rs`, called from `phase_standalone_base_env`; pinned by the `standalone_phase_env_layers_matching_package_env_build_vars` unit test). [D]
13. **DONE 2026-09-10 — resumed binary from `$PKGDIR`.** Resumed entries always carry `remote_binary: false`, so the binhost fallback can't fire; local `$PKGDIR` only. [B]
14. **Fetch candidates / mirrors — sliced 2026-09-14 (`docs/backlog_tier_1_sliced.opus.md` §2.4/§4).** Not a set of cosmetic cuts: the `GENTOO_MIRRORS` fallback built flat URLs, which 404 on `distfiles.gentoo.org` (it publishes only `0=filename-hash BLAKE2B 8`). **Landed 2026-09-14:** F1a mirror layout math + `layout.conf` parsing (`portage-fetch/src/layout.rs`, pinned to real Python output); F1b per-mirror `layout.conf` download + `.mirror-cache.json` in real's format, lazy resolution (`fetch.rs::mirror_url`; live-fetched `which-2.23.tar.gz` from `…/distfiles/80/…`, real portage reads portuale's cache); F2 each location attempted once (`tried_locations`); F3 (1)(2) second checksum failure jumps to the primary URIs + `PORTAGE_FETCH_CHECKSUM_TRY_MIRRORS` cap; **F3 (3)** renamed corrupt downloads with deterministic suffix (fetch.rs:783-806, `_checksum_failure_<N>`); F4 `fsmirrors` copy (`copy_from_fsmirrors`), `/`-rooted `GENTOO_MIRRORS` never a `wget` URL; F5 one candidate list per distfile (`group_by_filename`, literals last-listed first per real `fetch(listonly=1)`), `A`/`AA` deduped. **F6 confirmed as deliberate cut 2026-09-14:** the third-party mirror shuffle stays non-deterministic in real, portuale stays deterministic — listed under Deliberate cuts. [E]
15. **NOT A GAP — decided 2026-09-10 — `:=` literal-bound atom validation.** No "improper context for slot-operator built atom syntax" masking exists in the vendored checkout, site-packages 3.0.82.2, PyPI 3.0.82.2, or upstream main; all parse `:slot/sub=` and match it structurally, exactly as `portage-dep` already does. See `what-this-proves.md`. [A]
16. **DONE 2026-09-10 — slot-conflict `--color y` marker alignment.** Violated spans wrapped in red with markers placed on the displayed string (`slot_conflict_caret_idx` + `colorize_marked_spans`, `pretend.rs:7212`/`:11139`) — a deliberate divergence from real's pre-color-index marker drift (upstream bug; byte-parity unachievable anyway). [A]

## Tier 2 — larger / needs design first

17. **cluster I merge-order timing (F-B1 drain timing)** — B-series (025 plan, 2026-09-12) landed the trace harness (`TEST/scripts/mo-trace/`) plus two fixes: the installed-slot closure (`by_cp` now keeps every version; `MO_NODES` 398==398 on gtk:4) and bare multi-slot atom resolution in `build_digraph` (firefox #29→#37, thunderbird #30→#38). L0: clean 96→98, parity 0.800→0.817, order 22→20 raw (19 with the B5 gnome-shell explanation). Residue: F-B1 frontier drain timing only — gtk:4 runs 578 iterations vs real's 290 with identical node sets; the first divergence is the iteration-1 greedy batch order (the 14-probe installed-chain lever family). F-B3/F-B4 moved to #25. See `docs/025-tier2-closeout.deepseek.md` §11 + `TEST/findings/l0.md` "## I". [A]
18. **DONE 2026-09-10 — `_FrontierDigraph` perf layer.** `merge_order.rs::SerializeFrontier` shipped (`PORTAGE_SERIALIZE_FRONTIER_DISABLE` fallback, unit-pinned behaviour-neutral; see `scope-backlog.md` and `what-this-proves.md`). Stale entry corrected 2026-09-12 (025 B6). [A]
19. **DONE 2026-09-11 — DFS-partial merge-list truncation.** Slices 1–6 shipped (`4d5bffe` S1, `6b10b16` S2, `ffdd251` S3, `eef8494` S4, `354274e` S5, `e4e83c7` S6): outcome model + `PORTUALE_ABORT_PATH` gate, `abort_outcome` membership/order, exit 1 on masked/unsat/cycle aborts, the partial list + counters + `--json` `aborted`, and the error-block wiring. The three remaining strict xfails are **not** #19 (one `--debug` narration gap, two spec-§4e autounmask findings). Real-tree plasma-meta/podman residue is a `||`-selection gap (spec §4d), tracked separately. Detail: `scope-backlog.md` §A "DFS-partial abort path" entries; oracle `docs/abort-path-spec.md`. [A]
20. **DONE 2026-09-14 — `[use]`-dep unsat diagnostics (residue of #20).** A `[use]`-dep dependency atom with no autounmask flip now prints real `_show_unsatisfied_dep`'s separate "no ebuilds built with USE flags to satisfy `<atom>`." block: the `!!! One of the following packages is required…` header, the `- <cpv>::<repo> (<reason>)` rows (`Change USE: +flag -flag` for adjustable flags, real's "only show the latest version" rule; `Missing IUSE: <flags>` when the flag is not a valid IUSE at all), and the shared `(dependency required by …)` chain. Dual-language: `portage-repo::UseUnsatDepReport` + `use_unsat_candidates_for_atom` / `GraphResult::use_unsat_deps`, rendered in `pretend.rs`/`emerge_pretend_reference.py` ahead of the masked block (real's precedence), with the chain walked by the existing `masked_dep_chain`. The two existing pins were re-pinned to the new text; two new fixtures (`unsatuseiuse` Missing IUSE, `unsatusealtmultidep` latest-only) with pinned-output tests + oracle captures (portage 3.0.81.3); two Rust unit tests. Real's parent-conditional row and the masked-candidate suppression of the Missing IUSE fallback stay documented narrowings (same #19 park). Evidence: `what-this-proves.md` 2026-09-14. [A]
22. **DONE 2026-09-11 — `dep_zapdeps` finer choice bins.** `all_available`/`all_use_satisfied` split, `unsat_use_*` bins with the bug-515584 unmask gate, `all_installed_slots`, in-bin upgrade-preference ordering (slice 2, `use_reduce_flat_disjunctive`'s `tie_break` + `portage-repo::promote_tied_alternative`), and the `other_*` bins + `allow_masked` two-pass return (slice 4, `portage-repo::atom_matches_installed`) all shipped, both languages, one shared probe + tie-break pair. Formally cut, documented in `disjunction_preference`'s own doc comment (slice 5): `conflict_downgrade`/`installed_downgrade` (needs `downgrade_probe` + a live mutating `graph_db` neither language has — #35, carved out 2026-09-11 per `docs/023-backtracking_resolve.md` B2) and `circular_atom` (needs a `circular_dependency` dict from an earlier real pass, plus `--onlydeps`, neither threaded to this call depth); `minimize_slots` (needs `_overlap_dnf` DNF detection, not done) and `want_update` (needs the `--update`/`--newuse` "wanted" set, pinned `false`) stay cut per slice 2. Real `lib/portage/dep/dep_check.py::dep_zapdeps`. Code: `portage-repo` `disjunction_preference` + `promote_tied_alternative` + `portage-use-reduce` `resolve_disjunctions`; `solver_bridge.rs` is #34, not #22. Design brief: `docs/022-agent-task-22-zapdeps.fable.md`. Detail: `what-this-proves.md`'s 2026-09-11 `||`-resolution paragraphs. [A]
23. **DONE 2026-09-11 — backtracking resolver search.** Portuale's `'backtrack` loop is now real's `_backtrack_depgraph` node-stack search (`Backtracker`: DFS `get`, param-only dedup, bug-375573 `_check_runtime_pkg_mask`, config/slot-conflict/missing-dep `feedback`, `get_best_run`; `--backtrack=N` bounds `mask_steps`, config retries free; dead-end abandonment instead of settling masked graphs into the abort path), fed by a real-shaped `slot_conflict_mask_choices` (692746 append, version-desc, per-candidate `conflict_atoms`, stable len-sort, first-slot-only, one node per choice, similar-grouping) with puller pre-masking removed (parent downgrades arrive via the mask-aware missing-dep probe). Non-goals, explicitly out (see `docs/023-oracle.md` + `what-this-proves.md` 2026-09-11): `--debug` "backtracking due to slot conflict" narration (unpinned, merge-list parity only); "masked by backtracking" listing in `_show_unsatisfied_dep` (no such string; `get_best_run` rationale only); combined config+conflict exploration order (first-hit-wins kept); real's exact stopping rule (`max(1,(N+1)//2)` depth + total-restart cap); #35 downgrade guards (separate item). Follow-ups own the remaining divergences: #25 (btnr installed instances), #24 (a522084 `B-0` rebuild), #36 (mg3-bt1 selection fallback). Plan: `docs/023-backtracking_resolve.md` (+ inventory, oracle); shape: `ResolveCtx`/`BacktrackParams`/`run_pass`/`collect_feedback`/`assemble_result`. [A]
24. **DONE 2026-09-12 — slot-operator rebuild undo path.** Add backtracking for a rebuild that shifts another sub-slot, re-bind the rebuilt consumer's own `:=` deps, and the `--changed-slot` interaction (one missing piece, not three). `slot_operator_rebuild_entries`; citations in `history/scope-backlog-2026-09-05.md`. S1-S6 shipped 2026-09-12 (plan `docs/024-slot-operator-plan.md`, oracle `docs/024-oracle.md`): S1 complete-mode gate under `--deep` (the a522084 `B-0` miss was the missing auto-enabled pass, not the undo path); S2 22 oracle pins over the whole `test_slot_operator_*` family; S3 the rebuild is now a **walked graph node** -- `slot_operator_rebuild_scan` feeds `BacktrackParams::slot_operator_replace_installed` through `collect_feedback`, the next pass seeds it like real's `@__auto_slot_operator_replace_installed__` `SetArg` and flips the outcome in-walk, so deps, `:=` re-bind and merge order are real (`[A-2, B-0, C-0]`, bug 522652); no synthesised entries left on the default solver; S4 `_eliminate_rebuilds` -- the nine ordered rules + the graph-aware `:=` binder (`bind_slot_operator_deps`, real `_eval_deps` over `_graph_trees`), demotion via `slot_operator_undone` latch, `slotundo-unnecessary` now MATCHes (`[U] souprov`, `abi_rebuilds: []`); S5 `_slot_change_probe` -- bug 456208's slot-move-without-revbump now schedules the installed child (`slotchange-1`/`regslotchange` MATCH, `[rR]` child + plain parent, no block), real's rule 3 landed behaviour-neutrally, and two S2 expectations were corrected live (the `--changed-slot` flag half has no `r`/edge in real; conflict-mass is v2 `#24b`'s update probe, not the probe); S6 L0: 120 probes, 96 clean, parity 0.800, `portuale/` raw outputs byte-identical to the S0/S1 archives (the slot-op path does not fire on the corpus) -- no regressions, no findings to adjudicate (`TEST/findings/l0.md`). S7: final verdict table (`docs/024-oracle.md`), `what-this-proves.md` S1/S2/S5/S6 paragraphs, scope-backlog/agent-context. S4 finding: bug 614390's `complete` case is blocked by a **selection** gap (named bare `socc` resolves before meta's `=socc-1` through the already-installed fast path, which skips `resolved_slots`; real's `_add_pkg` slot-parent check catches it) -- #36-overlap, not the undo; stays strict-xfail. **Non-goals, stated:** the v2 probe family `#24b` (update probe + `check_reverse_dependencies`), `#24c` (`slot_operator_mask_built`; every binary half), `#24d` (`prune_rebuilds`), `#24e` (`_slot_conflict_backtrack_abi`), `#24f` (in-walk unsatisfied probe); `IUSE_EFFECTIVE` in the built-dep domain; `--rebuild-if-*` through the same path (G0.6); the `--debug` "backtracking due to missed slot abi update" narration and the `Dependency resolution took…` line (G0.7, cut). [A]
25. **`_complete_graph` installed nomerge nodes** — real carries every `@world`/`@system`-reachable installed package as a graph node; portuale keeps only their reverse-dep atoms. **Now owns the B4 superseded-in-edge model too** (`docs/025-tier2-closeout.deepseek.md` §11 F-B4, 2026-09-12): real drops an installed node's in-edges when a same-slot merge supersedes it, and draws no edge for a dep initially satisfied by installed (`nghttp2 -> systemd` gone; gedit #5/nautilus #8). Walk-order dependent; needs the resolver's insertion order, so it also subsumes F-B3's `_create_graph` pre-bias order work. **R3b DONE 2026-09-15** (`docs/025b-complete-graph-nodes.md` §7): the pre-bias divergence was the *seed* order, not a missing `insertion` field -- real `_resolve` processes each `SetArg`'s atoms `sorted(arg.pset.getAtoms(), key=str)` (`depgraph.py:5500`) while portuale used profile order; sorting the `@system` segment takes `-pe @system`'s 368-node `MO_ORDER` to three localized residues (a docbook 4.5/4.2 swap from real's own hash-seeded `_minimize_children` sets, `depgraph.py:4751-4781`; the portage-version visibility row; a 12-node `virtual/man` disjunctive bundle pop timing). L0 flipped-probe log: `TEST/findings/l0.md` "R3b"; probe artifacts `TEST/logs/r3b-20260915T000000Z/`. **R3c attempted 2026-09-15, STOPPED** (`docs/025b` §8, `TEST/findings/l0.md` "R3c attempt"): the edge target depends on real's per-atom insertion instant; the blunt drop fixes gedit's nghttp2 but regresses `_system`/`_world` (L0 `l0-20260915T051103Z`: clean 97 / order 20 vs 100 / 17), the DFS-rank gate preserves L0 but does not fix gedit (nghttp2/systemd insertion ranks disagree). **R3c and R3d are one change**: carry the insertion instant per entry (installed nodes first-class) first. Work reverted; tree at R3b/H4. [A]
26. **DONE-PARTIAL 2026-09-12 — installed-package metadata overlay (`FakeVartree._apply_dynamic_deps`).** The old "walk source doesn't switch" line was false (verified live). A1-A4 landed: `InstalledMetaLayer` + `installed_dep_string` (Raw/Effective, per-pass memo, `--ignore-built-slot-operator-deps` gate), `enqueue_dependencies`/scheduler closure/`GraphEntry::deps` read the same view, installed-parent `slot_pullers` recorded. The built-`:=` append itself stays behind `PORTUALE_DYNAMIC_DEPS_APPEND` (default off) because of **F-A1**: with it on, the Python mirror folded a solvable slot conflict real resolves via the slot-op probe (slotchange-case4), and the cascade probe drops a consumer. **2026-09-15:** the mirror is gone; with the gate on, only `test_oracle_slotop_undo_cascade` (the dropped consumer) still fails, so that one Rust case is the whole remaining blocker. Residue in `docs/025-tier2-closeout.deepseek.md` §11 F-A1; detail plan `docs/024-dynamic-deps_n_disagreement.deepseek.md`. [A]
27. **DONE-PARTIAL 2026-09-12 — `need_rebuild` slot-conflict trailer.** Renderer + installed-parent `slot_pullers` recording (025 A4) + a Rust-pinned fixture for the `--exclude` and `--useoldpkg-atoms` reasons, under the A1 append gate; gate-off negative control pinned. Residue: the `--usepkgonly` silence arm and the "ebuild is masked or unavailable" reason (needs the `_DynamicDepsNotApplicable` missing-ebuild fallback), (the Rust==Python parity half is moot: the reference was removed 2026-09-15). `docs/025-tier2-closeout.deepseek.md` §11 F-A2. [A]
28. **mrg-director: wire the remaining scaffolded slots** — after Tier 2.28 (`261e184`, re-verified 2026-09-13) and H1/H2/H3 (2026-09-14), **7/8 slots carry production traffic**: `SchedulerPolicy` (`run_build_scheduler`), `MergeEngine` (merge dispatch, `SourceEngine`/`BinaryEngine`), `NewsSelector` (`--check-news` via `FilesystemNews`), `Fetcher` (the `fetch_src_uri` candidate loop's `FetchRequest` dispatch), `PackagesDb` (H2: `find_owners`/`owns_path` read CONTENTS through `VdbReader`; `reverse_dependents` documented consumer-less), `BinpkgIndex` (H3: `merge_one_binary_entry`'s remote `Packages` lookup via `RemoteBinhostIndex::metadata_with_source`; `PkgdirBinIndex` still test-only -- the local path resolves by `$PKGDIR` scan), `RepoCache` (H4, 2026-09-15: `entry_metadata_env`'s per-build-entry metadata read goes through `mrg_director::Md5Cache`/`RepoCache`, whose `metadata` delegates to the #41 decision point `portage_repo::repo_aux_metadata` -- cache -> depcachedir -> depend phase -- so the seam carries the fallback unchanged; behaviour-neutral, L1 porttest unchanged). `Director` is test-only pending H5 (D6). Plan: `docs/backlog_tier_5_and_2_sliced.opus.md` §7 H4–H5. [H]
29. **DONE 2026-09-14 — L2, portuale as builder.** The bed is shipped (`TEST/run/l2-portuale-builder.sh` + `layers/l2/*` + `compare/gpkg-{structure,diff}.sh` + `diff.py --layer l2/--tolerate-payload`) and both tracks are green: the porttest fixture track `strict hard=0 soft=0` on every archive pair with all `l2-*` allowlists deleted (`l2-20260913T232955Z`), and the real `l1-merge.txt` set rc 0 (`l2-20260914T032915Z`) — cross-install direction A (real consumes portuale-built archives) 0 hard / 0 unexplained, control (portuale consumes the portage-built set) 0/0, residue only the tolerated payload set. The producer gaps that blocked it closed as #37/#38/#39/#40/#43. Findings: `TEST/findings/l2.md`; plan: `docs/029_portuale-as-builder.deepseek.md`. [I, K]
30. **DONE-PARTIAL 2026-09-14 — L3 source-build parity.** Harness shipped (`TEST/run/l3-source-parity.sh` + `TEST/layers/l3/build-and-merge.sh` + `l3-{smoke,core,system}.txt`; full-tree snapshot, `diff.py --layer l3 --tolerate-payload`, metrics JSON). The portage-vs-portage control pair is **0 unexplained on `l3-smoke`** (full-tree, no normalization rules). The candidate run surfaced four producer classes; fixed: `-e`/reinstall of a sub-slotted package aborting on its own files (`find_collisions` given the full `slot/sub_slot`), and incremental config semantics (a second `FEATURES=` in one file replaces for real, stacked for portuale). Filed with repros: ncurses `OWNER 1:1` vs `0:0`, and saved-env accumulation (`A`/`RESTRICT`/stray locals) in the vdb environment. `l3-core`/`@system` not started (S4 stop rule: >3 systemic classes). **P3 DONE 2026-09-15:** the full candidate+control pair `TEST/logs/l3-20260914T234404Z` is **0 hard / 0 unexplained on both** -- the last 12 rows were #47's `BUILD_TIME`/`metadata` gap, fixed in P3 -- so the S4 stop rule is lifted in `docs/030_L3-source-build-parity.deepseek.md` and `l3-core` (344 ebuilds) is unblocked for a user-triggered run. Evidence: `TEST/findings/l3.md` "P3"; plan: `docs/030_L3-source-build-parity.deepseek.md`. [I]
31. **L4 — `mrg` remote merge over SSH** — differential test of the remote binary-merge path; design in `remote-merge.md` §6. [I]
32. **L5 — lifecycle & fault injection** — `-C` / `--depclean` diffs, soname bump → preserved-libs, `CONFIG_PROTECT`, `--resume` after SIGKILL, disk-full / corrupt-archive / binhost-500. `TEST/`. [I]

## Tier 4 — `--solver=` real-tree correctness (pubgrub / resolvo)

33. **`--solver=resolvo` cycle linearization** — `install_order` can't order any closure with a toolchain cycle (glibc↔gcc↔perl); `solver_bridge.rs` prints raw ids. Non-functional on real targets. `solver-backends-analysis.md`. [J]
34. **`--solver=pubgrub` over-merge** — feeds pubgrub the over-approximated `flag?()` reachability closure as the real graph (`nodejs`: 48 vs 8). Feed the resolved graph instead. `solver_bridge.rs`. [J]

## Tier 5 — L2 producer parity (filed 2026-09-13, blocks #29's real half)

37. **DONE 2026-09-13 — build phase env completeness.** Source builds now run with the resolved config env (`portage_profile::phase_environ`: implicit USE, resolved `FEATURES`/`PORTAGE_FEATURES`, `MULTILIB_ABIS`/`DEFAULT_ABI`/`LIBDIR_*`, `SLOT`, `PORTAGE_REPO_NAME`/`REPO_REVISIONS`, `SOURCE_DATE_EPOCH`) on `emerge`/`--resume`/`--buildpkgonly`; the Rust execution gates read the resolved `FEATURES`; brush exports are shell-quoted. Porttest track green from a clean run, `l2-bpkgonly-env` allowlist rows deleted, oniguruma → `/usr/lib64`, `--buildpkgonly` ≡ `-b` archives (`gpkg-diff` strict hard=0). Plan: `docs/037_Build-phase-env-completeness.plan.md`; evidence: `TEST/findings/l2.md` S4/S5. [K]
38. **DONE 2026-09-13 — Packaging transforms: dostrip/splitdebug/docompress.** The real `install_qa_check` gates now fire on every source path (the `PORTAGE_TMPDIR` boundary default was the last byte difference, fixed as a #37 follow-up), so archives carry stripped binaries, `.debug`/`.build-id` trees with the oracle's build-ids, and `BIG.txt.bz2` docs; `porttest/restrict-strip` (`RESTRICT=strip`) pins the inverse cell. The merge-time complement `instprep` is implemented (`ebuild_phases::run_instprep`) on every source and binary merge from the resolved phase env; `--buildpkgonly` ≡ `-b`, L2 porttest 0 unexplained, L1 green, allowlists narrowed to the one real-nondeterministic `setuid` build-id link race. Filed #42 on the way (no `clean` before builds). Plan: `docs/038_Packaging-transforms.plan.md`; evidence: `TEST/findings/l2.md` "#38 S0-S5". [K]
39. **DONE 2026-09-13 — gpkg metadata completeness.** `write_post_install_metadata` now writes real's `IUSE` (always, empty included), the profile-computed `IUSE_EFFECTIVE`, the pre-transform `${D}`-walk `SIZE`, and evaluates dependency-atom use-deps before the `:=` bind (real `token_class=Atom`); the new `write_post_install_soname_deps` rewrites `NEEDED.ELF.2` with the 6th multilib-category field (`compute_multilib_category`), generates `PROVIDES`/`REQUIRES` (`SonameDepsProcessor` port), and injects `>=<libc>` into `RDEPEND` (`_inject_libc_dep`); the `Packages` stanza is real's own sorted/translated field set (`EAPI`/`DEFINED_PHASES`/`PROVIDES`/`REQUIRES`/`REPO`/`MTIME`). L2 porttest is `hard=0 soft=0` on every archive with all `l2-gpkg-*` allowlists deleted (`l2-20260913T232955Z`). Evidence: `TEST/findings/l2.md` "#39". [K]
40. **DONE 2026-09-13 — consumer-side env of portuale-built archives.** Fixed as a consequence of #39's `Packages`-index `EAPI`: real Portage no longer defaults the archive's EAPI to "0" (which made its merge phases export the EAPI≤6 `DESTTREE`/`INSDESTTREE`/`PORTDIR`/`ECLASSDIR` into the vdb env and skip `pkg_pretend`); the cross-install `porttest/phases` log now matches and the `l2-consumes-portuale-env-degraded` entry is deleted. Evidence: `TEST/findings/l2.md` "#39". [K]
41. **DONE 2026-09-15 — Ebuild fallback for a cache-less repo.** Real parses ebuilds when `metadata/md5-cache` is absent. **C0/C1/C2 DONE 2026-09-14:** C0 oracled real's fallback (depcachedir write, non-root in-memory, `_md5_` validation); C1 routed every aux-metadata read through `portage_repo::repo_aux_metadata` + `has_usable_md5_cache` and made cache-less listers walk `*.ebuild`; C2 registers the depend-phase provider (`ebuild_phases::depend_phase_metadata`) from the binary's `main`, so a cache miss runs the real `depend` phase, memoised per cp with a recursion guard for the phase's own nested metadata reads. **C3 DONE 2026-09-15:** the provider checks a valid `depcachedir` entry (`/var/cache/edb/dep/<repo path lstrip '/'>/<cat>/<pf>`, real `flat_hash`/`md5-dict` layout) before the phase and writes the rendered entry back after it (best-effort; unwritable keeps C2's in-process memo), sharing `regen::{entry_is_valid,render_entry,write_entry}` with `--regen`; pinned by `test_cache_less_repo_metadata_is_written_to_the_depcache` (second run does not re-run the phase; a content bump invalidates). The L2 `L2_CACHELESS=1` variant deletes the overlay's committed cache in both build containers: `TEST/logs/l2-20260915T004923Z` all ten archive pairs `hard=0 soft=0`, cross-install/control 0 unexplained (normal `l2-20260915T004600Z` rc 0). Rust-only per D2. Finding `l2-no-md5-cache-ebuild-fallback` FIXED. [K]
42. **DONE 2026-09-13 — `emerge` never pre-cleans `${PORTAGE_BUILDDIR}`.** `ebuild_phases::run_clean` (the real `bin/ebuild.sh clean` -> `__dyn_clean`) now runs at real's positions: before every source build (`merge_one_source_entry`, `build_one_source_entry`, `run_buildpkgonly`), after a `--buildpkgonly` package (unconditional, `keepwork`/`keeptemp` honored by the phase), and after a successful merge unless `FEATURES=noclean` (`run_merge`, `merge_one_built_entry`); `run_qmerge` stays clean-free like real `doebuild qmerge`. The #38 S4 repro no longer wipes the builddir between cells and is green (`TEST/logs/l2-instprep-20260913T222526Z`). Evidence: `TEST/findings/l2.md` "#42". [K]
43. **DONE 2026-09-13 — test-bed binpkg GPG check blocked the real-set cross-install.** `TEST/layers/l1/consume.sh` drops the image's signed, different-revision catalyst gentoo binrepo before the `-k --getbinpkg` merge (the `$PKGDIR` set + installed state is the set under test); portuale now honours a per-binrepo `verify-signature` via `binpkg::GpgVerify::from_binrepo` (real `gpkg.__init__`'s own folding, `gpkg.py:792-819`), used by `emerge_getbinpkg::merge_one_binary_entry` on fetched remote archives (`find_remote_binpkg` returns the repo). Evidence: `TEST/findings/l2.md` `l2-binpkg-gpg-check`. [K]
44. **DONE 2026-09-14 — L3 merge/install ownership of image `bin:bin` files.** `ebuild_merge.rs` now implements real `dblink._needs_move` (`vartree.py:6363`, bug #722270): an existing regular file with the same mode and byte-identical content is not moved, so its inode/ownership survive a byte-identical `-e` rebuild (real's `1:1` ncurses case); a differing file is still replaced and takes the source's ownership, and a diverted `._cfgNNNN_` write always moves. Root-owned regression test `test_merge_skips_an_identical_file_preserving_ownership` (fails pre-fix). L3 smoke `l3-20260914T132606Z`: **0 OWNER rows** (was 2121); L1 porttest `l1-20260914T135235Z` 0 findings. Evidence: `TEST/findings/l3.md` "P1". [I]
45. **DONE 2026-09-14 — saved phase env accumulates across phases in the vdb `environment`.** P2a's live per-phase bisect split the filed symptoms: `A` no longer reproduced (closed by intervening config work); `RESTRICT="test test"` was `flat_field_on` keeping both tokens of ncurses' use-conditional `RESTRICT` (real `config.py::_flatten` joins a *set*, deduped and sorted) and is fixed at that one shared helper; the stray `declare -- f`/`x=""` came from the source merge never setting `PORTAGE_UPDATE_ENV` before postinst (real `vartree.py:5334`), so the vdb env kept the install-time `build-info/environment` save instead of the filtered postinst regeneration -- `merge_after_install` now passes it plus the `A` value real `doebuild_environment` exports. Evidence: L3 smoke `l3-20260914T192147Z` hard findings 12, **0 environment rows** (was 6), 0 OWNER rows; L1 porttest on the same tree. Detail: `TEST/findings/l3.md` "P2b". [I]
46. **Validate an existing `metadata/md5-cache` entry against the ebuild (#41 C-track follow-up)** — real compares the entry's `_md5_` with the ebuild's own md5 and, on a mismatch, re-parses the ebuild through the `depend` phase and writes the fresh record to `depcachedir` (C0 P8; mtime alone does not invalidate, P7). Portuale reads the cache verbatim, so a stale-in-tree entry (the committed `porttest/docs-1.0` was one, now fixed) yields different metadata than real. This is the validation half C1's "do not change behaviour for repos that have a cache" must grow into; filed separately so C stays the fallback track. Evidence: `TEST/findings/l2.md` "C0". [K]

47. **DONE 2026-09-15 — vdb `metadata`/`BUILD_TIME` producer gap (from P2b).** L3's remaining 12 VDB rows were the per-package `BUILD_TIME` file and the `metadata` consolidated file. Real writes `int(time.time())` into `build-info/BUILD_TIME` in `_post_src_install_write_metadata` (`doebuild.py:2727-2732`, before any other key, unconditionally); the vdb copy and `_consolidate_to_metadata_file` both carry it. `ebuild_phases::write_post_install_metadata` now writes the same file at the top (before its repo-metadata early returns), pinned by the `install_writes_the_real_build_time_file` unit test. Evidence: L3 smoke candidate+control `TEST/logs/l3-20260914T234404Z` is **0 hard / 0 unexplained** (was 12); `TEST/findings/l3.md` "P3". [I]

## Tier 6 — test oracle after removing the Python reference (filed 2026-09-15)

The Python mirror was removed; `second_python_copy_removal.md` "Where each check stands" has the full table. Done on the removal branch: §1/§2 invariants, §3 primitive differential, §4 unparsed-token counter, §8 re-pin checklist, §9 repeated-run determinism, §11 harvested corpus. Open:

48. **L0 run with the new invariant inputs** — `TEST/run/l0-resolver.sh` now records `--json`/`--tree`/`--quiet` per probe and runs `TEST/compare/check-invariants.py` (fails the run on a violation or a non-zero unparsed-dependency-token count). Not yet run in the container; expect a longer L0 (three extra portuale runs per probe; `L0_SKIP_INVARIANTS=1` skips). [I]
49. **Real `emerge` as fixture oracle (§6)** — spiked: real `emerge -p` on a copy of `fixtures/` needs absolute `repos.conf` locations, `PORTAGE_REPOSITORIES`, a clean `/etc/portage` (the host `make.profile` leaked in), and rejects several portuale-only fixture inputs (`${PORTAGE_CONFIGROOT}` in `binrepos.conf`, comments in `profiles/updates`, `*/pkg` in `package.use.force`). Next: a container staging step, then one case. [A]
50. **Bulk translation of upstream resolver tests (§7)** — `3rdparty/portage/lib/portage/tests/resolver/` (106 files) → `fixtures/` trees + expected mergelists; spike one file first (`docs/023-oracle.md` "R2 — genuine upstream oracle" shows `ResolverPlayground` works). [A]
51. **Determinism under shuffled input order (§9, second half)** — a test mode that shuffles `read_dir` results (≈56 sites) and `repos.conf` section order, run over `CASES`. [A]
52. **Metamorphic tests (§5) and mutation testing (§10)** — duplicate IUSE tokens, unrelated packages/overlays, consistent category renames, reordered `package.use`; `cargo-mutants` on `portage-repo` (not installed on the dev host). [A]

---

## Deliberate cuts — do NOT pick these (documented non-goals)

- Backtrack **timing** report line `Dependency resolution took X s (backtrack: N/M)` — non-deterministic, portuale is a deterministic tool. [A]
- `soname` slot-conflict reason key — unreachable: `portage-dep` can't parse soname atoms, dep flattening drops them. [A]
- Third-party mirror list **shuffle** (real's `random.shuffle`; portuale stays deterministic) — every candidate is digest-verified regardless, so shuffle order affects only which mirror is tried first (load balancing), not correctness. Decided 2026-09-14 (D4). [E]
- `--autounmask-write` / any config-writing autounmask mode — "never writes `/etc/portage`". [Part 3]
- PyO3 / in-process FFI; EAPI 0/1/2/3/4/6; `bsd_chflags`; RPM; `emerge --sync`; GLSA/`@security`; `equery`/`portageq`; `clap` for the emerge/ebuild parsers; `os.listdir()`-order directory merge. [Part 3]
- `--metadata` (portuale reads `md5-cache` directly); `xpak` for `mrg`. [Part 3]
- Cycle `--tree` **nesting** — real's `[nomerge]` rows, duplicated nodes and row-counted `Total:` inside a circular-dependency tree. Portuale's tree model dedups by design (Gate G0.2); `large_cycle_count`, elementary-cycle enumeration (`merge_order.rs::elementary_cycles`) and the flat cycle re-display already match real (the `cyc4a` pin). Decided 2026-09-14 (D1, former backlog #21). [A]
