# Backlog tasks

One line per open task. Each has a code/doc pointer so a fresh model can
start without a full context load. Source of truth for detail:
[`scope-backlog.md`](scope-backlog.md) (section letters in brackets),
`what-this-proves.md` (what already shipped), `git log`, and the memory
notes under
`.claude/projects/-home-vivo-repo-PORTUALE-portuale/memory/`.

Rules that apply to every task: ship Rust + `python/emerge_pretend_reference.py`
in one commit, verify byte-identical via `tests/test_emerge_pretend_contract.py`,
run the full verification pass (`AGENTS.md` step 8). Real-execution-only
features have no Python mirror.

---

## Tier 1 — focused slices (scoped, ~one sitting each)

> **Audited 2026-09-13:** commit `67f3b0e` (2026-09-10, "Tier 1 backlog:
> ship 14 slices, re-pin brush, document two verdicts") shipped most of
> this list but never updated this file; the entries below are marked
> accordingly. Detail: `scope-backlog.md` Parts 1–2 and the
> `what-this-proves.md` Tier-1 paragraph.

1. **DONE 2026-09-10 — release-profile `lto`/`codegen-units`.** `rust/Cargo.toml` `[profile.release]` sets `lto = "thin"` + `codegen-units = 1` (15.5→13.2 MB); measured in `performances-tuning.md` §5.
2. **DONE 2026-09-10 — `--solver=` module-doc refresh.** `solver_bridge.rs`'s "Deliberate v1 cuts" list is current (blockers/ABI/merge-order edges/markers/circular deps all named as wired). [J]
3. **DONE 2026-09-10 — `--solver=` forced-flag markers.** The bridge render path calls the walk's own `forced_or_masked_flags` (`solver_bridge.rs:495-512`), so the `( )` wraps render. [J]
4. **DONE 2026-09-10 — `--solver=` notice fields.** `circular_deps` is wired from the bridge result via `find_hard_cycles` (`solver_bridge.rs:719`); `slot_conflicts`/`autounmask_*` stay empty **by construction** (an engine plan picks one version per CPN and no relaxation loop runs) — documented in the module doc, not a to-do. [J]
5. **brush PRs: submit staged fixes** — three fixes staged as `fix/*` branches on `vivo75/brush` + patches in `docs/brush-pr/`; still unmerged upstream. Opening them needs the user's GitHub auth; see `docs/brush-pin.md` "Still to do". [G]
6. **brush re-pin (recurring)** — re-pinned 2026-09-10 to upstream `812336dd` on the thin fork (`67c301a7`, one `invoke_function` API adaptation); the next periodic bump follows `brush-pin.md`'s checklist. Dropping the fork (and revisiting the `--shell` default) waits on #5. New 2026-09-13: a compiled ebuild's `src_compile` no-ops under `--shell brush` (empty image; `porttest/splitdebug` — see `docs/brush-pin.md`, "What is *not* tracked here"). [G]
7. **DONE 2026-09-10 — `FEATURES=compress-build-logs`.** `.gz` log paths + gzip pump + gunzip tails in `emerge_build.rs`. [B]
8. **DONE 2026-09-10 — elog `mail` / `mail_summary`.** Hand-rolled MIME, sendmail-binary/plain-SMTP delivery, per-package + run-wide summary; STARTTLS stays a documented cut. `rust/portuale/src/elog.rs`. [B]
9. **DONE 2026-09-10 — `env.d` eroot path.** `resolve_config` gained the `eroot` param; `env.d` reads `<eroot>/etc/profile.env` (not `config_root`). [C]
10. **DONE 2026-09-10 — `package.env`/`env.d` `${VAR}` expansion.** Per-entry expand map (`expand_package_env_files`), global-map + within-file/cross-file chaining. [C]
11. **DONE 2026-09-10 — `depend`-phase RESTRICT/PROPERTIES reduction.** Reduced on the config-USE set for the `depend` phase. [D]
12. **DONE 2026-09-10 — standalone `package.env`.** `ebuild <file> <phase>` applies per-package `package.env` via the shared md5-cache-identity matcher (`standalone_phase_env_layers_matching_package_env_build_vars`, `rust/portuale/src/ebuild_phases.rs:4761`). [D]
13. **DONE 2026-09-10 — resumed binary from `$PKGDIR`.** Resumed entries always carry `remote_binary: false`, so the binhost fallback can't fire; local `$PKGDIR` only. [B]
14. **`RESTRICT=primaryuri` / fetch order (remaining cuts).** Ordering + `primaryuri` + `force-mirror` shipped 2026-09-10; still cut per the `portage-fetch` module doc: deterministic third-party shuffle, live per-mirror `layout.conf` negotiation, on-filesystem `fsmirrors` copies, multi-URI-per-file interleave. [E]
15. **NOT A GAP — decided 2026-09-10 — `:=` literal-bound atom validation.** No "improper context for slot-operator built atom syntax" masking exists in the vendored checkout, site-packages 3.0.82.2, PyPI 3.0.82.2, or upstream main; all parse `:slot/sub=` and match it structurally, exactly as `portage-dep` already does. See `what-this-proves.md`. [A]
16. **DONE 2026-09-10 — slot-conflict `--color y` marker alignment.** Violated spans wrapped in red with markers placed on the displayed string (`slot_conflict_caret_idx` + `colorize_marked_spans`, `pretend.rs:7212`/`:11139`) — a deliberate divergence from real's pre-color-index marker drift (upstream bug; byte-parity unachievable anyway). [A]

## Tier 2 — larger / needs design first

17. **cluster I merge-order timing (F-B1 drain timing)** — B-series (025 plan, 2026-09-12) landed the trace harness (`TEST/scripts/mo-trace/`) plus two fixes: the installed-slot closure (`by_cp` now keeps every version; `MO_NODES` 398==398 on gtk:4) and bare multi-slot atom resolution in `build_digraph` (firefox #29→#37, thunderbird #30→#38). L0: clean 96→98, parity 0.800→0.817, order 22→20 raw (19 with the B5 gnome-shell explanation). Residue: F-B1 frontier drain timing only — gtk:4 runs 578 iterations vs real's 290 with identical node sets; the first divergence is the iteration-1 greedy batch order (the 14-probe installed-chain lever family). F-B3/F-B4 moved to #25. See `docs/025-tier2-closeout.deepseek.md` §11 + `TEST/findings/l0.md` "## I". [A]
18. **DONE 2026-09-10 — `_FrontierDigraph` perf layer.** `merge_order.rs::SerializeFrontier` shipped (`PORTAGE_SERIALIZE_FRONTIER_DISABLE` fallback, unit-pinned behaviour-neutral; see `scope-backlog.md` and `what-this-proves.md`). Stale entry corrected 2026-09-12 (025 B6). [A]
19. **DONE 2026-09-11 — DFS-partial merge-list truncation.** Slices 1–6 shipped (`4d5bffe` S1, `6b10b16` S2, `ffdd251` S3, `eef8494` S4, `354274e` S5, `e4e83c7` S6): outcome model + `PORTUALE_ABORT_PATH` gate, `abort_outcome` membership/order, exit 1 on masked/unsat/cycle aborts, the partial list + counters + `--json` `aborted`, and the error-block wiring. The three remaining strict xfails are **not** #19 (one `--debug` narration gap, two spec-§4e autounmask findings). Real-tree plasma-meta/podman residue is a `||`-selection gap (spec §4d), tracked separately. Detail: `scope-backlog.md` §A "DFS-partial abort path" entries; oracle `docs/abort-path-spec.md`. [A]
20. **`[use]`-dep unsat diagnostics (surviving residue of #20)** — the masked-dependency disclosure *and* abort for a dependency atom shipped 2026-09-11 with #19 (real's "All ebuilds … have been masked" block + `(dependency required by …)` chain, exit 1; `scope-backlog.md` §A "Masked-dependency abort parity"). What remains: a `[use]`-dep dependency atom with no autounmask flip must print real's `emerge: there are no ebuilds built with USE flags to satisfy "<atom>".` + `!!! One of the following packages is required to complete your request:` block with its `- <cpv>::<repo> (<reason>)` rows; portuale prints the bare `!!! no visible ebuild for dependency` line (exit 1 and Rust==Python already correct, pinned by `test_or_group_use_unsat_alternative_reports_the_dependency_it_enqueued_without_autounmask`, `tests/test_emerge_pretend_contract.py:3789`). [A]
21. **Cycle `--tree` nesting (`[nomerge]` rows, node duplication) — deliberate-cut candidate.** `large_cycle_count`, elementary-cycle enumeration (`merge_order.rs::elementary_cycles` + `_prepare_reduced_merge_list`) and the flat cycle re-display all shipped 2026-09-10/11 (`scope-backlog.md` §A circular-dep entry; the `cyc4a` pin asserts list + error block + lot-of-cycles trailer). Still cut: real's tree *nesting* — `[nomerge]` rows, duplicated nodes, row-counted `Total:` — portuale's tree model dedups by design (Gate G0.2); it belongs in Part 3's deliberate cuts unless the owner re-opens it. Circular-dep code in `portage-repo`. [A]
22. **DONE 2026-09-11 — `dep_zapdeps` finer choice bins.** `all_available`/`all_use_satisfied` split, `unsat_use_*` bins with the bug-515584 unmask gate, `all_installed_slots`, in-bin upgrade-preference ordering (slice 2, `use_reduce_flat_disjunctive`'s `tie_break` + `portage-repo::promote_tied_alternative`), and the `other_*` bins + `allow_masked` two-pass return (slice 4, `portage-repo::atom_matches_installed`) all shipped, both languages, one shared probe + tie-break pair. Formally cut, documented in `disjunction_preference`'s own doc comment (slice 5): `conflict_downgrade`/`installed_downgrade` (needs `downgrade_probe` + a live mutating `graph_db` neither language has — #35, carved out 2026-09-11 per `docs/023-backtracking_resolve.md` B2) and `circular_atom` (needs a `circular_dependency` dict from an earlier real pass, plus `--onlydeps`, neither threaded to this call depth); `minimize_slots` (needs `_overlap_dnf` DNF detection, not done) and `want_update` (needs the `--update`/`--newuse` "wanted" set, pinned `false`) stay cut per slice 2. Real `lib/portage/dep/dep_check.py::dep_zapdeps`. Code: `portage-repo` `disjunction_preference` + `promote_tied_alternative` + `portage-use-reduce` `resolve_disjunctions`; `solver_bridge.rs` is #34, not #22. Design brief: `docs/022-agent-task-22-zapdeps.fable.md`. Detail: `what-this-proves.md`'s 2026-09-11 `||`-resolution paragraphs. [A]
23. **DONE 2026-09-11 — backtracking resolver search.** Portuale's `'backtrack` loop is now real's `_backtrack_depgraph` node-stack search (`Backtracker`: DFS `get`, param-only dedup, bug-375573 `_check_runtime_pkg_mask`, config/slot-conflict/missing-dep `feedback`, `get_best_run`; `--backtrack=N` bounds `mask_steps`, config retries free; dead-end abandonment instead of settling masked graphs into the abort path), fed by a real-shaped `slot_conflict_mask_choices` (692746 append, version-desc, per-candidate `conflict_atoms`, stable len-sort, first-slot-only, one node per choice, similar-grouping) with puller pre-masking removed (parent downgrades arrive via the mask-aware missing-dep probe). Non-goals, explicitly out (see `docs/023-oracle.md` + `what-this-proves.md` 2026-09-11): `--debug` "backtracking due to slot conflict" narration (unpinned, merge-list parity only); "masked by backtracking" listing in `_show_unsatisfied_dep` (no such string; `get_best_run` rationale only); combined config+conflict exploration order (first-hit-wins kept); real's exact stopping rule (`max(1,(N+1)//2)` depth + total-restart cap); #35 downgrade guards (separate item). Follow-ups own the remaining divergences: #25 (btnr installed instances), #24 (a522084 `B-0` rebuild), #36 (mg3-bt1 selection fallback). Plan: `docs/023-backtracking_resolve.md` (+ inventory, oracle); shape: `ResolveCtx`/`BacktrackParams`/`run_pass`/`collect_feedback`/`assemble_result`. [A]
24. **DONE 2026-09-12 — slot-operator rebuild undo path.** Add backtracking for a rebuild that shifts another sub-slot, re-bind the rebuilt consumer's own `:=` deps, and the `--changed-slot` interaction (one missing piece, not three). `slot_operator_rebuild_entries`; citations in `history/scope-backlog-2026-09-05.md`. S1-S6 shipped 2026-09-12 (plan `docs/024-slot-operator-plan.md`, oracle `docs/024-oracle.md`): S1 complete-mode gate under `--deep` (the a522084 `B-0` miss was the missing auto-enabled pass, not the undo path); S2 22 oracle pins over the whole `test_slot_operator_*` family; S3 the rebuild is now a **walked graph node** -- `slot_operator_rebuild_scan` feeds `BacktrackParams::slot_operator_replace_installed` through `collect_feedback`, the next pass seeds it like real's `@__auto_slot_operator_replace_installed__` `SetArg` and flips the outcome in-walk, so deps, `:=` re-bind and merge order are real (`[A-2, B-0, C-0]`, bug 522652); no synthesised entries left on the default solver; S4 `_eliminate_rebuilds` -- the nine ordered rules + the graph-aware `:=` binder (`bind_slot_operator_deps`, real `_eval_deps` over `_graph_trees`), demotion via `slot_operator_undone` latch, `slotundo-unnecessary` now MATCHes (`[U] souprov`, `abi_rebuilds: []`); S5 `_slot_change_probe` -- bug 456208's slot-move-without-revbump now schedules the installed child (`slotchange-1`/`regslotchange` MATCH, `[rR]` child + plain parent, no block), real's rule 3 landed behaviour-neutrally, and two S2 expectations were corrected live (the `--changed-slot` flag half has no `r`/edge in real; conflict-mass is v2 `#24b`'s update probe, not the probe); S6 L0: 120 probes, 96 clean, parity 0.800, `portuale/` raw outputs byte-identical to the S0/S1 archives (the slot-op path does not fire on the corpus) -- no regressions, no findings to adjudicate (`TEST/findings/l0.md`). S7: final verdict table (`docs/024-oracle.md`), `what-this-proves.md` S1/S2/S5/S6 paragraphs, scope-backlog/agent-context. S4 finding: bug 614390's `complete` case is blocked by a **selection** gap (named bare `socc` resolves before meta's `=socc-1` through the already-installed fast path, which skips `resolved_slots`; real's `_add_pkg` slot-parent check catches it) -- #36-overlap, not the undo; stays strict-xfail. **Non-goals, stated:** the v2 probe family `#24b` (update probe + `check_reverse_dependencies`), `#24c` (`slot_operator_mask_built`; every binary half), `#24d` (`prune_rebuilds`), `#24e` (`_slot_conflict_backtrack_abi`), `#24f` (in-walk unsatisfied probe); `IUSE_EFFECTIVE` in the built-dep domain; `--rebuild-if-*` through the same path (G0.6); the `--debug` "backtracking due to missed slot abi update" narration and the `Dependency resolution took…` line (G0.7, cut). [A]
25. **`_complete_graph` installed nomerge nodes** — real carries every `@world`/`@system`-reachable installed package as a graph node; portuale keeps only their reverse-dep atoms. **Now owns the B4 superseded-in-edge model too** (`docs/025-tier2-closeout.deepseek.md` §11 F-B4, 2026-09-12): real drops an installed node's in-edges when a same-slot merge supersedes it, and draws no edge for a dep initially satisfied by installed (`nghttp2 -> systemd` gone; gedit #5/nautilus #8). Walk-order dependent; needs the resolver's insertion order, so it also subsumes F-B3's `_create_graph` pre-bias order work. [A]
26. **DONE-PARTIAL 2026-09-12 — installed-package metadata overlay (`FakeVartree._apply_dynamic_deps`).** The old "walk source doesn't switch" line was false (verified live). A1-A4 landed: `InstalledMetaLayer` + `installed_dep_string` (Raw/Effective, per-pass memo, `--ignore-built-slot-operator-deps` gate), `enqueue_dependencies`/scheduler closure/`GraphEntry::deps` read the same view, installed-parent `slot_pullers` recorded. The built-`:=` append itself stays behind `PORTUALE_DYNAMIC_DEPS_APPEND` (default off) because of **F-A1**: with it on, the Python mirror folds a solvable slot conflict real resolves via the slot-op probe (slotchange-case4), and the cascade probe drops a consumer. Residue in `docs/025-tier2-closeout.deepseek.md` §11 F-A1; detail plan `docs/024-dynamic-deps_n_disagreement.deepseek.md`. [A]
27. **DONE-PARTIAL 2026-09-12 — `need_rebuild` slot-conflict trailer.** Renderer + installed-parent `slot_pullers` recording (025 A4) + a Rust-pinned fixture for the `--exclude` and `--useoldpkg-atoms` reasons, under the A1 append gate; gate-off negative control pinned. Residue: the `--usepkgonly` silence arm and the "ebuild is masked or unavailable" reason (needs the `_DynamicDepsNotApplicable` missing-ebuild fallback), plus Rust==Python parity that waits on F-A1. `docs/025-tier2-closeout.deepseek.md` §11 F-A2. [A]
28. **mrg-director: wire the remaining scaffolded slots** — after Tier 2.28 (`261e184`, re-verified 2026-09-13), 3/8 slots carry production traffic: `SchedulerPolicy` (`run_build_scheduler`), `MergeEngine` (merge dispatch, `SourceEngine`/`BinaryEngine`), `NewsSelector` (`--check-news` via `FilesystemNews`). `Fetcher` shares `portage_fetch::download_via_wget` with `portuale::fetch`, but the trait is not dispatched on the production path; `PackagesDb` (`VdbReader`), `RepoCache`, `BinpkgIndex` are still conformance markers — `VdbReader`/`WgetFetcher`/`PkgdirBinIndex` are referenced only inside `rust/mrg-director/src/lib.rs` (+ its tests), and `installed_contents_files`/`installed_reverse_dependents` are called only by `VdbReader`. `Director` itself is test-only; the decomposition hasn't started. `rust/mrg-director/src/lib.rs`. [H]
35. **`downgrade_probe` + live `graph_db` for `dep_zapdeps`** — carved out of #22/#23 (2026-09-11, plan `docs/023-backtracking_resolve.md` B2): real's `conflict_downgrade`/`installed_downgrade` guards (`lib/portage/dep/dep_check.py` soft 476-521, bug 531656) demote a `||` alternative when the slot conflict it would cause is solvable by downgrade. Needs a live, mutating slot-conflict `graph_db` + `downgrade_probe` neither language models; #23's closure is not blocked on it. [A]
36. **Mask-aware candidate fallback in selection** — filed from #23's C4 (2026-09-11, `docs/023-oracle.md` case mg3): real's `_select_pkg_highest_available` tries versions highest-first with `dep_check` (masks applied), so a masked-dep version is skipped *within selection* and a lower version is picked on the same pass. Portuale selects the highest *visible* version and reports the masked dep as NVC, reaching the downgrade only via a second missing-dep mask step on the next node. Same fixpoint at the default budget (proven: `btparent`, `mgf`/`mgfa` settle identically), but tight `--backtrack` budgets exhaust where real settles (`mgfa --backtrack=1` reports, real merges). Needs selection-time satisfiability probing, a forward-pass feature outside the backtrack loop's scope. [A]

## Tier 3 — container test bed (L2–L5)

29. **DONE-PARTIAL 2026-09-13 — L2, portuale as builder.** The bed is shipped: `TEST/run/l2-portuale-builder.sh` + `layers/l2/*` + `compare/gpkg-{structure,diff}.sh` + `diff.py --layer l2/--tolerate-payload`; the porttest fixture track is green modulo the producer gaps filed as #37-#40, and no bed work remains — the real set's build-env gap (#37, fixed 2026-09-13) is behind it; the next blockers are #38/#39. Findings: `TEST/findings/l2.md`; plan: `docs/029_portuale-as-builder.deepseek.md`. [I, K]
30. **L3 — source-build parity** — both PMs build `@system` / a desktop `@world` from source with `SOURCE_DATE_EPOCH` + `-j1`; diff VDB metadata + CONTENTS structure. `TEST/`. [I]
31. **L4 — `mrg` remote merge over SSH** — differential test of the remote binary-merge path; design in `remote-merge.md` §6. [I]
32. **L5 — lifecycle & fault injection** — `-C` / `--depclean` diffs, soname bump → preserved-libs, `CONFIG_PROTECT`, `--resume` after SIGKILL, disk-full / corrupt-archive / binhost-500. `TEST/`. [I]

## Tier 4 — `--solver=` real-tree correctness (pubgrub / resolvo)

33. **`--solver=resolvo` cycle linearization** — `install_order` can't order any closure with a toolchain cycle (glibc↔gcc↔perl); `solver_bridge.rs` prints raw ids. Non-functional on real targets. `solver-backends-analysis.md`. [J]
34. **`--solver=pubgrub` over-merge** — feeds pubgrub the over-approximated `flag?()` reachability closure as the real graph (`nodejs`: 48 vs 8). Feed the resolved graph instead. `solver_bridge.rs`. [J]

## Tier 5 — L2 producer parity (filed 2026-09-13, blocks #29's real half)

37. **DONE 2026-09-13 — build phase env completeness.** Source builds now run with the resolved config env (`portage_profile::phase_environ`: implicit USE, resolved `FEATURES`/`PORTAGE_FEATURES`, `MULTILIB_ABIS`/`DEFAULT_ABI`/`LIBDIR_*`, `SLOT`, `PORTAGE_REPO_NAME`/`REPO_REVISIONS`, `SOURCE_DATE_EPOCH`) on `emerge`/`--resume`/`--buildpkgonly`; the Rust execution gates read the resolved `FEATURES`; brush exports are shell-quoted. Porttest track green from a clean run, `l2-bpkgonly-env` allowlist rows deleted, oniguruma → `/usr/lib64`, `--buildpkgonly` ≡ `-b` archives (`gpkg-diff` strict hard=0). Plan: `docs/037_Build-phase-env-completeness.plan.md`; evidence: `TEST/findings/l2.md` S4/S5. [K]
38. **IN PROGRESS (S0-S4 done 2026-09-13) — Packaging transforms: dostrip/splitdebug/docompress** — portuale archives ship unstripped binaries with no `/usr/lib/debug` and uncompressed docs (`BIG.txt` vs `BIG.txt.bz2`); real runs `dostrip`/`estrip` + `ecompress` at package time. Findings: `l2-gpkg-dostrip-splitdebug`, `l2-gpkg-docompress`. [K]
39. **gpkg metadata completeness** — `SIZE`/`IUSE`/`IUSE_EFFECTIVE`/`repository`/`REPO_REVISIONS`, ebuild-derived `RDEPEND`/`REQUIRES`/`PROVIDES`, `Packages` index `REPO`, and `NEEDED.ELF.2`'s trailing ELF-class field. Findings: `l2-gpkg-metadata-members`, `l2-needed-elf2-format`. [K]
40. **Consumer-side env of portuale-built archives** — real Portage merging a portuale archive runs merge phases with `MERGE_TYPE` unset and skips `pkg_pretend`. Finding: `l2-consumes-portuale-env-degraded`. [K]
41. **Ebuild fallback for a cache-less repo** — real parses ebuilds when `metadata/md5-cache` is absent; portuale reports "no ebuilds to satisfy". Finding: `l2-no-md5-cache-ebuild-fallback`. [K]
42. **`emerge` never pre-cleans `${PORTAGE_BUILDDIR}`** — real `EbuildBuild._start_pre_clean` runs the `clean` phase before every build (and after merge unless `noclean`); portuale reuses a dirty builddir, so phase markers (`.installed`) skip the rebuild and an already-`instprep`ped image is re-packaged (a `-B` after a source merge ships stripped binaries; the `-K` merge's `estrip` then dies). Found in #38 S4 (`TEST/findings/l2.md` "#38 S4"). [K]

---

## Deliberate cuts — do NOT pick these (documented non-goals)

- Backtrack **timing** report line `Dependency resolution took X s (backtrack: N/M)` — non-deterministic, portuale is a deterministic tool. [A]
- `soname` slot-conflict reason key — unreachable: `portage-dep` can't parse soname atoms, dep flattening drops them. [A]
- `--autounmask-write` / any config-writing autounmask mode — "never writes `/etc/portage`". [Part 3]
- PyO3 / in-process FFI; EAPI 0/1/2/3/4/6; `bsd_chflags`; RPM; `emerge --sync`; GLSA/`@security`; `equery`/`portageq`; `clap` for the emerge/ebuild parsers; `os.listdir()`-order directory merge. [Part 3]
- `--metadata` (portuale reads `md5-cache` directly); `xpak` for `mrg`. [Part 3]
