[workspace]
resolver = "2"
members = [
    "portage-versions",
    "portage-dep",
    "portage-repo",
    "versions-harness",
    "atom-harness",
    "use-reduce-harness",
    "multicall",
]

[workspace.package]
edition = "2021"

[profile.release]
panic = "abort"
