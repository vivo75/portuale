EAPI=8
DESCRIPTION="fixture: layout.conf masters= (middle tier) + repo-name= override -- exists only in this overlay, masked only by its layout.conf-declared master overlay"
SLOT="0"
KEYWORDS="amd64"
