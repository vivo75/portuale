# fixture binrepos.conf for --getbinpkg/--getbinpkgonly (real
# lib/portage/binrepo/config.py). A single binhost whose "Packages"
# index lives at <sync-uri>/Packages. `file://` + an absolute path
# (matching the committed make.conf's own absolute PKGDIR -- this
# fixture tree is not relocatable) so the index resolves without any
# network access; --pretend never downloads anyway.
[testbinhost]
sync-uri = file:///home/vivo/repo/portage/PORTING/fixtures/binhost
priority = 1
