DEFINED_PHASES=-
DESCRIPTION=fixture: bug-515584 masked demotion -- unsatusemaskedflag is masked (see profiles/package.use.mask), so a [use]-dep on it can never be fixed by autounmask and the whole || alternative falls to other (never selected)
EAPI=8
IUSE=unsatusemaskedflag
KEYWORDS=amd64
SLOT=0
_md5_=d6c23dfed8918337a3a4a5729986bd99
