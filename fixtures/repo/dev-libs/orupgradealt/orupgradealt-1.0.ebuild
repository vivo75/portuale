EAPI=8
DESCRIPTION="fixture: slice 2 in-bin ordering -- installed in slot 1, older version; RDEPENDs a marker so a --deep walk reveals which slot the || group chose"
SLOT="1"
KEYWORDS="amd64"
RDEPEND="dev-libs/orupgrademarker1"
