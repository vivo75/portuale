EAPI=8
DESCRIPTION="fixture: USE_EXPAND_HIDDEN -- cpu_flags_x86_* flags are real (sse2 enabled) but emerge -pv omits the CPU_FLAGS_X86 group from its display"
SLOT="0"
KEYWORDS="amd64"
IUSE="cpu_flags_x86_sse2 cpu_flags_x86_avx"
