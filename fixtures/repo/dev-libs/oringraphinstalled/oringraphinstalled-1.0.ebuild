EAPI=8
DESCRIPTION="fixture: slice 2 in-bin ordering -- installed, but never pulled into the graph any other way; RDEPENDs a marker so a --deep walk reveals if the || group wrongly chose this (losing) alternative"
SLOT="0"
KEYWORDS="amd64"
RDEPEND="dev-libs/oringraphinstalledmarker"
