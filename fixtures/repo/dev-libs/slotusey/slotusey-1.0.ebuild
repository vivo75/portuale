EAPI=8
DESCRIPTION="fixture: pulls >=dev-libs/slotusetarget-1.0[y] -- 2.0 does not declare y at all, so reuse fails with a missing-IUSE (unconditional) use key, rendered before the violated one"
SLOT="0"
KEYWORDS="amd64"
RDEPEND=">=dev-libs/slotusetarget-1.0[y]"
