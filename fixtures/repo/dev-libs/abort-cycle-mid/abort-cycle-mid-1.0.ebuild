EAPI=8
DESCRIPTION="fixture package: unserializable cycle between two leaves, so the DFS-partial list is distinguishable from the full list"
SLOT="0"
KEYWORDS="amd64"
RDEPEND="dev-libs/abort-leaf-a dev-libs/abort-cycle-a dev-libs/abort-leaf-b"
