EAPI=8
DESCRIPTION="fixture package: installed with a vdb repository file matching the repo that currently provides this version -- --newrepo's own reinstall trigger must NOT fire"
SLOT="0"
KEYWORDS="amd64"
