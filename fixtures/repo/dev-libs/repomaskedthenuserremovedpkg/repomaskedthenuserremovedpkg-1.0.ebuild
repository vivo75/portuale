EAPI=8
DESCRIPTION="fixture package: masked by repo-level profiles/package.mask, the mask atom itself removed via a -atom line in the user-level package.mask (cross-source -atom removal, not just within one file)"
SLOT="0"
KEYWORDS="amd64"
