EAPI=8
DESCRIPTION="fixture: shared target of unsatuseorder's || group, one flag off-by-default (unsatuseorflag) and one on-by-default (unsatuseotherflag)"
SLOT="0"
KEYWORDS="amd64"
IUSE="unsatuseorflag +unsatuseotherflag"
