EAPI=8
DESCRIPTION="fixture package: REQUIRED_USE 'foo? ( bar )' genuinely satisfied -- foo enabled globally, bar forced on by this package's own package.use entry"
SLOT="0"
KEYWORDS="amd64"
IUSE="foo bar"
REQUIRED_USE="foo? ( bar )"
