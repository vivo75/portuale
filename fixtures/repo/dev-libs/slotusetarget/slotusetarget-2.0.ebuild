EAPI=8
DESCRIPTION="fixture: slot 0, newer version with x default-off and y gone from IUSE -- resolved first, then USE-mismatching parents pull 1.0 alongside (real re-verifies USE on slot reuse)"
SLOT="0"
KEYWORDS="amd64"
IUSE="-x"
