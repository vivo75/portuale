EAPI=8
DESCRIPTION="fixture package: older version RDEPENDs bare on dev-libs/bttarget (loose); the newer -2.0 pins >=2.0. Backtracking masks -2.0 so this looser -1.0 is chosen and the slot conflict clears."
SLOT="0"
KEYWORDS="amd64"
RDEPEND="dev-libs/bttarget"
