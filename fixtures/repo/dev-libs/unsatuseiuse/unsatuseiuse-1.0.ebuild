EAPI=8
DESCRIPTION="fixture package: RDEPENDs an atom whose required flag is not in the target's IUSE at all -- real _show_unsatisfied_dep's 'Missing IUSE:' reason kind (backlog #20)"
SLOT="0"
KEYWORDS="amd64"
RDEPEND="dev-libs/unsatuseiusetarget[noiuse]"
