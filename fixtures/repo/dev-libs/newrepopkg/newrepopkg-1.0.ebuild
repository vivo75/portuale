EAPI=8
DESCRIPTION="fixture package: installed with a vdb repository file recording a different repo than the one currently providing this version -- --newrepo's own reinstall trigger"
SLOT="0"
KEYWORDS="amd64"
