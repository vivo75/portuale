EAPI=8
DESCRIPTION="fixture: --root-deps recursion -- a running-root build tool whose own IDEPEND must be walked against the running root (real IDEPEND always targets _running_root.root)"
SLOT="0"
KEYWORDS="amd64"
IDEPEND="dev-libs/rdrilib"
