EAPI=8
DESCRIPTION="fixture: backlog #22 slice 4 other_installed_any_slot bin (bug 522652) -- installed at version 1.0, but ofuzzy's || group names =dev-libs/ofuzzyinstalled-2.0 specifically, a version that exists nowhere (not the tree, not the vdb); only the bare cp is installed"
SLOT="0"
KEYWORDS="amd64"
