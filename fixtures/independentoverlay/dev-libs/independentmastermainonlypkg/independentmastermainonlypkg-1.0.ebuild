EAPI=8
DESCRIPTION="fixture package: exists only in independentoverlay, which explicitly declares masters = overlay (not the main repo) -- masked in the main repo's own package.mask, which must NOT apply here since main isn't a declared master"
SLOT="0"
KEYWORDS="amd64"
