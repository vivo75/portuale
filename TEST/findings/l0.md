# L0 findings — resolver parity backlog

Triage of `TEST/run/l0-resolver.sh` output. Each cluster is one suspected
root cause covering many probes. **None are allowlisted** — they are
portuale bugs or portuale backlog, to be fixed on `main` (not in the
`real-world-testing` branch). `known-divergences.yaml` stays empty until
a divergence is adjudicated *not* a bug.

Baseline run (2026-09-08): pinned `gentoo` @ `11c58b7a`, `buildovl` @
`3b1df681`, profile `default/linux/amd64/23.0/systemd`, portage
`3.0.82.2`, portuale `main`. **120 probes, 69 clean, parity 0.575.**
By probes-touched: order 25, exit 17, use 16, totals 11, advice 10,
missing 10, error 7, extra 6.

Update this file as clusters are fixed (strike them, note the commit).

**Fixed on `main`:** **A** (autounmask exit code, `c883662`), **B**
(`use.mask` interleave, `c883662`), **H** (`@world` ⊉ `@system`,
`72ea263`), **D** (`||` circular self-branch, `b5b256f`), **L**
(installed dep lacks `[flag]` → reinstall/autounmask, `e17c6e9` + a
vdb-USE follow-up; also closes **E** and **G**). Re-run after H+D: 73
clean / 0.608 (was 69 / 0.575); L closes several more.
Also **M** (firefox/thunderbird "version selection" — was a
`resolve-compare.py` multi-slot keying artifact, not a resolver bug;
comparator now slot-aware) and **J** (both probes: J-neovim = per-level
profile USE_EXPAND make.defaults fold in `portage-profile::
resolve_config`; J-libsdl2 = REQUIRED_USE error *rendering*, real's
`_show_unsatisfied_dep` block — neither was the autounmask gap the
finding assumed). Then **A 2nd half** — "backtracking has terminated
early" notice now emitted (dual-lang), and the comparator collapses the
autounmask merge-list *truncation* (plasma-meta/podman: real DFS-dies
at a masked node → partial list, portuale BFS keeps going) to one
`truncated` finding instead of ~500 `extra`. L0 UNEXPLAINED ~682→~161,
`extra` 591→~72. Then **N** — `MULTI_deep-update-world` `-uvD @world`
57→7 pkgs: `enqueue_dependencies` (the `--deep` recursion into an
installed package) evaluated its `flag? ( dep )` conditionals against
the *effective profile* USE instead of the package's recorded vdb USE
(real `_pkg_use_enabled` for a `built` pkg), so e.g. installed
`app-text/xmlto` (built `-text`) had its current `text? ( || (
virtual/w3m … ) )` walked and dragged in w3m + ~40 of its deps. Fixed
dual-lang; fixture `dev-libs/deepvdbuse{pkg,target,consumer}`.
Then **O** — `[blocks B]` soft-blocker noise (~18 probes):
`resolve_blockers` matched a blocker atom against candidates by
cpv/slot (`match_from_list`) but never checked its own `[use]` deps, so
`media-libs/mesa[-libglvnd(+)]` (mesa dropped the flag → `(+)` stands
in as enabled → `-libglvnd` unmet), `sys-apps/shadow[su]` (shadow built
`-su`), `glibc[crypt(-)]`, `coreutils[kill]` etc all fired spuriously.
Now evaluates them (`use_deps_satisfied`, `(+)`/`(-)` included) against
the blocked pkg's vdb / graphed USE and drops the satisfied no-op —
real's genuine `[blocks B]` (systemd-utils, no `[use]` deps) kept.
Fixture `dev-libs/blockusedep{consumer,target}`.
Remaining: merge order (I, ~28 — `_serialize_tasks` stuck-timing
sub-project), the *resolver* truncation itself (autounmask +
unresolvable circular dep — 2 probes; needs the backtracker's
`get_best_run` DFS-partial, deferred; see A), residual autounmask
`advice`/`error` (gcr[gtk]/xwayland[libei]/libcanberra "masked" vs
use-change — all `real-only`, downstream of the deferred truncation).

**`(N unsatisfied)` blocker classification — FIXED (2026-09-09).**
`BlockerConflict.unsolvable`: a blocked *installed* package that other
installed packages still `RDEPEND` (`installed_has_foreign_dependents`,
a direct vdb reverse scan — no complete graph) can't be unmerged, so
real `_serialize_tasks`' `unresolved_blocks` fires. Portuale now emits
`Conflict: N blocks (M unsatisfied)`, the `* Error: The above package
list …` block + handbook link, and exits 1 (the per-parent `pulled in
by` detail is a documented cut — needs real `_parent_atoms`).
`sys-apps/systemd-utils` `[exit]`×2 → 0. Fixture blocker tests
(`dev-libs/blockerpkg`/`blockerorderpkg`, `!!dev-libs/samepkg` which is
installed + RDEPENDed by 4 other installed fixtures) flipped rc 0→1 to
match real. `resolve-compare.py` now excludes `[blocks …]` lines from
the *ordered* comparison — real prints them in `_blocker_parents`
(digraph) iteration order, PYTHONHASHSEED-randomised. Dual-language.

---

## A. Autounmask required → exit code + merge-list disclosure  ·  HIGH · ~15 probes  ·  **exit code `c883662`; "terminated early" notice + comparator done**

**Exit-code half fixed** (`c883662`): real `action_build` returns 1
whenever `_have_autounmask_changes()` — before any merge, `--pretend`
included. portuale now exits 1 when any `autounmask_{keyword,mask,use,
license}_changes` is non-empty, except `--autounmask-only` / real
non-pretend `--autounmask-continue`.

**"backtracking has terminated early" notice** now emitted (dual-lang) —
real `_display_autounmask`'s tail, gated on
`_autounmask_backtrack_disabled` (autounmask changes + `--autounmask-
backtrack` off, the default; `--pretend` included). Cleared the
`[error] real-only message: * In order to avoid wasting time…` findings.
12 pinned autounmask contract tests re-pinned (`BACKTRACK_TERMINATED_
EARLY` constant).

**Merge-list-truncation half.** The *comparator* now detects real's
"terminated early" marker + a >2× Total gap and **suppresses the
merge-list set/order/totals comparison** (truncated-vs-complete is
noise, same as the `rrc != prc` bail), emitting one `truncated` finding
instead of ~500 `extra`. plasma-meta 484→1, podman 35→1: L0 `extra`
591→~72, UNEXPLAINED 682→~161.

**Trigger re-diagnosed 2026-09-09 (corrects the paragraph below).**
Full read of `_backtrack_depgraph` + `need_config_change`
(`docs/circular-dep-backtracking-plan.md`, branch
`feat/circular-dep-backtracking`): the truncation is **not** driven by
`get_best_run` or a circular-dep gate. `_create_graph` returns 0 the
instant a `want_restart_for_use_change` autounmask flip lands
(`_resolve:5676`); `_backtrack_depgraph` then breaks at `backtracked ==
0` via `need_config_change()` (autounmask changes +
`--autounmask-backtrack != y`), so the `get_best_run` re-run is skipped
and the **pass-1 partial digraph** — the DFS-discovery prefix up to the
restart node — is displayed. The `go` cycle is why real can't *complete*
later, not what truncates. Reproducing the prefix still needs the DFS
resolution walk (Step 1: 38 order-pinned contract regressions) or an
approximate prefix (verdict below: worse than status quo). Branch parked
`bb9e147`, not merged.

**Exact trigger (diagnosed 2026-09-09, resolver fix deferred).** Across
all 12 autounmask probes: `real Total == portuale Total` for every one
that has autounmask changes **and no circular dependency** (firefox 156,
gedit 142, gnome-shell 453, …). Real truncates **only** when autounmask
changes coincide with an **unresolvable circular dependency** — exactly
`podman` (`go` self-cycle → 8) and `plasma-meta` (`go` → 4). Mechanism:
`select_files` sets `_success_without_autounmask` (→ `need_config_change`
→ `backtrack_depgraph` breaks, full list shown) **only when autounmask
is the sole problem** (`depgraph.py` ~5628). A circular dep is an "other
problem" → flag stays false → the backtracker exhausts its retries →
`get_best_run()` returns the DFS-deepest *partial* graph = the top-level
atom + the first fully-resolved subtree(s), stopping at the first
`want_restart_for_use_change` autounmask node.

Real's partial set = {cycle nodes} ∪ {DFS-visited-before-the-first-
autounmask-restart-node}. For plasma-meta that is exactly the reverse-
reachability closure of `go` (go←gocryptfs←plasma-vault←plasma-meta);
for podman it is that closure **plus** `conmon` (a C sibling DFS-visited
before `iptables[nftables]`). Reproducing it faithfully needs
DFS-ordered graph construction + the backtracker's `get_best_run`
(cluster I / deferred backtracking work). An *approximate* BFS-prefix
truncation is worse than the status quo: it would bring portuale's
Total close to real's, defeating the comparator's size-gap suppression
and fanning back out to per-package `missing`/`extra` findings.

**Autounmask USE-change line rendering (2026-09-09).** Real
`_display_autounmask` emits **one line per package** (`>=<cpv> flag
-flag …`, every flipped flag of that pkg joined), in Python `set`
iteration order -- **PYTHONHASHSEED-randomised** (3 `emerge -pv gimp`
runs → `cairo lcms` / `cairo lcms` / `lcms cairo`). Portuale emitted one
line per flip site (`>=dev-qt/qtbase-6.11.1 cups` + `… vulkan`). Fixed:
resolver coalesces `autounmask_use_changes` by `atom` into one line
(dual-language); `resolve-compare.py` canonicalises a `use-change` line
to `<atom> <sorted flags>` so the flag *set* is compared, not its order
(same PYTHONHASHSEED treatment merge-order pairs get). Cleared the
`qtbase cups vulkan` (plasma-meta) + `gegl lcms cairo` (gimp)
real-only/portuale-only `advice` pairs. **advice 19→16.**

Original notes:


`podman`, `plasma-meta`, `gtk:4`, `gtk+:3`, `gnome-shell`, `nautilus`,
`gedit`, `firefox`, `thunderbird`, `libreoffice`, `gimp`, `nginx`,
`wireshark`, `i3`, `systemd-utils`, `MULTI_emptytree-system`.

Real portage, when autounmask changes are needed: prints the changes,
prints `* … backtracking has terminated early due to the above
autounmask change(s)`, **withholds the full merge list**, exits **1**.
Portuale: applies the changes, prints the **complete** merge list, exits
**0**.

A script that checks `$?` after `emerge -p` would treat "you must edit
package.use first" as success. Portuale should exit non-zero when
autounmask changes are required and neither `--autounmask-write` nor
`--autounmask-continue` is given, and match real's decision to withhold
the merge list.

Next: compare against real `_emerge/depgraph.py` `_display_autounmask`
and the `need_config_change` / backtracking-terminated path; check
portuale's autounmask-in-loop exit-code plumbing (memory:
`autounmask-in-loop-complete`).

## B. Spurious `use.force`/`use.mask` parens  ·  HIGH · ~13 probes  ·  **FIXED — `c883662`**

Root cause: real `UseManager.getUseMask`/`getUseForce` stack the four
sources (`use.mask`, `use.stable.mask`, `package.use.mask`,
`package.use.stable.mask`) **interleaved per profile level**, one
incremental `stack_lists`. `base/package.use.stable.mask` masks
`sysprof` for `dev-libs/glib`; `arch/amd64/use.mask` (a *later* chain
level) un-masks it globally with `-sysprof`. portuale flat-unioned the
four sources, so the per-package entry re-added `sysprof`
unconditionally → `(-sysprof)`.

Fixed: `portage_profile::Config::use_mask_force_levels` (per-chain-level
+ synthetic repo-level [0]); `portage_repo::resolved_use_mask_or_force`
replays the interleave; `effective_use_flags` +
`forced_or_masked_flags` use it. Fixture `dev-libs/crossmaskcancelpkg`,
dual-language, `test_later_level_global_use_mask_cancels_earlier_
package_use_stable_mask`.

## C. `app-crypt/gcr[gtk]` not mask-enforced  ·  ~~HIGH · 5 probes~~  ·  **partly cluster R (kdecore-meta), rest downstream of A**

`gnome-shell`, `nautilus`, `gedit`, `plasma-meta`, ~~`kdecore-meta`~~.
**`kdecore-meta`'s `gcr[gtk]` + `freetype[harfbuzz]` autounmask was
cluster R** (`||` in-graph preference) — `gcr[gtk]` there came only via
`gnome-keyring`, wrongly picked for `virtual/secret-service`; now
`[order]`-only. The other four pull `gcr[gtk]` by a genuine direct dep
and are still downstream of A/C.

Real: `- app-crypt/gcr-3.41.2-r2::gentoo (masked by: )` — **empty
reason.** No `package.mask`, no `use.mask` on `gtk` for gcr on amd64
(checked via `portageq`/`UseManager`). `gcr[gtk]` → `>=x11-libs/gtk+-3.22:3[…]`,
and `x11-libs/gtk+:3` is itself one of the autounmask-needing packages.
Real, with `--autounmask-backtrack=n` (default), stops after the first
autounmask batch and reports the still-unresolved `gcr[gtk]` subtree as
"masked", exiting 1. Portuale keeps re-resolving with changes applied
and reaches a full graph + rc 0.

**Fixing A's exit code narrows the gap; the remaining divergence
(portuale doesn't truncate the merge list / doesn't stop backtracking
early) is the merge-list-disclosure half of A — deferred, needs
`need_config_change()` early-termination.**

## D. `dev-lang/go-bootstrap` not pulled  ·  MEDIUM · 2 probes · **FIXED**

`dev-lang/go` (Total 1 vs real 2), `runc` (Total 3 vs 4).

go's `BDEPEND` is `|| ( >=dev-lang/go-<v> dev-lang/go-bootstrap )` with
nothing installed. The first branch's only tree match is `dev-lang/go`
itself (circular self-dep), so real `dep_zapdeps` falls through to the
`go-bootstrap` branch; portuale flat-ranked both as `Available` and took
the first, which then resolved to the package being built (self) → the
`||` contributed nothing.

**Fixed:** the `_disj_pref` / disjunctive `alternative_satisfiable`
closure (both the main `resolve_pretend_graph` walk and the
`_enqueue_dependencies` `--deep` recursion, dual-language) now treats an
atom whose `cat/pkg` is the entry's own and which nothing installed
satisfies as unavailable — so a self-only branch can't win. Fixture
`dev-lang/selfbootpkg` + 2 contract CASES. Verified in the L0 container:
`dev-lang/go` → Total 2, `runc` → Total 4 (both match real).

## L. installed dep lacks a `[flag]` use-dep → not autounmasked/reinstalled  ·  HIGH · many probes · **FIXED**

Root cause shared by the `nginx` exit finding, most of E, all of G, and
part of F/`kdecore-meta`/`gnome-shell`. A dependency atom `foo[bar]`
where `foo` is **installed without `bar`**: real portage reinstalls
`foo` with `bar` and, since `bar` needs `package.use`, autounmasks it
(`[ebuild R] … bar*`, exit 1, the `nghttp2`/… that `bar` pulls appear).
portuale treated the installed `foo` as satisfying `foo[bar]` and
returned `AlreadyInstalled` → exit 0, subtree missing.

**Fixed** (`e17c6e9` + follow-up): `resolve_pretend`'s
`candidate_is_installed(best)` branch forces a `Reinstall` (not
`AlreadyInstalled`) when the atom's `[flag]` use-deps aren't satisfied
by the installed **vdb-recorded** USE -- so the graph layer's own
child-flip block routes the entry, records an autounmask change *only*
when the resolved candidate's *effective* USE also can't satisfy it
(`mime-types[nginx]` -> rc 1), and otherwise just reinstalls
(`systemd[policykit]` -- `policykit` is a global flag `systemd`'s ebuild
now defaults on -> `[ebuild R] … policykit*`, rc 0). The first cut
checked the effective USE, which wrongly suppressed the reinstall for
those global-flag cases. Dual-language, fixture
`dev-libs/flipinstconsumer` + 2 contract CASES. Verified in the L0
container: `www-servers/nginx` `rc 0->1`; `sys-auth/polkit` /
`dev-lang/rust` (E) / `net-misc/networkmanager` (G) all now match real
exactly (24/15/67 packages).

The `:=` slot-op turned out not to strip the `[flag]`
(`net-misc/curl:=[http2,ssl]` parses with `use_deps` intact), and the
already-resolved-`other_outcomes` case did not actually apply here --
`curl`/`systemd` are reached first by their own `[flag]` atom.

## E. `dev-lang/rust` drops the curl/HTTP stack  ·  MEDIUM · 1 probe · **FIXED** (via L)

Total 11 vs real 15 — portuale omitted `net-misc/curl`, `net-libs/nghttp2`,
`nghttp3`, `ngtcp2`. rust DEPENDs `net-misc/curl:=[http2,ssl]`; installed
curl lacks `http2`. Fixed by L's vdb-USE reinstall check: `dev-lang/rust`
now resolves to the same 15 packages as real.

## F. Doc-toolchain chain dropped (`w3m` / `xmlto` / `boehm-gc` / `libatomic_ops`)  ·  MEDIUM · 3 probes

`kcoreaddons`, `kdecore-meta`, `vlc` — real pulls `app-text/xmlto`,
`www-client/w3m` + `virtual/w3m`, `dev-libs/boehm-gc`,
`dev-libs/libatomic_ops`; portuale omits all four.

`xmlto` → `w3m` (its text-dump backend), `boehm-gc` + `libatomic_ops`
→ `w3m[gc]` (or a docbook path). A whole `BDEPEND`/`doc`-USE subtree
portuale isn't entering. May share a root with E.

## G. `sys-apps/systemd` reinstall not proposed  ·  MEDIUM · 3 probes · **FIXED** (via L)

`networkmanager`, `polkit`, `kdecore-meta` — real reinstalls
`sys-apps/systemd` with `policykit*` (a `<consumer>` DEPENDs
`sys-apps/systemd[policykit]`, installed systemd lacks it -- `policykit`
was added to systemd's default USE after it was installed). Fixed by L's
vdb-USE reinstall check: `polkit` (24) and `networkmanager` (67) now
match real. `kdecore-meta` still diverges for an unrelated reason
(`gcr[gtk]` autounmask disclosure -- cluster C).

## H. `@world` didn't include `@system`  ·  HIGH · **FIXED** (`72ea263`)

`emerge -pv @world` collapsed to just the world file's atoms (a
documented simplification), so it omitted the entire `@system` set and
an empty world file gave `emerge: no package atoms to resolve` (rc 2).

**Fixed:** `@world` now = `@selected ∪ @system` (real `WorldSet`);
`@selected` alone stays the world file; a trailing dedup keeps first
position. `TEST/layers/l0/in-container.sh` seeds a small realistic
`@world` (`tmux`/`eix`/`gentoolkit`/`blake3`, none installed) so the L0
`@world` probes exercise the resolver. `emerge -pv @world` in the L0
container now produces the exact same 76-package set as real portage.
Dual-language, `test_world_missing_file_expands_to_system_not_an_error`
+ CASES.

Residual (not H): portuale emits a few `[blocks B]` soft-blocker lines
for `@world`/`@system` (`coreutils[kill]` vs `procps` etc.) that real
resolves silently -- a blocker-evaluation nuance, tracked separately.

## I. Merge order  ·  KNOWN-HARD · ~40 probes  ·  **root-caused 2026-09-09**

First-divergence position varies (`#0`–`#5`). Same package **set**, same
USE, same versions — only the merge **order** differs.

**Root cause (traced on `sys-apps/dbus`, 12-pkg tree).** Real
`emerge -p --debug`'s `digraph:` dump shows every installed nomerge node
(pulled in as a build/run dep of a merge-bound package, plus the whole
`@system` set) carries its **full recorded vdb dependency tree**,
recursively. Portuale's `merge_order::build_digraph` gives those same
installed nodes `(no children)` — it truncates them at depth 1.

Consequence for leaf selection: `_serialize_tasks` merges a package the
moment it becomes a leaf (no unmerged children). In real, `x11-libs/
xtrans`'s build deps (`elt-patches`→`gentoo-functions`, `pkgconfig`→
`pkgconf`) clear in ~2 rounds while `x11-base/xorg-proto`'s
(`meson`→`gpep517`/`setuptools`/`python`→…) take many — so `xtrans`
becomes a leaf first and merges first. In portuale both `elt-patches`
and `meson` are `(no children)` → instant leaves → `xtrans` and
`xorg-proto` become leaves together → `_merge_order_bias` orders them by
parent count (`xorg-proto` 9 > `xtrans` 4) → `xorg-proto` first. Real:
`xtrans, xcb-proto, xorg-proto`; portuale: `xorg-proto, xtrans,
xcb-proto`. Positions 3+ already match exactly.

**Slice 1 done (2026-09-09).** `merge_order::add_installed_dependency_
closure` — walks the forward transitive closure, fills `deps` on
installed entries from their vdb `*DEPEND` (`dep_edges_from_metadata`,
`built=true` → build deps `optional`), appends synthetic
`AlreadyInstalled` nodes for installed deps not already present; the
returned permutation still covers only `0..real_n` (synthetic filtered).
Gated on a `complete` trigger computed off `entries` (any Reinstall/
Upgrade/Downgrade, or a new-slot install) — the CLI's own `want_complete`
misses a reason-less `[ebuild R]`. Dual-language. **`sys-apps/dbus`
byte-identical to real; `sys-apps/systemd`/`sys-auth/polkit` `#4`→`#5`.
L0 clean 76→78, parity 0.633→0.650, order 40→38.** 1014 contract green,
Rust==Python on dbus/systemd/i3.

**Slice 2 done (2026-09-09).** `add_installed_dependency_closure` gained
a `virtuals_only` flag (real expands installed `virtual/*` provider
nodes even when *not* in complete mode — new-style virtuals are "free"
and always walked — but leaves every non-virtual installed node a
`(no children)` leaf; i3's real digraph: only `virtual/pkgconfig` /
`virtual/zlib` / `virtual/perl-ExtUtils-MakeMaker` carry children). Now
runs unconditionally: full forward closure in complete mode,
virtual-provider-only otherwise. `dev-perl/XML-Parser`, `dev-qt/qtbase:6`,
`net-libs/nodejs`, `media-video/ffmpeg` → **byte-identical**;
`x11-wm/i3` `#2`→`#27`, `dev-lang/ruby` `#3`→`#20`. **L0 clean 78→88,
parity 0.650→0.733, order 38→28.** 1014 contract, Rust==Python.

**Slice 3 done (2026-09-09) — graph completeness.** Two fixes so the
complete-mode digraph *node set* matches real exactly (verified 437=437
for `gtk:4`):
1. **`@system` seeding** — real `_complete_graph` pulls the whole
   `@system` set in as nomerge nodes even when nothing being merged
   depends on them; portuale's forward closure missed ~42 (`net-misc/
   openssh`, `sys-apps/grep`, `virtual/libc`…). `add_installed_
   dependency_closure` now also seeds from `config.system_packages`.
2. **`strip_libc_deps`** — real strips the implicit `virtual/libc` /
   `sys-libs/glibc` dep from every dep string (re-expressed purely as
   `seed_toolchain_asap`); keeping it wired the graph into one glibc
   blob so `find_smallest_cycle` picked a 13-node cycle where real
   picks 5. Filtered in `vdb_edges`.
`sys-apps/lsb-release` front-load fixed; divergence positions moved deep
(`gtk:4` `#2`→`#7`, texlive `#1`→`#7`, firefox `#4`→`#10`, libreoffice
`#12`→`#22`, gimp `#7`→`#13`). **L0 unchanged at clean 88 / order 28**
(no probe flipped fully clean — lists just much closer). 1014 contract,
Rust==Python.

**Slice 4 partial (2026-09-09).** (a) Reverted slice 3's `@system` seed —
real `_serialize_tasks` **prunes** every `@system` nomerge node no
merge-bound package reaches ("Prune 'nomerge' root nodes …",
`depgraph.py:9509`); its `--debug` dump shows the *pre*-prune graph, so
the 437=437 node-count match was misleading. Real's selection graph ≈
the forward closure. (Also fixes the Python-reference slowdown.) (b)
Added real's "select root nodes as a last resort" step
(`depgraph.py:10230`) before the `drop_satisfied` escalation. **L0
unchanged clean 88 / order 28** (`dev-lang/rust` went clean; `gimp`
traded `[order]`→a cosmetic USE-sort `[advice]`).

**Slice 4 core — still open. `_serialize_tasks` stuck-timing.** Traced
with an instrumented selection log: portuale reaches the "nothing
selectable in `DepPriorityNormalRange`" state ~50 iterations before real
does, then `drop_satisfied` + `find_smallest_cycle(SATISFIED)` frees
`sys-devel/gcc` and its `llvm-runtimes/clang-*-config` consumers via
`s_ignore_satisfied_runtime` while real still has `NORMAL`-range work.
Front-loaded stubs: `clang-{rtlib,stdlib,unwindlib}-config` (gtk:4/gtk+:3/
firefox/thunderbird/libreoffice/gimp — 6), `dev-perl/Module-Runtime`
(texlive×3), `media-libs/freetype` (kcoreaddons/networkmanager/i3),
`dev-lang/nasm` vs `dev-cpp/eigen` (systemd/polkit), `dev-ruby/matrix`
(ruby/rake), `app-eselect/eselect-fontconfig`. Matching real needs the
exact `_serialize_tasks` frontier consumption dynamics — a sub-project.
**`seed_toolchain_asap` fixed (2026-09-09).** It seeded `virtual/libc` /
`virtual/os-headers` (+ providers) into `asap_nodes` for any outcome
that wasn't `AlreadyInstalled` — including a bare `[ebuild R]` reinstall.
Real's gate is `pkg.operation == "merge" and not vardb.cpv_exists(pkg
.cpv)` — a genuine *new* cpv (`New`/`Upgrade`/`Downgrade`), never a
same-version reinstall. So a `@world` re-emerge front-loaded
`os-headers`+`libc` past dep-free `@system` leaves. `_system` `#0`→`#6`,
`_world` `#0`→`#8`, `MULTI_emptytree-system` `#0`→`#13` (all match real's
opening now). Dual-language.

**Slice 6 done (2026-09-09) — slot-blind digraph edges.** Real
`_create_graph` resolves each dep atom to one package
(`_select_pkg_highest_available`) before `_add_pkg` records the edge.
`build_digraph` looked every atom's `cat/pkg` up in `cp_indices` and
connected it to **every** scheduled instance of that `cat/pkg` — so a
slot-qualified atom (`app-text/docbook-sgml-dtd:4.5`) gained a phantom
edge to a sibling slot (`:3.0`) also being merged. The extra parent
skewed `_merge_order_bias`'s parent-count tier (both docbook slots hit
count 2, promoting them ahead of `app-eselect/eselect-postgresql`).
`required_by` is keyed by `cat/pkg` only, so its fallback-edge loop had
the same blind spot in reverse. Fix: `build_digraph` builds a
per-entry `cat/pkg-ver:slot/sub_slot::repo` candidate string and narrows
every forward edge (and the top-level-atom seed, and the `expand` walk)
to the entries the atom actually `match_from_list`es; the `required_by`
fallback is skipped when the owner already has a real edge to another
slot of the same `cat/pkg`. Keeps the edge whenever the candidate can't
be built or the atom won't parse (only ever removes a provably-wrong
edge). **`dev-db/postgresql` byte-identical to real**; two more probes'
first-divergence moved much deeper (`#21`→`#84`, `#13`→`#47`). Comparator
gained a matching guard: a multi-slot package merged in several slots at
verbosity 2 (`emerge -pe`, no `:slot` shown) collapses to one
`(type, cp, "")` map key, so a `version` finding off the survivor is
position noise — suppressed when a cp repeats without slot
disambiguation. **L0 clean 89→90, parity 0.742→0.750, order 28→27.**
1014+ contract, Rust==Python. Fixture `dev-libs/slotorderroot`
(+`slotordera`/`slotorderb`/`slotorderdual:1`/`:2`), Rust unit
`a_slot_qualified_dep_does_not_edge_to_a_sibling_slot`, contract
`test_slot_qualified_dep_does_not_edge_to_a_sibling_slot`.

**Slice 6 follow-up (2026-09-09) — `|| ( … )` branch dedup.**
`dep_edges_from_metadata` de-duplicated edges by `(cat, pkg, priority)`,
so a `|| ( >=dev-build/autoconf-2.73:2.73 >=dev-build/autoconf-2.72-r1:2.72 )`
group (in `dev-lang/nasm`'s `BDEPEND`) kept only the first branch's atom
text. Slice 6's `edge_matches` then filtered that atom out (installed
`autoconf` is slot `2.72`, atom asks `:2.73`), so `nasm → autoconf`
survived only as a `required_by` *runtime* fallback where real has it
*buildtime*. Fix: dedup key is now `(atom, cat, pkg, priority)` — both
`||` branches survive as `DepEdge`s and `build_digraph` keeps whichever
resolves. Dual-language; regression fix for slice 6, L0-neutral.

**Slice 7 — `_serialize_tasks` frontier timing (`sys-auth/polkit`:
real `eigen`#6 `nasm`#7; portuale `nasm`#6 `libjpeg-turbo`#7 `eigen`#8).**
Traced with a container patch to real's `_serialize_tasks` (an `RT_SEL`
stderr line after the per-iteration state reset) + an env-gated
`PORTUALE_MO_SEL` trace in `select_nodes`. Mechanism, confirmed:

* Real `_complete_graph` seeds `@world`/`@system` and walks them **deep**,
  so real's *post*-prune scheduler graph carries `@system`-deep installed
  nodes (`dev-libs/gmp`/`mpfr`/`mpc` reached via `glibc`→`gcc`;
  `app-shells/bash`→`readline`; wget/openssh/gnutls trees; ~45 cps for
  polkit) that portuale's forward-closure-only graph never has.
* Real drains ~90 of those one-at-a-time via
  `DepPriorityNormalRange._ignore_optional` (an installed pkg's own
  satisfied build deps) between `al=234` and `al=140` — which keeps real
  in NORMAL range long enough to merge `eigen`/`nasm`/`libjpeg-turbo`
  *before* the `drop_satisfied` iteration that breaks the
  `{glibc, libcrypt, libxcrypt, perl, locale-gen}` runtime cycle.
* Portuale runs out of NORMAL `_ignore_optional` leaves at `al≈234` →
  `drop_satisfied` fires early → `find_smallest_cycle(SATISFIED)` dumps a
  13-node glibc+perl cycle → the whole `dev-lang/perl` subtree merges
  before `eigen`, skewing the tail.
* Node *count* is nearly equal (real post-prune `al=301`, portuale
  `al=298`) — so slice-4a's "graph ≈ forward closure" was right by count,
  wrong on membership.

Two edge-fidelity bugs found along the way: (a) the `||`-dedup one above
(FIXED); (b) `add_installed_dependency_closure`'s `strip_libc_deps`
drops every `sys-libs/glibc` RDEPEND edge — real's `strip_libc_deps` is
`--changed-deps`-only, its scheduler graph keeps all 6 genuine glibc
in-edges. **Removing the strip was tried twice (once with the slice-8
graph) and reverted both times**: it fixes `nginx` `#0` but regresses
`dev-lang/rust` (new `#3` finding — `lsb-release` front-loads) and moves
~10 other probes' first-divergence *earlier* (gtk:4 `#7`→`#2`,
kdecore-meta `#84`→`#12`, libreoffice `#22`→`#12`, wireshark `#47`→`#5`,
`Module-Runtime` `#7`→`#1`). With the glibc edges restored, portuale's
still-premature `drop_satisfied` dumps the whole `glibc`+`perl`+
`lsb-release` cluster *even earlier*. **Kept as a documented
imperfection** in `merge_order.rs`; the real fix is the
`find_smallest_cycle` / premature-`drop_satisfied` work below, not the
edge.

**Slice 8 done (2026-09-09) — `@system` seed + real's nomerge-root prune.**
Re-added slice-3's `@system` seeding of `add_installed_dependency_closure`
(complete mode only) — real `_complete_graph` seeds a `SetArg` for
`@world`/`@system` and walks it deep — AND ported real's own "Prune
'nomerge' root nodes if nothing depends on them" loop
(`depgraph.py:9505-9518`), iterated to a fixpoint, into
`serialize_merge_order` right after `build_digraph`. Slice-4a had
reverted the seed *without* the prune, which is why it skewed leaf
timing. The prune is **restricted to the synthetic `i >= real_n`
nodes**: a real `AlreadyInstalled` entry is a nomerge root real drops
too, but portuale *displays* it and its `--tree` rendering wants it
after the deps it pulls in (leaving it in the scheduler produces that;
pruning drops it into `leftover` on its parent-first discovery rank —
wrong). Portuale's `sys-auth/polkit` pre-prune digraph is now 354 nodes
(was 298; real 367). **`sys-apps/systemd` / `sys-auth/polkit` tails now
match real** (the `dev-lang/perl` subtree is no longer front-loaded —
`systemd` at `#9` in both). **L0 unchanged** (clean 90 / order 27 —
every one of the 27 `[order]` probes diverges at the same position, so
the binary comparator can't see the tail improvement, same as slices
2–5). Dual-language; 1017 contract, 319 portage-repo unit tests, Rust ==
Python.

**Slice 9 investigation (2026-09-09) — the `clang-*-config` cluster
(6 probes: gtk:4/gtk+:3/firefox/thunderbird/libreoffice/gimp) root-caused.**
Traced `gui-libs/gtk:4` side-by-side (real `RT_SEL` patch +
`PORTUALE_MO_SEL`). Positions #0–#6 are byte-identical; the divergence
is `llvm-runtimes/clang-{rtlib,stdlib,unwindlib}-config` (three USE-only
stubs, each with a single `→ sys-devel/gcc (runtime, satisfied)` edge)
front-loaded at #7–#9 where real has them at #18+. **Both merge the
stubs the instant `sys-devel/gcc` (installed) leaves the graph** — real
removes `gcc` at its merge #18, portuale at merge #7. `gcc`'s installed
`RDEPEND` names `sys-libs/glibc`; portuale's `strip_libc_deps` drops
that edge so `gcc` frees ~11 merges early (its remaining hard deps —
`gmp`/`mpfr`/`mpc`/`zstd`/`zlib` — all clear by then). Restoring the
`gcc → glibc` edge only defers `gcc` to merge #8 (still front-loaded),
because portuale then breaks the `glibc` runtime cycle early anyway via
a premature `drop_satisfied` + an oversized `find_smallest_cycle`
closure (13 nodes vs real's 5). **NEXT = `find_smallest_cycle` /
`gather_deps` size parity + why portuale's NORMAL `_ignore_optional`
frontier drains ~11 merges before real's.** This is the single lever for
both the `clang-*-config` cluster (6) and the `sys-auth/polkit`
`{eigen, nasm, libjpeg-turbo}` triple.

**Slice 10 done (2026-09-09) — injected implicit-libc dep, FIXED.**
`find_smallest_cycle`/`gather_deps` were already faithful ports of
`depgraph.py:9640-9879` — the frontier drain was upstream, in the graph
edges. `add_installed_dependency_closure` reads each installed package's
**vdb `*DEPEND`**, and
`portage.package.ebuild.doebuild._inject_libc_dep` (bug #753500) appends
a bare `>=<libc-provider>-<version>` to *every* installed package's vdb
`RDEPEND` — one the ebuild never declared. `dev-libs/gmp` (ebuild
`RDEPEND=""`) carries a phantom `gmp -> glibc (runtime)` edge in the
vdb, which pinned `gmp`/`mpfr`/`mpc`/`zstd` behind `glibc`'s deep
subtree — draining the NORMAL frontier ~11 merges early → premature
`drop_satisfied` → the whole class of stub front-loads. Real, with
`--dynamic-deps` (default), walks the *current ebuild*'s deps, so it
never sees the injection. Fix: strip exactly the injected shape — a bare
`>=` atom on a libc-provider `cp` with no slot and no USE deps — and
nothing else (a genuine `sys-libs/glibc[-crypt(-)]`, USE-conditional or
not, survives, exactly as real keeps it). The over-broad slice-3
`strip_libc_deps` (every `sys-libs/glibc` / `virtual/libc` edge) is
replaced by this. Dual-language (`merge_order.rs::is_injected_libc` +
`emerge_pretend_reference.py::_is_injected_libc`).

**`sys-apps/systemd` / `sys-auth/polkit` / `app-text/texlive` /
`www-servers/nginx` all flipped byte-clean**; every remaining
`clang-*-config` / `Module-Runtime` / `freetype` front-load moved from
`#7`–`#13` to `#22`–`#59` (gtk:4 `#7`→`#30`, texlive-core `#7`→`#59`,
gimp `#13`→`#54`, kcoreaddons `#9`→`#56`). **L0 clean 90→94, parity
0.750→0.783, order 27→23.** 1017 contract, 319 unit, whole-workspace
`cargo test`, Rust == Python. No regression (`dev-lang/rust` stayed
clean).

**Slice 11 done (2026-09-09) — `|| ( … )` groups resolved to one branch
in the digraph.** `GraphEntry::deps` keeps every `||` branch (the
resolver's `use_reduce_flat_disjunctive` pick isn't threaded to
`build_digraph`), so an edge was drawn for every branch whose `cat/pkg`
had a graph node. When two branches both resolve — `libjpeg-turbo`'s
`|| ( nasm yasm )` (both merged), `libXdmcp`'s
`|| ( >=sys-libs/glibc-2.36 dev-libs/libbsd )` (glibc installed, libbsd
merged) — the phantom edge skewed `_merge_order_bias`'s parent counts
(`yasm` gained `libjpeg-turbo` as a third parent, slipping ahead of
`sys-libs/binutils-libs`). `split_disjunctive` now tags each disjunctive
atom `(group, branch)`; `build_digraph` picks one branch per group by
real `dep_zapdeps`' `choice_bins` order — first branch all of whose
atoms match a **merge-bound** node (`preferred_in_graph` + real's
line-793 all-in-graph promotion), else all-installed, else first-match —
and suppresses the rest from the edge loop + discovery walk. A group
where no branch fully resolves keeps the old over-inclusive behaviour.
First heuristic tried "installed first" and front-loaded
`clang-linker-config` in `xorg-server` (`#74`→`#4`); the corrected
in-graph-first order matches real. **`firefox`/`thunderbird` now match
real through #28 (was `#10` — yasm/binutils-libs transposed); no other
probe moved.** clean 94 / order 23 unchanged (firefox's next divergence
is the multi-slot `clang-*-config:21` vs `:22` interleave at #29).
Dual-language; 1017 contract, 319 unit, Rust == Python.

**Slice 12 done (2026-09-09) — same-slot install/reinstall is one node,
not two.** portuale's BFS built an `AlreadyInstalled` `GraphEntry` for a
`cat/pkg` and then, when a *later* atom forced a same-slot merge of it,
added a second `Reinstall`/`Upgrade` entry — `resolved_slots` (keys
New/Upgrade/Downgrade/Reinstall by `(cat,pkg,slot)`) and `other_outcomes`
(keys AlreadyInstalled/NoVisibleCandidate by `(cat,pkg)`) never
cross-check. `net-libs/nghttp2`'s `>=sys-apps/systemd-209` resolved to
installed systemd, then `sys-auth/polkit`'s `sys-apps/systemd:0=[policykit]`
forced the reinstall (installed systemd is `-policykit`) → two
`sys-apps/systemd` nodes, two digraph in-edges. Real dedups to one (the
reinstall). Fix: after the `required_by` fill, drop any `AlreadyInstalled`
entry whose `(cat, pkg, vdb-slot)` matches a merge-bound entry; the
merge-bound one already carries the union of both owners' `required_by`.
Dual-language (`lib.rs` `mergebound_cp_slots` retain +
`emerge_pretend_reference.py` mirror). **L0 clean 94→95, parity
0.783→0.792, order 23→22.** 1017 contract, whole-workspace `cargo test`,
Rust == Python.

**Remaining order 22 — inventory (2026-09-09):**
* `firefox`/`thunderbird` `#29`: `clang-*-config:21` vs `:22` multi-slot
  interleave — real groups a slot's stubs together, portuale interleaves.
* `gedit`/`nautilus` `#5`/`#8`: **residual after slice 12.** The two
  `sys-apps/systemd` nodes are now one, but the surviving
  `nghttp2 → sys-apps/systemd (merge, buildtime)` edge still makes
  `nghttp2` wait for the systemd merge; real's scheduler digraph has
  `nghttp2 → virtual/pkgconfig` only. Real's mechanism: `_select_package`
  for `>=sys-apps/systemd-209` picks the *installed* systemd and
  `_add_pkg`s it as a nomerge node with the `nghttp2` in-edge; when
  `polkit` later forces the merge systemd into the same slot, the
  installed node is *superseded* and its in-edge is silently lost — and
  this is **walk-order dependent** (real keeps `p11-kit → systemd`,
  `at-spi2-core → systemd` because those parents are walked *after*
  polkit). portuale doesn't model "an initially-satisfied dep draws no
  graph edge" nor "an installed node's in-edges vanish when a same-slot
  merge supersedes it". Resolver-architecture gap, deferred — 2 deep
  probes, fragile to chase.
* `_system`/`_world` `#6`/`#8`: `virtual/os-headers` vs `sys-devel/patch`
  transposed + `findutils` slips — emptytree leaf-order tie-break.
* `MULTI_emptytree-system` `#13`: portuale front-loads `mime-types` /
  `mpdecimal` / `gentoo-common` ahead of the `acct-group/*` run.
* `gnome-shell` `#0`: portuale front-loads `libmd` / `duktape` / `polkitd`
  ahead of `hwdata`.
* `ruby`/`rake` `#20`: `dev-ruby/did_you_mean` vs `dev-ruby/matrix`.
* `vlc` `#7`: `sys-libs/mtdev` vs `x11-libs/xtrans`.
* deep tail (`#22`–`#84`): kcoreaddons, kdecore-meta, gtk:4/gtk+:3,
  networkmanager, texlive-core/latex, libreoffice, gimp, wireshark,
  xorg-server, i3 — lists ≥90 % correct, binary comparator can't credit.

**`dev-python/pyproject-metadata` front-load — investigated 2026-09-09,
still the `_serialize_tasks` wall.** `kdecore-meta` `#95`, `gtk:4` `#30`,
`gtk+:3` `#30`, `wireshark` `#47` all diverge on the same node:
portuale merges `dev-python/pyproject-metadata` ~11 slots earlier than
real (real: `spirv-tools, xcb-proto, libgit2, cargo-c, icu,
pyproject-metadata, xorg-proto`; portuale: `spirv-tools,
pyproject-metadata, xcb-proto, libgit2, cargo-c, icu, xorg-proto`).
Verified against real's own `emerge -p --debug` digraph dump:
**the digraph is byte-identical** — same nodes, same edges, same
priorities; `pyproject-metadata` has exactly one parent
(`dev-python/scikit-build-core`, itself ~50 slots later), same as
`xcb-proto`/`libgit2`/`cargo-c`/`icu` (all parent-count 1); and the
`_create_graph` **discovery order is identical** too
(`scikit-build-core, pyproject-metadata, libxcb, xcb-proto, spirv-tools,
cargo-c, libgit2, icu`). All of `pyproject-metadata`'s deps (`packaging`,
`python`, `gpep517`, `flit-core`) are installed → it's a rung-`NONE`
leaf from round 1. `_merge_order_bias` (stable sort, parent-count desc)
therefore leaves it *ahead* of `xcb-proto` in both. Yet real defers it
past `xcb-proto`+3 more.

**Deeper dig (2026-09-09, second pass — traced real's `_serialize_tasks`
in the container + portuale's `select_nodes`).** The port is structurally
faithful: `_SerializeFrontier` (new in portage 3.0.81.3) is a pure perf
optimisation — `ready_nodes()` returns leaves in `mygraph.order`, same as
`leaf_nodes()`. Both real and portuale run the *whole* middle of the
merge **one node at a time** (not the greedy `NONE`-batch): a
`PDEPEND` promotes `llvm-runtimes/clang-runtime` into `asap_nodes` (bug
#180045) and it lingers there non-leaf, which disables the greedy batch —
portuale's asap promotion fires correctly here. The real divergence is
**installed-nomerge-node drain timing**. `pyproject-metadata` sits behind
a 4-deep chain of installed python build tools —
`installer → gpep517 → flit-core → packaging → pyproject-metadata`, wired
by `(optional)`-priority edges (`packaging → gpep517(opt)` etc.), all
correctly counted at rung `NONE` by portuale's `is_leaf`. Real drains
`spirv-tools, xcb-proto, libgit2, cargo-c, icu` (5 merge-bound nodes,
retlist 29→33) **before** that installed chain unblocks (retlist 34,
where `installer/gpep517/flit-core/packaging/wheel/…` all drain together,
then `pyproject-metadata`). Portuale drains the installed chain ~7 picks
earlier, so `pyproject-metadata` (bias idx 299) surfaces as a leaf and is
picked ahead of `xcb-proto` (bias idx 308). `packaging`/`gpep517`/
`flit-core` are at bias idx 175–192 — *far* ahead of both — so neither
resolver is picking "first leaf in bias order"; real is deferring the
installed chain because one of its links (`installer`, via
`installer → unzip(opt)` or similar) stays non-leaf a few iterations
longer in real than in portuale. Pinning down which installed node's
blocker clears at a different time would need per-node drain tracing
across the full ~400-node graph — the same wall slices 4–12 hit, ~4-probe
payoff. Not an edge/priority/discovery/asap bug. **A reusable
`PORTUALE_MO_SEL` eprintln trace in `select_nodes` (asap state + per-pick
node + live children of a watch set) was prototyped and reverted;
re-add from this note if resuming.**

**`llvm-core/llvmgold` ↔ `llvm-core/llvm-toolchain-symlinks`** is a
PYTHONHASHSEED-nondeterministic adjacent pair in real (3 `emerge -p`
runs flip them) — hits `dev-lang/rust` `#12`, `media-libs/mesa` `#36`;
portuale is deterministic. The comparator doesn't yet detect an
adjacent-transposition-only divergence, so these wobble
clean↔not-clean between L0 runs (clean 86–88, order 28–30).

## J. REQUIRED_USE hard-error  ·  MEDIUM · 2 probes · **BOTH FIXED** (neither was an autounmask gap)

`app-editors/neovim` (real rc 0, portuale rc 1): portuale
`REQUIRED_USE not satisfied … "^^ ( lua_single_target_luajit
lua_single_target_lua5-1 … )"`. **Root cause was NOT missing autounmask**
— real doesn't autounmask anything here, it resolves cleanly with
`LUA_SINGLE_TARGET="luajit"` because `profiles/base/package.use` sets
`app-editors/neovim lua_single_target_luajit -lua_single_target_lua5-1`.
portuale folded the profile-wide `LUA_SINGLE_TARGET="lua5-1"`
(`profiles/base/make.defaults`) once, last-wins, into the high-priority
`conf` USE tier — re-applied *after* that `package.use` — so BOTH single
flags ended up on and `^^` tripped. Real config.py (2849-2889) folds a
profile `make.defaults` USE_EXPAND value **per profile level**,
*prepended* before that level's own `USE=`, so the same level's
`package.use` still cancels it; the `defaults` configdict is then skipped
by the main USE_EXPAND re-fold (2964).

**Fixed** on `main`: `portage_profile::resolve_config` now folds each
level's own USE_EXPAND / USE_EXPAND_UNPREFIXED make.defaults values into
that layer's `make_defaults_use` (prepended); the global `conf`-tier fold
skips any var the profile chain settled and `make.conf`/env never
touched. Dual-language (`emerge_pretend_reference.py` mirror). Fixture
`dev-libs/singletargetpkg`, Rust unit
`profile_make_defaults_use_expand_is_folded_per_level_before_package_use`,
contract `test_profile_use_expand_default_is_folded_per_level`.

**J-libsdl2 also FIXED** (`a250943` + follow-up) — also NOT an
autounmask gap: `emerge -pv app-emulation/wine-vanilla` →
`media-libs/libsdl2[haptic,joystick,...]` REQUIRED_USE fails on **both**
resolvers. The divergence was pure error *rendering* — portuale printed
a one-liner `emerge: REQUIRED_USE not satisfied for … "<whole expr>"`
where real `_show_unsatisfied_dep` prints the `!!! The ebuild selected
to satisfy "<atom>" has unmet requirements.` block (candidate +
`pkg_use_display`, the minimal unsatisfied sub-expression
`wayland? ( gles2 )`, the complete expression, the `(dependency required
by … [ebuild/argument])` chain). Ported `portage_required_use::
unsatisfied_reduced` (real `_RequiredUseBranch` tree + `tounicode`) +
`human_readable`, `render_required_use_block` / `required_use_dep_chain`
in `portage-repo`, the caller's `!!!`-no-`emerge:`-prefix rule in
`pretend.rs`. Dual-language. **wine-vanilla now byte-identical to real
→ L0 clean 73→74, parity 0.617.** The 3 `requiredusebadpkg*` contract +
Rust unit tests were re-pinned to the block.

## M. "Version selection" (firefox/thunderbird)  ·  ~~22 `version` + 1 `use`~~  ·  **NOT A BUG — comparator artifact, FIXED**

All 22 `[version]` findings and the `[use] dev-lang/rust-bin LLVM_SLOT`
finding were the same comparator defect, not a resolver divergence.
`resolve-compare.py` keyed package identity by `(type, category/pn)`, so
a package installed in two slots at once — `llvm-core/llvm-21.1.8:21`
*and* `llvm-core/llvm-22.1.8:22`, both merged by both resolvers for
firefox — collapsed to one map entry (last row wins). Real lists llvm-22
before llvm-21; portuale lists llvm-21 before llvm-22; so the survivors
were `llvm:21` (real) vs `llvm:22` (portuale) — a phantom
`real 21.1.8 vs portuale 22.1.8`. Same for `clang`, `compiler-rt`,
`rust-bin`, the `-config`/`-symlinks` stubs, and `rust-bin`'s baked
`LLVM_SLOT`.

Fix: `split_cpv_slot()` parses the slot out of the merge-list token;
identity is now keyed by `(type, cp, slot)`; the merge-order multiset
comparison is slot-aware too. Firefox/thunderbird now show only their
real residue — cluster I merge order + one `[blocks B]` soft-blocker
line (see A's "Residual" note). Doctests on `split_cpv_slot`.

## P. `VIDEO_CARDS` (and every USE_EXPAND var) not stacked across profile levels  ·  MEDIUM · 3 probes · **FIXED**

`x11-base/xorg-server`, `x11-base/xorg-drivers`, `gnome-base/gnome-shell`:
real enables `video_cards_dummy` (→ pulls `x11-drivers/xf86-video-dummy`),
portuale had `-dummy`. Real's `VIDEO_CARDS` = union of
`profiles/default/linux/make.defaults` (`"dummy fbdev"`) and
`profiles/arch/amd64/make.defaults` (`"amdgpu fbdev intel nouveau radeon
radeonsi vesa"`); portuale read only the last-wins scalar, losing
`dummy`.

Root cause: `portage_profile::resolve_config`'s per-level USE_EXPAND
fold (real `config.py` 2853-2866) had a `level_final` guard that only
folded a level's value if it equalled the *final* `scalars` value — so
a var set at two profile levels kept only the deepest. Real folds
*every* level's own `make.defaults` value into that level's USE
contribution (USE is incremental → the union stands); the fold is
suppressed only when `make.conf`/env overrode the var (then real's
`is_not_incremental` pass replaces wholesale). Guard changed to
per-var `conf_overrode` (`profile_scalars.get(var) != scalars.get(var)`).
Dual-language; `use_expand_value_set_at_two_profile_levels_stacks_
incrementally`. xorg-server `[missing]`+`[use]`+`[totals]` → just
`[order]`; xorg-drivers `[use]` → just `[order]`; gnome-shell's
`xorg-drivers VIDEO_CARDS` `[use]` line cleared (its `libcanberra`
pulseaudio + `nasm` residue is the `advice` cluster, unrelated).

## Q. `--emptytree` false circular-dependency on an installed build-time cycle  ·  MEDIUM · 1 probe · **FIXED**

`MULTI_emptytree-system`: portuale aborted with `* Error: circular
dependencies:` (`app-arch/xz-utils` ↔ `app-portage/elt-patches`,
buildtime) + rc 1; real resolves it (rc 0). Both packages are already
installed, so real's `DepPriority.satisfied` (a build-time dep provided
by an installed package) lets `_serialize_tasks`' `DepPrioritySatisfied
Range` ignore that edge when breaking a cycle — real orders the two by
their installed versions.

Root cause: `EdgeKindMap` marked *any* `buildtime_hard` atom as an
unbreakable (`has_hard`) edge, contradicting its own "unsatisfied
build-time dep" contract. Now `has_hard` is set only when
`best_installed_for_atom` returns `None` for the atom; a build-time dep
an installed package satisfies is recorded as breakable. Dual-language;
fixture `dev-libs/instcycle{a,b}` (mutual `BDEPEND`, both in the vdb),
Rust unit `build_time_cycle_between_two_installed_packages_is_not_a_
hard_cycle` + contract
`test_build_time_cycle_between_two_installed_packages_is_not_circular`.
The genuine `hardcyclea`/`hardcycleb` cycle (neither installed) is still
reported.

## R. `||` group ignores real's `all_in_graph` choice bin  ·  MEDIUM · 2+ probes · **FIXED**

`kde-apps/kdecore-meta` (rc 1 + 53 extra packages + `gcr[gtk]` &
`freetype[harfbuzz]` autounmask, all gone) and `kde-plasma/plasma-meta`
(2 fewer autounmask advices).

`virtual/secret-service` RDEPENDs
`|| ( gnome-base/gnome-keyring >=kde-frameworks/kwallet-runtime-6.18.0[keyring]
app-admin/keepassxc )`. `kwallet-runtime` is pulled into the graph anyway
by the KDE stack (→ `libsecret` → PDEPEND `virtual/secret-service`), so
real `dep_zapdeps` files the `kwallet-runtime` branch in choice bin 0
(`all_in_graph`, `dep_check.py:636-649` — the same list
`preferred_installed` aliases to) and picks it. portuale modelled only
`Installed` vs `Available`, so it took the first-listed `gnome-keyring`
→ `gcr[gtk]` → the whole GTK/gdk-pixbuf/librsvg/mesa/llvm subtree +
`gcr[gtk]`/`freetype[harfbuzz]` autounmask.

Fix: `atoms_all_in_graph` — every non-blocker atom of the alternative is
satisfied by a merge-bound `GraphEntry` this run (`[use]` deps checked
against its `use_flags_display`) → `AltPreference::Installed`. Wired into
both `||` closures (main New/Upgrade walk + `enqueue_dependencies` deep
walk). Dual-language; fixtures `dev-libs/ingraph{any,svc,keyring,
keyringdep,wallet}` + `ingraphnoany` control, Rust unit
`or_dep_prefers_a_branch_already_merge_bound_in_the_graph`, contract
`test_or_group_prefers_a_branch_already_in_the_graph`. kdecore-meta
drops from `[exit]`+advice+suppressed to `[order]`-only (cluster I).

## S. Top-level all-masked atom → wrong report  ·  LOW · 1 L0 probe (many fixtures)  ·  **FIXED**

`emerge -p app-emulation/wine-staging` (hard-masked): real prints
`_show_unsatisfied_dep`'s `!!! All ebuilds that could satisfy "<atom>"
have been masked.` + `!!! One of the following masked packages is
required to complete your request:` + one `- <cpv>::<repo> (masked by:
<reason>)` line per masked candidate (descending version) + the "MASKED
PACKAGES section" doc footer. Portuale collapsed every no-visible-
candidate top-level atom to the bare `emerge: there are no ebuilds to
satisfy "<atom>".` line — which real only prints when the atom matches
*no ebuild at all* by version.

Fix (`backtracking_resolve`, dual-language): `all_masked_report` — when a
top-level `NoVisibleCandidate` atom still `match_from_list`es one or more
ebuilds by version and none of them is `is_visible`, emit real's masked
block instead. `candidate_masking_reasons` mirrors real
`getmaskingstatus._getmaskingstatus`'s reason strings: `package.mask`,
`<names> license(s)`, and the `~<arch>`/`-<arch>`/`missing` + `" keyword"`
KEYWORDS forms are byte-exact; `PROPERTIES`/`RESTRICT` are the bare key
name (portuale has no missing-token list for those; the L0 comparator
checks only the two `!!!` headers). `wine-staging` output is now
byte-identical to real.

Also flips ~14 fixture-based contract tests (`hardmaskedpkg`, `eulapkg`,
`repomaskedpkg`, `interactivepkg`, `keywordrevokedpkg`, the overlay/
masters ones, …) from the bare line to the correct block — portuale was
wrong on every one. `_all_masked` test helper; 2 Rust unit + contract
pins updated. A masked *dependency* atom's disclosure during backtracking
(gedit/nautilus `gcr[gtk]`, plasma-meta `xwayland[libei]`) is a separate
path, still deferred (cluster A / R residue). **L0 clean 95→96.**

## T. `--autounmask-use` blocked by a `(+)`-defaulted missing flag  ·  LOW · gnome-shell (partial)  ·  **FIXED**

`gnome-base/gnome-control-center` RDEPENDs
`media-libs/libcanberra[pulseaudio,sound(+)]`. `libcanberra` has
`pulseaudio` in IUSE (default off) but **not** `sound` — the `(+)` on
`sound` already stands in for "enabled", so only `pulseaudio` needs a
flip. `suggested_use_flip` bailed (`return None`) on *any* use-dep flag
absent from IUSE, defaults included, so it declared the whole atom
unfixable: portuale emitted `!!! no visible ebuild for dependency
"media-libs/libcanberra"`, recorded no autounmask change, and merged
`libcanberra` with `-pulseaudio`. Real autounmasks
`>=media-libs/libcanberra-0.30-r8 pulseaudio` and merges it with the flag
on.

Fix (dual-language): in `suggested_use_flip` / `_suggested_use_flip`, a
flag missing from IUSE `continue`s (nothing to flip) when its `(+)`/`(-)`
default already satisfies the requirement — matching `use_deps_satisfied`
— and only `return None`s when the missing flag is genuinely
unsatisfiable (no default, or a contradicting one). Fixture-free
(`dev-libs/useflagpkg[missingflag,nonexistentflag(+)]` CASE + Rust unit
`autounmask_use_flip_is_not_blocked_by_a_default_satisfied_missing_flag`).

Clears gnome-shell's `[use] libcanberra`, `[advice] use-change
libcanberra pulseaudio`, and `[error] portuale-only: no visible ebuild
for … libcanberra`. The probe's remaining `[extra] nasm` + `[totals]` +
`samba[client]` `[error]`/`[advice]` are one thing — real's backtracking
**terminated early** at the `>=net-fs/samba-4.0.0[client]` `show_missing_
use` failure and truncated its list; portuale keeps going (cluster A,
deferred). `[order] #0` is cluster I.

## K. Atom-list hygiene  ·  DONE

`sys-devel/make` → `dev-build/make`, `virtual/awk` →
`app-alternatives/awk`, `dev-util/meson-format` → `dev-build/ninja`
(stale names; real correctly said "no ebuilds", portuale resolved the
move target for `sys-devel/make` — mildly interesting but not tracked).
Fixed in `atomlists/l0-resolve.txt`.

---

## Notes for the comparator itself

- When exit codes differ, the merge-list set/order/totals comparison is
  complete-vs-truncated; `resolve-compare.py` now suppresses those and
  reports only `exit` + `advice` + `error`.
- ~~Package identity is keyed by `(type, category/pn)` — multi-slot
  packages in one list collide (last wins).~~ **Done** — now
  `(type, cp, slot)` via `split_cpv_slot()` (cluster M).
- `@world` probe would be far stronger with a populated `world` file in
  the image (see H).

---

## Slice-6 run: backlog #19 abort path + dep_zapdeps merge (2026-09-11)

Run `TEST/logs/l0-20260911T144609Z/` (branch `backlog/019-DFS-partial`
@ `354274e`, on top of the dep_zapdeps merge): same pinned tree as the
2026-09-08 baseline (`gentoo` @ `11c58b7a`, `buildovl` @ `3b1df681`,
profile `default/linux/amd64/23.0/systemd`, portage `3.0.82.2`),
**120 probes, 96 clean, parity 0.800** — identical topline and identical
24-probe divergent set to the Slice-4 confirmation (`a93be88`). Nothing
moved in either direction; no regressions, no new known-divergences
(`known-divergences.yaml` stays empty — adjudication lives here).

Per-probe verdicts, all against recorded clusters:
- `order` ×22 (ruby, rake, kdecore-meta, gtk4, gtk3, nautilus, gedit,
  networkmanager, texlive×2, firefox, thunderbird, libreoffice, gimp,
  wireshark, vlc, xorg-server, i3, _system, _world, MULTI_emptytree):
  cluster I (merge-order timing/sequence), unchanged.
- `truncated` ×2 (podman 8-vs-43, plasma-meta 4-vs-468): cluster A
  suppression, unchanged. The abort path does not clear these: real's
  partial comes from the in-graph `>=dev-lang/go` pass-1 choice that
  portuale's `circular_self` `go-bootstrap` pick never takes (backlog
  §A real-tree residue) — an `||`-selection gap, not a rendering gap.
- `advice`+`error` masked xwayland[libei] (plasma-meta) and gcr[gtk]
  (nautilus, gedit): real-only disclosures. Same cause: portuale
  resolves through a branch where the masked dep is never encountered
  (USE-flip / alternate branch), so no abort fires and no disclosure
  prints. Unchanged from baseline; clears with the `||`/USE-selection
  work, not with #19.
- gnome-shell bundle (`extra` nasm, `totals` 453-vs-454, samba USE
  `error`/`advice`, `order` #0): the recorded cluster-A deferral (real
  truncated at the samba `show_missing_use` failure; portuale keeps
  going), unchanged. The abort path is orthogonal: portuale completes
  this resolve (autounmask USE suggestion, exit 1), real abandons it.
- `MULTI_emptytree-system` `version`+`flags` (real portage 3.0.81.3 vs
  3.0.82.2): environmental — the container's pin upgrade did not take
  on this run. Unchanged pattern.

L1 scope check (plan: run L1 only if the non-`--pretend` path was
touched): `git diff 62625fe..HEAD -- rust/portuale/src/` names
`pretend.rs` only — no merge/unmerge/package/fetch/phase code changed
in #19 (the `portage-use-reduce` churn in range is the dep_zapdeps
merge, L0-confirmed at `a93be88`). L1 not run.

---

## Slice run: backlog #24 S1–S5 slot-operator rebuild (2026-09-12)

Run `TEST/logs/l0-20260912T120635Z/` (branch
`backlog/024_slot-operator` @ `69ed4f7`, S4 review follow-ups
`a7d0ef1` docs/comment-only): same pinned tree as the S0 archive
(`gentoo` @ `11c58b7a`, `buildovl` @ `3b1df681`, `porttest` @
`aef17684`, profile `default/linux/amd64/23.0/systemd`, portage
`3.0.82.2`), **120 probes, 96 clean, parity 0.800** — identical topline
and `UNEXPLAINED: 46` breakdown to the S0 archive and to S1's post-gate
run; `l0-report.json` diffs clean against both.

- **`portuale/` raw outputs: `diff -rq` clean** against both the S0
  archive (`l0-20260911T231643Z`, pre-S1) and S1's run
  (`l0-20260911T234323Z`). Stronger than the plan's S6 stop condition,
  which allowed added `rR` rows under `-uD` probes (S1) moving to real's
  position (S3): none of the 120 probes has a stale `:=` consumer, so
  the slot-operator rebuild path (scan, probe, or undo) does not fire on
  this corpus at all. Zero added rows, zero moved rows, zero regressions.
- **`real/` raw outputs:** differ from S0 only in the
  `Dependency resolution took N.NN s` timing line — every differing file
  checked line by line; no non-timing line changed. The merge-order
  timing cluster is untouched.
- **Adjudication:** nothing to adjudicate — no atom's status changed in
  either direction. `compare/known-divergences.yaml` stays empty; the 46
  unexplained findings are the same recorded clusters as the 2026-09-11
  runs (order ×22, truncated ×2, advice/error ×8, the gnome-shell
  cluster-A bundle, and the environmental `MULTI_emptytree` portage
  version skew).
- **S3 flag:** nothing to remove — Gate G0.2 was answered
  "unconditional", so S3 landed without a rollback flag.
- **L1 scope check:** `git diff 5d2e874..a7d0ef1 --stat` names only
  `rust/portuale/src/pretend.rs` and `rust/portage-repo/src/lib.rs` (the
  resolver) — no merge/unmerge/package/fetch/phase code in the range.
  L1 not run, per the plan.

---

## B-series run: #17 trace harness + closure/multi-slot fixes (2026-09-12)

Plan `docs/history/025-tier2-closeout.deepseek.md` (Phase B, `backlog #17`).
Harness: `TEST/scripts/mo-trace/` (RT_SEL/MO_SEL/MO_NODES/MO_ORDER +
`align-traces.py`, see its README). Runs: B1 `l0-20260912T181102Z`
(closure fix), B2 `l0-20260912T205626Z` (multi-slot edge fix; B3-B5 are
harness/docs-only on top).

- **Topline: clean 96 -> 98, parity 0.800 -> 0.817, order 22 -> 20 raw /
  19 after the B5 `known-divergences.yaml` gnome-shell entry.**
- `app-text/texlive-core`, `dev-texlive/texlive-latex`: clean (docbook
  NS row moved to real's position by the installed-slot closure fix).
- `www-client/firefox` #29 -> #37, `mail-client/thunderbird` #30 -> #38
  (multi-slot atom resolution; next divergence is now the
  `nasm`/`freetype` Lever-1 timing family).
- `net-misc/networkmanager` moved one row (still divergent).
- Adjudicated residue (docs/025 §11): **F-B1** gtk:4 frontier drains in
  578 iterations vs real's 290 with identical node sets (the
  installed-chain timing proper); **F-B3** pre-bias insertion order
  diverges at index 1 (`baselayout, findutils, patch, eselect, ...` vs
  `baselayout, awk, bzip2, ...`) with identical parent counts -- real's
  `_create_graph` order; **F-B4** superseded installed in-edges
  (`nghttp2 -> systemd` survives in portuale; real drops it when
  polkit forces the same-slot merge) -- walk-order dependent. The last
  two are filed under #25; B1's residue stays #17.

## R1 run: `[use]`-dep unsat block, backlog #20 (2026-09-14, wave 1 of the Tier5/2 slicing)

Plan `docs/history/backlog_tier_5_and_2_sliced.opus.md` §6 R1. The slice is a
resolver *display* change (real's "no ebuilds built with USE flags to
satisfy" block in place of the bare `!!! no visible ebuild for
dependency` line) plus the parent-conditional row; no selection, order
or graph-shape change.

- Run `TEST/logs/l0-20260914T122346Z` (`l0-latest`), full 120 probes,
  portage 3.0.82.2.
- **Topline: clean 98, parity 0.817, explained 6, UNEXPLAINED 38
  (advice 9 / error 6 / flags 1 / order 19 / truncated 2 / version 1)**
  -- byte-identical to the previous full run `l0-20260913T180053Z`,
  with an empty diff on the unexplained-findings list (`diff` of the
  `###`/`  [` lines is empty). No new `[error]`/`[advice]` rows, no
  order movement: R1 is regression-free at real-tree scale.
- The one USE-unsat-shaped probe (`gnome-base/gnome-shell`'s
  `>=net-fs/samba-4.0.0[client]`) keeps its `gnome-shell-cluster-a-abort`
  allowlist entry unchanged: real aborts the whole resolve at that dep
  before any list (Total 453), while portuale's autounmask path flips
  `net-fs/samba` `client` on and keeps going (F-B5,
  `docs/history/025-tier2-closeout.deepseek.md` §11), so portuale never reaches
  R1's block for this probe. New fixtures cover the block instead
  (`test_or_group_use_unsat_*`, `test_use_unsat_*`).

## R3b run: pre-bias order = real's insertion order -- the `@system` seed sort (#25, 2026-09-15, wave 4 of the Tier5/2 slicing)

Plan `docs/history/backlog_tier_5_and_2_sliced.opus.md` §6 R3b; design
`docs/history/025b-complete-graph-nodes.md` §7. Real `_resolve` processes every
`SetArg`'s atom list `sorted(arg.pset.getAtoms(), key=str)`
(`depgraph.py:5500`); portuale expanded `@world`/`@system` in the
profile's `packages` order, so the `-pe @system` graph walk -- and the
pre-bias order the scheduler's stable `_merge_order_bias` preserves --
diverged from index 1. Sorting each appended `@system` segment (per
arg; explicit atoms keep the user's own order) is the whole fix.

Probe (`MO_ORDER`, post-prune/pre-bias; artifacts in
`TEST/logs/r3b-20260915T000000Z/`, commands in `probe.sh` /
`ptl-probe.sh`):

- real: container `emerge --pretend -e @system --debug` with
  `real-trace.py` patched in; portuale: `PORTUALE_MO_SEL=1 emerge
  --pretend -e @system --debug`.
- before R3b the 368-node sequences diverged at index 2 (`findutils,
  patch, eselect` vs `awk, bzip2, gzip`);
- after R3b: **368 == 368, three localized residues**
  (`mo_order.diff`, 34 diff lines): the `docbook-xml-dtd` 4.5/4.2 swap
  (real's own hash-seeded `_minimize_children` set order,
  `depgraph.py:4751-4781`), one `sys-apps/portage` version row
  (`3.0.81.3` vs `3.0.82.2`, the pre-existing stable-keyword visibility
  divergence), and a 12-node `virtual/man` `||`-bundle block 22
  positions later (disjunctive pop timing, R3c/d territory).

L0 (D4), same binary: full run `TEST/logs/l0-20260915T005709Z`
(`l0-latest`):

| | baseline `l0-20260914T122346Z` | after R3b |
|---|---|---|
| clean | 98 | **100** |
| parity | 0.817 | **0.833** |
| UNEXPLAINED | 38 | **36** |
| order | 19 | **17** |

Per-probe order flips (D4):

- fixed: `_system` #11 (`findutils` vs `make`), `_world` #14 (same),
  `MULTI_emptytree-system` #13 (`acct-group/adm` vs
  `app-misc/mime-types`) -- exactly the three F-B3 downstream rows the
  plan names;
- moved later: `MULTI_emptytree-system` now first diverges at #304
  (`sys-auth/pambase` vs `sys-apps/portage`);
- every other probe's order row is unchanged; the advice/error/flags/
  truncated/version counts are unchanged.

The new invariant pass (`check-invariants.py`, backlog #48) reports 18
portuale "merges after its owner" rows on these probes; a pre-R3b
subset run with the same harness (`TEST/logs/l0-20260915T011138Z`,
the seven affected slugs) reports the byte-identical 18, so the slice
adds none (they are the known order-divergence family plus cycle
breaks, #48/#17).

## R3c attempt — satisfied-at-add edge drop: STOPPED (2026-09-15, wave 6)

Plan `docs/history/backlog_tier_5_and_2_sliced.opus.md` §6 R3c; design
`docs/history/025b-complete-graph-nodes.md` §4.2. Two formulations were
implemented and measured against the F-B4 gedit oracle
(`emerge --pretend --update --deep --verbose-slot-rebuilds=y
app-editors/gedit --debug`, real vs portuale, probe kept in
`TEST/logs/r3c-20260915/probe.sh`) and a full L0 run; **both were
reverted** and the design note's own "documented uncertainties" check
is the reason.

Real's datum (`depgraph.py:4400-4425`): `inst_pkgs =
vardb.match_pkgs(atom)` and `mypriority.satisfied = inst_pkg` at
*add* time, and the child the edge points at is whatever
`_select_package` chose **at that instant** -- the installed instance
when no merge-bound node for that `cat/pkg` was in the graph yet, the
merge node otherwise. Real's gedit walk shows exactly that: nghttp2 is
inserted at `.order` position 9336, the systemd merge node at 9740, so
nghttp2's `>=sys-apps/systemd-209` was added while only the *installed*
systemd was in the graph; when polkit later forces the same-slot merge,
the installed node's in-edges are dropped and nghttp2 keeps only
`virtual/pkgconfig`.

- **Variant 1 (blunt):** drop every `satisfied_at_add` edge to a
  merge-bound child. gedit's nghttp2 edge disappears (its merge-list
  position moves to match real's relative order) and L0's
  `app-editors_gedit` #5 and `gnome-base_nautilus` #8 rows are fixed,
  **but** `_system`/`_world` regress to a #1 divergence
  (`baselayout` vs `gzip`) and `MULTI_emptytree-system` to #0: an
  all-installed-rebuild graph loses most of its edges. L0
  `TEST/logs/l0-20260915T051103Z`: clean 97, parity 0.808,
  UNEXPLAINED 39, order 20 (was 100 / 0.833 / 36 / 17).
- **Variant 2 (rank-gated):** drop only when the child's discovery
  rank in `build_digraph`'s DFS is *after* the parent's (the walk-order
  datum the note names). `_system`/`_world` are preserved, but gedit's
  nghttp2 is **not** fixed -- portuale's DFS places the systemd merge at
  rank 96 and nghttp2 at 274, the opposite of real's insertion order
  (nghttp2 9336 < systemd 9740); the gedit merge-list diff even grows
  from 10 to 12 lines (`TEST/logs/r3c-20260915/`). The DFS reproduces
  real's insertion order for the `-pe @system` probe after R3b's seed
  fix, but not for gedit's polkit/systemd/nghttp2 sub-walk.

The missing piece is the genuine per-atom insertion-order model
(`docs/025b` §4.3's first-class installed nodes and their walk
positions), i.e. **R3c and R3d are one change, not two**: an edge's
target (installed vs merge) is decided by real's insertion instant,
which portuale does not carry per entry. R3c stays open; the tree is
back at the R3b/H4 state, and the next attempt should start from the
gedit oracle above and the `.order` positions quoted here.

## R3c revalidation — F-B4 is an aborted re-walk, not "initially satisfied → no edge" (2026-09-15)

Revalidates the wave-6 stop above with fresh `--debug` oracles (real run
unbuffered with stdout+stderr merged, so the walk, the slot-conflict
solver and the digraph dump are in one ordered stream). Probe scripts,
`analyze.py`, the compressed real/portuale logs and `analysis.txt` are in
`TEST/logs/r3c-revalidate-20260915/`. Binary: `backlog/tier_2_e_5` @
`d8e4c11` (R3b/H4 state). Portage 3.0.82.2, `PYTHONHASHSEED=0`, L0 world.

**The stop was right, its diagnosis was wrong.** Neither "carry the
insertion instant per entry" nor merging R3c into R3d would fix gedit,
because real's mechanism is different:

1. `nghttp2`'s `>=sys-apps/systemd-209` does resolve to the *installed*
   systemd at add time (`Child: … systemd-260.1-r2-1 … installed`), and
   polkit's `sys-apps/systemd:0=[policykit]` later adds the merge in
   the same slot (`Slot Conflict:`).
2. After `_complete_graph`,
   `_solve_non_slot_operator_slot_conflicts` (`depgraph.py:1774-2100`)
   keeps the merge, `_remove_pkg`s the installed node (which drops
   **all** its in-edges and orphaned children), and pushes
   `broken_packages` (the removed node's parents plus the kept package)
   onto `_dep_stack` for a `_create_graph()` re-walk that re-creates
   each parent's edge, now pointing at the merge node.
3. In gedit that re-walk **returns 0 at `gnome-base/gnome-keyring`**:
   `>=app-crypt/gcr-3.27.90:0=[gtk]` is unsatisfiable (the same
   autounmask problem the probe ends with, rc 1). `_create_graph` stops,
   the solver ignores the return value, and every broken parent still on
   the stack is never re-walked: `nghttp2`, `sys-libs/pam`,
   `sys-apps/shadow`, `sys-auth/pambase` and `virtual/service-manager`
   lose their systemd edge, while the parents walked earlier (`gvfs`,
   `gcr`, `p11-kit`, `dbus`, `util-linux`, `procps`, …) get it back.
   Real's final digraph confirms both halves: `gvfs → systemd (merge)
   (buildtime_slot_op)` present, `nghttp2 → virtual/pkgconfig` only.
   nautilus is the identical shape (same removal, same abort at
   gnome-keyring's `gcr[gtk]`). Which parents survive is the order in
   which `list(set_of_Package)` comes off a LIFO stack, i.e. CPython
   string-hash and set-table order under `PYTHONHASHSEED=0`.
4. When the re-walk completes, real **redirects** the in-edges instead
   of dropping them. `net-misc/networkmanager` (rc 0; the solver removes
   installed systemd too) keeps `pam`, `shadow`, `pambase`,
   `virtual/service-manager`, `dbus`, `polkit`, `util-linux`, … → merge
   systemd edges. That is what portuale already builds, and it explains
   why the blunt drop broke `_system`/`_world`.

Scope check over all 18 L0 probes carrying an `[order]` row (baseline
`l0-20260915T005709Z`; 16 in the batch run plus gedit/nautilus above):

| check | result |
|---|---|
| slot-conflict solver re-walk aborted | only `app-editors/gedit`, `gnome-base/nautilus` (gnome-keyring `gcr[gtk]`) and `gnome-base/gnome-shell` (gnome-control-center; already the `gnome-shell-cluster-a-abort` known divergence), all real rc 1 |
| solver ran and re-walk completed | `kdecore-meta`, `networkmanager`, `vlc`, `wireshark`, `libreoffice`: edges redirected, same as portuale |
| scheduler node set (union of `RT_SEL`/`MO_SEL` picks, installed `n:` nodes included) | **identical on 16/18** (gedit 427 == 427, nautilus 474 == 474); `MULTI_emptytree-system` differs only by the known portage `3.0.81.3`/`3.0.82.2` visibility row, gnome-shell by its cluster-A `nasm` extra |

Consequences:

- **F-B4 is cluster-A abort residue** (the F-B5 family), not a walk-order
  model portuale is missing. gedit #5 / nautilus #8 can only be matched
  by emulating CPython set iteration order in an aborted re-walk. Real
  users run with a random hash seed, where real is itself
  nondeterministic here, so that would be parity with noise.
- **R3c's premise is false for completed re-walks** (item 4). Any
  "satisfied-at-add ⇒ no edge" rule regresses correct graphs, as
  variant 1 did.
- **R3d's node-set goal is already met at the scheduler level** (table
  above). The post-walk `add_installed_dependency_closure` produces
  real's node set on every order probe except those two known rows, so "installed nodes
  first-class" has no L0-visible payoff left. The other 15
  order rows (all but the three re-walk aborts) are frontier/edge timing with equal node sets,
  i.e. #17.

No code change. Proposed re-plan: `docs/history/backlog_tier_5_and_2_sliced.opus.md`
"Wave 6 revalidated".

## R3e′ run — #25 closed on evidence (2026-09-15, D7 option 1)

`TEST/compare/known-divergences.yaml` gains
`gedit-nautilus-cluster-a-rewalk-abort` (`[order]` only, slugs
`app-editors_gedit`/`gnome-base_nautilus`, `match: "real net-libs/nghttp2
vs portuale"`). Two-probe subset `L0_SKIP_MULTI=1` run
`TEST/logs/l0-20260915T063724Z` (same binary as the baseline):

- both `[order]` rows are now explained by the new entry;
- the `[advice]`/`[error]` rows (real-only `gcr[gtk]` masked block) are
  unchanged and still unexplained: that is portuale's separate
  masked-dependency disclosure gap, not #25;
- portuale's raw output for both slugs is byte-identical to
  `l0-20260915T005709Z`, and the 3 invariant rows are the same 3 as in
  that baseline.

Net effect on the full-run baseline: order unexplained 17 -> 15, with
clean/parity unchanged (both probes stay non-clean on advice/error).
Residues filed under backlog #25: `virtual/man` bundle timing, the
`docbook-xml-dtd` hash swap, and the 023 `btnr` resolver-level
installed-instance case.

## R4 run — #35 `downgrade_probe` guards are L0-neutral (2026-09-15, wave 7)

Full run `TEST/logs/l0-20260915T070018Z` with the #35 guards
(`alternative_downgrade_demoted`) and the residual-conflict consumer skip:

| | `l0-20260915T005709Z` (pre-R3e′/R4) | after R3e′ + R4 |
|---|---|---|
| clean | 100 | 100 |
| parity | 0.833 | 0.833 |
| UNEXPLAINED | 36 | **34** |
| order | 17 | **15** |

The 17 -> 15 drop is R3e′'s `gedit-nautilus-cluster-a-rewalk-abort` entry.
Portuale's raw output is **byte-identical** to the baseline on all 120
probes (the multi-graph runs included), and the invariant rows are
unchanged: neither guard fires on the real tree at these probes. The
change is pinned by the fixture oracle
`test_or_choice_avoids_downgrade_into_the_graphed_update` instead.

## R5 — #17 closed as a deliberate cut (2026-09-15, D5 600 s time box, wave 8)

Plan `docs/history/backlog_tier_5_and_2_sliced.opus.md` §6 R5.

**Step 1 (re-measure after R3):** R3b (`9284f8c`, landed wave 4) sorts
each `SetArg`'s atoms the way real `_resolve` does
(`depgraph.py:5500`), but only in `pretend.rs`'s
`expand_top_level_atoms` -- the explicit `@system`/`@world`
top-level-argument path. Re-ran the `gui-libs/gtk:4` mo-trace pair
against the current binary (traces in
`TEST/logs/r3c-revalidate-20260915/{real,ptl}.gui-libs_gtk_4.log.xz`,
captured post-R3b during the wave-6 revalidation): **unchanged** --
real 290 iterations, portuale 578, node sets equal (398 == 398), first
divergence still at iteration 1's greedy batch
(`align-traces.py`: real position 9 `sys-libs/zlib-1.3.2-r1`, portuale
position 9 `app-misc/mime-types-2.1.54`, zlib pushed to ~39). Premise
disproved -- R3b's fix does not reach this code path.

**Step 2 (one lever, D5's time box):** the same unsorted-profile-order
seed survives in a second place: `merge_order.rs`
`add_installed_dependency_closure`'s complete-mode "Seed 1b" walks
`system_atoms` (`config.system_packages`) in raw profile order to add
every installed `@system` member as a graph node -- this fires on
*every* complete-mode probe (gtk:4, kdecore-meta, networkmanager,
firefox, ...), not just an explicit `@system`/`@world` argument. Ported
the identical fix: sort `system_atoms` before the seed loop, matching
real's own `sorted(arg.pset.getAtoms(), key=str)`.

**Result: zero effect, rigorously verified.**

- 14-probe container re-run (the full order-divergent set minus
  `MULTI_emptytree-system`, `TEST/logs/r3c-revalidate-20260915/` peers)
  against real: every probe's first-divergence position and merge-list
  *set* are byte-identical to the pre-fix run (`mergelist_cmp.py`).
- Full L0 `l0-20260915T073208Z`: clean 100, parity 0.833, UNEXPLAINED
  34, order 15 -- identical on every metric to the pre-fix
  `l0-20260915T070018Z`, and portuale's raw output is byte-identical on
  all 120 probes (diff against the baseline `portuale/` tree and
  `invariants.txt` both empty).

**Conclusion.** The lever is real (it makes `add_installed_dependency_
closure` match real's actual seeding order) but doesn't touch the wall:
`zlib`'s ancestor chain is reached identically either way, so
sorting the @system *seed* atoms can't reorder something the DFS
discovers through an entirely different path first. This confirms the
2026-09-09 "Deeper dig" conclusion above: the divergence is
**installed-nomerge-node drain timing** inside `_serialize_tasks`
itself -- which installed node's own dependency chain clears a round
earlier or later than real's -- not an edge, priority, discovery-order,
or seed-order bug. Pinning it down needs per-node drain tracing across
the ~400-node graph (the same wall slices 4-12 already hit across
several prior sessions).

**Lever table (D5):**

| lever | probes flipped | evidence |
|---|---|---|
| R3b `@world`/`@system` top-level-arg seed sort | 0 (already landed for a different symptom, F-B3) | confirmed no effect on the F-B1 family by direct re-measurement |
| R5: complete-mode `@system` closure seed sort (Seed 1b) | 0 | 14-probe container run + full L0, byte-identical |

**Disposition:** #17 closed as a **deliberate cut**
(`docs/backlog-tasks.md` #17). The seed-sort fix is kept (harmless,
verified L0-neutral, genuinely closer to real's own semantics) but
does not reopen the item. Next investigation, if resumed: per-node
`_ignore_optional` drain tracing across the full installed closure,
comparing exactly which installed node stays non-leaf longer in real
vs portuale at the iteration where the two frontiers first pick
differently -- the `PORTUALE_MO_SEL` harness (`merge_order.rs::
select_nodes`) plus `TEST/scripts/mo-trace/align-traces.py` already
support this; what's missing is the multi-session budget the prior
"Deeper dig" note estimated it needs.


## #48 — invariant triage: 18 -> 1, one filed bug (2026-09-15, wave 9)

Full run `TEST/logs/l0-20260915T092215Z` (`TEST/run/l0-resolver.sh`,
portage 3.0.82.2, same binary as the R5 baseline): comparator clean 100 /
parity 0.833 / UNEXPLAINED 34 (unchanged), and the invariant pass
reproduces the documented **18** portuale "merges after its owner" rows
with 0 control violations on real's own output.

**The recorded lead was wrong.** The 18 children are `dev-lang/go`,
`app-crypt/gcr`, `dev-lang/rust-bin`, `llvm-core/{clang,llvm}`,
`kde-plasma/{xdg-desktop-portal-kde,polkit-kde-agent}` and
`app-crypt/gocryptfs`; the new `vdb-list.txt` snapshot (320 installed
packages) contains **none** of them, so the `installed_child` exemption
was not what fired. It could not have: `check_json`'s
`any(entries[k]["outcome"] != "new")` clause was always false here, and
the missing vdb made the second disjunct unreachable -- but with the
snapshot in hand, neither would have helped, because the children are
not installed at all. The two real gaps are:

1. **cp-level edges vs per-instance constraints (14 of 18).** `--json`
   `required_by` aggregates by `cat/pkg`, while real's constraint is per
   package instance: `gcr-4.4` carries an edge `gcr-3.41` already
   satisfied against `gvfs` (same for `clang:22` vs `clang:21` against
   `libclc`/`mesa_clc`/`botan`, `llvm:22` vs `llvm:21` against
   `spirv-llvm-translator`, `rust-bin:1.96` vs `1.94` against
   `cbindgen`). `check_json` now exempts an edge when another
   merge-bound instance of the child cp merges before the owner -- the
   mirror of its existing multi-slot-owner rule. 14 rows gone.
2. **soft edges (3 of 18).** `check-invariants.py` passed no repo
   roots, so the checker could not tell an ordering edge from one real's
   `_serialize_tasks` `ignore_priority` ladder may legally drop
   (`DepPriorityNormalRange`: runtime/`RDEPEND` and runtime_post/
   `PDEPEND`; `ignore_medium` is `_ignore_runtime`). The container now
   snapshots `dep-classes.tsv` (8909 `(child, owner)` edges: which
   md5-cache variables name the child), and the checker exempts
   RDEPEND/PDEPEND-only edges. This explains `plasma-integration`'s
   PDEPEND on `xdg-desktop-portal-kde`, `kauth`'s PDEPEND on
   `polkit-kde-agent` and `plasma-vault`'s RDEPEND on `gocryptfs`.
   `vdb-list.txt` (installed cps) is recorded the same way, so the
   `installed_child` exemption is now available on L0 too.

**What remains is one genuine portuale order bug** (`app-containers_podman`):

    portuale app-containers_podman: merge order: dev-lang/go (#100)
      merges after its owner dev-go/go-md2man (#94)

`go-md2man`'s md5-cache says `BDEPEND=>=dev-lang/go-1.24.11:=` (hard,
never ignorable), portuale's own `--tree` output nests go *under*
go-md2man, and real merges go first -- but portuale's merge list puts go
at #100, after its dependent at #94. Filed as backlog **#53**; a fixture
repro is blocked on the probe's whole-resolution divergence (real Total 8
/ portuale Total 102, the `[truncated]` autounmask row), so the next
step is a minimal `:=`-BDEPEND fixture against the slot-operator bind
path, not this 102-entry graph.

Commands and result:

    TEST/run/l0-resolver.sh
    # -> TEST/logs/l0-20260915T092215Z (vdb-list.txt, dep-classes.tsv)
    python3 TEST/compare/check-invariants.py TEST/logs/l0-20260915T092215Z
    # -> invariants: 120 probes, 1 portuale violation(s) (the #53 row),
    #    0 control violation(s) on real output
    python3 -m pytest tests/test_output_invariants.py -q -k "checker or vdb"
    # -> 12 passed (each exemption and each past bug class has a self-test)

## #53 S0 — minimal fixture repro + bisection (2026-09-15)

`docs/06.053-merge_order.opus.md` planning built an 8-package scratch
repro (`r53*` names) mirroring the real `dev-lang/go` /
`dev-go/go-md2man` / `app-containers/{conmon,containers-common,
containers-storage,podman}` chain; S0 commits it under neutral names in
`fixtures/repo` and minimizes it further.

**Committed fixtures** (`fixtures/repo/{dev-lang,dev-go,app-misc}/mo*`):

Failing shape (`app-misc/mopodman`, 7 packages -- `app-misc/mostorage`
from the scratch repro turned out to be inessential, see bisection
below):

| package | deps |
|---|---|
| `dev-lang/mogo-1.26` | `BDEPEND="\|\| ( >=dev-lang/mogo-1.24 >=dev-lang/mogoboot-1.24 )"` |
| `dev-lang/mogoboot-1.24` | — |
| `dev-go/momd2man-2.0` | `BDEPEND=">=dev-lang/mogo-1.24:= app-misc/mounzip"` |
| `app-misc/mounzip-6.0` | — |
| `app-misc/moconmon-2.1` | `BDEPEND="dev-go/momd2man"` |
| `app-misc/mocommon-0.64` | `BDEPEND=">=dev-go/momd2man-2.0"` |
| `app-misc/mopodman-5.8` | `BDEPEND="dev-go/momd2man >=dev-lang/mogo-1.24:= app-misc/mounzip"`, `DEPEND`=`RDEPEND`=`"app-misc/moconmon app-misc/mocommon"` |

`portuale emerge -p app-misc/mopodman`:

    [ebuild  N     ] app-misc/mounzip-6.0
    [ebuild  N     ] dev-lang/mogoboot-1.24
    [ebuild  N     ] dev-go/momd2man-2.0        <- #2
    [ebuild  N     ] app-misc/moconmon-2.1
    [ebuild  N     ] app-misc/mocommon-0.64
    [ebuild  N     ] dev-lang/mogo-1.26         <- #5, after its owner
    [ebuild  N     ] app-misc/mopodman-5.8

`tests/output_invariants.py` `check_json` on the `--json` run confirms:
`merge order: dev-lang/mogo (#5) merges after its owner dev-go/momd2man
(#2)` -- the same shape as the live #48 finding.

Nearest-passing shape (`app-misc/mopodmanok`, same chain but `mogo`'s
BDEPEND drops the self-branch, `>=dev-lang/mogoboot-1.24` only; shares
the `mogoboot`/`mounzip` leaves): merge order is `mounzip, mogoboot,
mogook, momd2manok, moconmonok, mocommonok, mopodmanok` -- `mogook`
correctly before `momd2manok`, `check_json` reports no problems.

**Bisection** (one change at a time from the failing shape; position of
md2man vs go, 0-indexed as portuale prints them):

| change | md2man | go | bug? |
|---|---|---|---|
| baseline (7 pkgs) | 2 | 5 | **yes** |
| drop `app-misc/mostorage` + its RDEPEND edge from the 8-pkg scratch repro | 2 | 5 | yes -- **mostorage is inessential**, dropped from the committed fixture |
| drop the self-branch from `mogo`'s BDEPEND (`>=mogoboot-1.24` only) | 3 | 2 | **no** -- this is the nearest-passing shape |
| drop `moconmon` entirely (podman `DEPEND`/`RDEPEND` only `mocommon`) | 3 | 2 | **no** |
| drop `mocommon` entirely (podman `DEPEND`/`RDEPEND` only `moconmon`) | 3 | 2 | **no** |

So the bug needs **both**: go's own `||` self-branch, *and* at least two
separate packages that each hard-`BDEPEND` the md2man-analogue and are
both required by the top-level atom. A single BDEPEND-consumer chain
(podman `BDEPEND=go`, `RDEPEND=conmon` only) is not sufficient, matching
the planning doc's note in `docs/06.053-merge_order.opus.md` §1.

Real's own behaviour on this shape is not captured yet: it requires the
container fixture-oracle run (`TEST/run/l0-fixture-oracle.sh`), which
needs `sudo TEST/create-container.bash` to build
`localhost/test-portuale:latest` first (downloads a Gentoo stage3
tarball + repo clones) -- not run in this session; see the plan's `G1`
note and the live #48 evidence above for what real does on the
*unminimized* graph (a `* Error: circular dependencies:` self-loop on
`dev-lang/go`, not a merge list). Both shapes are recorded in
`TEST/atomlists/l0-fixture-oracle.txt` for when that run happens.

Commands:

    export PORTAGE_CONFIGROOT=$PWD/fixtures ROOT=$PWD/fixtures \
           PORTAGE_RUNNING_ROOT=$PWD/fixtures DISTDIR=$PWD/fixtures/distfiles
    rust/target/release/emerge -p app-misc/mopodman     # bug
    rust/target/release/emerge -p app-misc/mopodmanok   # clean
    rust/target/release/emerge -p --json app-misc/mopodman | \
      python3 -c 'import json,sys; sys.path.insert(0,"tests"); \
        import output_invariants as inv; \
        print(inv.check_json(json.load(sys.stdin), args=["--json"]))'

## #53 S1 — root cause: `alt_suppressed` re-derives the `||` branch instead
of trusting the resolver's own choice (2026-09-15)

**H1 confirmed, H2 ruled out.** `PORTUALE_DEBUG_MERGE_GRAPH=1` on the S0
failing fixture dumps the actual scheduling digraph:

    (dev-lang/mogo-1.26:0/1.26::testrepo, ebuild scheduled for merge) depends on
      (dev-lang/mogo-1.26:0/1.26::testrepo, ebuild scheduled for merge) (buildtime)
      (dev-lang/mogoboot-1.24:0/1.24::testrepo, ebuild scheduled for merge) (runtime)

`mogo` carries a genuine **`mogo -> mogo` buildtime self-edge** (H1's
predicted smoking gun), and its edge to `mogoboot` -- the branch the
*actual resolver* chose (`b5b256f`, L0 finding D: "fresh resolve must
pick bootstrap") -- has been demoted to a weak `runtime` fallback edge
instead of the real `buildtime` one.

**Mechanism**, `rust/portage-repo/src/merge_order.rs` `build_digraph`'s
`alt_suppressed` computation (~1442-1500, pre-fix): for each `||` group
this is a **second, independent** re-derivation of "which branch wins",
run purely for scheduling/edge purposes, using the same `all_graph` >
`all_inst` > `all_any` preference order as real `dep_zapdeps`/
`atoms_all_in_graph` -- but with no knowledge of the resolver's own
`circular_self` exclusion (`b5b256f`, lib.rs's `resolve_pretend`/
`enqueue_dependencies`). For `mogo`'s own BDEPEND `|| ( >=mogo-1.24
>=mogoboot-1.24 )`: branch 0's atom `>=dev-lang/mogo-1.24` resolves via
`cp_indices` straight back to `mogo`'s **own entry** (the only
`dev-lang/mogo` graph node is itself), which trivially counts as
`graph_m = true` -- so branch 0 (written first) wins `all_graph` over
branch 1 (`mogoboot`, also `all_graph`-true, but never reached: `pick`
takes the first `true` by ascending branch number). Branch 1 is
suppressed, dropping the real `mogo -> mogoboot` forward edge; the
`required_by` fallback loop (~1671-1703) re-adds it anyway, but at a
blanket `runtime` priority (that loop has no per-edge key information),
and the suppressed self-branch (edge 0) survives into the "Edges" loop
where the self-edge special case (`depgraph.py:3765-3770`, kept only for
an *unsatisfied build-time* self-dep) keeps it: `sat = satisfied(edge,
Some(mogo))` is `false` (mogo isn't installed), so `i == j &&
!(buildtime && !sat)` is `false` -- the self-edge is **added**, not
dropped.

**Why this misorders instead of erroring**: real's *actual* dependency
resolution (`dep_zapdeps`, run once, feeding both the merge decision and
the scheduling graph) picks the self branch too, and then aborts with
the literal `* Error: circular dependencies:` self-loop shown in the
live #48 evidence -- real never reaches `_serialize_tasks` on this
shape. Portuale's resolver (`resolve_pretend`/`enqueue_dependencies`)
deliberately special-cases exactly this pattern to avoid that abort
(`b5b256f`) and picks bootstrap, producing a *valid* graph -- but
`merge_order.rs`'s `alt_suppressed` never learns which branch that was
and mechanically re-derives its own, inconsistent answer.

**Why `mogo` lands after `md2man`/`moconmon`/`mocommon` specifically**
(`PORTUALE_MO_SEL=1` trace, `select_nodes`):

    MO_SEL iter=1 ... pick=m:app-misc/mounzip-6.0 m:dev-lang/mogoboot-1.24
    MO_SEL iter=3 ... drop_satisfied=1 pick=m:dev-go/momd2man-2.0
    MO_SEL iter=4 ... drop_satisfied=0 pick=m:app-misc/moconmon-2.1 m:app-misc/mocommon-0.64
    MO_SEL iter=6 ... drop_satisfied=1 pick=m:dev-lang/mogo-1.26
    MO_SEL iter=7 ... pick=m:app-misc/mopodman-5.8

`Digraph::is_leaf`/`child_nodes` (merge_order.rs ~522-537) apply the
`ignore_priority` filter uniformly, with **no self-edge exclusion**: a
plain unsatisfied `buildtime` priority is never dropped by any rung
(including the widest `DepPrioritySatisfiedRange.ignore_medium`), so
`mogo` never satisfies `is_leaf` at *any* ignore level and never enters
`find_smallest_cycle`'s `mergeable` set. `md2man`'s edge to `mogo` is
`buildtime_slot_op`, which *is* droppable at the widest rung -- so once
`unzip`/`mogoboot` (true leaves) are gone, `md2man` becomes `mergeable`
and `find_smallest_cycle` releases it (a size-1 `gather_deps` closure,
confirmed by `--debug`: no `runtime cycle digraph` line at all, since
`sub.len() == 1` never triggers that dump); `moconmon`/`mocommon` then
become literal leaves once `md2man` is gone (iter 4, strict rung, no
relaxation needed). By iter 6 only `{mopodman, mogo}` remain and `mogo`
*still* never satisfies `is_leaf` at any level (its self-edge is
permanent) -- so it is picked only by `select_nodes`' absolute last
resort (~2440-2448, `.or_else(|| g.order.iter().copied().find(|&i|
g.alive[i]))`, "fall back to plain bias order when even [the widest
filter] finds nothing"), landing it *after* every package whose edge to
it could legitimately relax.

**Fix (G1(a) chosen, see below)**: `alt_suppressed`'s branch-preference
loop must not let a `||` alternative count as "in graph" via a match
against the **owning entry's own index**, the same `circular_self`
carve-out `b5b256f` already applies to the actual resolve -- so the
independent re-derivation agrees with the resolver's real choice instead
of re-discovering (and losing) it. Minimal, localized: guard the inner
`for &j in idxs` loop in `alt_suppressed` (merge_order.rs ~1469) with
`if j == i && !g.installed[j] { continue; }` (`i` = the owning entry's
own index, added via `.enumerate()`). Does not touch `atoms_all_in_graph`
(lib.rs) or the resolver's own `circular_self` check -- this is a
second, independent occurrence of the same class of bug, in a different
function, not a shared root cause.

**G1 decision**: option (a) (ordering consistency, keep portuale's
bootstrap resolution, fix the merge-list inconsistency) -- not (b) (full
parity, matching real's circular-dependency abort), per the plan's own
default and rationale (reopening finding D's `b5b256f` fix is a
separate, larger change). No blocking question for the owner: the fix
is a straightforward consistency bug, not a design choice.

## #53 S3 — L0 + fixture-oracle verification (2026-09-15)

Container built (`sudo TEST/create-container.bash`, cached stage3 +
local `gentoo`/`buildovl` mirrors -- no fresh network fetch needed).

**Fixture oracle** (`TEST/run/l0-fixture-oracle.sh`, S0's two new
atoms): the *first* fixture-oracle case with a BDEPEND chain exposed two
pre-existing gaps in the harness itself, neither a portuale bug:

1. `stage.sh`'s "categories real will accept" step wrote only
   `/etc/portage/categories` (a local-config file). Real EAPI 7+ resolves
   a BDEPEND against the *running root*'s own settings object, built
   with `local_config=False` (deliberate upstream isolation from the
   target root's `/etc/portage`) -- so it never saw those categories at
   all and errored `'<cp>' has a category that is not listed`. Fix:
   also write each staged repo's own `profiles/categories` (unconditionally
   read regardless of `local_config`).
2. `in-container.sh` never set `PORTAGE_RUNNING_ROOT`, so *both* real and
   portuale resolved BDEPEND against the container's actual `/` --
   invisible until now because no earlier fixture-oracle case had a
   BDEPEND at all. Fix: pin it to the staged tree, mirroring portuale's
   own contract-test `fixture_env` fixture (`tests/conftest.py`).

After both fixes: `UNEXPLAINED: 0`, `clean: 7` (was 5, both #53 atoms
now clean). Real's raw output for `app-misc/mopodman` additionally shows
each BDEPEND-only consumer (`moconmon`/`mocommon`/the top-level
`mopodman` itself) twice -- once plain, once suffixed `to
<running-root-path>` (real's own build-root merge-task display,
portuale doesn't render the second copy) -- a pure presentation
difference the comparator already tolerates by cp/slot identity; **the
real, deduplicated merge order is byte-identical to portuale's**:

    mounzip, mogoboot, mogo, momd2man, moconmon, mocommon, mopodman

**Also notable**: on this isolated, minimized fixture, real does **not**
hit the circular-dependency abort the live #48 evidence showed -- it
resolves the bootstrap branch cleanly too, same as portuale (before and
after the fix, since this repro never triggers the wrong branch in
real's own `dep_zapdeps`). The live abort is a discovery-order effect
specific to the full ~100-package graph (`go` becoming "already in
graph" via an earlier edge before its own `||` is evaluated), not a
universal consequence of the `|| ( self bootstrap )` shape -- confirms
G1(a)/G1(b) are genuinely independent: fixing G1(a) here required no
change to real-parity expectations at all on this fixture.

**Live L0** (`TEST/run/l0-resolver.sh`, real Gentoo tree,
`L0_SKIP_PORTAGE_UPGRADE=1`): scoped probe
`app-containers/podman` (the original #48 evidence) --

    invariants: 1 probes, 0 portuale violation(s), 0 control violation(s) on real output

(previously 1 portuale violation, the "merges after its owner" row).
The probe's pre-existing `[truncated]` autounmask-backtracking
divergence (real Total 8 / portuale Total 43, finding A) is unrelated
and unaffected. Full-atomlist L0 sweep: run in background, see follow-up
note if numbers move.

Commits: harness fixes (`stage.sh`, `in-container.sh`) land with this
finding; no portuale source change in S3.

## #53 close-out (2026-09-15)

All four slices done: S0 minimal fixture + bisection (`e05fd28`), S1
root cause (`08bc8df`), S2 fix + pins (`a5921eb`), S3 verification
(`dbeea82`). The backlog's original `:=`-slot-operator/F-A1 attribution
for this bug was wrong; the actual mechanism is `merge_order.rs`'s
`alt_suppressed` re-deriving a `||` branch choice independently of the
resolver's own `circular_self` exclusion (`b5b256f`). Confirmed against
real Portage on both the fixture oracle (byte-identical deduplicated
merge order) and the live `app-containers/podman` L0 probe (0 invariant
violations, was 1). G1(b) (full parity with real's circular-dependency
abort on a graph where `go` genuinely becomes self-referential) stays
open as a candidate follow-up against finding D, not filed as a new
numbered item since S3 showed it does not reproduce on any repro built
so far -- revisit only if a new live probe exhibits it. Backlog:
`docs/backlog-tasks.md` #53. Memory: `merge-order-self-branch-edge`.

**Full-atomlist L0 sweep landed** (`TEST/logs/l0-20260915T152916Z`,
120 probes, `L0_SKIP_PORTAGE_UPGRADE=1`): `invariants: 120 probes, 0
portuale violation(s), 0 control violation(s)`. `app-containers/podman`
now carries only its pre-existing, unrelated `[truncated]` autounmask
finding (finding A) -- the merge-order row is gone. Comparator summary:
clean 100/120, parity 0.833, UNEXPLAINED 32 (15 `order`, all
already-tracked cluster-I residuals -- see `cluster-i-merge-order`
memory; none new). Net improvement over the last recorded L0 baseline
(clean 96, parity 0.800): satisfies G3 (order metrics may move, net not
worse). #53 is closed with no known regressions.

## #65 S0 — slot-operator rebuild scan vs `--with-bdeps`: the oracle matrix (2026-09-16)

Fixtures (`fixtures/repo/dev-libs/`): `provpkg-1.0` (`SLOT="0/1"`) vs
`provpkg-2.0` (`SLOT="0/2"`), plus five installed consumers with the
tree ebuild carrying `dev-libs/provpkg:=` in exactly one key each:
`consrdep` (RDEPEND), `consdep` (DEPEND), `consbdep` (BDEPEND),
`conspdep` (PDEPEND), `considep` (IDEPEND). The staged vdb variant lives
in `TEST/layers/l0-fixture-oracle/slotop-bdeps/` (knob `FX_SLOTOP_BDEP=1`
in `stage.sh`); atom list
`TEST/atomlists/l0-fixture-oracle-slotop.txt`. Full capture:
`TEST/logs/l0-fx-20260916T152216Z/`.

**The staged vdb dep must be real's versioned built atom,
`>=dev-libs/provpkg-1.0:0/1=`.** With a bare `dev-libs/provpkg:0/1=`,
real's dynamic-deps apply drops the binding (the tree ebuild's unbound
`:=` supersedes it) and real never rebuilds; with the versioned form
(the shape real itself records, e.g. runc's
`dev-lang/go-1.24.11:0/1.26.5=`) real rebuilds through the `:=` slot-op
dep. An `EAPI=8` file is also required in the staged vdb entry: without
it real parses `SLOT=0/1` as EAPI-0 and shows the installed instance as
`0/0`. Both were found the hard way in the ad-hoc container probes; the
bed's staging fragment carries the working form.

Real 3.0.82.2 behaviour (container, same staged tree as portuale), by
dep key and option set, `-uDvN`:

| key | bdeps off (`--usepkg`) | bdeps on (default / `--with-bdeps=y`) |
|---|---|---|
| RDEPEND | rebuild | rebuild |
| PDEPEND | rebuild | rebuild |
| IDEPEND | rebuild | rebuild |
| DEPEND | **no rebuild** | rebuild if argument/set-reached, **no** if world-only |
| BDEPEND | **no rebuild** | rebuild if argument/set-reached, **no** if world-only |

- The `@world` shape rebuilds all five keys with bdeps on, and the three
  runtime/install-time keys with bdeps off. So the gate is exactly
  `DEPEND`/`BDEPEND`.
- The argument shape (`--oneshot <consX>`, the other four world-only)
  with bdeps on rebuilds the argument-reached build-time consumer plus
  the runtime/install-time world-only consumers; with bdeps off, a
  DEPEND/BDEPEND argument produces **no merge list at all** (the
  provider is only reachable through the build-time dep).
- `--dynamic-deps=n` (control, out of #65's scope) makes real rebuild
  every key in `@world`; `--ignore-built-slot-operator-deps=y` behaves
  like the default. Portuale's vdb scan is *not* a dynamic-deps model,
  so that control is not a standard bed case (run it manually against
  `TEST/atomlists/l0-fixture-oracle-slotop.txt`; #26 owns that gap).
- The `--debug` dump confirms the mechanism rather than a key rule:
  post-backtrack `_slot_operator_deps` entries carry
  `buildtime_slot_op`/`runtime_slot_op`/`runtime_post` only for the
  consumers the walk actually reached with a non-optional priority; the
  installed ones that stay `optional` are never probed (real
  `_slot_operator_trigger_reinstalls`, `depgraph.py:3089-3132`).

Two residues the matrix exposed, not owned by #65:

1. **Runtime world-only consumers are partially graph-completion
   sensitive.** In the argument shape with bdeps off, real rebuilds
   `conspdep`+`consrdep` when either one is the argument (and
   `considep` too when the argument is `consdep`/`consbdep`/`considep`)
   but *not* `considep` when `consrdep`/`conspdep` is the argument.
   That is a real graph-completion quirk around IDEPEND, not a clean
   `want_update` rule; #65 S2 narrows only the build-time keys, exactly
   as the plan says, and leaves the runtime keys unscanned. If it is
   pursued it belongs next to #24's trigger work.
2. `--dynamic-deps=n` merge lists remain portuale-divergent (#26's
   parked dynamic-deps append; not #65).

Portuale before S1: rebuilds all five keys under every option set and in
every shape (`TEST/logs/l0-fx-20260916T152216Z/l0-report.txt`: 49
unexplained findings across the 19 cases). After S1 the world
`--usepkg` cell matches and the argument/bdeps-off cells collapse to the
same sets real produces; the remaining build-time world-only extras are
S2's scope.

### #65 S1/S2 results

Both slices changed the scan; the `FX_SLOTOP_BDEP=1` atom list is the
regression gate.

- S1 (`with_bdeps` parameter, DEPEND/BDEPEND gated): 49 unexplained →
  34 (`l0-fx-20260916T153110Z`). The `@world --usepkg` cell's merge set
  becomes exactly real's (runtime three).
- S2 (build-time keys only for walked consumers): 34 → 22
  (`l0-fx-20260916T153720Z`). Only *extra* findings left: the
  `considep` residue in the six `consrdep`/`conspdep` argument cells;
  every other delta is the comparator counting real's duplicated
  `[ebuild rR]` listing (totals rows).
- With the two adjudicated allowlist entries
  (`slotop-bdeps-rebuild-totals`, `slotop-idepend-world-only-arg-consumer`)
  the run is **green**: `l0-fx-20260916T153958Z`, 22 explained /
  0 unexplained, rc 0.
- Host `-uDpvN @world` with host defaults: the 19 Go consumers and
  `dev-lang/go`'s `r` are gone; sorted-name diff vs the post-#63 capture
  is exactly `removed = the 19 Go consumers`, `added = []`.

## #68 S0 — soft blocks vs a replaced version: cells a–h (2026-09-16)

Fixtures (`fixtures/repo/dev-libs/`): `bparent` (`RDEPEND="!<dev-libs/blocked-2.0"`),
`bparent3` (`!<dev-libs/blocked-3`), `blocked-1.0`/`-2.0` (`IUSE="+flip"`),
`bconsumer` (`RDEPEND="dev-libs/blocked"`). Captured in the container
(real 3.0.82.2) with a staged vdb per cell; portuale before the fix.

| cell | shape | real | portuale (pre-#68) |
|---|---|---|---|
| a | installed blocked-1.0:0, graphed upgrade to 2.0:0, bparent merging | **no block row, rc 0** | `[blocks B]` row, rc 0 |
| a′ (host) | as a + installed `bconsumer` walked | no block row, rc 0 | `[blocks B]` + `(1 unsatisfied)` + Error, **rc 1** |
| b | same-version `Reinstall` (USE flip, pinned `=blocked-1.0`), bparent merging | `B`, `(1 unsatisfied)`, rc 1 | `B` row but rc 0 |
| c | installed blocked-1.0 survives, walked consumer, bparent merging | `B`, rc 1 | `B`, rc 1 (for the wrong reason: vdb scan) |
| d | as c, consumer installed but **not** walked (`--oneshot bparent`) | `b` + `[uninstall] blocked-1.0`, rc 0 | `B`, rc 1 |
| e | merge-bound `blocked-2.0` (New) matching `!<blocked-3`, bparent3 merging | `B`, rc 1 | `B` row but rc 0 |
| f | installed `bparent` (nomerge) walked, installed blocked-1.0, no merge-bound match (`-uD @world`, `=blocked-1.0`) | **no block row, rc 0** | no block row, rc 0 (MATCH) |
| g | as f + merge-bound `blocked-1.5` matching the atom (pinned) | `B`, rc 1 -- the owner is an `@world` member, so it **has** the set as a graph parent | no block row, rc 0 -- **residue, #73** |
| h | strong `!!` variants of a and a same-slot self-block | `overlap.forbid`: same-slot skip does not apply | covered by Rust unit tests only (not container-captured) |

Other confirmed facts from the captures:

- Real also prints a fake-nomerge `[uninstall     ] blocked-1.0` row in
  cells d (and for the fixture `blockerpkg`/`blockerorderpkg` shapes);
  portuale does not model uninstall rows yet — filed as #72.
- The blocked fixture shapes after the fix: `emerge -p
  dev-libs/blockerpkg` → `b` + rc 0 (was `B` + rc 1), `dev-libs/
  blockerorderpkg` → `b` + rc 0, `dev-libs/graphblockerparent` → `B` +
  rc 1 (was rc 0). The container captures for those three are the
  oracle for the moved contract pins.
- `--oneshot bparent` in cell d must not carry `-D`: with `--deep` the
  complete-mode walk pulls the consumer in and real reports `B` again
  (an earlier capture used `-uDvN` and measured that, not cell d).

Residues filed: #72 (`[uninstall]` rows) and #73 (set-argument parents
are not counted by the nomerge-owner graph-parents test, cell g).

## #67 S3 — USE prefix wildcard: container oracle and host re-diff (2026-09-16)

New fixture `dev-libs/videodrv-1.0` (`IUSE="+video_cards_dummy
video_cards_intel"`). The shared fixture profile is never mutated: the
contract pin copies the whole tree and appends the wildcard line to the
copied leaf profile only.

Container oracle (real 3.0.82.2), installed vdb `USE="video_cards_intel"`,
`emerge -p --newuse --oneshot dev-libs/videodrv`:

| cell | real | portuale |
|---|---|---|
| stock fixture profile (`VIDEO_CARDS="nvidia"`) | `[ebuild R] … VIDEO_CARDS="dummy* -intel*"` | same (MATCH) |
| copied profile + `VIDEO_CARDS="-* intel"` | no merge list (rc 0) | same (MATCH) |

Host re-diff (`docs/evidence/2026-09-16-post63-probes/` baseline plus the
#65/#67 fixes): `-uDpvN @world` drops exactly
`media-gfx/sane-backends-1.3.1-r2`, `x11-base/xorg-drivers-21.1-r2`,
`x11-drivers/xf86-video-dummy-0.4.1-21`, `xf86-video-fbdev-0.5.1-15`,
`xf86-video-vesa-2.6.0-20`; nothing added. Both single-atom `--newuse`
probes now report 0 packages.

## #69 S0 — binpkg selection vs `--newuse`: the real-built matrix (2026-09-16)

**Pipeline note.** The committed fixture `.tbz2`s (including the crafted
`dev-libs/binnew-1.0.tbz2`, built from `binaryusemismatchpkg` with baked
`IUSE=foo USE=foo BUILD_TIME=1000`) are *portuale-only*: real's local
bintree rejects every one of the crafted fixture binaries ("there are no
binary packages to satisfy"), so a local-binhost oracle must build a real
binary. The capture below does exactly that inside the container: stage
the tree, add a temporary `binnew-1.0` ebuild with `IUSE="foo"`, real
`emerge --buildpkgonly` it (profile `USE` selects `foo`), real
`emerge --usepkgonly` it into the vdb, then rewrite the ebuild to
`IUSE="foo newflag"` and drop its cache entry -- the qttools shape (the
installed record and the binary agree; only the ebuild's IUSE gained a
flag). `dev-libs/binnew` is committed for portuale's own pins; the real
capture is the oracle.

Real-built matrix (real 3.0.82.2, installed `binnew-1.0`):

| options | real | portuale (pre-#69) |
|---|---|---|
| `--usepkg` (respect-use auto) | `[ebuild R] … USE="-newflag%"` | `[binary R]` |
| `--usepkg --binpkg-respect-use=n` | `[binary R]` | `[binary R]` (MATCH) |
| `--usepkg --newuse` | `[ebuild R] … USE="-newflag%"` | **no entry** |
| `--usepkg --newuse --binpkg-respect-use=n` | `[ebuild R]` | **no entry** |
| `--getbinpkg` | `[ebuild R]` | `[binary R]` |
| `--getbinpkg --binpkg-respect-use=n` | `[binary R]` | `[binary R]` (MATCH) |

Conclusions (matching the plan's §2 reading exactly):

- Real's gate is `reinstall_use or (not installed and respect_use)`:
  `--binpkg-respect-use=n` without `--newuse` keeps the binary; plain
  respect-use (`auto`) and any `--newuse`/`--reinstall` reject it when
  `_reinstall_for_flags` counts the IUSE-set difference (`newflag` exists
  only in the ebuild).
- With `--newuse` the rejected binary is skipped and the **ebuild** is
  compared with the vdb → `[ebuild R]`. Portuale's respect-use filter
  runs only under respect-use, evaluates the *binary's own* IUSE, and
  then lets the binary win the version tie -- which is why the plain
  `--usepkg`/`--getbinpkg` cells show `[binary R]` instead of the
  ebuild, and why `--newuse` cells drop the entry entirely (vdb ==
  binary's baked flags, `AlreadyInstalled`).
- The live `dev-qt/qttools` host capture (plan §1,
  `docs/evidence/2026-09-16-post63-probes/`) is the same shape with a
  remote binary: portuale omitted the rebuild from `-uDpvN --oneshot`
  and `@world`; real rebuilds with `USE="… -23%"`.


## #65 follow-up (slice 15) — the bdeps-on `--deep` walk residues (2026-09-16)

Probe: `emerge -puDvN --ignore-default-opts --oneshot app-containers/runc`
on this host (bdeps auto, dynamic deps on), real 3.0.82.2 vs portuale
after #65 S1/S2.

What now matches the plan's §5 expectation exactly:

- the slot-op rebuild set is precisely `dev-go/go-md2man` and
  `app-containers/runc` `rR`, with `dev-lang/go-1.27.1` `r U` -- the
  argument-reached BDEPEND cascade.

Remaining package-set divergence (no longer slot-op related):

- portuale extra: `dev-lang/rust-1.94.0` `NS`. Portuale walks the
  installed world-only consumer `sys-block/thin-provisioning-tools`
  (reached through the `--deep` complete closure), whose BDEPEND has
  `llvm_slot_21? ( || ( rust-bin:1.94.1[…] rust:1.94.1[…] rust-bin:1.94.0
  rust:1.94.0 … ) )`. The installed `llvm_slot_21`-enabled
  `dev-lang/rust-1.94.1` is the all-installed alternative, but the
  disjunction preference lands on the uninstalled `rust:1.94.0`.
  Real installs no rust at all here (the optional build-time dep of a
  world-only installed parent is not walked that way).
- real-only (13): `dev-build/gtk-doc-am-1.36.1`, `dev-util/gtk-doc-1.36.1`
  (blocked: real prints `[blocks B] <dev-util/gtk-doc-1.36.1`, rc 1),
  `dev-python/mdit-py-plugins-0.6.1`, `dev-python/myst-parser-5.1.0`,
  `media-libs/audiofile-0.3.6-r6` and the xemacs chain
  (`app-editors/xemacs`, `app-eselect/eselect-{ctags,emacs}`,
  `app-xemacs/{apel,emerge,fsf-compat,mule-base,xemacs-base}`). These are
  real's `--deep` walk of the same bdeps-on closure; portuale reaches
  fewer.

Both are general bdeps-on `--deep` walk divergences, out of #65's
slot-op scope (the plan's §7 said to file them); filed as backlog #74.
Nothing in the #65 S0-S3 oracle bed or the rebuild-scan pins is
affected.

## #68 S3 follow-up — the systemd-utils regression and the required-set closure (2026-09-16)

The first S3 landing (graph-entry parents only) regressed exactly one L0
probe: `sys-apps/systemd-utils` (real rc 1 / `Conflict: 2 blocks (2
unsatisfied)`, portuale rc 0 / `(all satisfied)`) -- L0 summary went
clean 100/parity 0.833/34 unexplained to clean 99/0.825/36
(`l0-20260916T182622Z`). Real's `B` comes from the blocked installed
`sys-apps/systemd` being a graph node with the `@system` set as parent
(`virtual/service-manager` is starred; `--implicit-system-deps` seeds
the required sets even for a plain single-atom resolve), which
portuale's `entries` never carry outside complete mode.

Fix: `ResolveCtx::installed_closure` -- the forward-installed closure of
`complete_seed_atoms ∪ args ∪ config.system_packages`
(`required_set_reachable_cps`), computed for every resolve.
`resolve_blockers`' installed-match arm is
`graph_has_parent(entry deps) || installed_closure.contains(target)`, so
a set-reachable blocked instance counts as "a digraph node with
parents" while a target outside the sets (S0 cell d: `--oneshot
bparent` with installed `bconsumer`) stays satisfied. The old vdb
reverse scan remains deleted.

Verified: full L0 `l0-20260916T191018Z` is byte-identical to the
pre-#68 baseline classes -- clean 100 / parity 0.833 / 34 unexplained
(9 advice, 6 error, 1 flags, 15 order), `systemd-utils` gone; the #68
fixture cells (a-h) and the host `@world` rc-0 shape still hold.

## Host baseline re-capture before `backlog/68-74` Phase A (2026-09-16)

Per `docs/02.68-74.md` §2 the phase baselines are re-captured fresh. Build
(T8): HEAD `476ec1313ea19bdb4930275dfefd282a7d76c94a`, `cargo build
--release` green, binary mtime `2026-09-16 22:49:18 +0200`.

Both sides ran with identical effective options (T9; host
`EMERGE_DEFAULT_OPTS` is empty in this environment):

    emerge -uDpvN @world                          # real 3.0.82.2, rc 0
    rust/target/release/portuale emerge -uDpvN @world   # rc 0

Result: **identical 27-package set** (sorted `[ebuild …]` lines diff
empty modulo the KiB tail), same `Total: 27 packages (25 upgrades, 2
reinstalls, 2 binaries), Size of downloads: 1478345 KiB`. Differences:

- real: `[blocks b      ] <dev-util/gtk-doc-1.36.1 ("<dev-util/gtk-doc-1.36.1"
  is soft blocking dev-build/gtk-doc-am-1.36.1)` printed **inline right
  after** `dev-util/gtk-doc-1.36.1` (line 21 of 28) + `Conflict: 1 block
  (all satisfied)`; portuale prints neither (Phase D's target, #68/#72).
- real has no `[f1r] Local copy of remote index` line in this capture
  (that line was in §2's plan-time baseline; out of scope either way).
- merge-order scheduling differs on three pairs (`linux-firmware` vs
  `gtk-doc-am`, `gtk-doc` vs `xfsprogs`, `gnome-keyring` vs the
  pango/gtk chain) -- the standing L0 "order" class (15 unexplained),
  pre-existing, not a plan-time regression. §2's "only diffs are the
  blocks line" was written against a different host world cut.

## #70 R1 — config parser keeps escaped `\$`, unescapes `\"` (2026-09-16)

Repro (plan §Phase A probe), a scratch example calling
`portage_profile::resolve_config("/", …)` and printing `other_vars`:

- before (HEAD `476ec13`, portage-profile pre-R1):
  `FETCHCOMMAND = Some("wget -t 3 -T 60 --passive-ftp -U \"Portage
  (Gentoo, https://www.gentoo.org) distfile-fetch\" -O
  \"\/var/cache/distfiles/\\" \"\\"")` -- `\"` kept its backslash,
  `\${DISTDIR}` got expanded by the old `${VAR}` regex into
  `\/var/cache/distfiles`, and `${FILE}`/`${URI}` (unset at config time)
  collapsed to `\\`. Same corruption for all seven command keys.
- real (same host, `python3 -c "import portage;
  print(repr(portage.settings[K]))"`, portage 3.0.82.2, captured
  2026-09-16):
  `'wget -t 3 -T 60 --passive-ftp -U "Portage (Gentoo,
  https://www.gentoo.org) distfile-fetch" -O "${DISTDIR}/${FILE}"
  "${URI}"'`, and likewise for `RESUMECOMMAND`, `*_RSYNC`, `*_SSH`,
  `FETCHCOMMAND_SFTP`, `PORTAGE_GPG_SIGNING_COMMAND`.
- after: byte-identical to real for all 8 keys (diff of the scratch
  probe's raw output vs the python capture is empty; the "after" run is
  in the same scratch example, deleted before the commit).

Real source split ported (T4 caller list):

| portuale caller | real loader it mirrors |
|---|---|
| `parse_kv_line` (all) | `getconfig`'s shlex value token (`util/__init__.py:782-859`, `shlex.py:read_token`, `posix=True`) |
| `substitute` via `process_lines` | `getconfig(expand=…)` for `make.globals` + profile `make.defaults` (`config.py:446-480`, `:531`) |
| `substitute` via `process_make_conf_file` | `getconfig(make.conf, expand=…)` |
| `substitute` via `resolve_config` env.d loop | real reads `<eroot>/etc/profile.env` with `expand=False` (`config.py:1171-1215`); portuale's eager-expansion approximation there predates #70 and is unchanged |
| `substitute` via `expand_package_env_files`/`read_env_file_kv` | `_grab_pkg_env`'s `getconfig(..., expand=map)` |
| `substitute` via `read_repo_make_defaults_use` | `config.py` `_repo_make_defaults` |
| `substitute` in `parse_binrepos` | real `binrepos.conf` is `configparser`, not getconfig -- unchanged |
| `parse_kv_line` in `read_envd_use_tokens` (no substitute) | `getconfig(..., expand=False)`: quote removal only |

`shlex_token` mirrors shlex's `"`/`'`/escape states (only `\"`/`\\` lose
the backslash inside `"`; `\$` survives), `substitute` mirrors
`varexpand` (`portage/util/__init__.py:885-1015`) including its
bug-compatible `\\` + quote/`$` tail, `$VAR`/`${VAR}`/unset->empty and
single-quote suspension.

Tests: `make_globals_fetch_commands_match_real_varexpand` feeds the
verbatim host `make.globals` lines 63-74 + 153 through `process_lines`
and asserts the real strings (T1); `shlex_and_varexpand_escape_handling_
matches_real_getconfig` pins the capture matrix. Full pass green:
`cargo fmt --check`, `cargo clippy --release --all-targets` (0 warnings),
`cargo test --release` (all crates), `pytest -q` 1704 passed / 2 skipped
/ 5 xfailed, no corpus drift warning. `emerge --info` pins unchanged.

## #70 R2 — an absent FETCHCOMMAND means the make.globals default (2026-09-16)

T2: `fetch_commands_from_config` built the family with
`config.other_vars.get("FETCHCOMMAND").cloned()`, which is `None` for a
config root with no `usr/share/portage/config/make.globals` (every
fixture), while `FetchCommands::select` documented `None` as "unset ->
error". Real always has that file loaded from
`GLOBAL_CONFIG_PATH`/`PORTAGE_BASE_PATH` (`config.py:446-480`), so a
resolved config never produces the "`FETCHCOMMAND` is unset" error.
Fix: start from `FetchCommands::default()` (which already carries the
shipped templates, `make.globals:63-64`) and let only present config
keys override; `None` stays reachable solely from a hand-built family.

Tests: `fetch_commands_from_config_defaults_an_absent_fetchcommand`
(unit), `fetch_src_uri_uses_the_make_globals_default_without_a_config_value`
(integration: `resolve_config` on a temp root with no make.globals ->
`fetch_commands_from_config` -> a real `wget` download from
`serve_once`), plus the default-selection asserts in portage-fetch's
`fetch_commands_select_the_protocol_variant_then_the_plain_name`.
Full pass green: fmt, clippy 0 warnings, `cargo test --release` 25
crate targets ok, `pytest -q` 1704 passed / 2 skipped / 5 xfailed.

## #70 R3 — real's `${FILE}` refusal condition (2026-09-16)

Real (`fetch.py:1667-1716`) selects and checks **both** the
`FETCHCOMMAND_<PROTO>`/`FETCHCOMMAND` and
`RESUMECOMMAND_<PROTO>`/`RESUMECOMMAND` for the literal `${FILE}`, prints
one `!!! <VAR> does not contain the required ${FILE} parameter.` line per
missing one plus the shared make.conf(5) hint, and **aborts only when the
distfile name differs from the URL basename** (`:1713-1715`
`if myfile != os.path.basename(loc): return 0`); on matching names it
falls through and runs the command (T3: the continuation after the
message, `wget -P "${DISTDIR}" "${URI}"` included).

`download_with_commands` now selects both families up front (fetch first,
matching real's unset-check order), builds the per-variable message block
from the selected variant names, and: names differ -> `Err` carrying the
message block, nothing spawned; names equal -> the same block on stderr
and the command runs. The unset-`FETCHCOMMAND` error is unchanged.

Tests: `download_with_commands_warns_for_both_commands_and_aborts_only_
when_renamed` -- (a) `${FILE}`-less command + matching basename runs
(stub `wget -P` shape), (b) renamed distfile -> abort, no spawn,
(c) only `RESUMECOMMAND` missing `${FILE}` -> named in the abort even on
a fresh fetch; the pre-existing refusal test still pins the message
bytes. Full pass green after a clean `/tmp` (an earlier run cascaded into
149/73 fixture errors from accumulated `/tmp/pytest-of-vivo` state; the
clean re-run is 1704 passed / 2 skipped / 5 xfailed).

## #70 R4 — real's fetch substitution variables (2026-09-16)

Real's fetch variables (`fetch.py:1795-1811`) are `URI`, `FILE`,
`DIGESTS` (`" ".join(f"{k.lower()}:{v}")` over the Manifest hashes, size
excluded), `DISTDIR` and `PORTAGE_SSH_OPTS`; `varexpand` expands
`${VAR}`/`$VAR` with an **unset name -> empty** (not left literal), and
`shlex.split` turns the result into argv. Portuale's `expand_and_split`
only replaced three known names and left unknown `${VAR}` literal; a
`bunzip2`/`${DIGESTS}`-using command or a `$PORTAGE_SSH_OPTS` template
diverged.

Changes:

- `portage_fetch::varexpand`: faithful port of real `varexpand` (same
  escape semantics as R1's config `substitute`, but no process-env
  fallback -- lookup misses expand to empty like real's `mydict`).
- `portage_fetch::digests_variable`: real's `DIGESTS` string; hash
  names sorted (real preserves Manifest order; see §6).
- `FetchCommandVars { digests, portage_ssh_opts }` threaded through
  `download_with_commands`; `FetchRequest::vars` carries it across the
  `mrg-director` seam (the trait doc's "no Manifest context" narrowing
  now says the *formatted* string is the one exception);
  `FetchOptions::portage_ssh_opts` + `portage_ssh_opts_from_config`
  supply it from the resolved config, and `ebuild_phases.rs`'s
  `resolved_fetch_commands` became `resolved_fetch_config` so the same
  resolved `Config` feeds both (that file is one beyond the plan's two
  named callers -- the config resolution had to happen once, not
  twice).
- `shell_split` bug fixed alongside: inside double quotes Python `shlex`
  strips the backslash only before `"` and `\`; portuale also stripped it
  before `$`/`` ` `` (POSIX-shell behaviour, not `shlex.split`). Caught
  by the new oracle against real's `varexpand`+`shlex.split`.

Oracle (2026-09-16, portage 3.0.82.2, `python3 -c "from portage.util
import varexpand; import shlex; print(shlex.split(varexpand(CMD,
mydict=VARS)))"`) with the real R1 `FETCHCOMMAND_SSH` and
`DISTDIR=/var/cache/distfiles`,
`URI=https://example.org/distfiles/foo-1.0.tar.gz`,
`FILE=foo-1.0.tar.gz`, `DIGESTS=blake2b:aa sha512:bb`:

- unset `PORTAGE_SSH_OPTS`: argv ends `..., 'rsync',
  '/var/cache/distfiles/foo-1.0.tar.gz',
  'https://example.org/distfiles/foo-1.0.tar.gz', ''`;
- set to `-o ServerAliveInterval=5 -o User=portage`: that string is the
  last argv element.

Both are pinned in
`expand_and_split_matches_real_varexpand_on_fetchcommand_ssh`; unknown
vars/escapes in
`expand_and_split_varexpand_unknown_vars_and_escapes_match_real`, and
`digests_variable_formats_like_real_and_skips_size`.

`FILE` is portuale's `dest` basename (`foo-1.0.tar.gz`), not real's
`basename(download_path)` (`foo-1.0.tar.gz.__download__`); the download
path is unchanged and both this and the skipped `FETCH_WRAPPER` port are
recorded as named cuts in `docs/02.68-74.md` §6 (T5).

Full pass green: fmt, clippy 0 warnings, `cargo test --release` 25 crate
targets ok, `pytest -q` 1704 passed / 2 skipped / 5 xfailed.

## #70 R5 — live fetch verification (2026-09-17)

Binary: HEAD `8cd0e35` + `cargo build --release`, mtime recorded at run
time. All scratch paths under `/tmp/opencode/`.

**(1) Production CLI, default `make.globals` command** (`ebuild <file>
unpack` runs `fetch_sources` for real):

    PORTAGE_TMPDIR=/tmp/opencode/fetchtest/tmp \
    DISTDIR=/tmp/opencode/fetchtest/distdir \
    rust/target/release/portuale ebuild \
      /.gentoo/repos/gentoo/sys-apps/which/which-2.23.ebuild unpack

`rc 0`; the real `wget` line saved
`/tmp/opencode/fetchtest/distdir/which-2.23.tar.gz` (201930 bytes) from
`http://distfiles.gentoo.org/distfiles/80/which-2.23.tar.gz` (the
filename-hash path), and the digest verifies against the Manifest:
`b2sum` -> `64a3ae1f23a4c389f945f6c0985e6f6062b46785…`, Manifest
`BLAKE2B 64a3ae1f23a4c389f945f6c0985e6f6062b46785…` (the fetch path's own
`verify_digests` already passed, else unpack would not have run). The
`chgrp … Operation not permitted` lines are the known non-root
environment artifact of the real phase, not a fetch failure.

**(2) A user `FETCHCOMMAND` from a real config root**:

    /tmp/opencode/curlroot/etc/portage/make.conf:
      FETCHCOMMAND="curl -f -o \"\${DISTDIR}/\${FILE}\" \"\${URI}\""
      RESUMECOMMAND="curl -f -C - -o \"\${DISTDIR}/\${FILE}\" \"\${URI}\""
    /tmp/opencode/curlroot/etc/portage/repos.conf/gentoo.conf:
      [DEFAULT]
      main-repo = gentoo
      [gentoo]
      location = /.gentoo/repos/gentoo

Real resolves both lines to `curl -f -o "${DISTDIR}/${FILE}" "${URI}"`
(checked with `PORTAGE_CONFIGROOT=… python3 -c "import portage; …"`).
Portuale run (a `PATH` wrapper logged curl's argv while delegating to
`/usr/bin/curl`):

    PATH=/tmp/opencode/fakebin:$PATH PORTAGE_CONFIGROOT=/tmp/opencode/curlroot \
    PORTAGE_TMPDIR=/tmp/opencode/curl-run/tmp DISTDIR=/tmp/opencode/curl-run/distdir \
    rust/target/release/portuale ebuild …/which-2.23.ebuild unpack

`rc 0`; the wrapper's log is exactly

    -f
    -o
    /tmp/opencode/curl-run/distdir/which-2.23.tar.gz
    http://distfiles.gentoo.org/distfiles/80/which-2.23.tar.gz

(the config value went through R1's parser, R2's default interplay, R3's
refusal check and R4's `${DISTDIR}`/`${FILE}`/`${URI}` substitution).

**(3) `.mirror-cache.json` compatibility both ways** (01.014 §7):

- portuale-written -> real reads it: real
  `async_mirror_url('http://distfiles.gentoo.org', 'which-2.23.tar.gz',
  settings-with-DISTDIR, <portuale cache path>)` resolved
  `…/distfiles/80/which-2.23.tar.gz` and left the portuale-written entry
  (timestamp `1789597293.46606`, `filename-hash BLAKE2B 8`) unchanged.
- real-written -> portuale reads it: the host's
  `/var/cache/distfiles/.mirror-cache.json` (real-written, 382 bytes,
  four mirrors) copied into a scratch DISTDIR; portuale's `unpack` run
  then downloaded from the hashed `…/distfiles/80/…` path with **no**
  `.layout.conf.*` re-read (the cache was used), wrapper-logged again.

**(4) L2 builder** (`L2_REBUILD=1 L2_MODE=strict`, fixture track
`TEST/atomlists/l1-porttest.txt`, distfiles dir emptied first so the
container downloads run through the repaired chain):

    L2_REBUILD=1 L2_MODE=strict TEST/run/l2-portuale-builder.sh TEST/atomlists/l1-porttest.txt
    -> TEST/logs/l2-20260916T222355Z/, rc 0

structure 0/0, archive diffs `hard=0 soft=0` on every pair, cross-install
0 unexplained, control 0/0; the only findings are the two adjudicated
`l2-gpkg-dostrip-splitdebug` known entries. `TEST/logs/_l2-distfiles/`
shows the fresh `portage-3.0.82.2.tar.bz2` fetched by the run.

**(5) Host re-diff after R1–R4** (fresh binary, T8/T9): real vs portuale
`emerge -uDpvN @world`, same options — identical 27-package sorted set,
same `Total: 27 packages (25 upgrades, 2 reinstalls, 2 binaries), Size of
downloads: 1478345 KiB`; the only content diff is still real's inline
`[blocks b] …gtk-doc-1.36.1` + `Conflict: 1 block (all satisfied)`
(Phase D's target). No regression from the fetch repair.

**R5 verification-pass note (test-infra flake, not a product bug).** One
`cargo test --release` run (made while pytest and the L2 build ran in
parallel) failed
`portage_fetch::tests::download_with_commands_runs_a_stub_command_and_refuses_one_without_file`
with `failed to spawn .../fetch.sh: Text file busy (os error 26)` on its
first spawn. That is the classic multi-threaded-fork `ETXTBSY` race in
the test helper pattern (`stub_fetch_script` writes the script, another
test thread's `fork()` snapshots the open-for-write fd, the exec of that
same inode then sees a writer): the harness runs tests in parallel and
several portage-fetch tests spawn stub scripts. Re-runs of the same
crate alone and the full pass with the machine otherwise idle are green
(twice, including the run recorded above); the helper pattern predates
this batch and no fetch semantics are involved. Left unfixed in this
docs-only slice; flagged for the owner.

## #69 R0 — oracle for the no-ebuild, forced-flag and pinned-atom arms (2026-09-17)

**Pipeline** (same real-built shape as "#69 S0", *not* the crafted
fixture `.tbz2`s real's local bintree rejects): inside the fixture-oracle
container (portage upgraded to 3.0.82.2), stage the tree, add a
temporary `dev-libs/binnew-1.0` (`EAPI=8`, `IUSE="r0flag"`; `r0flag`,
not the globally-profiled `foo`, so the *package* selection can be
dropped), add `dev-libs/binnew r0flag` to the leaf profile's
`package.use`, real `emerge --buildpkgonly` + `--usepkgonly` it, delete
the committed `metadata/md5-cache/dev-libs/binnew-*` entries, then
mutate per cell. `r0flag` is baked in the vdb (`IUSE=r0flag`,
`USE="elibc_glibc r0flag"`).

Real 3.0.82.2 vs portuale (same binary as HEAD `2fc4d19`, mounted at
`/usr/local/bin`), `-p --color=n`, full argv recorded below:

| cell | real (rc) | portuale now (rc) |
|---|---|---|
| **n1** `--usepkg =dev-libs/binnew-1.0`, ebuild removed, profile package.use no longer selects `r0flag` | `!!! All ebuilds that could satisfy "=dev-libs/binnew-1.0" for /tmp/r0/fixtures/ have been masked.` + `- dev-libs/binnew-1.0::testrepo (masked by: )` **(rc 1)** | `[binary   R    ] dev-libs/binnew-1.0-1  USE="-r0flag*"` **(rc 0)** |
| **n2** same state, `--usepkgonly` | `[binary   R    ] dev-libs/binnew-1.0-1 to /tmp/r0/fixtures/` (rc 0) | `[binary   R    ] dev-libs/binnew-1.0-1  USE="-r0flag*"` (rc 0) |
| **n3a** ebuild IUSE gains `maskflag`, profile `use.mask maskflag`, `--usepkg --newuse` | no package line (rc 0) | no package line (rc 0) |
| **n3b** ebuild IUSE gains `forceflag`, profile `use.force forceflag`, `--usepkg --newuse` | `[ebuild   R    ] dev-libs/binnew-1.0 … USE="(forceflag%*)"` + the ignored-binaries block `=dev-libs/binnew-1.0 -forceflag` (rc 0) | `[ebuild   R    ] … USE="(forceflag%*)"`, **no** ignored-binaries block (rc 0) |
| **n4** ebuild 1.0 IUSE `r0flag newflag`, ebuild 2.0 IUSE `r0flag`, `--usepkg =…-1.0` | `[ebuild   R    ] … USE="-newflag%"` + ignored block `-newflag` (rc 0) | `[ebuild   R    ] … USE="-newflag%"`, no ignored block (rc 0) |
| **n4b** same, `--usepkg --newuse` | as n4 | as n4 |

Instrumented real (`depgraph._reinstall_for_flags` patched, same
container run):

- n1 calls it for the binary with `built=True installed=False`,
  `orig_use=['elibc_glibc','r0flag']`, `orig_iuse=['r0flag']`,
  `cur_use=['elibc_glibc']`, `cur_iuse=['r0flag']` -> `{'r0flag'}`
  (reject). `cur_use` is `pkgsettings["PORTAGE_USE"]` for the binary's
  cpv/IUSE — the profile selection, **not** the binary's baked USE. This
  confirms the R0-prep probe (`portage.config(config_root=…)` +
  `setcpv(pkg)`: `PORTAGE_USE = "amd64 cpu_flags_x86_sse2
  globalforceflag"`, no `r0flag`).
- n2 **never calls it**: under `--usepkgonly` real accepts the binary
  with no comparison at all.

**T4 branch list of today's `binpkg_respect_use_ok`** (portage-repo, the
pre-R1 body):

| current branch | real line it mirrors | verdict |
|---|---|---|
| `binary_use` None -> keep | no real analogue (a real built pkg always carries USE) | portuale model gap, unchanged |
| `old_iuse` = binary `iuse` | `iuses = pkg.iuse.all` (`:8263`) | correct |
| ebuild-at-version present -> `candidate_iuse_and_use(ebuild)` | `if myeb: now_use = _pkg_use_enabled(myeb)`; `cur_iuse = myeb.iuse.all` (`:8265-8267`, `:8272-8273`) | correct |
| **no ebuild -> `(old_iuse, binary_use)`** | real `else: pkgsettings.setcpv(pkg); now_use = pkgsettings["PORTAGE_USE"].split(); cur_iuse = iuses` (`:8269-8271`) | **wrong (T4)**: the doc claims "exactly real's arm" but compares the binary with itself, so the comparison is always empty |
| first arm `newuse \|\| (respect_use && !changed_use)`: IUSE symdiff + enabled symdiff | `_reinstall_for_flags` first arm (`:3148-3153`) | correct except the missing `flags -= forced_flags` (R2) |
| second arm `changed_use \|\| respect_use`: enabled-only | `_reinstall_for_flags` second arm (`:3155-3158`) | correct |
| caller gate `binpkg_respect_use \|\| newuse \|\| changed_use` | `reinstall_use or (not installed and respect_use)` under `built and not useoldpkg and (not installed or matched_packages)` (`:8226-8260`) | width correct; the **reachability** half is not modelled — see the stop note |

**STOP — the oracle contradicts the plan's R1 Verify (Phase B halted
before R1).** `docs/02.68-74.md` §0.5: an oracle cell that contradicts
the plan stops the phase. Two contradictions, both from n1/n2:

1. The plan's R1 says the no-ebuild fallback becomes the profile
   selection and its Verify expects n1/n2 reproduced. But the cells are
   **not the same shape**: real reaches `setcpv(pkg)` under `--usepkg`
   (n1: comparison runs, `{'r0flag'}`, then the **masked error, rc 1**)
   and **never reaches it under `--usepkgonly`** (n2: binary accepted,
   rc 0, instrumented proof above). R1's change alone inverts n2 —
   portuale would start rejecting a binary real keeps under
   `--usepkgonly`, and would still not produce n1's masked error
   (that is real's `_equiv_ebuild_visible`/masked-packages path, not the
   flags comparison).
2. The plan (and the existing unit test's own label, "No same-version
   ebuild (--usepkgonly): own sets stand in") treats `--usepkgonly` as
   *the* no-ebuild arm's shape; the oracle shows it is precisely the
   shape where real does **not** run the arm.

Options for the owner (not chosen here): (a) scope R1's comparison to
real's reachable shape and model the `matched_packages` reachability at
the two call sites (touches `resolve_pretend`'s filter block and the
graph walk, beyond R1's stated Touch); (b) split the n1 masked-error
path into its own slice (`_equiv_ebuild_visible` for the no-ebuild
`--usepkg` case) and keep R1/R2 as the flag-comparison slices;
(c) re-cut Phase B entirely. Until answered, R1/R2 are not started.

**R0 correction (2026-09-17, made in the R1 slice).** Two claims in the
stop note above are wrong:

1. n2's keep is **not** `matched_packages` reachability. A
   `sys.settrace` line trace of real's
   `_wrapped_select_pkg_highest_available_imp` shows `respect_use=False`
   at the `:8259` gate under plain `--usepkgonly`:
   `create_depgraph_params.py:47-55` only sets
   `myparams["binpkg_respect_use"] = "auto"` when `--usepkgonly` is
   **absent**. Portuale already models exactly that
   (`pretend.rs`'s `binpkg_respect_use.unwrap_or(!usepkgonly)`, same
   citation), so R1's new fallback cannot change n2.
2. The R0 note's portuale column for n1 was polluted: the staged
   `$PKGDIR` still held the committed portuale-only fixture
   `binnew-1.0.tbz2` (IUSE=`foo`, which this profile *does* select), so
   portuale kept that crafted binary. Real's local bintree rejects every
   crafted fixture binary (the "#69 S0" pipeline note). The re-run below
   removes it from the staged pool and clears **both**
   `metadata/md5-cache/…` and the `metadata/cache/…` depcache real
   regenerates between cells.

## #69 R1 — the no-ebuild arm compares with the profile's selection (2026-09-17)

`binpkg_respect_use_ok`'s no-ebuild fallback was `(old_iuse,
binary_use)` — the binary compared with itself, while its doc claimed to
be real's `pkgsettings.setcpv(pkg)` arm (T4). It now computes
`effective_use_flags(config, &candidate.iuse, …)` for the binary's own
cpv: the profile's selection over the binary's own IUSE, real
`depgraph.py:8265-8271`, quoted in the function's doc comment.

Corrected matrix (clean `$PKGDIR`, both cache kinds cleared per cell;
post-R1 binary):

| cell | real (rc) | portuale pre-R1 | portuale post-R1 |
|---|---|---|---|
| n1 `--usepkg`, ebuild gone, selection dropped | masked block, **rc 1** | `[binary R] USE="-r0flag*"` rc 0 (polluted pool) | `emerge: there are no ebuilds to satisfy "=dev-libs/binnew-1.0".` **rc 1** |
| n2 `--usepkgonly`, same | `[binary R]` rc 0 | `[binary R]` rc 0 | `[binary R]` rc 0 |
| n3a masked flag, `--newuse` | nothing, rc 0 | nothing, rc 0 | nothing, rc 0 |
| n3b forced flag, `--newuse` | `[ebuild R] USE="(forceflag%*)"` + ignored block, rc 0 | same, no ignored block | unchanged |
| n4/n4b pinned 1.0 | `[ebuild R] USE="-newflag%"` + ignored block, rc 0 | same, no ignored block | unchanged |

Reachability needed no call-site change: real's gate is
`reinstall_use or (not installed and respect_use)`, and portuale's
filter gate (`binpkg_respect_use || newuse || changed_use` over the
resolved `binpkg_respect_use = …unwrap_or(!usepkgonly)`) already
reproduces it at both call sites (`resolve_pretend`'s filter and the
graph walk's `ctx.binpkg_respect_use`). The `matched_packages` half of
real's enclosing condition only ever gates `(not installed or
matched_packages)`, which is True for a PKGDIR/binhost binary — it never
blocks the comparison.

Residue, R1-external: n1's **message**. Real reaches its no-visible-
candidate path and prints the masked-packages block
(`_equiv_ebuild_visible` → masked machinery); portuale rejects the
binary the same way and exits 1 with
`emerge: there are no ebuilds to satisfy "=dev-libs/binnew-1.0".` +
similar-names block. No package is merged on either side; the exact
message is a separate slice candidate (real's masked-package display for
an ebuild-less binary), reported to the owner, not filed.

Tests: the same-version unit test's `None` arm now rejects when the
profile selection is empty and keeps when it still selects the baked
flag; the 55 binpkg-related contract tests (all six #69 S2 cells among
them) pass unchanged; full pass green (see the R1 commit).

## #69 R2 — forced and masked flags do not reject a binary (2026-09-17)

Real `_reinstall_for_flags` (`depgraph.py:3148-3150`) subtracts
`forced_flags` from the IUSE *presence* difference before unioning the
enabled-within-IUSE term; portuale's first arm OR-ed the raw symmetric
difference. `binpkg_respect_use_ok` now subtracts
`portage_repo::forced_or_masked_flags` — the same-version ebuild's own
set when one exists, the binary cpv's in the no-ebuild arm (real
`myeb.use.force ∪ myeb.use.mask` vs
`pkgsettings.useforce ∪ pkgsettings.usemask`). Granularity checked
first: `forced_or_masked_flags` is per-candidate (`cat/pkg-ver:slot/sub::
repo` + keyword stability) and restricted to that candidate's IUSE —
matching real's per-package `pkg.use.force/mask`; the IUSE restriction
is a no-op for the subtraction, whose input is already a subset of the
two IUSEs.

Bed cells (clean pool, both repo-local caches **and**
`/var/cache/edb/dep` cleared per cell — the depcache is where real's
regenerated metadata actually lands, and a stale entry there produced
two initially-confusing runs):

- n3a (masked flag gained by the ebuild): nothing scheduled on either
  side, unchanged.
- n3b (forced flag gained): both sides `[ebuild R] USE="(forceflag%*)"`
  — the forced flag rejects through the enabled-within-IUSE term, not
  the presence term, exactly real's line order.
- n1/n2/n4: unchanged from the R1 matrix.

The subtraction's observable delta is only for a **non-installed**
binary (with the installed package present, the installed-side
`reinstall_flags_for_use_change` already subtracts the same flags, so
both before and after R2 the CLI prints nothing). Pinned hermetically by
`binpkg_respect_use_subtracts_forced_and_masked_flags_like_real`, which
was confirmed to fail with the subtraction line removed. No *contract*
pin was added: the R0 n3a cell is byte-identical before and after, so a
CLI pin cannot discriminate, and the non-installed oracle shape has no
crafted-fixture staging path (real's bintree rejects the committed
`.tbz2`s). Named cut in `docs/02.68-74.md` §6.

Host `emerge -uDpvN @world` re-diff: identical 27-package set, same
`Total: 27 packages (25 upgrades, 2 reinstalls, 2 binaries), Size of
downloads: 1478345 KiB`. Full pass green.

## #73 S0 — nomerge owner set-parent oracle, cells g/g2/g3/g4/f (2026-09-17)

Container fixture-oracle session, real 3.0.82.2, staged `fixtures/`
(`stage.sh`), minimal vdb entries written per cell
(`CATEGORY`/`SLOT`/`repository`/`EAPI`/`PF`/`KEYWORDS`, `RDEPEND` on
`bparent-1.0` = `!<dev-libs/blocked-2.0`), world file per cell. Owner =
`dev-libs/bparent` (nomerge, installed); target = merge-bound
`dev-libs/blocked-1.5` (installed `1.0`). Probe column = temporary
`eprintln!` in a scratch build (in the call site before `resolve_blockers`
so `ctx.top_level_cps` was visible), **removed before S1**.

| cell | argv (`-pv --color=n`, both PMs) | real | portuale pre-S1 | owner in `installed_closure` | owner in `top_level_cps` |
|---|---|---|---|---|---|
| g | `--update --deep @world =dev-libs/blocked-1.5`, world {bparent,blocked} | `[blocks B      ] <dev-libs/blocked-2.0 ("<dev-libs/blocked-2.0" is soft blocking dev-libs/bparent-1.0)` + `Conflict: 1 block (1 unsatisfied)`, **rc 1** | `Conflict: 1 block (all satisfied)` (no row), rc 0 | **true** | **true** |
| f | same, `=dev-libs/blocked-1.0` (pinned at installed, no merge-bound match) | no block row, rc 0 | no block row, rc 0 | true | true |
| g2 | `--oneshot dev-libs/bparent =dev-libs/blocked-1.5`, world {blocked} | `B` + `(1 unsatisfied)`, **rc 1** | **already matches** (`B` + rc 1) | true | true |
| g3 | `--update --deep @world =dev-libs/blocked-1.5`, world {bparentdep,blocked}; `bparentdep` staged as an **installed** world member (`mkvdb`, vdb `RDEPEND=dev-libs/bparent`) plus a tree ebuild for visibility | `B` + `(1 unsatisfied)`, **rc 1** | no row, rc 0 | **true** | **false** |
| g4 | same argv, world {blocked}, `*dev-libs/bparent` appended to the leaf profile `packages` (owner in `@system`) | `B` + `(1 unsatisfied)`, **rc 1** | no row, rc 0 | **true** | true |

Conclusions (the data S1's predicate is chosen from):

- Real counts the set/atom parent in **every** variant (g, g2, g3, g4):
  `B`, rc 1. A plain argument (g2), a dependency-of-a-world-member
  reachability (g3, forward closure only) and `@system` membership (g4)
  all qualify.
- `installed_closure.contains(owner)` is **true in all four** cells,
  including g3 where `top_level_cps` is false. So the nominated
  `graph_has_parent(owner) || installed_closure.contains(owner)` fires
  exactly where real's `B` is and nowhere in the captured cells;
  `top_level_cps` alone would miss g3.
- g2 already matches today (portuale's walked-entry parent test finds
  the argument-reached owner), so S1 only adds g/g3/g4.
- f is the control: real refuses to act on an installed-only match in
  the nomerge arm regardless of set membership (no merge-bound match),
  and the pre-S1 portuale already matches; S1 must not move it.

## #73 S1 — the owner set-parent predicate (2026-09-17)

`resolve_blockers`' nomerge-owner arm now reads

```rust
merge_bound_match
    && (owner_entry.is_some_and(|e| graph_has_parent(/* owner */, entries))
        || owner_set_parent(&pb.owner_key, installed_closure))
```

(T11 parenthesisation: the set term must not apply when
`merge_bound_match` is false). `owner_set_parent` is the named helper
whose doc comment records the cp-keyed granularity rationale (T10):
real's own `_parent_atoms` edge records are cp-level, and the owner is
the blocker's single installed instance, so cp membership is exact here.
The owner arm is now the mirror of the #68 S3 target arm.

Post-S1 container cells (same staging as "#73 S0"; real vs portuale):

| cell | real | portuale post-S1 | portuale pre-S1 |
|---|---|---|---|
| g | `[blocks B      ] <dev-libs/blocked-2.0 (…)` + `Conflict: 1 block (1 unsatisfied)`, rc 1 | identical block/counter lines, rc 1 | `all satisfied`, rc 0 |
| f | no row, rc 0 | no row, rc 0 | same |
| g2 | `B` + `(1 unsatisfied)`, rc 1 | identical | same (already matched) |
| g3 | `B` + `(1 unsatisfied)`, rc 1 | identical | `all satisfied`, rc 0 |
| g4 | `B` + `(1 unsatisfied)`, rc 1 | identical | `all satisfied`, rc 0 |

Renderer note (required for that acceptance): the `[blocks B]` row of
g/g3/g4 belongs to real's trailing `Display.blockers` group
(`output.py:573-592`) and the owner prints **no** package line (it is
installed/kept). `print_entry_line`'s `AlreadyInstalled` arm now
collects that owner's **unsolvable** blocker rows into the shared
`blocker_lines` group; satisfied rows are deliberately not collected
there (real appends those inline only on a scheduled uninstall, #72).
`rust/portuale/src/pretend.rs` is therefore touched beyond the per-item
plan's file-conflict map, recorded here.

Unit tests: cell g with the owner only in `installed_closure` ->
`unsolvable`; cell f (closure member, no merge-bound match) -> no row
(the T11 guard); the existing walked-parent g case, a/c/d/e/h unchanged.
Beds: fixture-oracle `l0-fx-20260917T003115Z` rc 0 / 0 unexplained (no
new allowlist entry); contract `pytest -k "blocker or blocked or
bparent"` 22 passed; full pass green.

## #68/#72 B0 — oracle matrix for satisfied-blocker and uninstall placement (2026-09-17)

Container fixture-oracle sessions, real 3.0.82.2 vs portuale (HEAD
`df16031`, T8: release binary rebuilt, mtime recorded), `stage.sh`
staging. Two families: **p\*** — the host replacement shape on a staged
root (installed `blocked-1.0`, world {blocked, bparent}, `bparent`
installed 1.0 + a staged `bparent-1.1` upgrade); **u\*** — the shared
fixtures (`blockerpkg`/`blockerorderpkg`, installed `samepkg-1.0`).
Exact argv recorded per cell; the `!!! The following installed packages
are masked` block is running-root license noise, ignored.

### u-cells (shared fixtures; the real bytes are the B4 pin oracle)

| id | argv (`-p --color=n` unless noted) | real | portuale now |
|---|---|---|---|
| u1 | `dev-libs/blockerpkg` | `[ebuild  N     ] dev-libs/blockerpkg-1.0 to …` / `[uninstall     ] dev-libs/samepkg-1.0 to …` / `[blocks b      ] dev-libs/samepkg (…)` rc 0 | ebuild row + `b` row, **no uninstall row** |
| u2 | `-pv dev-libs/blockerpkg` | same rows at `::testrepo`/`::__unknown__` + `Total: 1 package (1 new, 1 uninstall), Size of downloads: 0 KiB` + `Conflict: 1 block (all satisfied)` | `Total: 1 package (1 new)` (no `uninst` detail) |
| u3 | `dev-libs/blockerorderpkg` | `newpkg`, `blockerorderpkg`, `[uninstall     ] samepkg-1.0`, `[blocks b      ] …` | no uninstall row (b row after both) |
| u4 | `-pv --oneshot dev-libs/bparent`, staged root installed `blocked-1.0`+`bconsumer-1.0`, world {bconsumer} | `B` + `Conflict: 1 block (1 unsatisfied)` + Error, **rc 1** (my staging differs from #68 cell d: the uninstall triggers real's complete-mode retry, which walks `bconsumer` and makes `blocked-1.0` a digraph node with parents) | `b` + `(all satisfied)`, rc 0 |
| u5a | u1 + `--columns` | **only the ebuild row**: the `[blocks b]` line is suppressed too (T7's `output.py:120`), rc 0 | ebuild row **+ the `b` line** (existing divergence, must be fixed with B2) |
| u5b | u1 + `-q` | `[ebuild  N    ] …` / `[uninstall    ] samepkg-1.0` (one less bracket space), rc 0 | no uninstall row |
| u5c | u1 + `--color=y` | the uninstall row's `uninstall` and cpv are red (`\x1b[31;01m`), rc 0 | no uninstall row |
| u6 | `dev-libs/blockerpkg dev-libs/graphblockerparent` | satisfied `b` **inline** (after the uninstall row, before the trailing unsatisfied group), then `[blocks B] blockerpartnerpkg` at the tail + Error, rc 1 | `B` line first, `b` second (both trailing) |

### p-cells (host replacement shape, staged root)

- **p1** (`bparent` merges New, no ordering constraint): real
  `-pv --update --deep --newuse @world` → `[ebuild U] blocked-2.0` before
  `[ebuild N] bparent-1.0`, **no block row, no `Conflict:`**, rc 0;
  portuale same semantics (order differs).
- **p1b** (same with `bparent` **installed + upgrading** to a staged
  `bparent-1.1`): real blocked-2.0 then bparent-1.1, no row, rc 0;
  portuale same.
- **p2b** (p1b + the replacement `blocked-2.0` `BDEPEND`s on `bparent`,
  the host ordering): real **flat `-pv`/`-p` shows no row**; real
  **`--tree` shows `[blocks b      ]  <dev-libs/blocked-2.0
  ("<dev-libs/blocked-2.0" is soft blocking dev-libs/bparent-1.1)` right
  after the `[nomerge] bparent-1.1` / before `[ebuild U] blocked-2.0`**,
  rc 0 in both. Portuale prints no row in either mode.
- **p3** `--tree`/`--columns` of p2b: tree as above; `--columns` output
  has no blocker row (the satisfied line is suppressed).

### Why p1/p2 do not differ only by order (the B0 stop rule)

Instrumenting real (`depgraph._serialize_tasks` wrapper + a
`sys.settrace` of the serializer) shows the difference is *scheduling*,
not display:

- Flat p2b's retlist is 7 `Package` nodes with **no `Blocker`** (the
  blocker was never solved by a scheduled uninstall).
- Tree p2b's retlist is 8 nodes and **contains the satisfied `Blocker`**
  right after `blocked-2.0`.
- `--tree` genuinely changes real's serialization:
  `depgraph.py:9680` `tree_mode = "--tree" in myopts` disables the
  greedy "pop all leaves at once" optimization (`:9778-9786`), which
  changes whether the `myblocker_uninstalls` branch (`:9998-10227`) ever
  schedules the uninstall; `scheduled_uninstalls` (`:10190`) is exactly
  the condition that then appends the solved blocker after the
  replacement (`:10335-10358`). Params are otherwise identical (dumped).
- The host flat `-uDpvN @world` **does** show the inline `b` (baseline
  capture), so the host's flat scheduling reaches the scheduled-uninstall
  state while this staged p2b does not.

**STOP — the B0 stop rule fired: p1/p2 do not differ only by order.**
The plan's B2 rule ("if the owner's merge position is before the
replacement's, print the `b` after the replacement's row") reproduces the
host flat acceptance but **not** the tree p2b capture (where the
scheduled uninstall, not the merge order, decides) and it does not
explain flat p2b (row absent with the owner's *upgrade* node after the
replacement). B1/J2–B5 are not started. Options for the owner:
(a) implement B2's merge-order rule for the host acceptance and record
the `--tree`/greedy-scheduling residue (p2b tree, u4) as a named cut;
(b) model real's `scheduled_uninstalls` condition instead (needs the
serializer's uninstall scheduling; portuale's `merge_order.rs` has no
equivalent state) — a larger change than B2's stated Touch;
(c) re-stage p2b until the flat row appears (matching the host) before
deciding, and treat the tree divergence separately.
