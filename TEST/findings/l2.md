# L2 findings — portuale as builder (structure + cross-install)

Triage of `TEST/run/l2-portuale-builder.sh` runs (L2 plan:
[`docs/029_portuale-as-builder.deepseek.md`](../docs/029_portuale-as-builder.deepseek.md)).
L2 has two tracks: **fixture** (`porttest/*`, strict archive comparison) and
**real** (`atomlists/l1-merge.txt`, structural/path parity). Portage is the
reference everywhere.

Status legend: **FIXED** (on `main`, this branch), **OPEN** (filed, not
fixed — carries a backlog id), **ADJUDICATED** (allowlisted with reason).

---

## S0 — recon spike (2026-09-13, `localhost/test-portuale:latest`, portage 3.0.82.2)

Commands (from the repo root; host dirs under `TEST/logs/_l2-spike/`):

```sh
# portuale: build two dep-free fixtures from source, archive-only
podman run --rm --security-opt seccomp=unconfined --cgroups=enabled \
  --cgroupns=private \
  -v "$PWD/rust/target/release:/usr/local/bin:ro" -v "$PWD:$PWD:ro" \
  -v "$PWD/TEST:/TEST:ro" -v "$PWD/TEST/logs/_l2-spike/pkgs-portuale:/pkgs" \
  -v "$PWD/TEST/images/overlay/porttest:/porttest-overlay:ro" \
  --entrypoint /bin/bash localhost/test-portuale:latest -c '
    rm -rf /var/db/repos/porttest; cp -a /porttest-overlay /var/db/repos/porttest
    PKGDIR=/pkgs BINPKG_FORMAT=gpkg \
    FEATURES="buildpkg binpkg-multi-instance splitdebug xattr -sign" \
    /usr/local/bin/emerge --buildpkgonly --oneshot --color=n porttest/docs porttest/phases'

# real portage consumes them
podman run ... -v .../pkgs-portuale:/pkgs:ro ... -c '
    rm -rf /var/db/repos/porttest; cp -a /porttest-overlay /var/db/repos/porttest
    PKGDIR=/pkgs /usr/sbin/emerge -k --getbinpkg --oneshot --color=n porttest/docs porttest/phases'
```

Results:

| Check | Result |
|---|---|
| `emerge --buildpkgonly` (dep-free fixtures) | works, rc 0 |
| `emerge -b` (build + merge + package) | works, rc 0, merged |
| multi-instance layout `<cat>/<pn>/<pf>-<BUILD_ID>.gpkg.tar` | works **iff** `FEATURES=binpkg-multi-instance` is exported |
| archive member shape vs real | identical (`<pf>-<id>/{gpkg-1,image.tar.zst,metadata.tar.zst,Manifest}`) |
| real Portage consumes the archives | after `l2-pkgindex-version-missing` is fixed: `[binary N]`, `>>> Emerging binary`, rc 0 |
| merged payload vs real | differs: docs compression, see `l2-gpkg-docompress` |

Chosen builder invocation (plan Gate G0.1): **`--buildpkgonly --oneshot`**
(archive-only; `-b` works and is exercised as an optional smoke). The
builder env must export `FEATURES` including `binpkg-multi-instance` (see
below) and `BINPKG_FORMAT=gpkg`.

---

## l2-pkgindex-version-missing · HIGH · FIXED

`<PKGDIR>/Packages` written by portuale carried a header with only
`TIMESTAMP:`. Real Portage's `bintree._load_pkgindex` discards any index
whose header fails `_pkgindex_version_supported`
(`lib/portage/dbapi/bintree.py:2429-2437`: needs a numeric `VERSION`).
With the image's `FEATURES=pkgdir-index-trusted`, portage does not walk
`$PKGDIR` to rebuild the index — so **every portuale-built archive was
invisible and `emerge -k` silently rebuilt from source**, i.e. the
cross-install test would have "passed" while testing nothing.

Evidence (before the fix):

```
$ emerge -pK porttest/docs
emerge: there are no binary packages to satisfy "porttest/docs".
```

After adding `VERSION: 0` to the header by hand: `[binary N]` for both
archives; `>>> Emerging binary … porttest/docs::__unknown__`; rc 0.

Fix: `ebuild_package.rs::packages_index_header` now emits
`TIMESTAMP: …\nVERSION: 0` (real's writer always stamps it,
`bintree.py:2388`/`:2397`); unit test
`write_packages_index_entry_creates_a_header_then_the_entry` pins it.
Real's config-fingerprint header fields stay omitted (real only uses
them when it rewrites the index itself). No Python mirror: real-execution.

## l2-no-md5-cache-ebuild-fallback · MEDIUM · OPEN (harness worked around)

Real Portage resolves ebuilds from a repo with no `metadata/md5-cache`
by parsing the ebuild (`cache-formats = md5-dict` is a *format*, not a
requirement). Portuale's repo reader only consults
`metadata/md5-cache` (`portage-repo/src/lib.rs::read_md5_cache`), so
every `porttest/*` atom was invisible to portuale:

```
$ emerge -p porttest/docs
emerge: there are no ebuilds to satisfy "porttest/docs".
```

L0/L1 never hit this (the real tree ships a cache; L1 only merged
binpkgs). Workaround: the generated `metadata/md5-cache` for all nine
fixtures is now committed under
`TEST/images/overlay/porttest/metadata/md5-cache/` (per AGENTS.md's
"fixtures + metadata/md5-cache" convention). The missing ebuild
fallback itself is a real drop-in gap for cache-less repos — filed,
not fixed.

## l2-gpkg-metadata-members · MEDIUM · OPEN

`metadata.tar.zst` of a portuale-built archive is missing members real
Portage always writes (porttest/docs, source build):

| member | real value | portuale |
|---|---|---|
| `IUSE` | present (empty for a no-IUSE ebuild) | absent |
| `IUSE_EFFECTIVE` | profile-computed flag set | absent (documented gap: needs a resolved `Config` at phase depth) |
| `SIZE` | installed size in bytes (`807`) | absent (`write_post_install_metadata` does not walk `${D}`) |
| `repository` | `porttest` | absent (phase env never sets `PORTAGE_REPO_NAME`, so real `phase-functions.sh:762` writes nothing) |
| `REPO_REVISIONS` | `{}` | absent (same; `PORTAGE_REPO_REVISIONS`) |
| `BUILD_ID` | present | present only under `FEATURES=binpkg-multi-instance` (deliberate conservatism in `ebuild_package.rs`; harness exports it) |

Consumer-visible effect: portage records `::__unknown__` and the merged
VDB has no `repository` file. Filed as a producer-parity follow-up.

## l2-gpkg-docompress · MEDIUM · FIXED (#37 S2 + #38 S1, 2026-09-13)

> Closed: #37 S2 threaded the resolved `FEATURES`/`PORTAGE_COMPRESS*` into
> the `install_qa_check` env, so the real `binpkg-docompress` gate fires;
> S0 froze the oracle and S1 added the host e2e. See "#38 S1" below. The
> text under this heading is the historical S0 filing.

Real `FEATURES=binpkg-docompress` (default-on via `make.globals`) runs
`ecompress` over `${D}` before packaging
(`bin/misc-functions.sh:147-149`); portuale implements no
`docompress`/`ecompress` at all. On the `porttest/docs` fixture, real's
archive image carries `BIG.txt.bz2`, portuale's carries `BIG.txt` —
a path-set diff in cross-install parity even though both merge cleanly.

Per-package `db` markers are unaffected; it is the payload path set.
Filed as a producer-parity follow-up; the fixture track will allowlist
it (owner: portuale-bug) until implemented.

## Adjudications

(no `l2` allowlist entries yet — see `TEST/compare/known-divergences.yaml`
once S3/S4 runs add them)

---

## S1 — `gpkg-structure.sh` (2026-09-13)

`TEST/compare/gpkg-structure.sh` validates one archive or a whole
`$PKGDIR`: outer member set (`gpkg-1`/`image.tar[.comp]`/
`metadata.tar[.comp]`/`Manifest` + optional `.sig`), inner tar roots,
the stable metadata-key set (the exact intersection of all 78 real
archives in the L1 pkgcache), the Manifest DATA record set and digest
lengths, filename `<->` inner-prefix `BUILD_ID` agreement, and with
`--packages` the `Packages` stanza (`PATH`/`SIZE`/`MD5`/`BUILD_ID`/
`BUILD_TIME`/`REPO`).

Host verification (no container):

```sh
TEST/compare/gpkg-structure.sh --dir TEST/logs/_l1-pkgcache --packages   # rc 0, 78/78 clean
TEST/compare/test-gpkg-structure.sh                                      # 10 passed, 0 failed
```

The self-test pins one mutation per category (truncated container,
Manifest member removed, Manifest DATA record dropped, metadata key
removed, corrupt `metadata.tar.zst`, renamed `BUILD_ID` copy, spoofed
`Packages` `MD5`, missing `REPO`) and proves each is caught.

Against the S0 portuale archives the checker reports exactly the filed
producer gaps — 5 metadata members per archive (`SIZE`, `IUSE`,
`IUSE_EFFECTIVE`, `repository`, `REPO_REVISIONS`) plus the missing
`Packages` `REPO` field — i.e. `l2-gpkg-metadata-members`, not new
findings.

The metadata-key intersection also settled the required set: real does
*not* always write `LICENSE`/`HOMEPAGE`/`INHERITED`/`NEEDED*` (virtual
and acct packages), so those are deliberately not required.

---

## S2 — `gpkg-diff.sh` + `diff.py --layer/--tolerate-payload` (2026-09-13)

`TEST/compare/gpkg-diff.sh` (wrapper over `gpkg_diff.py`) is the
normalised archive-vs-archive diff: outer member-kind set, metadata
after `BUILD_TIME`/`BUILD_ID`/`COUNTER` blanking + `NEEDED*` sorting +
`environment.bz2` through `normalize.py`'s own `norm_environment` (one
ruleset, imported not copied), image path + type/mode/uid/gid/symlink
attr parity, payload sha256, and Manifest DATA *names* only (digest
values are volatile by construction). `--mode strict` = payload diffs
hard (deterministic `porttest` fixtures); `--mode payload-tolerant` =
payload diffs reported as `[payload]`, non-fatal (the real set).

`diff.py` gained `--layer` (allowlist `layer:` filter, report
header/JSON name) and `--tolerate-payload` (CONTENT/SIZE and `CONTENTS`
`obj`-md5 differences become non-fatal `[PAYLOAD]`; path/type/mode/
owner/xattr/symlink and `sym` targets stay hard). Defaults are
byte-for-byte L1 behaviour -- the existing L1 run re-diffs rc 0 with the
identical `# L1 merge-parity report`.

Host verification (no container):

```sh
TEST/compare/test-gpkg-diff.sh              # 9 passed, 0 failed
python3 TEST/compare/test-diff-tolerance.py # all checks OK
```

Pins: two `porttest` instances hard-clean in strict, two real Portage
`tree` instances hard-clean in tolerant, and one mutation per bucket
(metadata key, payload byte, mode, removed path, truncated archive)
with payload severity flipping correctly between modes. A real bug in
`gpkg_diff.py` was caught by the tests themselves (class-level mutable
dicts shared between the two `Side` instances made every comparison
see one side twice -- fixed before commit).

---

## S3 — builders + orchestrator, `porttest` track (2026-09-13)

`TEST/layers/l2/build-portage.sh` / `build-portuale.sh` build the atom
list from source archive-only (`--buildpkgonly --oneshot`) into two
fresh `$PKGDIR`s with a shared `DISTDIR`; `TEST/run/l2-portuale-builder.sh`
orchestrates: portage build -> portuale build -> `gpkg-structure.sh`
both -> `gpkg-diff.sh` per atom -> three cross-installs (real Portage
merging the portage-built *reference* and the portuale-built
*candidate*; portuale merging the portage-built set as the L1 control)
-> `normalize.py` + `diff.py --layer l2`.

```sh
TEST/run/l2-portuale-builder.sh TEST/atomlists/l1-porttest.txt   # rc 0
L2_MODE=payload-tolerant TEST/run/l2-portuale-builder.sh         # the real set
```

Result (2026-09-13, `porttest` set, mode=strict): portage structure 0
findings; portuale structure 54 (all adjudicated); 9 archive pairs all
findings adjudicated; cross-install 161 hard / **0 unexplained**;
control (portuale <- portage) 0/0. `TEST/logs/l2-latest/`.

All adjudications are temporary: `owner: portuale-bug` entries in
`known-divergences.yaml` (layer l2) plus the orchestrator's
`KNOWN_FINDINGS` list, keyed to the finding ids below. Delete them the
moment the gap is fixed.

### The producer gaps this run filed

**`l2-bpkgonly-env` · HIGH.** `--buildpkgonly` runs the build with the
*standalone* ebuild env, not the resolved graph env the `emerge <atom>`
path uses (`emerge_build.rs::run_buildpkgonly` calls
`ebuild_package::run_package(&path, root, portage_tmpdir, options)`
with no `build_env`/`per_entry`; the merge path threads them). Evidence
from the archive diff vs real:

| metadata | real | portuale |
|---|---|---|
| `USE` | `abi_x86_64 amd64 elibc_glibc kernel_linux` | empty |
| `FEATURES` | resolved incremental list (`assume-digests …`) | raw process env (`buildpkg binpkg-multi-instance splitdebug …`) |
| `.keep_<cat>_<pn>-<slot>` | `.keep_porttest_emptydirs-0` | `.keep_porttest_emptydirs-` (SLOT unset) |
| `NEEDED` lib path | `/usr/lib64/libptsd.so.0.0.0` | `/usr/lib/libptsd.so.0.0.0` (no multilib `LIBDIR_*`) |
| `environment.bz2` | resolved env | bare env |

**`l2-gpkg-dostrip-splitdebug` · HIGH.** The packaging path runs no
`dostrip`/`estrip` and no `splitdebug`: portuale archives ship
unstripped binaries (setuid fixture: 15424 vs 14384 bytes) and zero
`/usr/lib/debug/**` objects where real (default `binpkg-dostrip` +
`FEATURES=splitdebug`) ships `.debug` files + `.build-id` symlinks.

> **FIXED (#37 S2 + #38 S2, 2026-09-13).** #37 S2 made the `estrip`
> gate fire; S0 then traced the residual byte/build-id difference to the
> CLI's wrong `PORTAGE_TMPDIR` default (`/var/tmp/portage` →
> `portage/portage/<cat>/<pf>`), fixed as a #37 follow-up in S2. The
> archive diff is now clean except #39 metadata. See "#38 S2" below.

**`l2-needed-elf2-format` · MEDIUM.** portuale's own `NEEDED.ELF.2`
records 5 fields; real writes the trailing ELF-class field
(`X86_64;/usr/bin/x;;;libc.so.6;x86_64`).

**`l2-consumes-portuale-env-degraded` · MEDIUM.** Real Portage merging
a portuale-built archive runs the merge phases with a degraded saved
environment: `MERGE_TYPE` is unset (fixture prints `?`) and
`pkg_pretend` is skipped, so `porttest/phases`'s
`/var/lib/porttest/phase.log` is 226 B where the portage-built
reference gives 322 B (`pretend setup preinst postinst` vs
`setup preinst postinst`). Reproduced directly; root cause not yet
isolated (the archive's `DEFINED_PHASES`/`EAPI` are correct).

**`l2-gpkg-metadata-members` · MEDIUM (expanded at S3).** Beyond the
S0 five (`SIZE`, `IUSE`, `IUSE_EFFECTIVE`, `repository`,
`REPO_REVISIONS`), the source-build path also omits the ebuild-derived
`RDEPEND`/`REQUIRES`/`PROVIDES` and writes no `Packages` index `REPO`
field.

**`l2-gpkg-docompress` · MEDIUM.** Unchanged from S0 (no
`docompress`/`ecompress`; `BIG.txt` vs `BIG.txt.bz2`).

### Harness fixes made while adjudicating (not bug-hiding)

- `normalize.py`: `BINPKGMD5` blanked (VDB records the *source
  archive* md5; L2 compares different builds by design) and
  presence-only paths (regenerated caches) now have their `size`
  blanked too -- `/etc/ld.so.cache` drifts legitimately.
- `diff.py`: `CONTENTS` line diffs are no longer capped at 8/side; a
  `... +N more` aggregate cannot be adjudicated precisely.
- `gpkg_diff.py`: the `only in a`/`only in b` image labels were
  inverted (found by comparing the pair output against a direct tar
  listing). Fixed.

---

## S5 — real-set bring-up (`atomlists/l1-merge.txt`) (2026-09-13)

Command: `L2_REBUILD=1 L2_MODE=payload-tolerant L2_BUILD_MODE=deep
TEST/run/l2-portuale-builder.sh TEST/atomlists/l1-merge.txt`

Gate G0.1 was revisited before this run: real `--buildpkgonly` refuses
when a dependency is not already merged ("--buildpkgonly requires all
dependencies to be merged"), and the real set pulls not-installed deps
(`libmd`, `miscfiles`, `acct-*`, …). Both builders therefore gained a
`deep` mode (`-b --deep --usepkg=n` / `-b --deep`) that builds and
merges the closure into the throwaway builder first; `bpkgonly` stays
the default for the porttest set.

Portuale built and merged **13 packages from source** (oniguruma,
libmd, tree, pv, dmidecode, libbsd, htop, lsb-release, miscfiles,
gentoo-bashcomp, acct-group/user/nullmail, plus their phase chains)
before hitting a systemic blocker at `app-misc/jq`'s configure
(`Package 'oniguruma' not found`).

### l2-filesdir-symlink-missing · HIGH · FIXED

First blocker: `app-misc/jq`'s `src_prepare` died on
`eapply ... jq-1.6-r3-never-bundle-oniguruma.patch`, with the patch path
resolved under `${PORTAGE_BUILDDIR}/files` (nonexistent). Real portage
sets `FILESDIR = ${PORTAGE_BUILDDIR}/files` (`doebuild.py:527`) and
then makes that path a **symlink to the ebuild's own repo `files/`**
(`prepare_build_dirs._prepare_fake_filesdir`, `:504-515`). Portuale
computed `FILESDIR` the same way but never created the link, so any
ebuild patch/aux file (`eapply`, `FILESDIR`) failed. Fixed in
`ebuild_phases::compute_environment` (idempotent symlink, stale target
replaced); unit test
`compute_environment_links_builddir_files_to_the_repo_filesdir`.
After the fix jq logs `Applying jq-1.6-r3-never-bundle-oniguruma.patch`.

### l2-bpkgonly-env (scope widened) · HIGH · FIXED (#37 S1-S4, 2026-09-13; commits `ff9a2f6`/`de73b5c`/`5b45a50`/`c70df1f`) — S5 stop rule invoked

The build-phase environment is a curated whitelist, not the resolved
config environment. Evidence beyond the S3 `--buildpkgonly` findings,
now also on the **normal** `-b` build path:

| | real portage | portuale |
|---|---|---|
| oniguruma installed path | `/usr/lib64/{libonig.so.5.5.0,pkgconfig/oniguruma.pc}` | `/usr/lib/…` |
| archive `metadata/USE` | `abi_x86_64 amd64 elibc_glibc kernel_linux` | `abi_x86_64` (implicit profile flags missing) |
| archive `metadata/FEATURES` | resolved incremental list | raw process env |

`/usr/lib` vs `/usr/lib64` is `toolchain-funcs::get_libdir` falling
back to `lib` because the phase env carries no `MULTILIB_ABIS`/
`DEFAULT_ABI`/`LIBDIR_*`; jq's `econf` then cannot find
`oniguruma.pc` (pkg-config searches `lib64`). Same family as the S3
findings: missing implicit USE (`amd64`, `elibc_glibc`,
`kernel_linux`), resolved `FEATURES`, `SLOT`, `PORTAGE_REPO_NAME`.

**Stop rule:** this is a systemic core-path gap (threading the
resolved config environment into the build phase env), not a small
fix; more than three distinct portuale bugs block the real-set build
(`l2-filesdir` fixed; `l2-bpkgonly-env`/build-env, `l2-dostrip`,
`l2-needed-elf2`, `l2-consumes-portuale-env-degraded`,
`l2-metadata-members`, `l2-docompress` open). Per the plan, S5 stops
here: findings filed, no green run landed, and the real-set
cross-install (S4's real half) is blocked until the build-env gap is
fixed. The S4 **mechanism** is delivered and proven on the fixture
track (below).

#### S0 recon (#37, 2026-09-13) — exhaustive var-by-var

Ran per `docs/037_Build-phase-env-completeness.plan.md` S0: no product
code, archives only. Real-vs-portuale pairs from the last L2 run
(`TEST/logs/l2-20260913T014713Z`, mode=strict, porttest track):
`_l2-pkgcache-portage/porttest/{emptydirs,docs,splitdebug}` vs
`_l2-pkgcache-portuale/...`; normalized with
`TEST/compare/normalize.py::norm_environment` (imported, not copied).
The three fixtures give the **same** shape, so the tables below are
`emptydirs`' keys (counts: real 160, portuale 45; 118 only-real, 3
only-portuale, 2 value-differs). Repro:

```
tar xf TEST/logs/_l2-pkgcache-<pm>/porttest/emptydirs/emptydirs-1.0-1.gpkg.tar -C /tmp/x
tar xf /tmp/x/emptydirs-1.0-1/metadata.tar.zst -C /tmp/x/m
python3 - <<'PY'
import bz2, importlib.util
spec = importlib.util.spec_from_file_location("n", "TEST/compare/normalize.py")
n = importlib.util.module_from_spec(spec); spec.loader.exec_module(n)
print(n.norm_environment(open("/tmp/x/m/metadata/environment.bz2","rb").read()))
PY
```

**Value differs (2) — the core symptoms:**

| var | real | portuale | real setter | slice |
|---|---|---|---|---|
| `USE` | `abi_x86_64 amd64 elibc_glibc kernel_linux` | `""` (empty-IUSE fixture; `abi_x86_64` on oniguruma) | `config.py:3329` (`USE=PORTAGE_USE`), computed `:2261`; implicit part from `IUSE_EFFECTIVE` | S1/S2 (G3) |
| `FEATURES` | resolved incremental list (`assume-digests binpkg-docompress binpkg-dostrip … splitdebug … xattr`) | raw harness env (`buildpkg binpkg-multi-instance splitdebug xattr filecaps -cgroup -ccache -distcc -sign parallel-fetch`) | `config.py:3326` (`PORTAGE_FEATURES=FEATURES`); incremental fold over `make.globals:77-84` + profile + env | S1/S2 |

**Only real (118), grouped exhaustively** (one row per setter class;
every key named):

| group | keys | real setter | slice |
|---|---|---|---|
| `PORTAGE_FEATURES` | (as `FEATURES`) | `config.py:3326` | S1 |
| implicit IUSE | `IUSE_EFFECTIVE`, `IUSE_IMPLICIT` | `config.py:2218-2252`; profile `base/make.defaults:48` | S1 |
| arch/multilib | `ARCH`, `ELIBC`, `KERNEL`, `ABI`, `DEFAULT_ABI`, `MULTILIB_ABIS`, `LIBDIR_{amd64,x86,x32}`, `SYMLINK_LIB` | `profiles/arch/amd64/make.defaults:4,15-28`; profile chain | S1/S2 |
| multilib toolchain | `CHOST_{amd64,x86,x32}`, `CFLAGS_{amd64,x86,x32}`, `LDFLAGS_{amd64,x86,x32}` | same `make.defaults:26-28` | S1 |
| `COMMON_FLAGS` | | `profiles/base/make.defaults` | S1 |
| USE_EXPAND machinery | `USE_EXPAND_IMPLICIT`, `USE_EXPAND_UNPREFIXED`, `USE_EXPAND_VALUES_{ARCH,ELIBC,KERNEL}`, `PROFILE_ONLY_VARIABLES` | profile `base/make.defaults:48`; `config.py:2218-2252` | S1 |
| empty USE_EXPAND placeholders | `ABI_MIPS ABI_S390 ABI_X86 ADA_TARGET ALSA_CARDS AMDGPU_TARGETS APACHE2_MODULES APACHE2_MPMS CALLIGRA_FEATURES CAMERAS COLLECTD_PLUGINS CPU_FLAGS_ARM CPU_FLAGS_PPC CPU_FLAGS_X86 CURL_QUIC CURL_SSL GCC_SPECS GPSD_PROTOCOLS GRUB_PLATFORMS GUILE_SINGLE_TARGET GUILE_TARGETS INPUT_DEVICES L10N LCD_DEVICES LIBREOFFICE_EXTENSIONS LLVM_SLOT LLVM_TARGETS LUA_SINGLE_TARGET LUA_TARGETS NGINX_MODULES_HTTP NGINX_MODULES_MAIL NGINX_MODULES_STREAM OFFICE_IMPLEMENTATION OPENMPI_FABRICS OPENMPI_OFED_FEATURES OPENMPI_RM PERL_FEATURES PHP_TARGETS POSTGRES_TARGETS PYTHON_SINGLE_TARGET PYTHON_TARGETS RUBY_TARGETS RUST_SYSROOTS SANE_BACKENDS SELINUX_POLICY_TYPES UWSGI_PLUGINS VIDEO_CARDS VOICEMAIL_STORAGE XTABLES_ADDONS` (each `=""`) | `config.py:2247` `addLazySingleton(k, …)` — one lazy empty value per `USE_EXPAND` var not otherwise set | S1 |
| portage/config scalars | `PORTAGE_COMPRESSION_COMMAND` (=`zstd -T1`), `PORTAGE_REPO_REVISIONS`, `ENV_UNSET`, `BOOTSTRAP_USE`, `MULTILIB_STRICT_{DENY,DIRS,EXEMPT}` | `doebuild.py:750` (dynamic); `EbuildPhase.py:73-108`; profile `base/make.defaults:44,122` | S1/S2 |
| make.globals scalars | `BINPKG_GPG_SIGNING_{BASE_COMMAND,DIGEST}`, `BINPKG_GPG_VERIFY_{BASE_COMMAND,GPG_HOME}`, `GPG_VERIFY_{USER,GROUP}_DROP` | `make.globals:47-60` | S1 |
| calling env / env.d | `L2_BUILD_MODE L2_JOBS L2_PORTAGE_PIN L2_SKIP_PORTAGE_UPGRADE LEX MANPAGER TZ PYTHONDONTWRITEBYTECODE TWISTED_DISABLE_WRITING_OF_PLUGIN_CACHE ac_cv_c_undeclared_builtin_options enable_year2038 gl_cv_compiler_check_decl_option gl_cv_func_getcwd_path_max container` | `config.py:551,567` — **real imports the whole process env as `configdict["env"]`** | S1 (G1 — see below) |
| locale | `LANG LC_*` | harness locale; `norm_environment` drops them | not a finding |

**Only in portuale (3) — divergences:**

| var | real | portuale | cause | action |
|---|---|---|---|---|
| `AA` | absent (popped) | `""` | `config.environ()` pops `AA` when `eapi_exports_AA` is false — EAPI 8 here (`config.py:3331-3333`) | S2: stop exporting `AA` (EAPI floor is 5+, so always) |
| `O` | absent | `/var/db/repos/porttest/porttest/emptydirs` | real's `environ_filter` contains `O` (`special_env_vars.py:300`) | S2: add `O` to the builder's filter before `extra_env` (or drop it in `run_commands`) |
| `LC_ALL` | absent | `C.UTF-8` | harness container export; dropped by `norm_environment` | not a finding |

**Same key, wrong export state — `SLOT` (the `.keep_*` bug).** Both
envs carry `SLOT=0`, but real has `declare -x SLOT="0"` (exported) and
portuale `declare SLOT="0"` (a plain ebuild-global assignment, not
exported). `keepdir` is the **external subprocess**
`bin/ebuild-helpers/keepdir`, which reads `${SLOT%/*}` from its
environment — so under portuale it sees nothing and writes
`.keep_porttest_emptydirs-` where real writes `-0`. Mechanism confirmed;
the S2 fix is to export the graph entry's `SLOT` in the phase env
(`declare -x`), not merely to have it set in the shell.

**G1 evidence — the export set must be the full resolved layer, not the
whitelist.** Real's saved env is the accumulated **setup-phase full
dump**: real loads `os.environ` wholesale as the top config layer
(`config.py:551,567`), `config.environ()` exports every string value
except `environ_filter` (`special_env_vars.py:257-348`) and, on the
first phase, every key (the `environ_whitelist` restriction kicks in
only once `$T/environment` exists, `config.py:3275-3305`), and later
phases carry that dump forward. Portuale runs every phase in a fresh
shell (`run_commands_async` loops `run_one_phase`, no source of
`$T/environment`) with only 45 keys, so a whitelist-only builder cannot
make the porttest track's `metadata/environment.bz2 differs` row pass.
**S0 revises the plan's G1 recommendation to deepseek's route:
`(resolved config scalars ∪ process env) − environ_filter −
PORTUALE_COMPUTED`, plus synthesized empty `USE_EXPAND` placeholders.**
The `PORTUALE_COMPUTED` guard stays mandatory (computed paths must win);
the per-key review is now an explicit allow/deny list rather than a
narrow whitelist. Owner decision still required (plan §4 G1).

Pre-existing nuance, not #37's: real's later phases only re-export the
whitelist, so an ebuild `unset` persists; portuale's fresh shell re-adds
the full env each phase. File separately if it ever shows in a fixture
(bug 189417 behaviour).

**S5 real-set confirmation (oniguruma).** Retained evidence: the
`l2-20260913T014700Z` classification (portuale-built oniguruma missing
metadata members) plus the cache-less debug dir `TEST/logs/_l2-jqtest/
dev-libs/oniguruma/oniguruma-6.9.10-{1,2}.gpkg.tar` (portuale-built;
ad-hoc session — treat values as evidence of the mechanism, not a
reference). Portuale env: `USE="abi_x86_64"` (no implicit flags),
`MULTILIB_ABIS="default"`, `DEFAULT_ABI="default"`, `LIBDIR_amd64`
absent, `ABI`/`ARCH`/`ELIBC`/`KERNEL` absent, `PORTAGE_REPO_REVISIONS`
absent, `PORTAGE_FEATURES` absent, `COMMON_FLAGS` absent; image installs
`/usr/lib/libonig.so.5.5.0` and `/usr/lib/pkgconfig/oniguruma.pc`. The
`"default"` values are **written by real `multilib.eclass:23-24`**
(`export MULTILIB_ABIS=${MULTILIB_ABIS:-"default"}`) because the phase
env lacked them — that is exactly why `toolchain-funcs::get_libdir`
falls back to `lib` and jq's `econf` cannot find `oniguruma.pc`. Real
oracle (`TEST/logs/_l1-pkgcache/dev-libs/oniguruma/...-1`): `USE="abi_x86_64
amd64 elibc_glibc kernel_linux"`, `MULTILIB_ABIS="amd64 x86"`,
`DEFAULT_ABI="amd64"`, `LIBDIR_amd64="lib64"`, `ABI="amd64"`,
`COMMON_FLAGS="-O2 -pipe"`, `NEEDED=/usr/lib64/libonig.so.5.5.0`.

**Routing:** everything above lands inside #37 S1/S2 (the builder +
threading) except the metadata-member half (`SIZE`, `IUSE`, `repository`,
`REPO_REVISIONS`, `NEEDED.ELF.2` class) which stays #39, and the
`l2-dostrip`/`l2-docompress` payload rows which stay #38 (their triggers
are the same resolved `FEATURES`/`PORTAGE_COMPRESS` S1 threads). New
sub-findings filed here and absorbed into #37 S2: **`l2-env-aa-exported`**
(AA must be popped, EAPI ≥4), **`l2-env-o-exported`** (O is
`environ_filter`ed by real), **`l2-env-setup-full-dump`** (the G1 scope
finding above). No new backlog items needed.

#### S2 — resolved phase env threading (#37, 2026-09-13)

`emerge <atom>`/`--resume`/`--buildpkgonly` now run every phase with
the resolved-config env (`portage_profile::phase_environ` + the
per-entry `entry_phase_env_tail`), and `AA`/`O` are no longer exported
(real pops `AA` for EAPI ≥ 4 and `environ_filter`s `O`). Rust e2e
covering both backends: `emerge_build::
source_merge_with_resolved_config_threads_use_and_slot_into_the_phase`
(new `phaseenvpkg` fixture: keepdir marker + `USE`/`SLOT`/`FEATURES`
echoes), plus the `--buildpkgonly` archive/`Packages` test. Full
evidence: `TEST/logs/l2-20260913T131426Z` (`L2_REBUILD=1`, porttest
track).

**Now matching real (rows deleted):**
`metadata/USE` (`abi_x86_64 amd64 elibc_glibc kernel_linux`),
`metadata/FEATURES` (resolved incremental), `metadata/SLOT` export (and
thus `.keep_porttest_emptydirs-0` from the external
`ebuild-helpers/keepdir` subprocess), `PORTAGE_REPO_NAME` (the
`repository` file is written; the saved env drops it exactly like
real's readonly filter), `PORTAGE_REPO_REVISIONS="{}"`,
`MULTILIB_ABIS`/`DEFAULT_ABI`/`LIBDIR_amd64`/`ABI`, O/AA absence.
Consequences beyond #37: **`ecompress` and `estrip`/`splitdebug` now
fire** (the gates read the resolved `FEATURES` + `PORTAGE_COMPRESS`
from `make.globals`) — `docs` archives carry `BIG.txt.bz2` (the
`l2-gpkg-docompress*` allowlist entries are deleted), `splitdebug`
archives carry the `/usr/lib/debug/**` + `.build-id/**` tree with the
same path set as real. The `l2-gpkg-dostrip-splitdebug` residual rows
are now compiled-artifact byte differences only (the main binary
payload differs for the same reason). Track after the allowlist
cleanup: **0 unexplained**, 89 explained, cross-install 0/0, control
0/0.

**Residual `environment.bz2` differences (17 normalized lines, filed):**
- `l2-env-pkg-vars-unexported` — real exports pkg metadata vars from
  `configdict["pkg"]` (`declare -x DEFINED_PHASES/KEYWORDS/LICENSE`);
  portuale carries them as ebuild globals (plain `declare`, same
  values). Fix: export the md5-cache pkg fields with the per-entry
  env.
- `l2-env-compression-command` — real's `doebuild_environment()` sets
  `PORTAGE_COMPRESSION_COMMAND` (`doebuild.py:707-750`); portuale only
  computes it inside the packaging call. Fix: derive it in the
  per-entry env from `BINPKG_COMPRESS` (placeholder `PORTUALE_COMPUTED`
  currently excludes it).
- `l2-env-profile-only-vars` — `PROFILE_ONLY_VARIABLES` loses the
  initial `ARCH` and gains a leading space (a `${VAR}`-substitution
  difference in the profile chain, `profiles/base/make.defaults:48`);
  S1-class, not threading.
- `l2-env-path-aclocal` — the saved `aclocal`/`autoconf`/`automake`/
  `libtoolize` wrapper functions resolve to `/usr/sbin/*` under
  portuale and `/usr/bin/*` under real (phase `PATH` order), a
  pre-existing PATH-shape difference the saved function bodies expose.

#### S3 — resolved FEATURES for the Rust-side gates (#37, 2026-09-13)

Every phase-execution decision now reads the **resolved** `FEATURES`
list threaded on `extra_env` (`ebuild_phases::features_string`) instead
of the raw process env: the sandbox family (`sandbox`/`usersandbox`,
`{network,ipc,mount,pid}-sandbox`), `distlocks`/`force-mirror`, plus
`split-log`/`compress-build-logs` (`emerge_build::resolved_features`),
`split-elog` (`elog::process_batch` now takes it), the merge-time
tokens (`MergeOptions::set_resolved_features` re-derives
`collision-protect`/`protect-owned`/`config-protect-if-modified` and the
GPKG verify policy), `FEATURES=buildpkg` (`buildpkg_from_config`) and
the `PackageOptions` packaging tokens. The raw reads survive only as
documented standalone fallbacks (`ebuild <file>`, and
`execute_unmerge`, which receives no config).

L2 porttest evidence (`TEST/logs/l2-20260913T140214Z`, `L2_REBUILD=1`;
classification re-run `l2-20260913T140802Z`/`...1408xx`): the resolved
list now drives portuale's own isolation gates — the build log carries
portuale's `!!! Unable to unshare (for FEATURES="network-sandbox /
ipc-sandbox / mount-sandbox / pid-sandbox")` degrade (real's own EPERM
behaviour in the container), where before S3 portuale never even
attempted isolation. Track 0 unexplained after allowlisting two
`SYMLINK` rows: `pt-setgid`/`pt-sticky` are byte-identical binaries
sharing one build-id, so the shared `.build-id` link target is
`estrip`'s find-traversal order (either name is correct;
`l2-gpkg-dostrip-splitdebug` gained the SYMLINK category).

Filed residual: `execute_unmerge`'s `unmerge-backup`/`split-elog` keep
the raw env (no resolved config reaches that helper yet); standalone
`ebuild <file>` deliberately does the same.

#### S4 — container proof + allowlist cleanup (#37, 2026-09-13)

**Porttest track, allowlist row deleted.** `l2-bpkgonly-env` is gone
from `l2-portuale-builder.sh` `KNOWN_FINDINGS` and from
`known-divergences.yaml`. The four S2 residuals were phase-env gaps
and are fixed:

- `l2-env-pkg-vars-unexported` → `emerge_build::entry_metadata_env`
  exports `DEFINED_PHASES`/`KEYWORDS`/`LICENSE` from the md5-cache
  (real `_setcpv_aux_keys`; the other aux keys are `unset` by
  `bin/ebuild.sh` before sourcing, so these three are the ones that
  stay `declare -x`).
- `l2-env-compression-command` → `emerge_build::run_wide_phase_env`
  adds `PORTAGE_COMPRESSION_COMMAND` for every build via
  `ebuild_package::phase_compression_command` (`doebuild.py:697-750`:
  `BINPKG_COMPRESS[_FLAGS_<NAME>]`, `{JOBS}` from `MAKEOPTS` through a
  port of `makeopts_to_job_count`) — `zstd -T1` under `MAKEOPTS=-j1`,
  not the host CPU count.
- `l2-env-profile-only-vars` → `PROFILE_ONLY_VARIABLES` is in real
  `const.INCREMENTALS`; `portage_profile::TRACKED_INCREMENTALS` now
  folds it and `phase_environ` exports the sorted fold.
- `l2-env-path-aclocal` → real `_doebuild_path` drops the calling env's
  `PATH` when `env.d` sets one (`doebuild.py:365-378`); `Config::
  envd_sets_path` + `phase_environ` export the config-stack `PATH`,
  and `ebuild_phases::phase_path` prepends the helper dir onto it
  (deduplicated like real). The saved `aclocal`/`autoconf` wrappers now
  resolve to `/usr/bin`.

Same slice, from `doebuild.py:646-653`: `MAKEOPTS=-j<nproc>` +
`GNUMAKEFLAGS` are filled when neither `MAKEOPTS` nor `MAKEFLAGS` is
set.

Evidence: `L2_REBUILD=1 TEST/run/l2-portuale-builder.sh
TEST/atomlists/l1-porttest.txt` → `TEST/logs/l2-20260913T174459Z`: every
archive `metadata/environment.bz2` equals real's after
`norm_environment` (no allowlist row covers it), **0 unexplained**,
cross-install 0 / control 0, rc 0.

**New finding (not #37): `l2-pkgindex-eapi-missing`** · MEDIUM · OPEN,
#39. The cross-install vdb `environment` of every portuale-built package
gained `DESTTREE`/`INSDESTTREE`/`PORTDIR`/`ECLASSDIR`. Cause: portuale's
`Packages` stanza has no `EAPI` (nor `DEFINED_PHASES`, real
`bintree._pkgindex_aux_keys`); under `pkgdir-index-trusted` real reads
the binpkg's EAPI from the index, defaults it to `"0"`
(`bintree.py:615`), and its merge-time phases then take the EAPI ≤ 6
branches (`phase-helpers.sh:6-8`, `config.py:3387-3390`). The producer
env itself is identical (see above). Allowlisted narrowly as
`l2-pkgindex-eapi-missing` (VDB `porttest/*/environment`); the vdb
consolidated `metadata` file moved under `l2-gpkg-metadata-members`
(its residual lines are `IUSE=`/`PROVIDES`/`RDEPEND`/`REQUIRES`).

**Real set** (`L2_REBUILD=1 L2_MODE=payload-tolerant L2_BUILD_MODE=deep
TEST/run/l2-portuale-builder.sh TEST/atomlists/l1-merge.txt` →
`TEST/logs/l2-20260913T174940Z`): **both builders build the whole
closure (18 packages)**. oniguruma installs `/usr/lib64/libonig.so.5.5.0`
+ `/usr/lib64/pkgconfig/oniguruma.pc` (`metadata/USE="abi_x86_64 amd64
elibc_glibc kernel_linux"`, `NEEDED=/usr/lib64/libonig.so.5.5.0`); jq
configures against it and installs into `/usr/lib64`. Two more env gaps
surfaced on the way and are fixed in S4:

- `l2-env-src-uri-use` — bash-completion `src_prepare` died on
  `eapply ${WORKDIR}/bashcomp-2.0.3/…patch: No such file`:
  `fetch::fetch_src_uri` reduced `SRC_URI` with every `flag?` off, so
  the `eselect? ( bashcomp-2.0.3.tar.gz )` distfile was never fetched
  or in `A` while `use eselect` was true. `FetchOptions::use_flags` now
  carries the threaded `USE` (real `use_reduce(SRC_URI,
  uselist=PORTAGE_USE)`).
- `l2-env-path-trailing-slash` — pv/htop `KERNEL_DIR="//usr/src/linux"`:
  real strips the trailing `/` from `D`/`ED`/`ROOT`/`EROOT` from EAPI 7
  on (`eapi.py:307`, `config.py:3392-3395`), exporting `ROOT=""` for
  `/`; `ebuild_phases::eapi_path_var` now does the same.

After both, the real-set archive diff has **no environment row**. The
remaining 8 unexplained archive rows are `metadata/{DEPEND,RDEPEND}`
(pv, libbsd, jq, htop, sudo): real's install-time dep rewrite —
`[flag?]` use-deps evaluated, `:SLOT/SUB=` bound, the injected
`>=sys-libs/glibc` (`doebuild._inject_libc_dep`) — filed as
**`l2-gpkg-dep-metadata-rewrite`** (#39). The cross-install then stops
before diffing: real Portage does not select most portuale-built
binaries (the `Packages` stanzas carry no `REPO`/`EAPI` and unreduced
USE-conditional `RDEPEND`), falls back to the image's
`/var/cache/binhost` for `oniguruma-6.9.10-6`, and that fails GnuPG
verification in the consume container — the same #39 index gap plus a
harness binhost/GPG setting, **not env completeness**. New stop point
recorded; the S4 exit ("the real set's next blocker is not env
completeness") holds.

**Note (owner, 2026-09-13) — disable the GPG check for binary packages
in the test bed; fix both the container and portuale.** Filed as
`l2-binpkg-gpg-check`, OPEN; now also tracked as backlog #43 (see
"#38 S5" below).

- *Container:* the catalyst stage ships
  `/etc/portage/binrepos.conf/gentoo.conf` (`location =
  /var/cache/binhost/gentoo`, `verify-signature = true`), and
  `TEST/layers/l1/consume.sh` merges with `-k --getbinpkg`, so any
  binary real Portage won't take from `$PKGDIR` falls back to that
  binhost and dies on `GnuPG verification failed`
  (`l2-20260913T174940Z/cand.merge.log`, `oniguruma-6.9.10-6`). Fix in
  the image / consume env: `verify-signature = false` (or drop the
  gentoo binrepo) and/or `FEATURES=binpkg-ignore-signature` in
  `consume.sh` / `build-*.sh`; the `$PKGDIR` sets both builders produce
  are unsigned anyway.
- *portuale:* `binpkg::GpgVerify` ignores a per-binrepo
  `verify-signature = false` (documented cut, `binpkg.rs:1107-1113` —
  `FEATURES` alone decides). Once the container disables verification
  through `binrepos.conf`, portuale must honour the same key or the two
  consumers diverge again; `binpkg-ignore-signature` via the resolved
  `FEATURES` already works (#37 S3 `GpgVerify::from_features`).

### S4 — cross-install direction A + tolerance wiring

The mechanism shipped with S3's orchestrator: real Portage merges the
portuale-built candidate and the portage-built reference in identical
fresh containers, and `diff.py --layer l2 --tolerate-payload` compares
them; the portuale-consumes-portage control runs alongside. On the
`porttest` track it is green modulo the filed gaps (161 hard findings,
all explained; 0 unexplained). On the real set it cannot run until the
build-env gap above is fixed; the payload-tolerance discriminator
design (two-Portage-build nondeterminism set) was therefore not needed
yet -- `--tolerate-payload` is wired and its semantics are pinned by
`test-diff-tolerance.py`.

### S5 — docs closeout + `--buildpkgonly` == `-b` proof (#37, 2026-09-13)

**Fresh porttest-track re-run (reproducibility of the S4 proof).**

```
L2_REBUILD=1 TEST/run/l2-portuale-builder.sh TEST/atomlists/l1-porttest.txt
```

→ `TEST/logs/l2-20260913T182502Z`, rc 0: 0 unexplained (88 known
`l2-gpkg-*` rows, all #38/#39); cross-install 95 hard / 95 explained /
0 unexplained, control 0/0. `porttest/docs`'s portuale-built archive
(`TEST/logs/_l2-pkgcache-portuale/porttest/docs/docs-1.0-1.gpkg.tar`)
carries the same metadata as the portage-built pair:

```
A=TEST/logs/_l2-pkgcache-portuale/porttest/docs/docs-1.0-1.gpkg.tar
for f in USE FEATURES SLOT; do
  tar -xOf "$A" docs-1.0-1/metadata.tar.zst | zstd -dc | tar -xOf - "metadata/$f"
done
# USE:      abi_x86_64 amd64 elibc_glibc kernel_linux
# FEATURES: assume-digests binpkg-docompress binpkg-dostrip ... xattr
# SLOT:     0
```

and each archive's normalised `metadata/environment.bz2` equals real's
(no allowlist row covers it); `.keep_porttest_emptydirs-0` matches (S2).

**DoD — `--buildpkgonly` == `-b` archive metadata.** Two throwaway
containers built the same fixture with the same env — one via
`/TEST/layers/l2/build-portuale.sh` (`--buildpkgonly`, PKGDIR=a), one
via `emerge -b --oneshot porttest/docs` (PKGDIR=b) — then:

```
TEST/compare/gpkg-diff.sh --mode strict \
  a/porttest/docs/docs-1.0-1.gpkg.tar b/porttest/docs/docs-1.0-1.gpkg.tar
# gpkg-diff: mode=strict hard=0 soft=0     (rc 0)
```

The archives are identical modulo the tool's volatile set
(`BUILD_TIME`/`BUILD_ID`/`COUNTER`, normalised `environment`, mtimes,
Manifest hashes) — the S2 exit bar. Artifacts retained under
`TEST/logs/_l2-b-vs-B/` (`buildpkgonly-docs-1.0-1.gpkg.tar`,
`build-docs-1.0-1.gpkg.tar`, `gpkg-diff.txt`).

**DoD — `SOURCE_DATE_EPOCH` reaches the phase from config (L3 G0.6).**
`portage_profile::phase_environ` passes the `make.conf` scalar through
(S1); unit test
`phase_environ_exports_the_profile_family_and_folded_incrementals`
(`rust/portage-profile/src/phase_environ.rs`) asserts
`SOURCE_DATE_EPOCH="1740000000"` from the seeded `make.conf` reaches the
built env. L3 status pointer recorded in
`docs/030_L3-source-build-parity.deepseek.md` under G0.6.

**Current real-set stop point (not env).** With #37 closed, the next
blockers are #38 (dostrip/splitdebug/docompress) and #39 (install-time
`*DEPEND` rewrite + `Packages` index `EAPI`/`REPO`), then the test-bed
GPG check (`l2-binpkg-gpg-check`); portuale builds all 18 closure
packages and oniguruma is `/usr/lib64` (S4).



---

## #38 recon — packaging transforms under #37's env (S0, 2026-09-13)

Plan: [`docs/038_Packaging-transforms.plan.md`](../docs/038_Packaging-transforms.plan.md) S0.
Input run: `TEST/logs/l2-20260913T182502Z` (`L2_REBUILD=1`, porttest track,
post-#37 `f5157a0`); portuale `metadata/FEATURES` carries
`binpkg-docompress binpkg-dostrip` (precondition met). No code changed.

**Expected trees frozen from the real oracle** (`TEST/logs/_l1-pkgcache/
porttest/{docs,splitdebug,setuid}/*-1.0-1.gpkg.tar`, built 2026-09-08 by
real portage; non-dir entries, mode size path):

```
lst(){ tar xOf "$1" "$2-1.0-1/image.tar.zst" | zstd -dc | tar tvf - ; }
# docs
-rw-r--r--   279 usr/share/doc/docs-1.0/BIG.txt.bz2
-rw-r--r--     6 usr/share/doc/docs-1.0/RENAMED.txt
-rw-r--r--    14 usr/share/doc/docs-1.0/html/index.html
-rw-r--r--     6 usr/share/doc/docs-1.0/small.txt
-rw-r--r--     7 usr/share/doc/docs-1.0/sub/NESTED.md
-rw-r--r--    27 usr/share/info/pt.info
-rw-r--r--    33 usr/share/man/man1/pt.1
# splitdebug
-rwxr-xr-x 14536 usr/bin/pt-splitdebug
lrwxrwxrwx     0 usr/lib/debug/.build-id/7c/f6c1b1efe7dea4a0c7a37e93e3082272c930df -> ../../../../../usr/bin/pt-splitdebug
lrwxrwxrwx     0 usr/lib/debug/.build-id/7c/f6c1b1efe7dea4a0c7a37e93e3082272c930df.debug -> ../../usr/bin/pt-splitdebug.debug
lrwxrwxrwx     0 usr/lib/debug/.build-id/e8/95ec8e694b01b2ed90cf7608cc0195b4d4f796 -> ../../../../../usr/lib64/libptsd.so.0.0.0
lrwxrwxrwx     0 usr/lib/debug/.build-id/e8/95ec8e694b01b2ed90cf7608cc0195b4d4f796.debug -> ../../usr/lib64/libptsd.so.0.0.0.debug
-rw-r--r--  9152 usr/lib/debug/usr/bin/pt-splitdebug.debug
-rw-r--r--  7728 usr/lib/debug/usr/lib64/libptsd.so.0.0.0.debug
lrwxrwxrwx     0 usr/lib64/libptsd.so -> libptsd.so.0.0.0
lrwxrwxrwx     0 usr/lib64/libptsd.so.0 -> libptsd.so.0.0.0
-rwxr-xr-x 14008 usr/lib64/libptsd.so.0.0.0
# setuid
-rwxr-sr-x 14384 usr/bin/pt-setgid
-rws--x--x 14384 usr/bin/pt-setuid
-rwxr-x--T 14384 usr/bin/pt-sticky
lrwxrwxrwx     0 usr/lib/debug/.build-id/e0/2277d7bad071a775099e30a4087cdd635e22b0 -> ../../../../../usr/bin/pt-{setuid|setgid|sticky}   (race, see below)
lrwxrwxrwx     0 usr/lib/debug/.build-id/e0/2277d7bad071a775099e30a4087cdd635e22b0.debug -> ../../usr/bin/pt-{same}.debug
-rw-------  7512 usr/lib/debug/usr/bin/pt-setgid.debug
-rw-------  7512 usr/lib/debug/usr/bin/pt-setuid.debug
-rw-r--r--  7512 usr/lib/debug/usr/bin/pt-sticky.debug
-rw-------     7 usr/share/porttest/pt-0600
```

**Fixture × branch table** (`gpkg-diff.sh --mode strict` real-L2 vs
portuale-L2, plus the path+size+link diff of each against the oracle):

| fixture | branch | fired? | output = oracle? | cause class |
|---|---|---|---|---|
| docs | `ecompress` (`misc-functions.sh:149`) | yes | **yes** — path set, sizes, `BIG.txt.bz2` 279 B, `small.txt` < 128 B plain, `html/` skipped; `pt.1` (33 B) and `pt.info` (27 B) plain — under `PORTAGE_DOCOMPRESS_SIZE_LIMIT=128`, not because man/info are skipped (the README's "doman compressed" is a claim the oracle doesn't bear out for this fixture) | fixed (by #37) |
| splitdebug | `estrip` (`:243`) + `splitdebug` | yes | path set yes; **build-ids + `.debug` bytes no** (+8 B each, `5c…`/`84…` vs `7c…`/`e8…`) | **portuale-env** |
| setuid | `estrip` (`:243`) + `splitdebug` | yes | sizes/modes/build-id yes; `.build-id` link target no | real-divergence (nondeterministic in real) |

- **splitdebug → portuale-env: wrong `PORTAGE_TMPDIR` default.**
  `strings` over the two `pt-splitdebug.debug` files differs in exactly one
  line: real `/var/tmp/portage/porttest/splitdebug-1.0/work`, portuale
  `/var/tmp/portage/portage/porttest/splitdebug-1.0/work` (the DWARF
  `DW_AT_comp_dir`; +8 B = `portage/`, which also changes the salted
  build-id). Real `make.globals:35` is `PORTAGE_TMPDIR="/var/tmp"` and
  `PORTAGE_BUILDDIR=${PORTAGE_TMPDIR}/portage/${CATEGORY}/${PF}`; portuale's
  CLI boundaries fall back to `/var/tmp/portage` (`pretend.rs`
  `run`×2/`run_info`/`run_config_action`/`execute_unmerge`, `ebuild.rs`
  `run`, `regen.rs`) and then join `portage/<cat>/<pf>` on top. Real is
  reproducible here: the 2026-09-08 oracle and today's real L2 build carry
  the same `7c…`/`e8…` build-ids. This also **corrects #37 S2's
  classification** above ("compiled-artifact byte differences") — the
  bytes differ because of the build path, not the compiler.
  Repro: `S=$(mktemp -d); for w in portage portuale; do mkdir $S/$w; tar
  -xOf TEST/logs/_l2-pkgcache-$w/porttest/splitdebug/splitdebug-1.0-1.gpkg.tar
  splitdebug-1.0-1/image.tar.zst | zstd -dc | tar -xC $S/$w; done; diff
  <(strings $S/portage/image/usr/lib/debug/usr/bin/pt-splitdebug.debug)
  <(strings $S/portuale/image/usr/lib/debug/usr/bin/pt-splitdebug.debug)`.
  Per G6 this is fixed as a #37 follow-up (CLI default → resolved config),
  then S2 re-grades.
- **setuid → real-divergence.** `pt-setuid`/`pt-setgid`/`pt-sticky` are one
  compiled binary installed three times (identical bytes, one build-id);
  `estrip` processes ELF files through `___parallel` and `__try_symlink`
  keeps whichever name lands first. Real disagrees with itself: the oracle
  links `pt-setuid`, today's real L2 build links `pt-sticky`, portuale
  `pt-setgid`. Not gradable; stays an adjudicated row (narrowed to this
  one symlink pair in S2).
- **docs → fixed.** The `l2-gpkg-docompress` entry above is stale: its
  allowlist rows were already deleted in #37 S2; closed with evidence in S1.

**Tool presence** (`localhost/test-portuale:latest`, both PMs share it):

| tool | path / version | used by |
|---|---|---|
| `debugedit` | `/usr/sbin/debugedit` 5.3 | build-id salt (`estrip:160-170`) |
| `x86_64-pc-linux-gnu-{objcopy,strip,readelf,ranlib}` | `/usr/sbin/` (binutils) | `estrip` `name_of` |
| `scanelf` | `/usr/sbin/scanelf` (pax-utils 1.3.10) | NEEDED writer, `estrip` ELF scan |
| `dwz` | **absent** | only `dedupdebug` (off) — no effect |
| `bzip2` | `/usr/sbin/bzip2` | `PORTAGE_COMPRESS` |

Image `/usr/share/portage/config/make.globals` has `PORTAGE_TMPDIR="/var/tmp"`
(base portage 3.0.81.3, upgraded to the 3.0.82.2 pin by both builders).
Version skew between the vendored `bin/` and 3.0.82.2 produced no
transform-output row.

**Real `instprep` call order (G2 data).**

| step | real | portuale |
|---|---|---|
| 1 | `install` chain → `install_qa_check` (default `binpkg-*` on: `ecompress`/`estrip` run here, `misc-functions.sh:147-152,239-250`) | same, `run_commands_async` after `install` |
| 2 | `package` (`__dyn_package`, `:540`) when building a binpkg | same |
| 3 | `dblink.treewalk()` → `doebuild_environment(..., "instprep")` + `EbuildPhase("instprep")` **before** the copy loop, on *every* merge incl. `EMERGE_FROM=binary` (`vartree.py:4440-4450`; `doebuild.py:880` `"instprep": ["install"]`) | **never run** (`ebuild.rs` stub list) |
| 4 | `__dyn_instprep` (`:265-308`): no-op if `.instprepped`; `ecompress` iff `PORTAGE_COMPRESS` set **and** `binpkg-docompress` absent; `estrip` iff `binpkg-dostrip` absent; `chflags` save/restore; touch `.instprepped` | — |

With default `FEATURES` step 4 does nothing observable, so the divergence
only shows under `-binpkg-docompress`/`-binpkg-dostrip`; the L2/L3 harness
sets neither today → S4 default stays "file #38b" pending the user's call.

Systemic non-env blockers: 0. `portuale-env` rows: 1 (tmpdir) → G6.

---

## #38 S1 — docompress (2026-09-13)

Plan: [`docs/038_Packaging-transforms.plan.md`](../docs/038_Packaging-transforms.plan.md)
S1; S0 froze the oracle trees above. The gate (`misc-functions.sh:147-152`)
already fires post-#37; S1 adds the proof and the host end-to-end test.
No product code changed (the `l2-gpkg-docompress` allowlist rows were
already deleted in #37 S2).

**Container (oracle) evidence.** Fresh `L2_REBUILD=1 TEST/run/l2-portuale-builder.sh
TEST/atomlists/l1-porttest.txt` → `TEST/logs/l2-20260913T191450Z`, rc 0:
`porttest/docs`' portage-built and portuale-built archives differ only in
the filed #39 metadata rows (`IUSE`, `IUSE_EFFECTIVE`, `SIZE`); the image
path set, modes, symlink targets and payload sha256 are `gpkg-diff.sh
--mode strict` clean. The frozen oracle tree above is what both carry:
`BIG.txt.bz2` 279 B, `small.txt` plain (< `PORTAGE_DOCOMPRESS_SIZE_LIMIT`),
`html/` skipped, `pt.1`/`pt.info` plain (33 B/27 B).

**Host e2e (Rust).** `cargo test --release -p portuale
install_qa_check_docompress_compresses_docs_and_repairs_symlinks` —
`fixtures/repo/dev-libs/doccompresspkg` runs the real `install` chain +
real `install_qa_check` under the default `bash` backend with
`PORTAGE_COMPRESS=bzip2` and `FEATURES=binpkg-docompress`; asserts
`BIG.txt.bz2` exists, `BIG.txt` is gone, `small.txt` stays plain, and the
link into the compressed doc is repaired to `link-to-big.txt.bz2 ->
BIG.txt.bz2` by `ecompress`'s own `fix_symlinks`.

**Host e2e (CLI).** `python3 -m pytest tests/test_portuale.py -q -k
docompress` — `emerge dev-libs/doccompresspkg` with the same env merges
the compressed tree and the merged VDB `CONTENTS` records
`/usr/share/doc/doccompresspkg-1.0/BIG.txt.bz2` (and no plain
`BIG.txt` line). Both host tests skip with an explicit reason when
`bzip2` is absent.

## #38 S2 — dostrip + splitdebug (2026-09-13)

Plan: S2. One product fix, in #37's boundary (G6): the CLI defaults were
`PORTAGE_TMPDIR=/var/tmp/portage` and then appended `portage/<cat>/<pf>`,
doubling the component. Real `cnf/make.globals:35` is `/var/tmp`; the
double path changed the DWARF `DW_AT_comp_dir` (`…/portage/portage/porttest/
splitdebug-1.0/work`) and therefore `debugedit`'s salted build-id and the
whole compiled payload.

**Fix.** `portage_repo::PORTAGE_TMPDIR_DEFAULT = "/var/tmp"` +
`portage_tmpdir_from_env()` / `portage_tmpdir_from_config()` (the calling
env is real's highest config layer; else the resolved config scalar from
`Config::other_vars`; else the real default). Every CLI boundary now uses
them: `pretend.rs` (`run`, `run_resume`, `run_info` via the resolved
config; `execute_unmerge`, `run_config_action` via the env default),
`ebuild.rs`, `regen.rs`. Unit test
`portage_repo::tests::portage_tmpdir_comes_from_the_resolved_scalar_then_the_make_globals_default`.

**Container (oracle) evidence** (`l2-20260913T191450Z`). The portage-built
and portuale-built `porttest/splitdebug` archives now have identical image
path sets, modes, symlink targets and payload sha256 — including the
`.debug` objects and the `.build-id` links, which carry the **oracle's own
build-ids** again (`7c/f6c1b1…`, `e8/95ec8e…`), i.e. the exact bytes the
2026-09-08 real oracle froze. `porttest/setuid` is back to 14384 B per
binary with the oracle's modes; `gpkg-diff --mode strict` reports only the
filed #39 metadata rows (`IUSE`/`IUSE_EFFECTIVE`/`SIZE`/`RDEPEND`/...,
`NEEDED.ELF.2` field count) — no `usr/bin/pt-*`, `libptsd`, `.build-id` or
`usr/lib/debug` row.

**Allowlist cleanup.** `KNOWN_FINDINGS` lost all eight broad
`l2-gpkg-dostrip-splitdebug` rows; only the narrowed `usr/lib/debug/
\.build-id/e0/2277.*` row stays for the genuinely ungradable race below.
`known-divergences.yaml` lost `-libptsd`, `-dirs`, `-libdir`, `-lib64` and
the broad base entry; the base is now `category: SYMLINK` on
`/usr/lib/debug/.build-id/e0/2277*` and `-contents` is narrowed to
`porttest/setuid-1.0/CONTENTS` + `sym /usr/lib/debug/.build-id/e0/2277`.
This run did not hit either (both builds picked `pt-setuid`), but real
disagrees with itself between runs (`pt-setuid` oracle, `pt-sticky` on
2026-09-13) so the narrowed pair is kept as real-divergence, never
portuale-vs-real.

**G3 brush smoke — filed, not an estrip/ecompress issue.** `emerge
--shell brush` was run against the overlay in the test image:

- `porttest/docs` builds and `ecompress` fires (`BIG.txt.bz2` in the
  image) — the transforms work under a brush parent.
- `porttest/splitdebug` silently produces an **empty image** (rc 0,
  "Final size of installed tree: 1 KiB", archive image = `image/` only):
  brush's phase execution no-ops the generated `src_compile` (the same
  `src_compile` shape — `cat > file <<-'EOF'` plus `tc-getCC` — works under
  `--shell bash`), leaving `work/` empty and `src_install` with nothing to
  `dobin`, plus `declare: cannot mutate readonly variable` /
  `env: '': No such file or directory` noise. This reproduces with
  `ebuild --shell brush <fixture> install` and with the full L2
  `FEATURES`, so it is a brush-backend gap *before* any transform code
  runs. Filed under the brush-pin workflow (`backlog-tasks.md` #6,
  `docs/brush-pin.md`); brush stays available, default stays `bash`
  (G3: don't drop brush).

Acceptance: `l2-gpkg-dostrip-splitdebug` closed with the evidence above;
porttest track 0 unexplained (80 known = #39 metadata + the #39-ledger
rows), cross-install 61/61 explained / 0 unexplained, control 0/0.

## #38 S3 — `RESTRICT=strip` cell (2026-09-13)

Plan: S3. New fixture `TEST/images/overlay/porttest/porttest/restrict-strip/`
(`EAPI=8`, `RESTRICT="strip"`, `inherit toolchain-funcs`, compiles a `-g`
binary) + md5-cache entry + README/atomlist rows. Both PMs build it on the
L2 bed (`l2-20260913T191450Z`):

- archive diff strict: same image path set, mode, payload sha256 — the
  binary is byte-identical **and unstripped** (`pt-restrict`, 16576 B,
  `with debug_info, not stripped`, identical build-id `d69a589e…`).
- no `.debug` object under `/usr/lib/debug` for either PM (both leave only
  the empty directory); `FEATURES=splitdebug` is on in the bed, so
  `estrip`'s `has_restriction[strip]` skip is what holds.
- `FEATURES=nostrip` is a separate cell the bed cannot configure
  per-fixture; not claimed here (filed in the plan's S3 step 3).

**Trap honoured** (`misc-functions.sh:241`): this is `RESTRICT=strip`, not
`FEATURES=-binpkg-dostrip`; no "no-strip" claim is made for that token.

**Verification (step 8).** `cargo fmt --check`, `cargo clippy --release
--all-targets` (zero warnings), `cargo test --release` (whole workspace),
`python3 -m pytest tests -q` (1566 passed / 2 skipped / 8 xfailed) all
green; `L2_REBUILD=1 TEST/run/l2-portuale-builder.sh
TEST/atomlists/l1-porttest.txt` → `l2-20260913T191450Z` rc 0 (0
unexplained); `TEST/run/l1-merge-from-binpkg.sh
TEST/atomlists/l1-porttest.txt` → `l1-20260913T194808Z` rc 0 (0 hard
findings, both PMs merge all 10 fixtures including `restrict-strip`).
The four pre-existing `--ask` prompt tests (broken on `main` since the
2026-09-04 TTY gate, `7dc0486`) now run with a real pty on stdin and
pass; that repair is its own commit.

---

## #38 S4 — merge-time `instprep` (2026-09-13)

Plan: S4 / G2. User decision: **implement** (not file #38b).

**Repro (before).** `TEST/run/l2-instprep-repro.sh` (in-container half
`TEST/layers/l2/instprep-repro.sh`): one fresh container per PM,
`FEATURES="-binpkg-dostrip -binpkg-docompress"`, `porttest/{docs,setuid}`;
cell `src` = `emerge -1`, cell `bin` = `emerge -B` + unmerge + `emerge
-1K`. `TEST/logs/l2-instprep-20260913T195828Z` rc 1:

| cell | real | portuale (before) |
|---|---|---|
| src | `BIG.txt.bz2`; `pt-*` 14384 B stripped; `/usr/lib/debug/usr/bin/pt-*.debug` | `BIG.txt`; 15424 B not stripped; no `.debug` |
| bin | archive `pt-setuid` 15424 B (unstripped) → merged 14384 B stripped + `.debug`, `BIG.txt.bz2` | same unstripped/uncompressed merge as src |

Real: `dblink.treewalk()` → `doebuild_environment(..., "instprep")` +
`EbuildPhase("instprep")` as its **first** step, before
`INSTALL_MASK`/collision-protect/`pkg_preinst` (`vartree.py`, 3.0.82.2
`treewalk` top; `doebuild.py` `"instprep": {"cmd": misc_sh}`), on every
merge incl. `EMERGE_FROM=binary`. `__dyn_instprep`
(`misc-functions.sh:265-308`) carries the complement gates.

**Fix.** `ebuild_phases::run_instprep` = `misc-functions.sh
__dyn_instprep` with `EBUILD_PHASE=instprep` and the resolved phase env
(`MergeOptions::build_env`: `FEATURES`, `PORTAGE_COMPRESS*`); for a
binary merge `${T}/environment` is seeded from the binpkg's
`environment.bz2` plus `EMERGE_FROM`/`MERGE_TYPE=binary` (shared
`seed_saved_environment`/`binary_merge_env`, factored out of
`run_phase_from_saved_env`). Called at the top of
`ebuild_merge::merge_after_install` (every source merge: `emerge`,
scheduler, `ebuild <file> merge|qmerge`) and in `merge_binpkg` after
`pkg_setup`, before `INSTALL_MASK`. Non-zero → `!!! instprep failed`,
merge returns 1 (real). A binpkg without a saved env gets no phase (the
hooks' existing degrade). No transform logic in Rust; no `bin/*` edited;
default `FEATURES` → the script's gates are false, near no-op.

**Repro (after).** `TEST/logs/l2-instprep-20260913T201638Z` rc 0: both
cells identical between PMs; both `-B` archives carry the unstripped
15424 B `pt-setuid`, both merges strip it.

**Found on the way — backlog #42.** The first post-fix run
(`l2-instprep-20260913T201319Z`) failed the `bin` cell: portuale's `-B`
build reused the `src` cell's `${PORTAGE_BUILDDIR}` (already
instprepped image, `.installed` marker → install skipped), so the archive
was already stripped and the `-K` instprep's `estrip` died (`debuglink
section already exists`). Real `EbuildBuild._start_pre_clean` runs the
`clean` phase before every build; portuale's emerge never pre-cleans. Not
a transform bug and out of #38 scope (scheduler); the repro wipes
`/var/tmp/portage/porttest` between cells (the state real's pre-clean
leaves) and cites #42. Fresh-container harnesses (L1/L2) never hit it.

**Tests.** Rust `run_instprep_applies_only_the_complement_of_the_install_qa_gate`
(install with `-binpkg-docompress` leaves `BIG.txt`; instprep with the
token is a no-op; without it compresses + repairs the link + writes
`.instprepped`). pytest
`test_emerge_instprep_compresses_at_merge_for_source_and_binary`
(source merge and `--buildpkgonly` → `--usepkgonly` of the plain-`BIG.txt`
gpkg both merge `BIG.txt.bz2` into tree + `CONTENTS`).

**Verification (step 8).** `cargo fmt --check`, `cargo clippy --release
--all-targets` (0 warnings), `cargo test --release` (workspace), `python3
-m pytest tests -q` (1567 passed / 2 skipped / 8 xfailed) green;
`L2_REBUILD=1 TEST/run/l2-portuale-builder.sh TEST/atomlists/l1-porttest.txt`
→ `l2-20260913T201941Z` rc 0 (80 known / 0 unexplained, same as S3; the
control leg has portuale merging the portage-built set through the new
binary instprep); `TEST/run/l1-merge-from-binpkg.sh
TEST/atomlists/l1-porttest.txt` (portage upgraded to 3.0.82.2) →
`l1-20260913T202308Z` rc 0 (0 hard findings, 70 mtime-only = S3).

---

## #38 S5 — closeout (2026-09-13)

Plan: S5. No product code changed; this section is the fresh end-to-end
re-run and the doc/backlog closeout for #38.

**Fresh porttest track.**

```
L2_REBUILD=1 TEST/run/l2-portuale-builder.sh TEST/atomlists/l1-porttest.txt
```

→ `TEST/logs/l2-20260913T203727Z`, rc 0: **82 known / 0 unexplained**
(the 80 #39 metadata rows of S2 plus the 2 narrowed `setuid` build-id
race rows, which were actually hit this run — the portage-built pair
picked `pt-setgid` while portuale picked `pt-setuid`; the 2026-09-08
oracle itself picked `pt-setuid`, i.e. the filed real-vs-itself
nondeterminism, not a portuale-vs-real diff), cross-install 67 hard /
67 explained / 0 unexplained (same 67 as S2), control 0/0, and all 10
fixtures' archives compare — each pkgdir also carries the builder's own
`sys-apps/portage-3.0.82.2` gpkg from the upgrade step, hence the
report's "11 archives".

**L1 porttest (mandatory after S4's merge-path change).**

```
TEST/run/l1-merge-from-binpkg.sh TEST/atomlists/l1-porttest.txt
```

→ `TEST/logs/l1-20260913T204335Z`, rc 0: 0 hard findings, 70 mtime-only,
both PMs merge all 10 fixtures (109 files each). The archive consumer is
unaffected by the new merge-time `instprep`.

**Real set (as far as #39 allows).**

```
L2_REBUILD=1 L2_MODE=payload-tolerant L2_BUILD_MODE=deep \
  TEST/run/l2-portuale-builder.sh TEST/atomlists/l1-merge.txt
```

→ `TEST/logs/l2-20260913T204249Z`. Both PMs build all **19 binpkgs**.
The stop point is unchanged from #37 S4 and is all #39:

- archive-vs-archive: 105 known metadata rows + 8 unexplained, all
  `metadata/{DEPEND,RDEPEND}` install-time rewrite (pv, libbsd, jq,
  htop, sudo) = `l2-gpkg-dep-metadata-rewrite`, filed under backlog
  #39 (description widened this slice).
- `consume cand` (real Portage merging the portuale-built set) rc 1
  after 5/18 merged: real takes only 5 archives from `$PKGDIR`
  (`[binary N] ::__unknown__`) and for the rest picks the image's
  gentoo binhost (`[binary N g]`) or source — some lose to
  higher-revision binhost copies (oniguruma `-6`, sudo `-6`, nullmailer
  `-12`, dmidecode `-2`, same-CPV libmd `-1`), the `[ebuild N]` cases
  (pv, bash-completion, jq, htop) to the unusable #39 index stanzas.
  The merge then dies extracting `oniguruma-6.9.10-6` from
  `/var/cache/binhost/gentoo` on `GnuPG verification failed` (no public
  key) — the `Packages` index gaps plus `l2-binpkg-gpg-check`, now
  filed as **backlog #43** with the owner note. No env (#37) and no
  transform (#38) rows.

**Docs closeout.** `docs/what-this-proves.md` gained the S4/S5
paragraph (live `tar tf`/sha commands); `scope-backlog.md` §K's "no
packaging transforms" bullet is closed and the GPG entry references
#43; `backlog-tasks.md` #38 is DONE, #39 mentions the dep rewrite, #43
is new; `TEST/images/overlay/porttest/README.md` and the `docs` ebuild
no longer claim `doman` output is compressed (`pt.1` is 33 B and stays
plain under `PORTAGE_DOCOMPRESS_SIZE_LIMIT=128`); `docs/030` G0.5/S3
now say #38 landed and the L3 S3 worklist is verification-mode. No
`KNOWN_FINDINGS`/yaml row is dead: the fresh run's cross-install hit
every yaml entry (50 `l2-gpkg-metadata-members`, 10
`l2-pkgindex-eapi-missing`, 2 `l2-gpkg-dostrip-splitdebug` + 4
`-contents`, 1 `l2-consumes-portuale-env-degraded`), its archive diff
hit the setuid-race builder row, and the real set hit
`l2-gpkg-dep-metadata-rewrite`.

**Verification (step 8).** `cargo fmt --check`, `cargo clippy --release
--all-targets` (0 warnings), `cargo test --release` (1025 passed / 0
failed), `python3 -m pytest tests -q` (1567 passed / 2 skipped / 8
xfailed) all green.
