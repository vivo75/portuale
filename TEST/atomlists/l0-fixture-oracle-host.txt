# Fixture-oracle cases that need host-exact roots (`FX_HOST_ROOTS=1`,
# `ROOT=/` with the staged fixture vdb copied in): their shape resolves
# a `BDEPEND` between the target and running roots, which real's
# `create_trees` splits when `ROOT=$FX` (B0b, `TEST/findings/l0.md`
# "#68/#72 B0b"; the slot-op matrix `l0-fixture-oracle-slotop.txt` needs
# the same knob).
#
#   TEST/run/l0-fixture-oracle.sh TEST/atomlists/l0-fixture-oracle-host.txt
# with FX_HOST_ROOTS=1 exported.
#
# #76 B0 q1: disjtarget-2.0's `BDEPEND="|| ( ~dev-libs/disjowner-1.1
# dev-libs/absent )"` keeps the owner-merge branch (dep_zapdeps'
# all-in-graph bin), so the serializer waits and the satisfied
# `[blocks b      ]` row prints right after `disjtarget-2.0`.
=dev-libs/disjtarget-2.0
